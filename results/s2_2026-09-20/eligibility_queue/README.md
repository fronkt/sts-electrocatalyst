# Eligibility work queue, 2026-09-20

This queue retains all 28,191 candidate keys in the pinned OpenAlex export. Every row is UNREVIEWED and NOT_CODED. It is a work-order aid, not a screened population, inclusion list, or search-completeness claim.

The ordered groups use explicit complementary DOI matches and broad title signals to choose an initial reading order. Absence of any title signal is never an exclusion. All 27,320 rows in the final work-order group remain available and require eligibility resolution under the same rule. No top-N sampling rule applies.

The manifest pins the source export and CSV hashes. Independent eligibility evidence belongs in separate review records, retaining source IDs and unresolved access. Future additional pages require an additive queue refresh; this file describes only the pinned export.