# Registered complementary literature inventory — 2026-09-19

Discovery and local access inventory only. All records remain `NOT_SCREENED` and `NOT_CODED`. No symmetry, vibration or magnetic-state codes, inclusion decisions, hypothesis results or frozen denominator are present.

The five registered source entries comprise all 25 references on Divanis ESI page S29, the two August 15 syntheses, and the explicit Xu 2015 / Man 2011 known sources. Divanis references 1–24 retain their table-source role; reference 25 retains its methodological role. Every record has its source SHA256, line range, verbatim identity and full citation context. Context includes the syntheses' original verification caveats and potentially superseded claims; retaining it does not adopt those claims.

This inventory contains 271 citation occurrences and 120 identity keys: 46 explicit DOI keys and 74 unresolved keys. The identity-key count is not a distinct-paper count: named mentions, arXiv versions, repository links and Divanis references remain separate when reconciliation is incomplete. Repeated mentions are retained as discovery edges. Outside-topic citations and named data/documentation sources are retained for later accounting.

DOIs must contain the literal registry prefix and slash; prose-final periods are excluded. Page numbers, numerical expressions and arXiv identifiers are never promoted to DOIs. The existing DOI and title/year/first-author identity policy is retained: incomplete metadata stays unresolved. `possible_identity_keys` are explicit reconciliation leads only, never automatic merges. Examples needing reconciliation include Man in Divanis reference 1 and the known-source DOI; Mom 2014's title-free reference and the cached SI; Berlijn's volume/article/year citation; Dickens' unspecified defect-site/scaling source; Reuter and Scheffler's unspecified framework source; unresolved ESSI origin; and article/preprint/repository versions of the same research.

`access_inventory.jsonl` records candidate local files for 17 identity keys, with hashes and byte sizes. These are filename/catalog matches, not verified article identities or completed full-text reads. The local papers catalog is used only for matching files, not to expand discovery. A missing candidate file is not an unavailable-access finding. The earlier wrong-file Xu candidate `jp5b05338.pdf` is not attached to Xu; the catalog identifies it as Curnan and Kitchin, a different work. Primary article and SI identity checks and acquisition remain open.

No API requests were made. No recursive/backward citation traversal beyond the registered Divanis reference list occurred. The two syntheses were not followed into their own linked local memos. No pinned search source, raw API cache, base candidate list or inclusion state changed.

Validation: source hashes; 25 consecutive Divanis reference numbers; all literal DOI/arXiv/URL token occurrences in both syntheses; unique occurrence IDs; occurrence-to-access joins; all source line/context matches; all recorded local-file hashes; and absent method-code fields. Independent source/context/hash and join checks passed; the verification receipt is `../continuation_verification.json`. Exact bytes of all six source/catalog/identity-rule files are retained in `source_snapshot.zip` with its manifest, independently of historical Git checkout line endings.
