# Core (all present in the recon'd env; round-1 selector runs on CPU)
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
pytest>=7.4

# Optional — real OC22 adsorption-energy backend (needs GPU; run on Vast.ai)
#   pip install fairchem-core
# Optional — BoTorch/Ax multi-objective Bayesian optimization for round-2
#   pip install botorch ax-platform
