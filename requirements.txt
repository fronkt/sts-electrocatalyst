# Core (round-1 heuristic selector + tests run on CPU)
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
ase>=3.23
# src/dft/pourbaix_multi.py, src/dft/pourbaix_r2.py, src/hea_oer/data.py and
# src/hea_oer/surfaces_rutile.py import pymatgen at module level; without it the
# repo-tests CI job errors at collection (38 of 38 runs red, 2026-08-27 .. 2026-09-05).
pymatgen>=2024.1.27
pytest>=7.4
# src/dft/lowtail_launch_ops.py, src/s2/lowtail_dft/lt_anvil_snapshot.py and
# src/dft/lowtail_slab_scf_diag_launch.py import paramiko at module level; without it
# three launcher test modules error at collection (repo-tests red since 2026-09-18).
paramiko>=3.4

# Optional — real adsorption-energy backend (needs GPU; run on Vast.ai):
#   uv pip install fairchem-core   # + accept UMA license + huggingface-cli login
# Optional — BoTorch/Ax multi-objective Bayesian optimization for round-2:
#   pip install botorch ax-platform
