# Active interface for the core authorized 2026-09-13. Scientific gates unchanged.
[cli]
census_cmd = "python -m silentgate.cli census --paths-from {paths_file} --json"
oc20_cmd = "python -m silentgate.cli census --oc20 {sample_dir} --json"
version_cmd = "python -m silentgate.cli --version"

[schema]
runs_array = "/runs"
path = "/path"
n_symops = "/n_symops"
nosym_in_deck = "/nosym_in_deck"
locked_two_witness = "/locked_two_witness"
locked_force_only = "/locked_force_only"
locked_axes = "/locked_axes"
n_adsorbate = "/n_adsorbate"
unidentified = "/unidentified"
n_if_pos_excluded = "/n_if_pos_excluded"
header_form = "/header_form"
per_step_exact_zero_count = "/per_step_exact_zero_count"
locked_any_atom_force_only = "/locked_any_atom_force_only"
