# `tests/silentgate` — the S1 suite

`python -m pytest tests/silentgate -q` → **40 passed, 6 skipped** today.

| file | what it checks |
|---|---|
| `test_gate_fails_closed.py` | that the gate CANNOT go green while the core, the provenance record or the OC20 verdict is missing |
| `test_fixture_manifest.py`  | that the corpus the controls are scored against has not drifted |
| `test_open_questions.py`    | that the six open registered decisions are not forgotten |
| `test_face_end_to_end.py`   | that the face is *passable*, and goes red for each registered failure one at a time |
| `fixtures/manifest.toml`    | 13 cited fixtures + corpus-wide invariants, all measured |
| `fixtures/regenerate_manifest.py` | regenerates the above; run it when a test says to |
| `fixtures/fake_census.py`   | a test double for `silentgate census`; parses nothing |
| `spec_rulings.toml`         | the six questions; every answer blank on purpose |

## Why there are no expected census values here

Writing them would mean writing a per-atom / per-axis / per-step force-block
parser — which is `silentgate/readers/*`, which is core (docs/43:1840) — and
would put a second, unreviewed detector in the repo, leaving a real question
(*whose census is right?*) with no registered answer. A9.2 exists to catch
exactly that failure mode.

So the manifest records **structural** facts only: hashes, byte counts, marker
lines, block counts. Numeric expectations enter later as **golden files**
generated by the entrant's core and reviewed by him.

Hashes are LF-normalised. `.gitattributes` carries no rule for `*.out` and
`core.autocrlf=true` on the entrant's machine, so the same tracked blob is CRLF
in the Windows working tree and LF on Linux CI; every content check normalises
first.

## The six skips

`test_open_questions.py` skips while `silentgate/` is absent — a question about
how to score the core cannot bind before the core is written. The moment the core
is committed they become failures until each ruling is recorded with its dated
line. Today's red is carried by the control face, not by this suite: the suite
checks that the *harness* is sound, so if it ever goes red that is a real
regression.
