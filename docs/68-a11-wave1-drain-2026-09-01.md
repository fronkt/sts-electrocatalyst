# docs/68 — A11 wave-1 drain: what the 106 SCFs returned, what they decide, and what is still owed (2026-09-01)

**Read with:** docs/43 AMENDMENT 11 (the registration), docs/61 (its draft and guards),
docs/66 (the entrant's elections), docs/67 (Mn AFM arm), docs/62 (Stage 0), and the census
reports of record, `tasks/review/a7_3_spin_census_2026-09-01_runA_as-committed.json`,
`..._runB2_reformatted.json` and `..._runC_ext-deck-built.json`. Headline discipline
unchanged (A11.5 / A11.R2): **the as-built A7.3 score of 3-of-6 remains the registered
headline; nothing below promotes it, and no middle-band count is quoted bare.**

## 1. The wave, as Slurm and as physics

Eight arrays, 106 SCFs, submitted 2026-08-31 (tasks/todo.md, commit a04280c). Drained state
at 2026-09-01 19:05 EDT, from `sacct` (never from memory):

| array | family | SCFs | Slurm COMPLETED | converged | hit `electron_maxstep=200` | not run |
|---|---|---|---|---|---|---|
| 20262064 | Ru/Ir Stage 1 (`m_a0spin_s1`) | 20 | 18 | 12 | 6 | 2 (OOM, a171) |
| 20262088 | Ru/Ir re-read (`m_a0spin_reread`) | 20 | 20 | 14 | 6 | 0 |
| 20262090 | Cr/Mn/Fe seed search (`m_cmf_seed_search`) | 28 | 27 | 27 | 0 | 1 (OOM, a171) |
| 20262091 | Ti Stage 1 (`m_a0spin_s1_ti`) | 12 | 11 | 11 | 0 | 1 (hung, a171) |
| 20262104 | Ti re-read (`m_a0spin_reread_ti`) | 12 | 12 | 12 | 0 | 0 |
| 20262287 | Ru AFM probe (`m_h_afm_probe`) | 4 | 4 | 2 | 2 | 0 |
| 20262289 | probe robustness (`m_h_afm_robust`) | 6 | 6 | 4 | 2 | 0 |
| 20262290 | Mn AFM order (`m_mn_afm_order`) | 4 | 4 | 4 | 0 | 0 |
| **total** | | **106** | **102** | **86** | **16** | **4** |

Slurm "COMPLETED" is the runner exiting 0, not convergence; the converged column is
`convergence has been achieved` + `JOB DONE` read from every `.out`. `projwfc.x` ran inline
on all 76 converged A0-spin points (A6.5(1)); the 10 s0/ rows carry no projection by design.

**Node a171.** Every task Slurm placed on a171 died or hung and nothing placed elsewhere did:
20262064_3, 20262064_6 and 20262090_1 were OOM-killed at 0 SCF iterations
(`Detected N oom_kill events`, 3–9 min wall); 20262091_1 sat RUNNING 19 h 11 min at 0
iterations with its `.out` untouched for 23 h while its 11 sibling rows finished in 4–8 min
(`scontrol show node a171`: FreeMem 333 MB of 257 GB, CPULoad 325 on 128 CPUs). Cancelled
2026-09-01 19:06 EDT; ≈2,450 SU of idle burn. Balance 69,783.7 → **65,096.2 SU**
(4,687.5 SU for the wave, roughly half of it the hung job). The four casualties are
re-queued byte-identical as `runs/a0/m_a11w1_retry_a171.txt` (array **20300641**, a171
excluded); each partial `.out` was preserved first as `<stem>.out.a171_2026-08-31` so the
retry is a recorded second attempt (docs/45).

**Pull.** 484 files tarred on Anvil with a per-file md5 list, scp'd, `md5sum -c` ALL MATCH
locally; `.out` / `.projwfc.in` / `.lowdin.txt` (extracted here by the committed extractor,
86/86 `--check` PASS) enter the tree; `.projwfc.out` stay local and gitignored as
registered; the runner's `.run.in` scratch copies were over-pulled and deleted (not a banked
class anywhere); Slurm logs sit in the session scratchpad, their `oom_kill` lines quoted in
docs/45.

## 2. The finding of the wave: spin-polarised Ru does not converge at U = 9

**16 of 16 spin-polarised Ru SCFs at U = 9.0 eV hit the 200-iteration ceiling; 0 of 16
converged** (the count read 15 while the retry row was still running; corrected 2026-09-02). That is 12 nspin = 2 FM-seeded 1×1 rows (slab, s0_O, s0_OH, s0_OOH × seeds 0.10/0.30/0.50; the twelfth landed in the retry) **plus** the 4 AFM 2×1v rows of the S0(h) probe and its
robustness twins. The trajectories are not slow creep: `estimated scf accuracy` oscillates
between ~3×10⁻³ and ~3×10⁻¹ Ry for 200 steps while the total magnetization wanders 19–24
μB/cell (1×1) or 2.6–4.3 μB/cell (2×1v AFM), a spin/charge sloshing that `mixing_beta = 0.3`
with Marzari-Vanderbilt 0.01 Ry smearing never damps. Every nspin = 1 Ru row at U = 9, every
Ru spin row at U = 0 (admitted 66–88 meV below the floor), and every Ir / Cr / Mn / Fe / Ti
spin row at either endpoint converged, with a single exception: Ir s0_OOH u000 seed 0.30 is a
near-miss (accuracy 10⁻⁶–10⁻⁴ Ry, moment −0.3…−0.7) whose siblings converged, so its cell
is FINAL regardless.

What the registration does with it (nothing was invented today):

- A11.6's rule is "lowest **converged** total energy across the three seeds AND the banked
  nspin = 1 energy". An unconverged row is recorded UNCONVERGED, excluded from the pool, and
  never wins (census test S6, committed before any of these `.out` existed). Every
  (Ru, state, u900) cell therefore resolves **EQUALISED-BY-SELECTION(nspin=1)**: the U = 9
  endpoint of the spin-equalised Ru ladder *is* the as-built row.
- Hence dc_Ru(9.0) = 0 by construction and D_Ru = −dc_Ru(0). The U = 0 winners are magnetic
  (seeds 0.10–0.50 admitted), the U = 9 winners are the non-magnetic incumbents, so once the
  retry row lands, **guard 3 (docs/61 §A11.7) will flag Ru BRANCH-CONDITIONAL** and the pair
  may not be scored into a span. The census reports the flag; it never auto-clears it.
- The S0(h) probe's own U = 9 AFM legs (the compute docs/58 said "can change a banked
  verdict") are in the same 0-of-16. The NM legs at U = 9 converged (2×1v off-plane, −3297.39
  / −3338.89 Ry) and the AFM-geometry NM twins agree with them to 0.1 / 0.8 meV.

**What this resolves.** A7.3 conditionality fact (2), "Ru is 15.5 meV from flipping the
verdict, inside the open NM-vs-AFM class whose S0(h) re-run acts on that metal", is now a
measured sentence rather than an open one: **under the registered numerics no spin-polarised
Ru state exists to be measured at U = 9, so the arm cannot price that 15.5 meV, and the
as-built 3-of-6 stands with that reason attached.** That is exactly the outcome A11.5 was
written for ("select which caveat sentence is true"); it is not a rescue and must not be
written up as one.

**The one live decision (the entrant's, D1).** A6.5(2)'s escalation ladder, (i)
neighbouring-U density, (ii) halve β, (iii) record NOT_CONVERGED as a gap, is registered
for the A0 grid; AMENDMENT 11 registered no ladder for the spin arm and rung (i) has no
meaning at a fixed endpoint. Running rung (ii) (β 0.3 → 0.15, `mixing_ndim` 16) on the 16
Ru U = 9 spin rows would be an **unregistered** extension: ≈16 SCFs × 20–60 min × 128 ranks
≈ 640–1,900 SU. Recommendation: **do not run it to rescue the cell.** If the entrant wants
the numerics question answered for the report, license it first as a dated A11 line whose
outcome, either way, still cannot promote A7.3 (A11.5).

## 3. The Ir cells: one contingency fires, one guard asks for adjudication

- **Ir slab u000: CONTINGENCY-OPEN → extension built.** All three grid seeds land above the
  nspin = 1 floor (+0.590 / +0.597 meV today; +0.583 meV banked at 0.50), the same ~0.59 meV
  offset at every seed with the moment collapsing to |m| ≤ 0.2 μB: a non-magnetic solution
  reached by a slightly different path, not a search that found anything. Per docs/43 A11.R3
  the pre-named 0.05 seed runs next: built 2026-09-01 by `src/dft/build_ir_slab_ext.py`
  (trigger re-derived from disk; the census's own CEN-d passes on the deck; run C of the
  census with the deck present is identical to run A on `per_metal`), manifest
  `runs/a0/m_ir_slab_ext005.txt`, one SCF, u000 only, submitted as array **20301497**. **u900 did not fire** (0.10 and 0.30
  admitted at −15.1 meV; 0.50 landed +2,835 meV above with a 6.8 μB moment, a genuinely
  different and much higher spin state, rejected), and Rider 1 keeps 0.05 out of S there.
  If 0.05 also lands above, the cell resolves EQUALISED-BY-SELECTION(nspin=1) with the full
  rejection record; the census already implements that rule.
- **Ir equalised row (provisional over the open slab cell):** span/2 = **0.0591 V** vs
  as-built 0.0637 V, D_M = +0.0091 eV, below the 0.10 V floor either way. **Guard 3 flag
  REVIEW-REQUIRED:** the s0_OH winners are the incumbent at U = 0 (three seeds within 0.01
  meV of the floor; tie → smallest |seed|) and seed 0.10 at U = 9 (m = 1.62 μB); the s0_OOH
  winners are seed 0.50 at U = 0 (m = −1.07) and 0.10 at U = 9 (m = +0.42). Whether these
  are "different magnetic branches" is the guard's adjudication, reported, never auto-cleared:
  **the entrant rules on it (D2)**; the Ir row cannot be scored into a span until then.

## 4. Mn AFM arm, stage 1: the E1 rule executes, FM stands, the arm stops

Four fixed-geometry 2×1 SCFs at U = 3.9 (docs/67; the `m_mn_afm_order.txt` header is the
rule of record, fixed before any AFM energy existed):

| pattern | E (Ry) | ΔE vs FM | total / absolute magnetization (μB/cell) | Mn moments |
|---|---|---|---|---|
| FM | −3532.71646866 | 0 | 36.00 / 46.15 | +2.7…+3.1 on all 12 |
| P-A | −3532.66762447 | **+664.6 meV** (+55.4 meV/Mn) | 4.33 / 42.46 | +3.05/−2.57/+2.67/−2.49/+3.10/−2.61 per layer |
| P-B1 | −3532.62683703 | **+1,219.5 meV** (+101.6 meV/Mn) | 0.00 / 43.14 | layer 1 all +, layer 2 all − |
| P-B2 | −3532.62683721 | +1,219.5 meV | 0.00 / 43.13 | the mirror labelling of P-B1 |

Both AFM solutions were genuinely **held** (the M_abs witness: 42–43 vs 46 μB, total moment
0–4.3 vs 36), so this is a measured ordering energy, not a collapse. P-A's residual 4.33 μB
is the inequivalence of the two sublattices in a slab, reported as measured. P-B1 and P-B2
agree to 0.002 meV, as two labellings of one pattern must: an internal determinism control.
**Rule outcome: no AFM candidate is ≤ FM + 20 meV → "measured null, FM stands";
MN-AFM-CORE does not trigger; the arm stops at stage 1.** The A7.5 strike lifts by running,
on this outcome as on any other (docs/67 §1, §6); no number from this arm enters A7.2/A7.3;
the E6 limitation (collinear, fixed FM-relaxed geometry, 2×1 frame, U = 3.9) travels with
every Mn sentence. **Owed: the entrant's dated countersign line (docs/67 §7 item 3, D3)** and
the corresponding update of `a0main_readout.py`'s `mn_afm` text from "CURRENTLY UNMET" to
"RUN 2026-09-01: FM stands (measured null)".

## 5. Cr / Mn / Fe seed search and Ti

- **Mn and Fe: equalised = as-built exactly** (D_M = 0.0000 eV; every seed converged to the
  banked FM row's energy or above, so the incumbents stand). Both remain EXCEEDS.
- **Cr: PENDING** on the retry row (s0_OH u000 seed 0.10). Worth watching when it lands: at
  u900 the seed-0.10 row sits **0.635 eV below** seeds 0.30/0.50 (−1593.27502 vs −1593.22833
  Ry), a lower magnetic branch reached from the smallest seed, exactly the seed dependence
  this arm exists to expose. The census decides what it does to Cr's row; nothing is
  concluded here.
- **Ti: PENDING** on the retry row (s0_OH u900 seed 0.10); its two sibling seeds already
  agree to 0.001 meV (−1256.69667 Ry), and every Ti seed spread in the wave is ≤ 10 meV. Ti
  is as flat under the spin convention as it was under U (docs/60).

## 6. Instrument change, disclosed with its control

`a0spin_census.py` CEN-d refused to run on the drained tree: its file-class whitelist
(`.in`, `.out`, `.projwfc.out`) predated the three sidecar classes a drain leaves beside
the evidence: the A6.5(1) `<stem>.lowdin.txt` artifact (banked under runs/a0/main since
tranche 1), the runner's `<stem>.projwfc.in`, and the preserved attempt
`<stem>.out.<tag>_YYYY-MM-DD`. **Run A** executed the census exactly as first committed
with those files moved aside (`..._runA_as-committed.json`); the whitelist was then widened
by class (never by name; every other class still dies; test S8 added, suite 115 passed) and
**run B2** executed on the full tree (`..._runB2_reformatted.json`). **A and B2 are
identical on every scored field** (`a7_3_spin`, `controls`, `gates`); run C, after the 0.05
deck was built, is identical again on `per_metal` and flips only the Ir slab u000 status to
PENDING-RUN. The whitelist gates presence, never a number.

## 7. What is left, in order

1. **Compute (queued):** retry array 20300641 (4 SCFs: Ru s0_OH u900 m010, Ru s0_OOH u000
   m010, Cr s0_OH u000 m010, Ti s0_OH u900 m010) and the Ir-slab 0.05 extension (1 SCF; array
   **20301497**). Pull in the docs/62 shape; extract Löwdin; **re-run the census to a
   new dated path**; it then reports FINAL rows for Cr, Ru, Ti and the Ir slab resolution.
2. **The entrant's lines:** D1 (β-rung on the 16 Ru U = 9 spin rows; see §10); D2 (guard-3
   adjudication for Ir, and for Ru once final); D3 (Mn E1 countersign, docs/67 §7.3);
   D4 (whether to file the a171 ticket with RCAC; draft in docs/45; the entrant sends).
3. **Docs:** docs/58 conditionality fact (2) restated as measured (§2 above); docs/60 §6 /
   docs/63 §6 pointers; `a0main_readout.py` mn_afm text; the report's methods sentence on
   the spin arm ("0 of 16 spin-polarised Ru SCFs converge at U = 9 under the registered
   numerics").
4. **Nothing else in AMENDMENT 11 is unrun** once (1) drains; the balance (65,096 SU) is
   untouched by anything above except ≈40–80 SU for the five queued SCFs.

## 8. FINAL census (2026-09-02 01:10 UTC): the spin-equalised sensitivity is also 3 of 6

The five queued SCFs landed (retry 20300641 ×4, extension 20301497 ×1; 118.4 SU; balance
**64,977.8 SU**; queue empty). Delta pull 18 files, per-file md5 ALL MATCH; 4 new Löwdin
artifacts extracted and `--check` PASS (90 total). Report of record:
`tasks/review/a7_3_spin_census_2026-09-02_FINAL.json` (every CEN assertion PASS, 280 inputs
md5-swept, `n_final_rows = 6`).

- **Ru s0_OH u900 seed 0.10: 200 iterations, not converged** (accuracy 3×10⁻³–10⁻¹ Ry,
  moment 21.6–23.0 μB), the same sloshing as its siblings. **The Ru count is final: 0 of 16
spin-polarised SCFs converge at U = 9.** Every (Ru, state, u900) cell is
  EQUALISED-BY-SELECTION(nspin=1).
- **Ir slab u000: FINAL, EQUALISED-BY-SELECTION(nspin=1)** with the full rejection record,
  four seeds all above the floor: 0.05 **+0.474**, 0.10 +0.590, 0.30 +0.597, 0.50 +0.583 meV
  (|m| ≤ 0.18 μB in every case). The contingency is closed exactly as docs/43 A11.R3 wrote it.
- Ru s0_OOH u000 seed 0.10 converged −66.49 meV below the floor, 0.23 meV above seed 0.30;
  inside the 1 meV tie band, so the smaller seed takes the cell (tie-break applied, recorded).
  Cr s0_OH u000 and Ti s0_OH u900 seed 0.10 reproduce their siblings to < 0.01 meV.

| metal | as-built span/2 (V) | equalised span/2 (V) | D_M (eV) | dc_M(0) / dc_M(9) (eV) | vs 0.10 V | guard 3 |
|---|---|---|---|---|---|---|
| Cr | 0.3435 | **0.3435** | −0.0000 | +0.000 / −0.000 | EXCEEDS | REVIEW-REQUIRED (seeds 0.1/0.3 → 0.6/0.1; all FM, m = 11.0) |
| Mn | 0.6307 | **0.6307** | −0.0000 | 0 / 0 | EXCEEDS | — |
| Fe | 0.6102 | **0.6102** | +0.0000 | 0 / 0 | EXCEEDS | — |
| Ru | 0.0922 | **0.0957** | −0.0070 | +0.0070 / 0 (by selection) | below, **8.5 meV short** | REVIEW-REQUIRED (magnetic at U = 0, forced nspin = 1 at U = 9) |
| Ir | 0.0637 | **0.0591** | +0.0091 | −0.0092 / −0.0001 | below | REVIEW-REQUIRED (seed 0.0/0.5 → 0.1/0.1) |
| Ti | 0.0438 | **0.0522** | −0.0169 | −0.1161 / −0.1330 | below | REVIEW-REQUIRED (s0_OOH hi winner = Rider-2 null row by tie-break) |

**Reading, under A11.5 (sensitivity only, never a confirmation count, never quoted bare):**

1. **`equalised_over_floor = {Cr, Fe, Mn}`: 3 of 6, the same three metals as the as-built
   census.** Equalising the spin convention, to the full extent the registered numerics allow,
   moves no metal across the floor. **Distances to the floor are quoted in docs/60's
   convention throughout: 2 × (0.10 V − span/2), the change in |Δc_M| a metal would need to
   cross** (that is why Ru's as-built distance is 15.5 meV, not 7.8). Under it each metal's
   change in distance is exactly its D_M: **Ru closes from 15.5 to 8.5 meV, Ti from 112.5 to
   95.6 meV, and Ir widens from 72.6 to 81.7 meV.** (Corrected 2026-09-02: this paragraph and
   the Ru row above first quoted 4.3 / 3.5 / 8.4 / 4.6 meV, i.e. half-distances in the
   volt convention, which are not commensurate with the 15.5 meV they invite comparison to
   — docs/63 §6 and docs/60 §6 forbid exactly that mixing. Found by the docs/70
   completeness critic and re-derived from the banked census before correcting.) The caveat sentence this census selects is therefore: *the
   3-over/3-under split is not an artifact of the nspin = 2 / nspin = 1 partition; it
   survives spin equalisation, with the Ru cell equalised by selection because no
   spin-polarised Ru SCF converges at U = 9.* Conditionality fact (1) of docs/60 §6 is
   weakened by measurement; fact (2) is closed as unpriceable (§2 above).
2. **Ti carries the largest spin-convention effect in the arm and it cancels:** the
   odd-electron *OH and *OOH rows sit 116–133 meV below their nspin = 1 twins at both
   endpoints, and c_M keeps only the 17 meV difference. The nspin = 1 Ti rows in the banked
   census are therefore ~0.12 eV too high in absolute energy while their span is nearly
   right, which is what the spin caveat of docs/59 predicted qualitatively.
3. **Guard 3 flags four metals REVIEW-REQUIRED**, as designed: the census reports every
   endpoint pair whose winners differ in seed and never auto-clears. Two are almost certainly
   same-branch (Cr: every candidate is the m = 11 FM state; Ir: seeds differ, moments 0–1.6
   μB), one is genuinely cross-branch (Ru: magnetic winners at U = 0, forced non-magnetic
   at U = 9), and Ti's is a tie-break onto the null row. **The entrant adjudicates each with
   a dated line (D2); until then none of the four equalised spans is scoreable, and the
   as-built headline is unaffected either way.**

**Nothing in AMENDMENT 11 remains unrun.** What remains is signatures (D1–D4) and the
docs follow-through listed in §7.

## 10. Decisions of 2026-09-02 (the entrant's), and the count correction

The D1–D4 elaboration was put to the entrant in session; the answers, verbatim: *"Lets go
ahead with B. Does it also widen the candidate pool? Wait for D2 until after D1. Sign D3. Send
D4."*

- **Count correction.** With the retry row terminal, the unconverged Ru set is **16**, not the
  15 quoted while that row was running: 12 nspin = 2 rows (four states × three seeds) + 4 AFM
  2×1v rows. §2, §8 and the pointer notes in docs/58, docs/60 and docs/63 were corrected
  today; nothing else in the readout moves.
- **D1 → LICENSED as docs/43 A11.R6.** Two pre-named rungs: rung 1 β 0.15 / maxstep 400
  (A6.5 rung ii literally; local-TF was already on every deck, so the "local-TF" rung of the
  elaboration was vacuous and is replaced by a second halving), rung 2 β 0.075 / ndim 16 /
  maxstep 600 on rows rung 1 leaves unconverged; the ladder ends there. **Pool widening:**
  only inside the twelve (Ru, state, u900) cells, only by candidates that converge, under the
  unchanged A11.6 rule; the AFM rows feed docs/63's readout only. Interpretation of all three
  outcomes fixed in A11.R6 before any deck exists. Census extension and decks follow in that
  order: registration f4ae5d3 → census extension + 16 rung-1 decks 2183a5e → arrays **20305768** (12 a0 rows) and **20305769** (4 AFM rows), submitted 2026-09-02 00:48 UTC.
- **D2 → HELD** until A11.R6 returns (the entrant's ordering).
- **D3 → SIGNED**, docs/67 §7 dated line; `a0main_readout.py` `mn_afm` text updated; the
  banked readout JSON is not regenerated.
- **D4 → the ticket is drafted in the entrant's mailbox for the entrant to send** (standing
  rule: the assistant never sends mail); a171 stays on the EXCLUDE list regardless.

## 11. A11.R6 executed: the ladder is exhausted, and the negative is now a three-setting measurement (2026-09-02)

Both rungs ran on all sixteen rows. **Rung 1** (β 0.15, 400 steps; arrays 20305768 + 20305769,
2,170.8 SU) and **rung 2** (β 0.075, `mixing_ndim` 16, 600 steps; arrays 20315861 + 20315873,
3,045.9 SU) each returned **0 of 16 converged**. Outputs pulled in the docs/62 shape, per-file
md5 ALL MATCH both ends; no projwfc on any row (A6.5(1) applies to converged points only).
Ladder cost 5,216.7 SU; balance **59,761.1 SU**; queue empty. Census of record:
`tasks/review/a7_3_spin_census_2026-09-02_LADDER-EXHAUSTED.json` (328 inputs md5-swept, every
CEN assertion PASS, suite 19 passed).

**A6.5(2)(iii) applies: the sixteen rows are NOT_CONVERGED, recorded and plotted as gaps,
never interpolated across.** Every (Ru, state, u900) cell stays EQUALISED-BY-SELECTION(nspin=1)
and the a7_3_spin table is **byte-identical to the 2026-09-02 FINAL census**: Cr 0.3435,
Mn 0.6307, Fe 0.6102 over the floor; Ru 0.0957, Ir 0.0591, Ti 0.0522 under it;
`equalised_over_floor = {Cr, Mn, Fe}`, 3 of 6. Outcome (a) of A11.R6, as pre-interpreted.

**What the ladder measured.** 16 rows × 3 mixing settings = **19,200 SCF iterations**, and:

| quantity | value |
|---|---|
| rows converged, any rung | 0 of 16 |
| closest approach to `conv_thr = 1e-6`, any row, any rung | 5.96×10⁻⁴ Ry — **595× the threshold** (Ru *OOH, seed 0.10, rung 2) |
| median per-row best accuracy over all three rungs | 1.94×10⁻³ Ry |
| rung 2 better than rung 1 (per-row best) | 10 of 16 — and **worse on 6**; no systematic gain |
| moment excursion at rung 2 | 17.67–24.00 μB (1×1), 1.86–6.83 μB (2×1v AFM) |

Halving β twice and tripling the Broyden history did not move the failure by an order of
magnitude on any row; the residual error plateaus in the 10⁻³ Ry decade while the magnetization
sweeps 6 μB. This is a landscape with no isolated minimum for the mixer to find, not an
under-relaxed one.

**The control that makes it a physics sentence rather than a numerics excuse.** In the *same*
2×1v cell, on the *same* geometry, with the *same* code, pseudopotentials, k-set, smearing and
`conv_thr`, the **nspin = 1 twins converge in 25 iterations** at 5.90×10⁻⁷ and 8.50×10⁻⁷ Ry
(`runs/s0/h_afm_robust/s0_{OH,OOH}__2x1v_off__afmgeo__u900`). Spin off: 25 steps. Spin on:
1,200 steps across three mixing settings, never within 595× of the same threshold. The
non-convergence is a property of the spin-polarised solution space of RuO₂ at U = 9 eV under
this functional and projector — not of the mixer, the walltime, or the machine.

**What the report may now say** (the docs/68 §2 sentence, hardened): *"No spin-polarised
self-consistent solution for RuO₂ at U = 9.0 eV could be obtained under the registered numerics
or under two pre-registered escalations (β = 0.30, 0.15, 0.075 with local-TF mixing, up to 600
iterations, three FM seeds and two AFM patterns across four adsorbate states — 48 attempts,
19,200 SCF iterations, 0 converged, closest approach 595× the convergence threshold), while the
non-magnetic twins in the same cells converge in ~25 iterations. The 15.5 meV that separates Ru
from A7.3's floor therefore cannot be priced at the upper endpoint, and the as-built 3-of-6
stands."*

**What it may NOT say:** that RuO₂ has no spin-polarised state (this is a statement about
obtaining one self-consistently under one functional, projector and code, not about nature);
that the failure supports or undermines any altermagnetism claim; that a different mixer,
occupation-matrix control, or a smaller U would not converge it — **none of those was run, and
A11.R6 ends the ladder.** A further rung requires a new dated line.

**D2 is unblocked, and Ru's answer is now measured, not provisional.** The entrant held the
guard-3 adjudication for this result (docs/43 [D2 … HELD]). Ru's endpoint pair is
**BRANCH-CONDITIONAL** in the strongest available sense: the U = 0 winners are magnetic
(m = 1.11–1.36 μB) and no spin-polarised U = 9 solution exists to compare them against, under
three mixing settings. Cr and Ti remain same-branch by inspection, Ir branch-conditional
(non-magnetic → magnetic across U on s0_OH); those three are unchanged by the ladder.

**A11.R4 scale disclosure.** +32 fixed-geometry SCFs (16 rung 1 + 16 rung 2): running total
~372 → **~388 licensed and run**, the ceiling A11.R6 named. Nothing in AMENDMENT 11 remains
unrun.
