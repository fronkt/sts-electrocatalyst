# Anvil/local result reconciliation — 2026-09-12

The full Anvil runs tree contains 44,024 files. This audit inventoried the complete tree and computed SHA-256 for all 5,567 records outside the established large-file exclusions. There were no missing plain pw.x outputs or differing plain output files. A broader historical-attempt check found one missing compressed failed-run log, which is now recovered.

The recovery contains 76 files totaling 76,245 bytes: 65 expanded execution manifests, ten projection input decks, and the compressed failed attempt. Every recovered file and all 242 separately preserved remote variants match the remote byte count and SHA-256. An independent local rehash verified all 318 transfers. Existing local files were not overwritten.

The compressed record, runs/s0/h_afm_relax/s0_O__2x1v_off__afm__relax.out.attempt2-scf-maxstep.gz, is a historical failure: electronic convergence was not achieved after 200 iterations, with final estimated SCF accuracy 0.00041542 Ry. Its JOB DONE marker does not make it converged. This file closes a retention gap and does not add an accepted endpoint. The mirror checker now recognizes archived .out suffixes, including .out.gz and .out.attempt…gz; an offline regression covers both missing and differing archived attempts.

All 242 collisions predate this continuation. For 68 legacy inputs, CRLF normalization reproduces the exact remote SHA-256. For 160 Lowdin records, LF normalization and removal of the local leading comment reproduces the exact remote SHA-256, demonstrating unchanged numerical content. The remaining fourteen files are historical metadata differences: seven JSON manifests lack later local rung, tranche, OOH-job, result or precondition updates; seven text files lack later launch/result/correction comments or use an older cross-reference. The local versions remain authoritative for those later annotations, and exact remote versions are preserved under remote_versions/ so neither history is lost.

The 1,626 remaining non-excluded Anvil-only files total 2,121,982,019 bytes and reside under working scratch directories: 1,536 HEA restart intermediates, 88 hp.x response files, and two optimizer scratch files. They are explicitly inventoried and hash-recorded as remote-retained scratch. Large wavefunction, density, projection and pseudopotential classes remain covered by the established remote-retention policy; this audit does not claim their full local replication. The diagnostic's 27-file retained clone and accepted source were additionally rehashed on Anvil during its separate readout.

The result mirror reconciliation is complete within that stated retention policy. There are no unresolved missing scientific output records or competing numerical output versions in the audited scope. The earlier audit's zero missing .out count was correct for literal .out filenames but did not cover the archived compressed attempt; the broader check closes that blind spot without altering any calculation or acceptance rule.

Evidence is in [results/anvil_mirror_2026-09-12](../../results/anvil_mirror_2026-09-12/): remote_inventory.json, local_inventory.json, initial_comparison.json, recovery.json, collisions.json, retained_scratch.json and reconciliation.json. The independent verification covers exact file hashes and historical classification; it does not convert the remote scratch inventory into a claim of local backup.
