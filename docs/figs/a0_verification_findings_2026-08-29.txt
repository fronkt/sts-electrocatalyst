A0 tranche 2c wave-4 adversarial audit -- 2026-08-29
======================================================================

Harness: 5 auditors on distinct dimensions (registration integrity;
numbers+QC; the Ti geometric diagnosis and re-anchored deck; the readout
code changes; docs-vs-evidence), one adversarial refuter per finding
instructed to default to real=false, then one completeness critic asked
what nobody had been assigned. 59 agents, 5.95M tokens, 1301 tool calls.

RAISED 53  |  REFUTED 38  |  SURVIVED 15  |  SWEEP 9

######################################################################
SURVIVED ADVERSARIAL REFUTATION
######################################################################

----------------------------------------------------------------------
[1] BLOCKER  (registration dimension)
TITLE: The readout banks "all 3 registered repairs also failed" for Fe u450 while two of the three are still in flight
WHERE: src/dft/a0main_readout.py:333-336

CLAIM:
The `elif cand:` branch collapses every non-winning repair candidate into "failed", using len(cand) rather than the `lost` list it just built, which already distinguishes "not run" from "failed QC" (lines 296-301). With s0_O__u450__r2 and __r2b launched but not yet returned, the banked Fe row asserts that all three registered repairs failed - i.e. that A6.5(2)'s escalation ladder is EXHAUSTED at Fe U=4.5 and rung (iii) (terminal: "recorded NOT_CONVERGED and plotted as a gap") has been reached. The same script gets this right for Ti, where it prints "7 with no output banked (not yet run; not a convergence event)".

EVIDENCE:
Banked row from a fresh run of the current script:
  {"u": 4.5, "gap": ["s0_O"], "gap_why": ["qc-fail (all 3 registered repairs also failed: s0_O__u450__r1, s0_O__u450__r2, s0_O__u450__r2b)"]}
On disk: `ls runs/a0/main/Fe/` shows s0_O__u450__r2.in and s0_O__u450__r2b.in (both written 15:16) and NO corresponding .out; commit 1c60cc2 records them as launched under array 20214003. Only s0_O__u450__r1.out exists and it genuinely failed (200 "iteration #" blocks, "convergence NOT achieved").
Code: `why[st] = ("qc-fail (all %d registered repairs also failed: %s)" % (len(cand), ", ".join(cand)))` - a0main_readout.py:334-336.

FIX AS PROPOSED:
Use the `lost` list that is already built, and only claim ladder exhaustion when every registered candidate actually has an output. Replace `src/dft/a0main_readout.py:333-336`:

```python
                    elif cand:
                        failed = [j for j, _b, c in lost if c == "failed QC"]
                        pending = [j for j, _b, c in lost if c == "not run"]
                        if pending:
                            why[st] = ("qc-fail (%d of %d registered repairs "
                                       "failed: %s; %d NOT YET RUN, A6.5(2) "
                                       "escalation NOT exhausted: %s)"
                                       % (len(failed), len(cand),
                                          ", ".join(failed) or "none",
                                          len(pending), ", ".join(pending)))
                        else:
                            why[st] = ("qc-fail (all %d registered repairs also "
                                       "failed: %s -- A6.5(2)(iii) NOT_CONVERGED)"
                                       % (len(cand), ", ".join(cand)))
```

Apply the same split at line 327-329 in the `won` branch so `"did not converge:"` lists only the `"failed QC"` candidates, with any `"not run"` ones reported separately as still pending (e.g. `"; still pending: %s"`). Then re-run and re-check `gap_why` before banking `docs/figs/a0main_readout.json` — with r2/r2b still queued the Fe U=4.5 row must read as escalation-in-progress, not as the terminal rung.

----------------------------------------------------------------------
[2] MINOR  (registration dimension)
TITLE: A6.6's "zero relaxations / does not license any relaxation in any cell" is never addressed anywhere for the Ti arm
WHERE: docs/43-prereg-week1-factorial.md:1285-1289 vs docs/59-a0-roster-correction-2026-08-28.md:44-48 and 71-78; src/dft/build_a0main_w2c.py:build_ti_r2/build_ti_r3

CLAIM:
Amendment 6, the amendment that registers block 6A / A0-main, carries an explicit scope guard: "This amendment registers ~160 fixed-geometry SCFs and zero relaxations (~140 A0-main + 20 A0-cell) ... It does not license an oxyhydroxide tier, an SQS/HEA tier, re-derivation of the production tier at nonzero Ru/Ir U, or any relaxation in any cell." The Ti arm added under docs/59 is built on relaxations: slab + 3 adslab relaxations (tranche 3), s0_OOH_r1 (tranche 2b), and now s0_OOH_r2 + s0_OOH_r3 (tranche 2c) - seven relaxations inside block 6A. docs/59 s2 discloses the SCALE overage from the same A6.6 paragraph ("the registered '~140 SCFs' is exceeded ... Total ~= 243 jobs against '~140'. The scale clause was a budget estimate, not a cap") but never mentions the separate relaxation prohibition in the same sentence. No amendment licenses these relaxations: A7.4 gate (g) licenses exactly ONE TiO2 relaxation, in the 2x1v cell, for timing.

EVIDENCE:
grep -rn "zero relaxations|any relaxation in any cell" over docs/ and src/ returns only the two lines of docs/43 itself (1285, 1289) - no builder, no ledger entry, no docs/59 line, no readout caveat cites A6.6's relaxation clause. grep -rn "A6.6" over docs/58, docs/59, docs/45, tasks/todo.md and src/dft/build_a0main*.py returns only src/dft/build_a0main.py:54,257, both quoting A6.6's "no post-hoc refinement" logic, not its scope guard. Ti relaxations on disk: runs/Ti_slab/{slab,s0_O,s0_OH,s0_OOH,s0_OOH_r1}.out plus the new s0_OOH_r2.in / s0_OOH_r3.in.

FIX AS PROPOSED:
Add one dated sentence to docs/59 §3 (or §3c, beside the existing "NOT in the registration" paragraph), so the entrant countersigns it with the rest of the document. Nothing in code or in the readout changes, and banking is not held for it. Suggested text:

"**Calculation-class disclosure (added 2026-08-29).** A6.6 registers block 6A as '~160 fixed-geometry SCFs and zero relaxations' and states it 'does not license ... any relaxation in any cell' (docs/43:1285-1289). The Ti geometry chain — 4 relaxations (slab + 3 adslabs) plus 3 escalation repairs (s0_OOH_r1/r2/r3) — is new compute outside that footprint, and is stated here rather than absorbed. It is registered pre-launch as the geometry INPUT the A0 ladder stands on, not as A0 points: every Ti number scored is a fixed-geometry SCF and A6.4 is untouched. Its warrant is later and deposited: A7.2/A7.3 (2026-08-16) register a six-metal A0 census naming Ti as blind and condition A7.3's denominator on 'a converged *OOH geometry', and Ti has no geometry anywhere in the campaign; TiO2 relaxations are separately licensed for the 2×1v cell under A8/S3 (docs/54 §2.8, which lists the 1×1 corners as a GAP), so the 1×1 chain is the only route to the registered Ti rows. A6.1(b)'s reuse-the-banked-geometry rule binds A0-cell only; A6.1(a) states none."

----------------------------------------------------------------------
[3] MINOR  (registration dimension)
TITLE: "Moving where the walk starts cannot bias where it ends" contradicts the basin-gap rule two sentences later
WHERE: src/dft/build_a0main_w2c.py:115-122; docs/59-a0-roster-correction-2026-08-28.md:144-145; commit bcd3cc0 message

CLAIM:
The sentence carrying the "not tuned toward an outcome" defence - "A relaxation's answer is its local minimum; changing where the walk STARTS cannot bias where it ENDS" - is immediately followed by "If both converge, the LOWER total energy is the banked s0_OOH geometry ... and the difference is reported as the measured basin gap." If the start could not bias the end there would be no basin gap to measure, and no reason to build r3. As written the claim is false: the start selects which local minimum BFGS reaches, which is the stated purpose of the re-anchoring.

EVIDENCE:
build_a0main_w2c.py:115-117 "A relaxation's answer is its local minimum; changing where the walk / STARTS cannot bias where it ENDS..." immediately followed at :119-122 by "SELECTION RULE... the LOWER total energy is the banked s0_OOH geometry (docs/41 s6d, A6.4) and the / difference is reported as the measured basin gap." Same pairing at docs/59:144-145 and in the bcd3cc0 commit message.

FIX AS PROPOSED:
Replace the false general claim with the narrower, true one the file already uses for Fe (:53-55).

In src/dft/build_a0main_w2c.py, replace lines 115-118:

    A relaxation's answer is its local minimum; changing where the walk
    STARTS cannot bias where it ENDS, and the re-anchor distance is
    computed mechanically from Ti's own converged states, not chosen to
    produce an outcome.

with:

    Different starts CAN land in different local minima -- that is why
    both are run, and the energy difference between them is what the
    basin gap below measures. What is fixed in advance is the CHOICE
    RULE, not the answer: the re-anchor distance is computed mechanically
    from Ti's own converged Ti-O bonds (not chosen to produce an
    outcome), and the banked geometry is whichever of the two converges
    lower -- fixed independently of what it turns out to be.
    DISCLOSED LIMIT: two starts are not a basin search; a third start
    could in principle find a lower minimum than either.

Mirror the same replacement at docs/59-a0-roster-correction-2026-08-28.md:144-145, replacing "A relaxation's answer is its local minimum, so moving where the walk starts cannot bias where it ends." with a one-line form of the above, e.g. "Different starts can land in different minima -- that is the point of running both; what is fixed before launch is the choice rule (mechanical re-anchor distance, lower converged energy banked), not the answer, and two starts are not a basin search."

The bcd3cc0 commit message cannot be edited without rewriting history, which the pre-registration discipline forbids; record the correction as a dated erratum line in docs/59 s3c (and in docs/45 as a trap, alongside traps 8-10) noting that the sentence as committed in bcd3cc0 was wrong and is superseded by the wording above. Purely editorial -- no deck, no number, and no verdict changes, so it does not gate banking a0main_readout.json.

----------------------------------------------------------------------
[4] MINOR  (registration dimension)
TITLE: Rung (ii)'s force progress is reported as 0.281 -> 0.125 Ry/bohr; the file says 0.173 -> 0.125, with 0.281 belonging to the previous run
WHERE: src/dft/build_a0main_w2c.py:69-72; commit bcd3cc0 message

CLAIM:
The registration states "Rung (ii) ran as array 20204308: mixing_beta 0.15 banked TWO ionic steps -- the energy fell 0.023 Ry and the total force fell 0.281 -> 0.125 Ry/bohr". runs/Ti_slab/s0_OOH_r1.out contains exactly two "Total force" lines, 0.172552 and 0.124679. The 0.281208 value is the single force printed by runs/Ti_slab/s0_OOH.out, i.e. the pre-rung-(ii) run at the previous geometry. The energy figure (0.023 Ry) is correct.

EVIDENCE:
grep -a "Total force" runs/Ti_slab/s0_OOH_r1.out ->
     Total force =     0.172552     Total SCF correction =     0.003296
     Total force =     0.124679     Total SCF correction =     0.002153
grep -a "Total force" runs/Ti_slab/s0_OOH.out ->
     Total force =     0.281208     Total SCF correction =     0.002009
Energies in s0_OOH_r1.out: -1301.68764608 -> -1301.71065326 Ry (delta 0.0230 Ry, as stated).

FIX AS PROPOSED:
Two parts, because a registered document is not silently edited under docs/43 discipline.

1. Correct the prose at src/dft/build_a0main_w2c.py:68-69. Replace "the energy fell 0.023 Ry and the total force fell 0.281 -> 0.125 Ry/bohr" with the run's own numbers plus the parent's, e.g.: "the energy fell 0.023 Ry and the total force fell 0.173 -> 0.125 Ry/bohr over its own two SCF points (the 0.281 Ry/bohr often quoted for this walk is the PARENT run's force at the built 3.167 A geometry, one BFGS move before rung (ii) started; the three-run chain is 0.281 -> 0.173 -> 0.125)."

2. Add a dated "### CORRECTION" entry under docs/45's "## A0 tranche 2c" section (the file already uses this convention at :1516, :1709, :1803), recording that commit bcd3cc0's message misattributed the parent run's 0.281 Ry/bohr to rung (ii), that rung (ii)'s own progress is 0.173 -> 0.125 Ry/bohr with dE = -0.02301 Ry, and that nothing downstream consumed the number. The commit message itself cannot be rewritten, so the ledger entry is the correction of record.

3. Optional but consistent with the docstring's own promise: extend rederive() to pull both "Total force" values out of s0_OOH_r1.out and die if they are not 0.172552/0.124679, so this fact is re-derived from disk like the others rather than typed.

----------------------------------------------------------------------
[5] MINOR  (registration dimension)
TITLE: docs/59's opening scope citation points at the wrong line and a differently-scoped sentence
WHERE: docs/59-a0-roster-correction-2026-08-28.md:11

CLAIM:
docs/59 opens by asserting "Nothing in this document invents a threshold; every rule it cites is quoted from an already-deposited registration", then cites: 'docs/43:279 sets the scope rule: changes happen "only through its own dated amendment."' docs/43:279 is a row of the 1B hp.x internal-gate table. The quoted phrase appears at docs/43:932, inside amendment 5, and governs only the five DEFERRED literature items (D3, D5, D7, D9, D10): "Any of these may still happen, but only through its own dated amendment". It is not a global scope rule.

EVIDENCE:
grep -n "only through its own dated amendment" docs/43-prereg-week1-factorial.md -> single hit at line 932, inside "Deferred and NOT registered: D3 ... D10". `git show ef56943:docs/43-prereg-week1-factorial.md | sed -n '275,283p'` (the state when docs/59 was written) shows lines 275-279 are the q-mesh / response-matrix / perturbation-amplitude / symmetry-equivalent gate table - so the line number was wrong at authoring time too, not a later drift.

FIX AS PROPOSED:
Edit docs/59-a0-roster-correction-2026-08-28.md:11 to cite the rule that actually exists and to stop borrowing amendment 5's deferred-item sentence as a global rule. Replace:

  docs/43:279 sets the scope rule: changes happen "only through its own dated amendment."

with:

  docs/43:4-5 sets the scope rule: "Nothing in this document may be edited after that deposit; corrections go in a dated addendum at the bottom with the reason." (docs/43:932's "only through its own dated amendment" is amendment 5's rule for the deferred literature items D3/D5/D7/D9/D10 specifically; the practice it names — every amendment dated and committed before the jobs it governs run — is what this document follows.)

No other text in docs/59 changes; the section-1 argument is unaffected because the rule it needs is genuinely registered at docs/43:4-5. While editing, spot-check the document's remaining pinpoint citations against the current docs/43 (A6.3:1244 and qe_slab.py:175 both resolve correctly as written).

----------------------------------------------------------------------
[6] MAJOR  (arithmetic dimension)
TITLE: Ti's 7 grid holes are labelled "not a convergence event" when their cause is two failed SCF relaxations
WHERE: src/dft/a0main_readout.py:398-401

CLAIM:
Ti's whole grid is a hole because no s0_OOH deck exists; the readout classifies all 7 as `n_abs` and prints "7 with no output banked (not yet run; not a convergence event)". The reason no deck exists is that the Ti *OOH relaxation SCF-failed twice. The parenthetical is a causal claim about the campaign's single most consequential non-convergence, and it is false.

EVIDENCE:
Readout stdout: "GAPS: 7 point(s) -- 7 with no output banked (not yet run; not a convergence event)."
qe_qc scan of the upstream relaxations:
  runs/Ti_slab/s0_OOH.out    verdict=POISONED  n_scf_fail=1  reasons=['1 SCF cycle(s) did not converge (ionic step(s) [1])', "no 'bfgs converged' line", 'free-atom fmax 0.1429 Ry/au (3.674 eV/A) > 0.01 Ry/au']
  runs/Ti_slab/s0_OOH_r1.out verdict=POISONED  n_scf_fail=1  reasons=['1 SCF cycle(s) did not converge (ionic step(s) [2])', ..., 'free-atom fmax 0.0660 Ry/au (1.696 eV/A) > 0.01 Ry/au']
A8.4 (docs/43:1594-1603): "A dropped non-convergence is a selection effect: the states that fail are the magnetically frustrated ones -- exactly the ones carrying the effect."

FIX AS PROPOSED:
Two small edits in src/dft/a0main_readout.py — stop asserting causality the readout cannot see, and make the cause travel into the artifact.

1) src/dft/a0main_readout.py:400-401 — drop the unfounded clause:

    if n_abs:
        msg += (f" -- {n_abs} with no A0 deck present in runs/a0/main/{metal} "
                f"(this readout sees only that directory and cannot tell an "
                f"unrun point from one whose upstream geometry is blocked -- "
                f"see runs/a0/main/manifest.json, docs/45, docs/59)")

2) src/dft/a0main_readout.py:275-278 — source the reason from the manifest the builders already write, so it lands in `gap_why` and therefore in docs/figs/a0main_readout.json (docs/45 wave-2 trap 4, same rule already honoured for the XU-ANCHOR label):

    if not os.path.exists(p):
        E[st] = None
        why[st] = _blocked_why(metal, st) or "absent"
        continue

where `_blocked_why` reads runs/a0/main/manifest.json once and, for (Ti, s0_OOH), returns the gate text already banked there, e.g.:
    "absent -- UPSTREAM GEOMETRY BLOCKED BY A CONVERGENCE FAILURE: the Ti
     s0_OOH relax failed SCF twice (A6.5(2)(i) inapplicable to a relaxation;
     (ii) beta 0.15 limit-cycled at ~1.3e-4 Ry); diagnosis geometric
     (built 3.167 A off the nearest Ti, walking UP to 3.414 A); repairs
     s0_OOH_r2 / s0_OOH_r3 in flight (manifest tranche_2c). Not 'not yet run'."

Then count these separately from genuinely unrun holes in the GAPS line (n_abs / n_blocked / n_fail), so the A8.4 failure accounting is not silently absorbed into a scheduling bucket.

----------------------------------------------------------------------
[7] MINOR  (arithmetic dimension)
TITLE: A8.5 cross-machine threshold mis-converted by 10x in the control comment, and the "reported against both" claim is not implemented
WHERE: src/dft/a0main_readout.py:80-82

CLAIM:
The comment reads "A8.5 cross-machine parity, threshold 1e-5 Ry = 0.014 meV -- tighter than the 5 meV extraction tolerance, reported against both". 1e-5 Ry x 13.605693122 eV/Ry = 1.3606e-4 eV = 0.136 meV, not 0.014 meV (0.014 meV is 1e-6 Ry). Separately, no code path compares any drift against a Ry threshold: `ok` is set solely by `abs(d) > args.tol_mev` (default 5.0), and neither stdout nor the JSON carries a 1e-5 Ry pass/fail.

EVIDENCE:
docs/43:1612-1616 gives the registered threshold and its own correct conversion: "an Anvil re-run of a banked deck agrees when |dE| <= 1e-5 Ry... Against its own parent, the same Anvil number agrees to 6.7e-7 Ry (0.009 meV)" -- 6.7e-7 x 13.6057 = 0.0091 meV, consistent, confirming the 1e-5 -> 0.014 meV line is the error. Code: line 238 `if abs(d) > args.tol_mev: ok = False` is the only threshold test. Measured cross-machine drifts (own extraction): Mn worst 6.4e-7 Ry (0.0087 meV), Fe worst 1.5e-7 Ry (0.0020 meV) -- both pass under either number, so no verdict changes today.

FIX AS PROPOSED:
Two one-line edits in src/dft/a0main_readout.py, no behaviour change needed to bank:

1. Line 80-82, correct the conversion and name the provenance of 0.014 so it cannot be re-conflated:

    # machine (determinism); Mn/Fe re-run Vast audit decks on Anvil (A8.5
    # cross-machine parity, registered threshold |dE| <= 1e-5 Ry = 0.136 meV,
    # docs/43:1614 -- 37x tighter than the 5 meV extraction tolerance this
    # script enforces. (docs/46's 0.0143 meV is the MEASURED platform delta,
    # not the threshold.) Only the 5 meV tolerance is scored here; the Ry
    # drift is printed for the reader.

2. Either drop the "reported against both" claim (as done above), or implement it — after line 239, additionally record the Ry drift and an A8.5 flag for cross-machine metals, e.g.:

    drift_ry[st] = a0 - pb
    if cfg["control_class"].startswith("cross-machine") and abs(a0 - pb) > 1e-5:
        a85_ok = False

and add `rerun_drift_Ry=drift_ry, a8_5_parity_ok=a85_ok` to the m_out dict at line 409. If implemented, also widen the printed drift format at line 241 from '%+.2f' to '%+.4f' meV so the printed number can resolve the tighter gate. Nothing in docs/figs/a0main_readout.json needs correcting — the wrong figure never reached it.

----------------------------------------------------------------------
[8] MINOR  (ti-geometry dimension)
TITLE: The sentence licensing r3's single start is falsified four times by the campaign's own measurements
WHERE: src/dft/build_a0main_w2c.py:115-118 (and docs/59-a0-roster-correction-2026-08-28.md:144-145)

CLAIM:
build_a0main_w2c.py:115 asserts "A relaxation's answer is its local minimum; changing where the walk STARTS cannot bias where it ENDS". This is the load-bearing claim: it is what makes a SINGLE re-anchored start sufficient and what licenses banking r3's answer as Ti's *OOH geometry under the pre-declared selection rule at :118-124 ("If exactly one converges it is the geometry"). The campaign has already MEASURED the opposite, in this exact system (rutile(110) cus site) and for this exact adsorbate, at least four times. None of the four is cited anywhere in the tranche-2c registration.

EVIDENCE:
src/hea_oer/surfaces_rutile.py:195-199: "The adsorbate therefore lands 3.07-3.13 A off the cus metal it is supposed to bind. From that start the 2026-07 DFT campaign trapped Cr's `*O` at 2.016 A (1.396 eV above the true minimum) and left `*OOH` desorbed on Mn, Fe *and* Ni -- four chemically-wrong structures that passed every numerical QC check".
docs/34-prereg-sixth-point.md:49-52: "Multi-start is not decoration: Cr's `*OOH` relaxes to a **desorbed 3.013 A** from the builder start but to 1.951 A and 0.088 eV lower when pulled in".
docs/34:115: "| `Ni_slab/s0_OOH` | TRUSTWORTHY | 39 | **3.080 A** | **desorbed - must be re-run** |" (39 ionic steps, still unbound).
docs/34:123-126: "**The builder's `*OOH` placement is systematically wrong for 3d metals.** It puts the adsorbate 3.07-3.13 A out, and from there DFT has now failed on Mn, Fe *and* Ni -- and MACE says Co would fail the same way (builder start -> 2.983 A; pulled-in start -> 2.105 A at **0.427 eV lower**)."
Citation check: `grep -rn "adsorbate_starts|multi-start|single-start|PULL_TO|docs/34|docs/36" docs/59-...md src/dft/build_a0main_w2c.py src/dft/build_a0main_w3.py runs/Ti_slab/manifest.json runs/Ti_slab/m_ti_relax_r2.txt` -> zero hits.

FIX AS PROPOSED:
Doc-only edit; no deck, number or verdict changes. Replace build_a0main_w2c.py:115-118 (and the same sentence at docs/59:144-145) with the true version plus the coverage limit:

    A start selects WHICH local minimum the walk reaches -- measured on this
    exact system: the builder placement left *OOH desorbed on Mn, Fe and Ni,
    and pulled-in starts found minima 0.088-0.427 eV lower (docs/34 s4b,
    surfaces_rutile.adsorbate_starts:189-205). That is why TWO starts run and
    the LOWER converged energy is banked: the re-anchor distance is derived
    mechanically from Ti's own converged states, so adding a start can only
    move the banked energy downhill, never toward a chosen value.
    DISCLOSED LIMIT: r2 (3.414 A) and r3 (1.782 A) bracket but do not sample
    the 2.20-3.00 A "weak" tier, where Fe *OOH's real minimum sat at 2.552 A,
    0.376 eV below the desorbed original (adsorbate_qc.py:33-44). If exactly
    one converges, its geometry is banked labelled SINGLE-START and untested
    against that tier.

Optional, cheap, and in the campaign's own idiom if a third deck is still affordable before Ti scores: add s0_OOH_r4 at PULL_TO 2.10 to cover the skipped tier. Not required to bank the readout, since Ti *OOH is 7/7 GAP and unscored.

----------------------------------------------------------------------
[9] MAJOR  (ti-geometry dimension)
TITLE: The banked readout's spin caveat says Ti has "no moment to order"; two of the four Ti states have odd electron counts, and it contradicts docs/59's own premise
WHERE: src/dft/a0main_readout.py:634-636

CLAIM:
The caveat that travels with every table states: "Ti runs nspin=1 as a d0 closed shell by construction (production convention; no moment to order, unlike the RuO2 case gate (h) measured)." Two of the four Ti states are not closed shell -- pw.x reports an ODD electron count for s0_OH and s0_OOH -- so under nspin=1 there is a half-occupied state, which is the exact opposite of "no moment to order". It is also directly contradicted by docs/59:134-135, which bases the whole Ti diagnosis on "the chain's nspin = 1 convention cannot spin-split the resulting half-occupied state". Both cannot be true, and this one is in the file being banked.

EVIDENCE:
`grep -m1 "number of electrons" runs/Ti_slab/*.out`:
  slab      144.00  (even)
  s0_O      150.00  (even)
  s0_OH     151.00  (ODD)
  s0_OOH    157.00  (ODD)
And s0_OH -- odd-electron, started at the same 3.167 A -- converged with no pathology: `bfgs converged in 56 scf cycles and 52 bfgs steps` (runs/Ti_slab/s0_OH.out:44281), so odd-electron-count alone also does not support docs/59:134's causal story.

FIX AS PROPOSED:
Text-only edit in src/dft/a0main_readout.py, replacing the final sentence of the spin_state caveat (lines 634-636). Delete "Ti runs nspin=1 as a d0 closed shell by construction (production convention; no moment to order, unlike the RuO2 case gate (h) measured)." and substitute a statement that keeps the construction fact, states the measured electron counts, and preserves docs/59's open question, e.g.:

  "Ti runs nspin=1 by construction (qe_slab.py emits nspin=2 only where a "
  "species carries a non-zero starting magnetization; Ti and O carry none). "
  "That is a SUBSTRATE d0 argument and does NOT extend to the adsorbates: "
  "pw.x reports 151 and 157 electrons for Ti s0_OH and s0_OOH -- both odd, "
  "so both are open-shell and nspin=1 cannot spin-split them. docs/59 s3c "
  "registers exactly this as the diagnosis of the Ti *OOH failure and leaves "
  "nspin=2 for the Ti arm as an OPEN question for the entrant; every Ti row "
  "here is spin-convention-conditional in a way the nspin=2 Cr/Mn/Fe rows "
  "are not."

No code-path, numeric, or schema change; the same run then banks. (Optional, not required to clear the gate: since the readout already carries totmag per row, nothing else needs to move -- Ti rows return None there, which is itself consistent with nspin=1 and needs no extra note.)

----------------------------------------------------------------------
[10] MAJOR  (readout-code dimension)
TITLE: Repairs that have not run are banked as "failed" — Fe u=4.5 records 3 convergence failures where only 1 exists
WHERE: src/dft/a0main_readout.py:333-336

CLAIM:
The new `elif cand:` branch builds its why-string from `cand` (every registered repair name) instead of from `lost`, so a repair with no `.out` on disk is reported as having failed. The code already computed the correct reason two lines up — `lost.append((j, None, "not run"))` at :298 vs `lost.append((j, None, "failed QC"))` at :302 — and then throws `lost` away entirely in this branch. Tranche 2c's r2/r2b were launched at 1c60cc2 and have not returned, yet they are counted as failures.

EVIDENCE:
Readout JSON gaps for Fe:
  [[4.5, ["s0_O"], ["qc-fail (all 3 registered repairs also failed: s0_O__u450__r1, s0_O__u450__r2, s0_O__u450__r2b)"]]]
On disk (`ls runs/a0/main/Fe/`):
  s0_O__u450__r1.in   s0_O__u450__r1.out    <- ran, failed (grep -c 'convergence NOT achieved' = 1)
  s0_O__u450__r2.in                          <- NO .out
  s0_O__u450__r2b.in                         <- NO .out
Only r1 has produced any output at all.

FIX AS PROPOSED:
Derive the why-string from `lost` (which already carries the correct per-repair reason) instead of from `cand`, and keep the two classes apart. Replace src/dft/a0main_readout.py:333-336:

    elif cand:
        ran  = [a for a, _b, c in lost if c != "not run"]
        pend = [a for a, _b, c in lost if c == "not run"]
        why[st] = ("qc-fail (%d of %d registered repairs failed: %s%s)"
                   % (len(ran), len(cand), ", ".join(ran) or "none",
                      ("; NOT YET RUN, ladder not exhausted: "
                       + ", ".join(pend)) if pend else ""))

With the current disk state that yields "qc-fail (1 of 3 registered repairs failed: s0_O__u450__r1; NOT YET RUN, ladder not exhausted: s0_O__u450__r2, s0_O__u450__r2b)", which is true and makes clear A6.5(2)(iii) is not yet reachable.

Same edit should fix the twin at :328-331, which labels the whole lost list "did not converge:" even for entries whose reason is "(not run)" — split it into "did not converge: ..." and "not yet run: ..." from the same two partitions.

No other change is needed; n_fail at :396-398 already counts gap points correctly and the substring test still matches the new string.

----------------------------------------------------------------------
[11] MINOR  (readout-code dimension)
TITLE: A control deck that RAN AND DIED is now reported as "not yet run" instead of UNTRUSTED
WHERE: src/dft/a0main_readout.py:226-233

CLAIM:
`_final_ry` (:125-132) returns None both when the file is absent and when the file exists but contains no `!  total energy` line — which is exactly what an SCF that hit electron_maxstep produces. The new branch routes both into `uncompared` and :259-263 prints the unverified positive claim "no output on one side yet ... the control completes when those states run". Before this change that case set `ok = False` and the metal was marked UNTRUSTED. The code never distinguishes `os.path.exists(path)` False from `_final_ry` returning None on an existing file, and never QC-checks the control decks at all (unlike the grid, which uses `qc.trusted_energy_ev(strict=True)`).

EVIDENCE:
This failure class exists on disk in this very campaign:
  runs/a0/main/Fe/s0_O__u450.out    : bang_lines=0  scf_notconv=1
  runs/a0/main/Fe/s0_O__u450__r1.out: bang_lines=0  scf_notconv=1
  runs/a0/main/Fe/s0_O__u300.out    : bang_lines=0  scf_notconv=1
All three exist, all three yield `_final_ry -> None`. Probe over every control pair confirms the currently-uncompared case is genuinely absent on both sides (`Ti s0_OOH a0_exists=False cmp_E=None cmp_exists=False`), so the regression is latent, not live.

FIX AS PROPOSED:
Keep the third state the code is missing: a deck that RAN and produced no energy is a convergence event, not pending data. Capture the comparator path (it is currently thrown away in both branches) and split the None case at a0main_readout.py:226.

    for st in STATES:
        a0p = os.path.join(ROOT, "runs", "a0", "main", metal, f"{st}__{tok}.out")
        a0 = _final_ry(a0p)
        ov = cfg.get("control_override", {}).get(st)
        if ov is not None:
            _, ov_stem, ov_why = ov
            cmpp = os.path.join(ROOT, "runs", "a0", "main", metal, f"{ov_stem}.out")
            override_notes[st] = f"comparator {ov_stem}: {ov_why}"
        else:
            cmpp = os.path.join(ROOT, "runs", "probe", probe_dir,
                                f"{st}__{probe_sfx}.out")
        pb = _final_ry(cmpp)
        if a0 is None or pb is None:
            drift[st] = None
            dead = [p for p, v in ((a0p, a0), (cmpp, pb))
                    if v is None and os.path.exists(p)]
            if dead:
                # the deck ran and produced no '!  total energy': an SCF
                # failure, an A8.4 event -- never "not yet run".
                ok = False
                control_failed[st] = dead
            else:
                uncompared.append(st)
            continue

with `control_failed = {}` initialised beside `uncompared`, a corresponding printed line ("control deck(s) RAN AND FAILED (no energy): ... -- this metal's rows are reported but marked UNTRUSTED until reconciled") emitted under the existing `if not ok:` branch, and `control_failed_states=control_failed or None` added to `m_out` so the distinction travels into docs/figs/a0main_readout.json alongside `control_uncompared_states`. Also tighten the :259-263 wording so the "not yet run" narrative is only ever printed for states where neither file exists.

----------------------------------------------------------------------
[12] MINOR  (readout-code dimension)
TITLE: The A8.5 1e-5 Ry cross-machine threshold is documented as "reported against both" but is never computed or printed
WHERE: src/dft/a0main_readout.py:82

CLAIM:
The METALS comment (:79-82) says Mn/Fe re-run Vast decks on Anvil under "A8.5 cross-machine parity, threshold 1e-5 Ry = 0.014 meV -- tighter than the 5 meV extraction tolerance, reported against both". The only comparison in the code is `if abs(d) > args.tol_mev` (:239) with default 5.0 meV; the printed line and the JSON carry the drift and `control_ok` against that single tolerance. Neither 1e-5 Ry nor a pass/fail against it appears anywhere in the output.

EVIDENCE:
Recomputed from the banked drifts: Mn slab +3.4e-07 Ry, s0_O +4.8e-07, s0_OH +3.4e-07, s0_OOH +6.4e-07; Fe +1.5e-07/+1.5e-07/+9.0e-08/-2.9e-07 Ry — all far inside 1e-5 Ry, so nothing is currently mis-passed. Printed verdict for both metals is the 5 meV `OK`.

FIX AS PROPOSED:
Two-line minimum: fix the arithmetic and stop claiming the report. At src/dft/a0main_readout.py:80-82 replace "threshold 1e-5 Ry = 0.014 meV ... reported against both" with "threshold 1e-5 Ry = 0.136 meV (0.014 meV is docs/46's MEASURED panel delta, 1.05e-6 Ry, not the threshold); A8.5's own enforcement is anvil/30-32_parity*.slurm's TOL_RY under the PARITY_PASS gate, NOT this script -- the verdict printed below is the 5 meV extraction tolerance only."

Better, and nearly as small -- actually compute it, so the stricter class is scored where it is named. Add `ap.add_argument("--tol-ry-xmach", type=float, default=1e-5)` beside :165; keep the raw Ry delta at :237 (`dry = a0 - pb; d = dry * qc.RY_EV * 1000`); accumulate `xm_ok = xm_ok and abs(dry) <= args.tol_ry_xmach` for metals whose `control_class` starts with "cross-machine" and whose state is not redirected by `control_override` (Fe s0_OOH's comparator is same-machine, both Anvil, so it must be excluded from the A8.5 arm); then append to the printed line and to `m_out` a second pair, e.g. `control_drift_ry` and `control_ok_a85`, with the bound named in the text. On today's evidence this prints PASS for both metals at 20-100x margin, so it changes no verdict -- it closes the hole for a future drift in (0.136, 5.0) meV.

----------------------------------------------------------------------
[13] MAJOR  (docs-vs-evidence dimension)
TITLE: Readout reports two never-launched decks as "failed" — would bank a false convergence outcome
WHERE: src/dft/a0main_readout.py:333-336 (and the same bug at :328-331)

CLAIM:
The repair-selection code builds a `lost` list that correctly distinguishes "not run" (:298) from "failed QC" (:302), but only uses that distinction in the success branch. In the failure branch it collapses both into "qc-fail (all N registered repairs also failed: ...)". Because the new multi-manifest registry globs m_a0_repairs*.txt, Fe s0_O u450 now has three candidates: r1 (really failed QC) plus r2 and r2b, which were launched in 1c60cc2 and have produced no output at all. The readout asserts all three failed. The success branch has the same defect: `"; did not converge: %s"` is applied to entries whose reason string is "not run".

EVIDENCE:
Rendering the artifact (`--json` to a scratch path) gives:
  /metals/Fe/rows/3/gap_why/0 => "qc-fail (all 3 registered repairs also failed: s0_O__u450__r1, s0_O__u450__r2, s0_O__u450__r2b)"
  /metals/Fe/gaps/0/2/0     => same string
On disk: `ls runs/a0/main/Fe | grep u450` -> s0_O__u450__r1.in, s0_O__u450__r1.out, s0_O__u450__r2.in, s0_O__u450__r2b.in. There is no s0_O__u450__r2.out and no s0_O__u450__r2b.out. Both were submitted as array 20214003 in commit 1c60cc2 and are still in flight.

FIX AS PROPOSED:
Stop collapsing "not run" into "failed", in both branches of src/dft/a0main_readout.py. `lost` already carries the reason as element [2]; just partition on it.

Failure branch, replace :333-336:

                    elif cand:
                        failed = [a for a, _b, c in lost if c == "failed QC"]
                        pending = [a for a, _b, c in lost if c == "not run"]
                        msg = "qc-fail"
                        if failed:
                            msg += (" (%d registered repair(s) also failed: %s)"
                                    % (len(failed), ", ".join(failed)))
                        if pending:
                            msg += ("; %d registered repair(s) NOT YET RUN, "
                                    "outcome PENDING (A6.5(2)(iii) not yet "
                                    "determinable): %s"
                                    % (len(pending), ", ".join(pending)))
                        why[st] = msg

Success branch, replace :328-331 with the same split so a pending sibling is never described as having failed:

                        failed = [a for a, _b, c in lost if c == "failed QC"]
                        pending = [a for a, _b, c in lost if c == "not run"]
                        if failed:
                            note += "; did not converge: %s" % ", ".join(failed)
                        if pending:
                            note += "; not yet run: %s" % ", ".join(pending)

Nothing else changes: n_fail's substring test on "qc-fail" still matches the new failure-branch string (it is the leading token), so the reported gap and failure counts are unchanged. After the fix, re-run and confirm /metals/Fe/gaps names only s0_O__u450__r1 as failed and lists r2/r2b as pending, then bank. Re-run once tranche 2c drains so the selection rule in build_a0main_w2c.py decides the u450 point on its own evidence.

----------------------------------------------------------------------
[14] MAJOR  (docs-vs-evidence dimension)
TITLE: "plateaued at ~1.5e-5 Ry ... for all 200 iterations" describes only the last ~17 iterations
WHERE: src/dft/build_a0main_w2c.py:18-19; docs/59-a0-roster-correction-2026-08-28.md:112; docs/45-error-ledger.md:2234; runs/a0/m_a0_repairs2.txt:2 (the header of the manifest that was actually launched)

CLAIM:
Rung (i) on Fe s0_O u450 is characterised as sitting flat at ~1.5e-5 Ry for the whole 200-iteration run — the evidence offered for "a density seed can put a point ON a branch but cannot break a tie between two". The trajectory shows a wildly oscillating residual, not a plateau; ~1.5e-5 is a last-lines reading generalised to the run.

EVIDENCE:
runs/a0/main/Fe/s0_O__u450__r1.out, all 200 `estimated scf accuracy` values:
  median of iterations 101-200 = 5.659e-04 (40x the quoted plateau)
  102 of 200 iterations are above 1e-3
  max after iteration 50 = 7.928e-02
  last 40 values still include 1.22e-02, 4.24e-03, 2.28e-03, and a 4.77e-03 spike 7 iterations from the end
  only the final 17 values lie in the 1.2e-5 - 4e-5 band; final value 1.892e-05; best value in the whole run 1.233e-05 at iteration 197
For contrast the same documents describe the UNSEEDED u450 as "oscillating 1.8e-6 -> 1.1e-5 Ry" (its own median over iterations 101-200 is 2.843e-03).

FIX AS PROPOSED:
Replace the false clause in the four editable locations — `src/dft/build_a0main_w2c.py:18-19` (docstring), `src/dft/build_a0main_w2c.py:450-452` (HDR_FE), `docs/59-a0-roster-correction-2026-08-28.md:111-113`, `docs/45-error-ledger.md:2234` — with a description that matches the trajectory. Suggested wording, every figure verifiable from `runs/a0/main/Fe/s0_O__u450__r1.out`:

"held totmag 21.98 (190/200 iterations; excursions only at it. 145-149, max 22.08) but never converged against conv_thr 1e-6. The residual did NOT plateau: it swung 3.8 decades over the run (102/200 iterations above 1e-3, median of it. 101-200 = 5.7e-4, spike to 7.9e-2 at it. 147) and only settled into a flat 1.2-4.2e-5 band over the final 17 iterations, still >12x conv_thr. The moment is what held; the charge is what would not converge."

Then adjust the inference that clause supports, in the same three prose places: the near-degeneracy argument rests on the LADDER moments (u300 22.90 vs u530+ 21.98), not on this run's behaviour — within r1 the moment never hunted between branches, so state the tie as a ladder-level inference, not as something the run exhibited.

Two housekeeping items that belong in the same pass:
1. `runs/a0/m_a0_repairs2.txt` is a launched artifact and the commit messages of 7c84ec9/bcd3cc0 are immutable, so add a one-line dated correction under the docs/45 tranche-2c entry recording that the "plateaued ... for all 200 iterations" characterisation was a last-lines reading and pointing at the corrected numbers. Regenerating the manifest header from the fixed HDR_FE would change a launched file — leave it and let the correction note cover it.
2. The same last-line habit sits at `src/dft/build_a0main_w2b.py:35,308` and `docs/45-error-ledger.md:2192` ("accuracy oscillating 1.8e-6 -> 1.1e-5 Ry"): those are literally the final two values of a run whose it. 101-200 median is 2.8e-3 with spikes to 4.5e-2. "Oscillating" is at least the right qualitative word there, so this is MINOR, but correcting the range in the same edit is the honest completion of the fix.

----------------------------------------------------------------------
[15] MINOR  (docs-vs-evidence dimension)
TITLE: "force 0.281 -> 0.125 Ry/bohr" is attributed to rung (ii) but spans two different runs
WHERE: src/dft/build_a0main_w2c.py:67-68; bodies of commits 7c84ec9 and bcd3cc0

CLAIM:
The parenthetical "Rung (ii) banked TWO ionic steps -- the energy fell 0.023 Ry and the total force fell 0.281 -> 0.125 Ry/bohr" presents both numbers as rung (ii)'s. The energy figure is r1-internal; the force figure is not — 0.281 belongs to the original relax, before rung (ii) existed.

EVIDENCE:
runs/Ti_slab/s0_OOH_r1.out has exactly two "Total force" lines: 0.172552 and 0.124679, and two `!` total energies -1301.68764608 -> -1301.71065326 (a fall of 0.02301 Ry, matching the quoted 0.023). runs/Ti_slab/s0_OOH.out has a single "Total force = 0.281208" at the built geometry, and total energy -1301.63158987.

FIX AS PROPOSED:
In src/dft/build_a0main_w2c.py:67-68, make the two numbers share one scope. Minimal edit: "... banked TWO ionic steps -- the energy fell 0.023 Ry and the total force fell 0.173 -> 0.125 Ry/bohr (across the whole walk, 0.281 at the built geometry -> 0.125) -- and then the third step's SCF limit-cycled ...". Optionally have rederive() extract the "Total force" lines from s0_OOH.out and s0_OOH_r1.out the way it already extracts totmag and ionic-step counts, so the docstring's numbers are re-derived rather than typed. The commit bodies of 7c84ec9 and bcd3cc0 are immutable; if the record is to carry the correction, add one dated line to docs/45-error-ledger.md noting that the "0.281 -> 0.125" quoted there spans the parent run and the rung-(ii) continuation, and that rung (ii)'s own force reduction was 0.173 -> 0.125. No change is needed in docs/59 s3c, which quotes no force values.

######################################################################
COMPLETENESS CRITIC (found by asking what nobody was assigned)
######################################################################

----------------------------------------------------------------------
[S1] MAJOR
TITLE: A7.5's registered Mn condition ("either the AFM arm runs or every materials-facing Mn sentence is struck") is unmet, and the artifact banks a brand-new 8-point absolute eta(U) column for FM-initialised beta-MnO2 with no AFM label
WHERE: src/dft/a0main_readout.py:624-636 (spin_state caveat) vs docs/43-prereg-week1-factorial.md:1403-1407; src/dft/gen_rutile.py:66,95-97

CLAIM:
No auditor was assigned to A7.5, and Mn is new in this bank. docs/43:1405-1407 registers, verbatim: "beta-MnO2 is antiferromagnetic and `gen_rutile.py` initialises it FM -- either the AFM arm runs or every materials-facing Mn sentence is struck." The AFM arm has never run for Mn: `grep -rn "AFM|antiferromagn"` over build_a0main_w2.py, _w2b.py, _w2c.py, _w3.py, docs/59 and runs/a0/main/manifest.json returns ZERO hits, and gen_rutile.py has no AFM code path at all (:95-97 emits a single `starting_magnetization(1)` for the one metal species, so every rutile it builds is FM by construction). The readout's spin_state caveat -- the artifact's only magnetic-state disclosure -- carries the exactly analogous AFM finding for the RuO2 anchors ("gate (h) measured 4/4 ADOPT_AFM ... 0.033-0.064 eV") and says of Mn only "Cr, Mn and Fe run nspin=2". Nothing anywhere in the file records that the Mn column is in the wrong magnetic order for the real phase.

EVIDENCE:
docs/43:1403-1407 -- "Tier strata: TiO2, beta-MnO2, RuO2, IrO2 = REAL-AMBIENT-UNDISTORTED; ... beta-MnO2 is antiferromagnetic and `gen_rutile.py` initialises it FM -- either the AFM arm runs or every materials-facing Mn sentence is struck."  ||  src/dft/gen_rutile.py:66 -- `"MnO2": dict(a=4.404, c=2.876, u=0.3050, U=3.9, mass=54.938, mag=0.5),  # beta-MnO2 pyrolusite (real rutile)`; :95-97 -- `if p["mag"] > 0.0: spin_block = (f"  nspin = 2\n  starting_magnetization(1) = {p['mag']}\n" f"  starting_magnetization(2) = 0.0\n")` (one species, one sign = FM only).  ||  runs/a0/main/Mn/slab__u000.in: single `Mn` species, `starting_magnetization(1) = 0.5`; runs/a0/main/Mn/slab__u000.out:1686-1687 -- "total magnetization = 18.00 Bohr mag/cell / absolute magnetization = 18.76 Bohr mag/cell" (6 Mn x 3 muB, fully ferromagnetic).  ||  `grep -rn "AFM|antiferro" src/dft/build_a0main_w2.py build_a0main_w2b.py build_a0main_w2c.py build_a0main_w3.py docs/59-*.md runs/a0/main/manifest.json` -> no output.  ||  src/dft/a0main_readout.py:626-636 (banked spin_state) mentions AFM only for RuO2; the string "Mn" appears there once, in "Cr, Mn and Fe run nspin=2".  ||  docs/28-electrocatalyst-revival-plan.md:65 already knows: "beta-MnO2 is AFM (we ran FM starts everywhere)".

CONSEQUENCE:
The caveat block is the declared mechanism by which labels travel into the report ("CAVEATS (registered + measured; they travel with every table above)", a0main_readout.py:613; docs/45 wave-2 trap 4). Banking as-is puts eight absolute eta(Mn) values -- 0.740 to 1.520 V, including both Mn pls flips that the A7.2 CONFIRMED census now counts -- into the report's input artifact with no signal that A7.5's Mn precondition is unsatisfied. beta-MnO2 is one of only four metals A7.5 classes REAL-AMBIENT-UNDISTORTED, so it is precisely the column a reader will read as a materials claim. Fix is text-only (add the A7.5 Mn condition to the spin_state caveat, or a `phase_reality` caveat), but it must be in the file that gets banked.

----------------------------------------------------------------------
[S2] MAJOR
TITLE: A7.3 (P-FLOOR-U) is decidable from the exact rows being banked, currently stands at 3 of 5 -- below its registered >=4 threshold -- and appears nowhere in the artifact, which reports only the sibling prediction that CONFIRMED
WHERE: src/dft/a0main_readout.py:581-608 (a7_2 block; no a7_3 anywhere) vs docs/43-prereg-week1-factorial.md:1361-1379 and docs/59-a0-roster-correction-2026-08-28.md:174-177

CLAIM:
docs/43:1368-1371 registers P-FLOOR-U: "Quantity: span(c_M)/2 in volts, at FIXED endpoints U = 0 and U = U_max ... PREDICTION: span(c_M)/2 exceeds 0.10 V on >=4 of the 6 metals with a converged *OOH geometry." The readout already computes c_M = dG_OOH - dG_OH at :347 for the A7.2 identity check, and every endpoint it needs is in the banked rows. Computed from the fresh JSON: Cr 0.3435, Mn 0.6307, Fe 0.6102 clear 0.10 V; Ru 0.0922 and Ir 0.0637 do not; Ti has no endpoints. With Ti's *OOH geometry unconverged the registered denominator shrinks to 5 (the campaign's own wording, registered in three pre-launch places), so the count is 3 -- the >=4 threshold is NOT met, and it is not FALSIFIED either (falsification needs <=1). Ti is therefore the single deciding metal, and its two repair decks are still in flight. The banked JSON carries `a7_2: {status: CONFIRMED}` plus a robustness note, and no `a7_3` key at all. `grep -n "A7.3" src/dft/a0main_readout.py` returns exactly two hits, both incidental strings (:198 arm label, :653 caveat text) -- no computation.

EVIDENCE:
Recomputed from the freshly generated JSON: `Cr c_M(0)=3.4437 c_M(9)=2.7567 span/2=0.3435 EXCEEDS 0.10 | Ru 3.1801/2.9956 -> 0.0922 below | Ir 3.6523/3.5249 -> 0.0637 below | Mn 3.6172/2.3558 -> 0.6307 EXCEEDS | Fe 3.5181/2.2977 -> 0.6102 EXCEEDS | Ti NO ENDPOINTS`.  ||  Banked-JSON top-level keys (fresh run): `['metals', 'ordering', 'a7_2', 'caveats', 'gas_reference_disclosure']` -- no `a7_3`.  ||  The denominator-shrink consequence is registered pre-launch in three places: src/dft/build_a0main_w3.py:49-50 "A7.3's own registered text shrinks the span denominator (\"a converged *OOH geometry\" conditions it)"; src/dft/build_a0main_w2c.py:124; runs/Ti_slab/m_ti_relax_r2.txt:21.  ||  docs/59-a0-roster-correction-2026-08-28.md:174-177 -- "**A7.3 (span(c_M)/2 at fixed endpoints)** becomes scorable on the full 6-metal denominator once Ti's *OOH converges; its own text conditions the denominator on 'a converged *OOH geometry', so a Ti convergence failure shrinks the denominator rather than blocking the readout."  ||  docs/43:1437-1438 (P-DISPOSITION) -- "Any prediction not scored by Oct 15 is marked WITHDRAWN-UNSCORED with its date."  ||  runs/Ti_slab/: s0_OOH_r2.in and s0_OOH_r3.in exist (15:16), no .out -- array 20214014 still in flight.

CONSEQUENCE:
docs/figs/a0main_readout.json is the artifact the report cites for the A0 verdicts. As it will be banked, it scores exactly one of the two registered predictions this grid was built to decide, and that one reads CONFIRMED with a monotonicity note saying it can only get stronger. The other -- registered on the same rows, named twice inside the file's own caveat text, and explicitly named in docs/59 as what the blind-metal extension was for -- currently falls one metal short, turns entirely on the one metal the same artifact reports as unscored, and is neither scored nor marked WITHDRAWN-UNSCORED. That is asymmetric registered-prediction reporting in a pre-registration artifact. (Distinct from the refuted 'cannot flip a verdict' finding, which was about a bound asserted in the registration text; this is about what the artifact does and does not report.)

----------------------------------------------------------------------
[S3] MAJOR
TITLE: The banked JSON carries no scoring timestamp, commit, or version, while its own caveat text is explicitly time-live ("Live status at scoring time") and one feeding array is still in flight -- and one a7_2 field has already silently changed meaning between banked versions
WHERE: src/dft/a0main_readout.py:709-716 (json.dump of `result`); result assembled at :173, :601-608, :660; docs/figs/a0main_readout.json

CLAIM:
The artifact has five top-level keys and none of them records WHEN it was scored, from what commit, or against which manifests. Its coverage_shortfall caveat nevertheless says "Live status at scoring time: Mn scored complete; Fe scored complete; Ti NOT YET SCORED", and the Ti control note says "the control completes when those states run" -- statements whose truth value changes the moment array 20214014 (Ti s0_OOH r2/r3, launched at 1c60cc2, no .out on disk) drains. The file is also overwritten in place with no version discriminator, which is demonstrable: the copy currently on disk (mtime 2026-08-28 13:43) is a 3-metal version whose coverage_shortfall says the opposite thing ("Nothing exists for Mn/Fe/Ti; their A7.2/A7.3 rows are unrun, not failed"). Worse, the `a7_2.unrun_blind_metals` field survives across both versions but has silently changed meaning: it was `["Mn","Fe","Ti"]` when it meant "no data", and is now `[]` even though Ti still has no data (Ti moved to the new `pending_data` key, computed at :587-590 by a different test). A consumer keyed on the old field now reads "every blind metal ran".

EVIDENCE:
Fresh-run top-level keys: `['metals', 'ordering', 'a7_2', 'caveats', 'gas_reference_disclosure']` -- no generated_at / commit / manifest hash.  ||  `ls -la docs/figs/a0main_readout.json` -> `-rw-r--r-- 21302 Aug 28 13:43`; its `metals` keys are `['Cr','Ru','Ir']`.  ||  OLD caveats.coverage_shortfall: "... Nothing exists for Mn/Fe/Ti; their A7.2/A7.3 rows are unrun, not failed."  vs  NEW: "... Live status at scoring time: Mn scored complete; Fe scored complete; Ti NOT YET SCORED."  ||  OLD a7_2.unrun_blind_metals = ["Mn","Fe","Ti"];  NEW a7_2.unrun_blind_metals = [], a7_2.pending_data = ["Ti"]. Same key, opposite reading, same filename, no version field.  ||  a0main_readout.py:586-590 -- `unrun = [m for m in A72_ROSTER if m not in result["metals"]]` (Ti IS in result["metals"], so it drops out of `unrun`).  ||  runs/Ti_slab/s0_OOH_r2.in, s0_OOH_r3.in present, no matching .out -- the array that decides Ti's status has not returned.

CONSEQUENCE:
After banking, nothing inside the artifact distinguishes "Ti unscored as of 2026-08-29 with r2/r3 queued" from "Ti unscored, escalation exhausted" -- exactly the distinction the surviving BLOCKER on the Fe repair branch is about, one level up. Under docs/43's dated-record discipline the report's A0 evidence file cannot say what date its own live-status sentence describes, and a re-run tomorrow overwrites the record with no trace that the prior state existed. Adding `generated_utc`, the git SHA, and the mtimes/hashes of runs/a0/main/manifest.json + runs/a0/m_a0_repairs*.txt is a few lines and makes every time-live caveat self-dating.

----------------------------------------------------------------------
[S4] MAJOR
TITLE: coverage_shortfall's remediation chain names tranches 2/2b/3 but omits tranche 2c -- the only tranche that produced the banked Fe U=4.5 point, and therefore the only reason the same sentence can say "Fe scored complete"
WHERE: src/dft/a0main_readout.py:658-660 (caveats.coverage_shortfall), vs src/dft/build_a0main_w2c.py and runs/a0/m_a0_repairs2.txt:18-19

CLAIM:
The caveat exists specifically to record that the blind-metal coverage was extended without a dated amendment, and to name the pre-launch commits that license the extra compute: "tranches 2/2b/3 (build_a0main_w2/_w2b/_w3, each committed pre-launch) cover the blind metals." build_a0main_w2c.py (tranche 2c, registered at bcd3cc0, launched at 1c60cc2) is not in that list, yet it is the builder of `s0_O__u450__r2b` -- the deck whose energy is banked as Fe's U=4.5 s0_O point. Before tranche 2c, Fe had a hole at 4.5 and the same sentence's generated clause would have read "Fe scored with 1 hole(s)". So the artifact's own registration chain omits the tranche that changed the artifact's own live-status text.

EVIDENCE:
src/dft/a0main_readout.py:658-660 -- `"extension 2026-08-28 ('Do them over Mn/Fe/Ti then'); tranches " "2/2b/3 (build_a0main_w2/_w2b/_w3, each committed pre-launch) " "cover the blind metals. Live status at scoring time: "`.  ||  runs/a0/m_a0_repairs2.txt:18-19 -- `a0/main/Fe s0_O__u450__r2 .in 4 s0_O__u530` / `a0/main/Fe s0_O__u450__r2b .in 4 s0_O__u300__r1`; header :10-13 names `build_a0main_w2c.py` as the record of the selection rule.  ||  Fresh JSON, Fe row u=4.5: `'repaired': {'s0_O': 'A6.5(2) REPAIR s0_O__u450__r2b, RESTARTED FROM s0_O__u300__r1 DENSITY ...; totmag 23.44; did not converge: s0_O__u450__r1 (failed QC), s0_O__u450__r2 (failed QC)'}` and `metals.Fe.gaps = []`.  ||  `git log --oneline`: `bcd3cc0 Register tranche 2c before launch ...` / `1c60cc2 ... launch tranche 2c (20214003 Fe u450 x2, 20214014 Ti s0_OOH r2+r3)`.  ||  `ls src/dft/build_a0main_w2c.py` exists; the string "w2c" does not appear in src/dft/a0main_readout.py.

CONSEQUENCE:
The one point in the whole 6-metal grid whose licensing commit is not named in the artifact is the point that closed Fe's only hole. A reader auditing "which pre-launch commit registered this number?" from the banked JSON is sent to w2/w2b/w3, none of which contains the r2b deck or the two-branch selection rule that chose it. One-word fix ("tranches 2/2b/2c/3 (build_a0main_w2/_w2b/_w2c/_w3 ...)") but it must land before banking, since the sentence is the artifact's audit trail.

----------------------------------------------------------------------
[S5] MINOR
TITLE: A7.5's tier strata are absent from the caveat block although the artifact now banks absolute eta for both metals A7.5 restricts by name -- Fe (MODEL PHASE) and Cr (REAL-UNDISTORTED-METASTABLE)
WHERE: src/dft/a0main_readout.py:613-666 (the six caveats) vs docs/43-prereg-week1-factorial.md:1400-1406

CLAIM:
A7.5 registers: "CrO2 = REAL-UNDISTORTED-METASTABLE ...; FeO2, CoO2, NiO2 = MODEL PHASE, method test systems only" and "The report may therefore never quote an absolute eta for Cr, Fe, Co or Ni as a materials claim; they appear only inside paired within-metal differences." The readout prints and banks an absolute eta column for Cr (19 rows) and now for Fe (8 rows), and its caveat dict has no phase/stratum key at all -- `grep -n "A7.5|MODEL PHASE|REAL-AMBIENT|stratum|strata" src/dft/a0main_readout.py` returns nothing. Fe is new in this bank; Cr's omission predates it but is not disclosed anywhere in the file either.

EVIDENCE:
docs/43:1400-1406 -- "The report may therefore never quote an absolute eta for Cr, Fe, Co or Ni as a materials claim; they appear only inside paired within-metal differences. Enforced by the pre-submission script (S7)." ... "CrO2 = REAL-UNDISTORTED-METASTABLE ... FeO2, CoO2, NiO2 = MODEL PHASE, method test systems only."  ||  `grep -n "A7.5|MODEL PHASE|REAL-AMBIENT|METASTABLE|stratum|strata|phase-reality" src/dft/a0main_readout.py` -> no matches (the only `A7.5` string in the repo's A0 code path is absent).  ||  Fresh JSON `caveats` keys: `fixed_geometry, spin_state, ir_ooh_basin, cell, coverage_shortfall, u000_decks` -- no phase key.  ||  Banked absolute eta: Fe 0.826-1.349 V over 8 rows; Cr 0.381-1.558 V over 19 rows.

CONSEQUENCE:
A7.5 puts enforcement at the pre-submission script (S7), so this is not a false statement -- it is a missing label on the artifact that feeds the report, in a caveat block whose stated contract is that its labels "travel with every table above". A one-line `phase_reality` caveat naming each metal's stratum makes the restriction travel with the numbers instead of depending on S7 catching it later.

----------------------------------------------------------------------
[S6] MINOR
TITLE: Ti banks `pls_flips: []` -- indistinguishable from a fully measured metal with no flip -- and the readout prints the affirmative "no pls flip inside the measured band" for a metal with zero measured rows
WHERE: src/dft/a0main_readout.py:474-484

CLAIM:
`seq = [r for r in full if not r["anchor"]]` is EMPTY for Ti (all 7 rows are gaps), so the zip loop produces no flips and control falls to the `else` at :483, which prints "no pls flip inside the measured band" -- an affirmative measurement claim over an empty measurement. :484 then banks `m_out["pls_flips"] = []`, the identical value a metal with a complete, genuinely flat grid would bank. The A7.2 census at :582-590 rescues Ti only because it applies a separate `pending` test on the rows; nothing rescues a consumer that reads `metals[m]["pls_flips"]` directly.

EVIDENCE:
src/dft/a0main_readout.py:475 `seq = [r for r in full if not r["anchor"]]`; :479-483 `if flips: ... else: print("no pls flip inside the measured band")`; :484 `m_out["pls_flips"] = [list(f) for f in flips]`.  ||  Live stdout, Ti section: `GAPS: 7 point(s) -- 7 with no output banked (...)` immediately followed by `no pls flip inside the measured band`.  ||  Fresh JSON: `metals.Ti.pls_flips = []` and `metals.Ru.pls_flips = [[7.5, 9.0, 3, 2]]` -- Ti's value is structurally identical to what a measured-flat metal returns.  ||  a0main_readout.py:582-584 reads exactly this field: `flips_by_metal = {m: result["metals"][m].get("pls_flips", []) for m in result["metals"]}`.

CONSEQUENCE:
A7.2 names the U at which each metal's pls flips as "a first-class deliverable" (docs/43:1351-1352). The deliverable's own field records a false negative for Ti, and any report figure or downstream script driven off `pls_flips` plots Ti as measured-with-no-flip rather than unmeasured. Guard the else-branch on `if seq:` and bank `pls_flips: null` (or a `pls_flips_status` of "no measured rows") when the sequence is empty.

----------------------------------------------------------------------
[S7] MINOR
TITLE: "no output on one side yet ... the control completes when those states run" is false for Ti: NEITHER side exists, and the comparator side cannot exist until the twice-failed *OOH relaxation converges
WHERE: src/dft/a0main_readout.py:226-233 and 259-263

CLAIM:
The uncompared branch is entered when `a0 is None or pb is None` and prints "control not evaluable for ['s0_OOH'] (no output on one side yet)". For Ti both sides are missing: runs/a0/main/Ti/ has no `s0_OOH__u000.out`, and runs/probe/Ti_audit/ contains only slab, s0_O and s0_OH base decks -- there is no `s0_OOH__base.*` file of any kind. The comparator is not late; it was never built, because probe_decks.py can only build a base deck from a converged relaxation and Ti's *OOH relaxation failed twice (s0_OOH.out, s0_OOH_r1.out). "The control completes when those states run" presents a blocked chain as a queue position.

EVIDENCE:
`ls runs/probe/Ti_audit/` -> `probe_manifest.json, s0_O__base.{in,out,lowdin.txt,projwfc.in}, s0_OH__base.{...}, slab__base.{...}` -- no s0_OOH entries.  ||  Ti_audit probe_manifest.json jobs: `slab relax_ref=-16566.52 prov=final | s0_OH relax_ref=-17148.26 prov=final | s0_O relax_ref=-17129.12 prov=final` -- three jobs, no s0_OOH.  ||  `ls runs/a0/main/Ti/ | grep s0_OOH` -> nothing.  ||  src/dft/a0main_readout.py:226 `if a0 is None or pb is None:` -> :232 `uncompared.append(st)`; :260-263 prints "(no output on one side yet); the 3 state(s) that could be compared all agree. Not a disagreement -- the control completes when those states run."  ||  runs/Ti_slab/s0_OOH.out and s0_OOH_r1.out both present and non-converged; s0_OOH_r2/r3 have .in only.

CONSEQUENCE:
Same class as the surviving MINOR about a control deck that ran and died, but the wrong half of the sentence: here it is the count, not the QC state. The banked `control_uncompared_states: ["s0_OOH"]` plus `control_ok: true` for Ti, narrated as a scheduling delay, understates that Ti's extraction control cannot be completed at all until the geometry chain is repaired. Correct the wording to "neither side has output" when both paths are absent, and say what blocks the comparator.

----------------------------------------------------------------------
[S8] MINOR
TITLE: The fixed_geometry caveat names the relaxation U for only 3 of the 6 metals and implies Ti inherits a production-tier relaxation that docs/59 states never existed
WHERE: src/dft/a0main_readout.py:616-618 vs docs/59-a0-roster-correction-2026-08-28.md:71-73

CLAIM:
The caveat reads "every point is a fixed-geometry single-point SCF on the production-tier geometry (relaxed at each tier's own U -- Ru/Ir tier carries no U, Cr tier U = 3.70)". Mn (production_u 3.90), Fe (5.30) and Ti (0.0) -- the three metals this bank newly adds -- are not named, and for Ti the phrase "the production-tier geometry" is not accurate: docs/59:71-73 states "TiO2 has no slab or adsorbate geometry anywhere in the campaign (only bulk hp.x work)", and the geometry under every Ti A0 deck was built and relaxed inside this campaign in tranche 3 (runs/Ti_slab/slab.out, s0_O.out, s0_OH.out, 2026-08-28/29). It is new compute presented as an inherited tier, and the *OOH member of that chain does not exist at all -- which is exactly why the Ti grid is 0/7.

EVIDENCE:
src/dft/a0main_readout.py:616-618 -- `"on the production-tier geometry (relaxed at each tier's own U -- " "Ru/Ir tier carries no U, Cr tier U = 3.70). A0 measures the "`.  ||  docs/59-a0-roster-correction-2026-08-28.md:71-73 -- "**Tranche 3 -- Ti (geometry chain first).** TiO2 has no slab or adsorbate geometry anywhere in the campaign (only bulk hp.x work). Chain: `qe_slab.py build Ti --supercell 1` (production builder; d0, nspin=1, **U = 0 by the same MP convention that sets Ru/Ir to zero**) -> relax slab + 3 adslabs -> probe-style base SCFs -> 7-point REF_GRID ladder".  ||  src/dft/a0main_readout.py METALS: `"Mn": ... production_u=3.90`, `"Fe": ... production_u=5.30`, `"Ti": ... production_u=0.0` -- the field exists and is never used in the caveat text.  ||  `ls runs/Ti_slab/` -> slab.out, s0_O.out, s0_OH.out, s0_OOH.out (failed), s0_OOH_r1.out (failed), s0_OOH_r2.in, s0_OOH_r3.in.

CONSEQUENCE:
The caveat is the artifact's only statement of geometry provenance, and it is silent for exactly the three metals whose provenance is new and contested (docs/59 discloses the Ti chain as compute outside A6.6's footprint -- see the surviving MINOR on the relaxation prohibition). A reader takes "production-tier geometry" at face value for Ti and never learns the Ti tier was created for this extension. Enumerate all six tiers from the `production_u` field the table already carries, and name Ti's chain as built-in-campaign.

----------------------------------------------------------------------
[S9] MINOR
TITLE: The readout's only arithmetic self-check is switched off on every pls in {1,4} row, and those are exactly the rows where the dG ladder has gone non-monotonic -- including the upper endpoint of Fe's sole contribution to the A7.2 CONFIRMED census
WHERE: src/dft/a0main_readout.py:346-351 and 386-389

CLAIM:
The docstring advertises the closed-form identity as the script's arithmetic self-check (":22-27: A7.2's closed form ... is verified on every such point as an identity check on our own arithmetic"), but :347 gates it on `r.potential_limiting_step in (2, 3)`, so `closed_form_dev` is banked as `null` on the three pls=1 rows this tranche adds (Mn U=7.5, Mn U=9.0, Fe U=9.0). Those three rows are also the ones where the OER ladder has gone non-monotonic: recomputing dG1..dG4 from the banked rows, Mn U=7.5 has dG4 = -0.144 eV, Mn U=9.0 has dG3 = -0.125 and dG4 = -0.185, Fe U=9.0 has dG3 = -0.199 (and Fe U=7.5, Cr U=9.0 also have a negative dG3 while still pls=2). Fe's entire contribution to the A7.2 census is the 2 -> 1 flip at [7.5, 9.0], whose upper endpoint is one of the unchecked rows; nothing in the artifact flags either fact.

EVIDENCE:
src/dft/a0main_readout.py:346-351 -- `# A7.2 closed-form identity check on pls in {2,3}` / `ident = None` / `if r.potential_limiting_step in (2, 3): ... ident = abs(eta_cf - r.overpotential)`.  ||  Fresh JSON: `Mn u=7.5 {'pls': 1, 'closed_form_dev': None}`, `Mn u=9.0 {'pls': 1, 'closed_form_dev': None}`, `Fe u=9.0 {'pls': 1, 'closed_form_dev': None}`; every other row has dev <= 2.2e-16.  ||  Recomputed ladder (dG1, dG2, dG3, dG4) from the banked dG columns: `Mn 7.50  2.523  2.387  0.154 -0.144  pls 1` ; `Mn 9.00  2.750  2.481 -0.125 -0.185  pls 1` ; `Fe 9.00  2.579  2.497 -0.199  0.044  pls 1` ; `Fe 7.50  2.422  2.490 -0.050  0.058  pls 2` ; `Cr 9.00  2.040  2.788 -0.031  0.123  pls 2`.  ||  Fresh JSON a7_2.flip_brackets: `"Fe": [[7.5, 9.0, 2, 1]]` -- Fe's only flip.  ||  a0main_readout.py:386-389 prints CLOSED-FORM DEV only when the value is not None and > 1e-9, so a null is silent in stdout as well.

CONSEQUENCE:
Does not change the A7.2 verdict -- Cr, Mn, Ir and Ru alone give 4 flips against the >=3 threshold -- but the artifact gives no way to see that one of the five census members qualifies solely through a flip terminating on a row the script does not check, and no way to see that eight of the 51 scored rows carry a negative OER step. The `closed_form_dev: null` value is also ambiguous between "not applicable" and "not computed". Bank a per-row reason (e.g. `closed_form_dev_status: "n/a: pls 1"`) and a one-line caveat noting which rows have a negative dG step; both are derivable from data already in the file.

######################################################################
REFUTED (recorded WITH their refutations -- a wrong finding kept is
cheaper than the same wrong finding raised again next wave)
######################################################################

----------------------------------------------------------------------
[R1] (registration) The Ti ladder extension's "cannot flip a verdict" bound is false: Ti *OOH is the single pivotal metal for A7.3
REFUTED BECAUSE:
The auditor's NUMBERS reproduce exactly — I re-ran the readout and confirmed c_M = dG_OOH − dG_OH at U = 0 / U = 9: Cr 3.444→2.757 (span/2 0.3435), Ru 3.180→2.995 (0.0925), Ir 3.652→3.525 (0.0635), Mn 3.617→2.355 (0.6307), Fe 3.518→2.297 (0.6102), Ti all 7 rows GAP. So 3 of 5 measured clear 0.10 V. Everything after that fails.

(1) THE DISCLOSURE THE FINDING DEMANDS ALREADY EXISTS — three times, including once in the very file it quotes. build_a0main_w2c.py:123-126, ten lines ABOVE the sentence complained of at :132-135, is the Ti selection rule: "If neither converges, A6.5(2)(iii) stands: Ti s0_OOH is NOT_CONVERGED, and A7.3's own registered text shrinks the span denominator ('a converged *OOH geometry' conditions it) rather than blocking the readout." docs/59:174, in a section literally titled "What the extension can and cannot change," is more explicit still: "A7.3 (span(c_M)/2 at fixed endpoints) becomes scorable on the full 6-metal denominator once Ti's *OOH converges." The requested fix ("A7.3's dependence on the extension disclosed, before banking") is already satisfied verbatim.

(2) THE DEPENDENCE IS NOT CREATED BY THE EXTENSION. The identical sentence predates tranche 2c: build_a0main_w3.py:49, registered at commit 92b8d87 BEFORE tranche 2b/3 launched, already says a Ti NOT_CONVERGED shrinks A7.3's denominator. A6.5(2) rungs (i) and (ii) are registered and exist precisely to convert gaps into points; had Ti's registered rung (ii) converged, A7.3 would have gone 3/5 → 4/6 in exactly the way the auditor calls a "flip." The argument therefore condemns the registered escalation ladder, not the unregistered bump — it isolates nothing tranche 2c added.

(3) THE BUMP CANNOT CHANGE ANY A7.3 VALUE. mixing_ndim (Broyden history depth) and electron_maxstep (iteration cap) are acceptance-neutral; conv_thr and forc_conv_thr are explicitly untouched (builder :113-116; manifest ladder_extension.untouched). A point converging under the extension meets exactly the criterion rung (ii) would have applied, so c_M(Ti) is whatever it is. And the sentence immediately before "cannot flip a verdict" states outright that filling the gap is the one thing the round CAN do. Reading "flip a verdict" as "make A7.3 scorable" contradicts the clause the auditor himself quotes; read as "turn a measured answer into a different measured answer," it is true.

(4) THE PIVOTAL-METAL PREMISE IS FALSE ON THE READOUT'S OWN BANKED CAVEAT. "Ru/Ir are complete and can no longer move" is contradicted by the [spin_state] caveat the script prints and banks: gate (h) measured 4/4 ADOPT_AFM on the RuO2 anchors with 0.033–0.064 eV adsorption-energy movement, AFM re-run OWED (S0(h)). Ru sits 7.5 meV under the 0.10 V line — 4–8× smaller than that measured, disclosed class. A6.4 ("where A0 and a relaxed point disagree, the relaxed point wins") makes every fixed-geometry row provisional. A Ru AFM re-run alone could carry the count to 4 with the Ti gap left standing, so Ti is not the single route to 4 and the BLOCKER's load-bearing premise collapses.

(5) THE GATED ARTIFACT CARRIES NO A7.3 CLAIM. a0main_readout.py's docstring names the three registered questions it answers "and nothing else" (P7 bound, A6.3 ordering, A7.2 census); grep finds no span/A7.3 scoring anywhere in src/; the JSON keys are metals, ordering, a7_2, caveats, gas_reference_disclosure, and the only "A7.3" string in it sits inside the coverage_shortfall caveat. Banking the readout records no A7.3 verdict and no bounded_by sentence, so "banking ... records a false claim of harmlessness" is a non-sequitur.

(6) EVEN THE COUNT IS NOT SETTLED FACT. A7.3 scores at "U = 0 and U = U_max," and the registration's own disclosed non-blind Cr value (0.223 V) reproduces exactly at U_max = 5.00 on this grid (3.444 → 2.997), not at U = 9 (0.3435). U = 9 is the only coherent A0-main reading (Ru/Ir have no 5.00 point), but the registration's worked example uses a different endpoint — so the "only 3 exceed" tally the BLOCKER rests on is one reading, presented as settled.

At most this is a five-word wording tightening of one sentence whose meaning is already pinned by the two paragraphs around it. Not a defect, and nowhere near a banking gate.

----------------------------------------------------------------------
[R2] (registration) The "lower converged energy is the banked point" selection rule is attributed to A6.4, which contains no such rule, while the registered analogue (A8.3) says the opposite
REFUTED BECAUSE:
The finding has one true grain wrapped in four refuted claims, and it does not survive as reported.

WHAT IS TRUE. A6.4 (docs/43:1256-1265) is quoted accurately: three bullets, and its only selection sentence is "Where A0 and a relaxed point disagree, the relaxed point wins" — provenance (relaxed vs frozen), not energy ranking between two SCFs. So the parenthetical "(docs/41 s6d, A6.4)" at build_a0main_w2c.py:43 and :121 is loose in its A6.4 half. That is the whole of what survives.

WHAT IS REFUTED.

(1) "Presented as pre-existing registered text" is contradicted by the same sentence. build_a0main_w2c.py:41 and :119 both open the paragraph with "SELECTION RULE, FIXED HERE BEFORE EITHER RUNS." The commit message says "Selection rule fixed before launch". Nothing here claims the rule pre-existed.

(2) The runs/a0/m_a0_repairs2.txt evidence is simply wrong. Its header (lines 8-12) reads "SELECTION RULE (fixed before launch, build_a0main_w2c.py docstring): if both converge the LOWER total energy is the banked point..." — no A6.4, no docs/41. Same for runs/Ti_slab/m_ti_relax_r2.txt ("SELECTION RULE (fixed before launch)"), for manifest.json's two "selection_rule" fields (build_a0main_w2c.py:533, :560 — uncited), and for a0main_readout.py:307-310, whose comment reads "Pre-declared before either deck ran (build_a0main_w2c.py)". Every machine-readable artifact that travels into the banked readout carries the rule as fixed-before-launch with no registration citation.

(3) The proposed fix is already in the document of record. docs/59 §3c ("The second escalation round (added 2026-08-29, before it ran)", lines 105+) states it plainly and uncited: "The selection rule is fixed before launch: lower converged energy is the banked point, the difference is reported as the measured branch splitting at U = 4.5, one converged means that one (labelled with its branch), neither means A6.5(2)(iii)." docs/59 §5.1 asks the entrant to "Re-author / countersign this document". So the entrant is already countersigning a stated rule, not a supposed quotation — which is exactly what the finding asks for.

(4) A8.3 does not "say the opposite", and is not the applicable registered analogue. Its preamble scopes it to S3 ("S3 needs the magnetic analogue"), and its second THRESHOLD triggers on "a __g1 child that lands above its parent". __g1 is a block-1A/LIT/S3 deck token — it appears in build_cellsym_pilot.py, build_s3*.py, build_lit*.py and never in any build_a0main_*.py. Structurally it cannot apply: A0's u450 has NO parent energy (the original s0_O__u450 SCF failed at 200 iterations), so "lands above its parent" is not evaluable. And A8.3 never banks the higher of two solutions — it refuses the higher child and, after two failures, withholds the PAIR from a contrast statistic. That is compatible with lower-wins, not opposite to it. The BASIN_DRIFT corroboration cuts the other way too: the registered default there is am.4 §2 (docs/43:788), "the GATE-1 SCF energy is the corrected value... and the pair is scored from it" — a provenance rule, unrelated to lower-vs-higher — while docs/43 §5's GATE-1 loop ("If that SCF lands >= 5 meV lower, the state is re-relaxed from it and the loop repeats") is registered text whose semantics ARE lower-supersedes. The substance is registered; only the section number in one parenthetical is off. And docs/41 §6d, the other half of the citation, carries the measured principle in both sign directions ("the production relaxation found the lower solution and it was the audit SCF that got trapped"; for Ni the fresh SCF "lands below... the production relaxation carried an excited state"), and is cited in exactly this role at build_a0main_w2.py:64 and docs/59:61.

(5) The consequence is factually dead. I re-ran the readout: Fe u450 banks "A6.5(2) REPAIR s0_O__u450__r2b, RESTARTED FROM s0_O__u300__r1 DENSITY; totmag 23.44; did not converge: s0_O__u450__r1 (failed QC), s0_O__u450__r2 (failed QC)". Confirmed on disk: s0_O__u450__r2.out ends "convergence NOT achieved after 200 iterations" (still pinned at 21.98), s0_O__u450__r2b.out "convergence has been achieved in 18 iterations" at -2515.36930103 Ry, totmag 23.44. Exactly ONE repair converged, so the clause that fired is "one converges -> that one, labelled with its branch" — not the contested lower-wins tie-break, which decided nothing. The finding's stated stakes ("decides which of two magnetic branches, 21.98 vs 22.90 muB") are wrong twice: the tie-break never ran, and the banked moment is 23.44, neither of the two branches named.

RESIDUE. A one-word imprecision in two docstring parentheticals and an immutable commit message, already superseded by correctly-worded text in docs/59 §3c, both run manifests, manifest.json and the readout itself. It gates nothing and does not block banking.

----------------------------------------------------------------------
[R3] (registration) The readout hard-codes "(measured branch splitting)" for any two converged repairs and does not bank the loser's magnetisation
REFUTED BECAUSE:
Refuted on three independent grounds.

1. UNREACHABLE ON THE REGISTERED INPUTS. The tranche-2c results have landed since the auditor looked. runs/a0/main/Fe/s0_O__u450__r2.out (16:02) carries "convergence NOT achieved" and no "!" total-energy line -> it enters `lost`, not `won`. Only s0_O__u450__r2b.out converged (-2515.36930103 Ry, totmag 23.44). So len(won) == 1 and the `if len(won) > 1:` block at a0main_readout.py:322-327 never executes. I re-ran the readout and banked its JSON to a scratch path: `"branch splitting" in json` is False. The banked Fe U=4.5 note is exactly the registered one-converger branch -- "REPAIR s0_O__u450__r2b, RESTARTED FROM s0_O__u300__r1 DENSITY ...; totmag 23.44; did not converge: s0_O__u450__r1 (failed QC), s0_O__u450__r2 (failed QC)" -- which matches build_a0main_w2c.py's registered "if exactly one converges it is the point, labelled with the branch it landed on". The only other registered multi-deck pair, Ti s0_OOH_r2/_r3, cannot reach this code at all: the repair registry globs only runs/a0/m_a0_repairs*.txt (a0main_readout.py:181-182) while the Ti pair lives in runs/Ti_slab/m_ti_relax_r2.txt, and every Ti s0_OOH row is a GAP. No registered input anywhere in the campaign can currently produce len(won) > 1, so nothing false is banked.

2. THE CITED REGISTERED INSTRUMENT IS MIS-SCOPED AND POINTS THE OTHER WAY. A8.3 (docs/43:1560-1569) sits under Amendment 8, whose Governs line (docs/43:1483-1486) reads "Governs: S3 -- tier_v3 crossed coverage x symmetry x basin ...; the CONFOUND rule; ...". It is the magnetic analogue of section 5's P16 rule for CONTRAST pairs (constrained vs free solution; Cr mir vs off), and its operative effect is to EXCLUDE a moment-differing pair from the contrast statistics because "its energy difference mixes the intended contrast with a basin change". Applied literally to r2/r2b it produces the opposite of what the auditor asks: a genuine branch splitting (delta-mu > 0.05 uB) would be CONFOUNDED and excluded, while a same-branch pair would pass unremarked. There is no "intended contrast" for r2/r2b to be confounded against -- they are two attempts at the same point, and the governing rule is the standing "lower converged state is the state of record" (docs/41 s6d, A6.4), which A8.3 does not touch. So "the campaign has a registered instrument for exactly this decision ... It is not applied" is false.

3. THE WORDING IS ITSELF PRE-REGISTERED, AND CHANGING IT NOW WOULD BE THE VIOLATION. "the difference between them is reported as the measured branch splitting at U = 4.5" is committed pre-launch in bcd3cc0 in three places: the build_a0main_w2c.py docstring ("SELECTION RULE, FIXED HERE BEFORE EITHER RUNS"), the runs/a0/m_a0_repairs2.txt manifest header, and manifest.json tranche_2c.fe_s0_O_u450.selection_rule. a0main_readout.py:306-310 cites that pre-declaration and implements it verbatim. Adding an unregistered totmag discriminator to the readout after the decks have run is precisely the post-hoc rule change the registration discipline forbids.

Residual, and why it still does not reach MINOR-must-fix: even if a future tranche produced two convergers on the same branch, the note prints the magnitude next to the label ("LOWER wins by %.1f meV"), which for a same-branch pair would read 0.0 meV against a banked per-metal rerun-determinism figure in the same JSON; the winner's moment is printed and banked in row["totmag"]; and the two seed parents' moments (22.90 / 21.98) are banked in manifest.json branch_context. The specific complaint that "the losing row's moment is never banked" is moot on the real data -- the loser never converged, so it has no converged moment, and banking a moment off a non-converged SCF would itself breach the strict-QC rule; its .out is retained on disk and named in the note under A8.4.

----------------------------------------------------------------------
[R4] (registration) Fe rung (ii) keeps electron_maxstep = 200 - the cap the same commit judged inadequate for a less-converged Ti cycle - while rung (iii) is terminal
REFUTED BECAUSE:
The finding fails on all three of its legs — the stated grounds, the physics, and the consequence — and is additionally refuted by outputs already on disk.

1) THE CONSEQUENCE DID NOT OCCUR; it is falsified by data the auditor did not check. Both tranche-2c Fe decks have landed (runs/a0/main/Fe/s0_O__u450__r2.out and __r2b.out, both mtime 2026-08-29 16:02, both "JOB DONE"):
  r1  200 iters, NOT achieved, totmag 21.98
  r2  200 iters, NOT achieved, totmag 21.98   (seeded from u530)
  r2b  18 iters, "convergence has been achieved in 18 iterations", totmag 23.44, E = -2515.36930103 Ry   (seeded from u300__r1)
Exactly one converged, so the pre-registered selection rule's second branch fires, not A6.5(2)(iii). The readout already banks it: `4.50 ... eta 1.196 [s0_O: A6.5(2) REPAIR s0_O__u450__r2b, RESTARTED FROM s0_O__u300__r1 DENSITY ...; totmag 23.44; did not converge: s0_O__u450__r1 (failed QC), s0_O__u450__r2 (failed QC)]`. Fe U=4.5 is a filled, labelled point. The finding's whole consequence clause is conditioned on "If r2/r2b stop at iteration 200" — that condition is false.

2) THE 200 CAP WAS NEVER THE BINDING CONSTRAINT. r2b converged in 18 iterations at beta 0.15 — 11x under the cap — which directly refutes the auditor's premise that halving mixing_beta "generally needs MORE iterations, not fewer" as applied here; the seed branch dominated, not the iteration budget. Meanwhile r2 got WORSE with the halved beta: minimum estimated scf accuracy 3.815e-05 over 200 iterations vs r1's 1.233e-05, with its final 40 iterations wandering between 2.8e-4 and 5.1e-3 and no downward trend. A cap of 400 or 8000 would not have converged it. The same test applied to the auditor's own r1 evidence gives the same answer: r1's last 17 iterations oscillate 1.23e-05 → 2.84e-05 → 1.89e-05, flat. Both are limit cycles. "Distance from threshold" (the 12x vs 76x comparison) is the wrong axis — for a limit cycle it does not predict whether more iterations help; the trend does, and by that test neither case was iteration-limited.

3) THE BUILDER NEVER "JUDGED 200 INADEQUATE" FOR TI — that is a misquote. build_a0main_w2c.py:100-104 names the lever explicitly: "mixing_ndim 8 -> 16 (more Broyden history, THE TEXTBOOK LEVER AGAINST A LIMIT CYCLE) and electron_maxstep 200 -> 400." The Broyden history is the fix; maxstep is its companion, in a relax run where the cap applies per ionic step over many cycles (runs/Ti_slab/s0_OOH_r2.in:2 calculation='relax' vs runs/a0/main/Fe/s0_O__u450__r2.in:2 calculation='scf'). Reading "it limit-cycled at the cap" as "the cap was insufficient" inverts the argument: limit-cycling AT a cap is evidence the cap is not binding.

4) THE ASYMMETRY IS THE REGISTRATION DISCIPLINE, AND IS ALREADY DISCLOSED in the exact terms the auditor's remedy asks for. docs/59-a0-roster-correction-2026-08-28.md:100 heads the Fe section "**Fe s0_O at U = 4.5 -- inside the ladder**"; :127 heads the Ti section "**Ti s0_OOH -- the ladder is exhausted**". Ti had already exhausted rung (ii) and stood at (iii); Fe had only failed rung (i) and still had a registered rung left. The operative rule is: while a registered rung remains, execute that rung and only that rung; the extension is a last resort where the ladder is silent. build_a0main_w2c.py:246-251 enforces it in code — it dies unless EXACTLY two lines differ from the rung-(i) deck (prefix, mixing_beta), which is what makes r2/r2b an uncontaminated execution of A6.5(2)(ii). docs/59:147-150 then states the extension and its scope verbatim: "The one thing here that is NOT in the registration is mixing_ndim 8 -> 16 and electron_maxstep 200 -> 400, which A6.5(2) does not name. They are declared as a dated extension of the ladder for relaxations", bounded so it "cannot move a threshold, change a convergence criterion, or flip a verdict." Same text at build_a0main_w2c.py:128-137 and in manifest.json tranche_2c.ti_s0_OOH.ladder_extension. That is a disclosed caveat, which the audit brief excludes.

5) THE PROPOSED FIX WOULD HAVE BEEN THE ACTUAL DEFECT. electron_maxstep = 200 is the campaign-wide convention across 791 decks under runs/; the only three 400s are the two Ti relaxation decks and their manifest. Widening the extension to cover the Fe SCF would apply an unregistered numerics knob to a point whose total energy enters the registered U-grid and feeds eta, at a moment when a registered rung was still available and untried. Pre-registration exists to prevent exactly that. The builder declined it, and it turned out not to be needed.

----------------------------------------------------------------------
[R5] (registration) docs/59 s5 still tells the entrant there is nothing to countersign, while s3c - added in the same commit - introduces the ladder extension and two new banking rules
REFUTED BECAUSE:
Refuted on every load-bearing element.

(1) The title's central assertion is contradicted by the cited line. docs/59:184 reads "Re-author / countersign this document (no thresholds to set - ...)". Item 1 IS the countersignature instruction, and its object is "this document", which as of bcd3cc0 includes s3c. s5 does not tell the entrant there is nothing to countersign; it tells him to countersign, and qualifies only what he must DECIDE.

(2) The pointer is correctly routed and is not asked to enumerate. build_a0main_w2c.py:131 points at s5 for the COUNTERSIGNATURE, and s5:184 is where that obligation lives. The builder routes the SUBSTANCE separately and correctly: line 143 "conventions are the entrant's to set. Recorded in docs/59 s3c", plus lines 485 and 550 which tag the extension "docs/59 s3c". Two pointers, two targets, both accurate. "The pointer target does not list the thing that points at it" fails because the pointer asks "where is my countersignature obligation recorded" and s5 answers it.

(3) The stated CONSEQUENCE is factually false. docs/59 is not "the one document that is the entrant's action list" -- docs/59:188 itself designates tasks/todo.md as the carrier of open items, and `git show bcd3cc0 -- tasks/todo.md` shows the SAME commit added, live on HEAD at tasks/todo.md:727: "[ ] **Frank's call, deliberately not made here: should the Ti arm run nspin=2 throughout?** ... it is a CONVENTION change across 4 states + 24 banked SCFs. docs/59 s3c", alongside the retained tasks/todo.md:721 "[ ] **Frank: countersign + deposit docs/59**". The decision the auditor says is unlisted has its own checkbox in the live action list, added in the same commit as s3c. The auditor checked the docs/59 diff and stopped; the commit touched 11 files.

(4) The cited authority is misattributed. A7.7 (docs/43:1435-1447) is P-DISPOSITION plus the authorship rule: "Amendments are drafted research infrastructure, disclosed in the provenance record. The protection sits at report time: no sentence of any amendment is reproduced verbatim..." It contains no clause that "the entrant, not the assistant, owns thresholds and conventions". That phrasing is a docs/47/docs/50 DRAFT-header convention, and docs/43:1470-1479 records it was discharged for A8 by en-bloc adoption ("I went through them and they pass with me. Go ahead."), summarised as "A7.7's model is unchanged: the draft proposes, the entrant decided."

(5) The disclosure the audit brief tells me to check for exists, in the same section. docs/59:147-154 flags the unregistered numerics change by name (mixing_ndim 8->16, electron_maxstep 200->400) and bounds it; docs/59:156-162 carries a boldfaced "An open question for the entrant, deliberately not settled here" ending "conventions are the entrant's to set, not the assistant's". Both sit three paragraphs above s5, inside the document s5 item 1 orders him to re-author.

What survives is narrow and not what was reported: the half-clause "the only judgment call already made is the three pilot guesses" is now stale, since s3c added the both-parents seeding, the numerics extension and the re-anchor distance. The other half ("no thresholds to set") remains true -- s3c's extension is numerics-only by its own text with conv_thr/forc_conv_thr/degauss/smearing/nspin/mixing_mode/mixing_beta untouched, the Fe and Ti selection rules apply the campaign's standing lower-converged-state rule (docs/41 s6d, A6.4 at docs/43:1256-1260 "the relaxed point wins") and A6.5(2)(iii)'s already-registered terminal outcome (docs/43:1267+), and nothing asks the entrant to author a new number. That residual is one-clause editorial staleness in a file whose line 3 is "Status: DRAFT for the entrant" and whose entire purpose is the entrant's re-authoring. It is not a MAJOR, it does not gate banking the readout, and it is not the defect that was reported.

----------------------------------------------------------------------
[R6] (registration) The Ti selection rule banks whichever candidate converges, under a representability failure the same registration diagnoses - a directional selection with no label
REFUTED BECAUSE:
REFUTED on four independent grounds; the auditor's measurements are right and the inference is not.

(1) The caveat the finding calls missing is the finding's own quoted evidence. The claim is "Nothing in the registered label records that the loser's failure is a convention artifact rather than evidence about TiO2 binding." src/dft/build_a0main_w2c.py:86-90 -- the very lines the auditor cites -- states that the desorbed region is where "the Ti chain's nspin = 1 convention cannot spin-split the resulting half-occupied state." :137-143 escalates it to a named, dated open decision ("whether the Ti arm should run nspin = 2 throughout ... would remove the radical-state pathology at the root ... Recorded in docs/59 s3c"). docs/59-a0-roster-correction-2026-08-28.md:105-166 mirrors both verbatim. runs/a0/main/manifest.json banks it as structured fields: tranche_2c.ti_s0_OOH.diagnosis ("walking UP (3.167 -> 3.414 A over 3 steps) into the desorbed-radical region") and tranche_2c.ti_s0_OOH.entrant_decision_open. HDR_TI (:469-502), written into runs/Ti_slab/m_ti_relax_r2.txt beside the launch rows, repeats it a fourth time. The brief's explicit test -- "check whether the disclosure exists before calling something hidden" -- is failed.

(2) The rule makes no energy claim in the branch attacked. :123-124 reads "If exactly one converges it is the geometry, labelled with its provenance." It never asserts the loser was higher, so the demanded clause ("a non-converged competitor in the desorbed region is not evidence that the bonded basin is lower") corrects an inference the registration does not make. The registration already carries the symmetric non-inference explicitly at :94: "Three ionic steps is also far too short a walk to conclude that TiO2 does not bind *OOH: *O needed 36 and *OH needed 56", repeated in HDR_TI at :476.

(3) In the one-converges case there is nothing to select between. The only registered alternative is A6.5(2)(iii) NOT_CONVERGED (docs/43:1267), scoped to the neither-converges case. A non-converged walk yields no total energy under any rule, so no legitimate competitor is excluded and no direction is imposed. The auditor also picked the benign branch: the sharper hazard is the unexamined both-converge case, where "lower total energy wins" could bank a 3.4 A desorbed nspin=1 configuration -- not an adsorption geometry at all -- as the *OOH state of record.

(4) The consequence rests entirely on an unmeasured prediction about jobs still in flight. runs/Ti_slab/ contains s0_OOH_r2.in and s0_OOH_r3.in and NO corresponding .out. Running PYTHONPATH=src python src/dft/a0main_readout.py: the Ti table is seven "GAP (s0_OOH)" rows, "GAPS: 7 point(s) -- 7 with no output banked (not yet run; not a convergence event)"; A7.2 prints "pending data: ['Ti']"; A7.3 is unscored; the coverage_shortfall caveat already says "Ti NOT YET SCORED". The claimed "modal outcome" is an assertion, not a measurement -- r2 received mixing_ndim 8->16 (the docstring's own "textbook lever against a limit cycle") and electron_maxstep 200->400, and rung (ii) was descending, with total force falling 0.281 -> 0.125 Ry/bohr. Nothing here can affect what docs/figs/a0main_readout.json banks today, which is what the audit gates. A "MAJOR / must fix before banking" severity is therefore inapt on its face.

Additionally, the labelling machinery the finding presumes will fail is already live: the Fe U = 4.5 row in the current readout prints its banked repair together with its named losers -- "[s0_O: A6.5(2) REPAIR s0_O__u450__r2b, RESTARTED FROM s0_O__u300__r1 DENSITY ...; totmag 23.44; did not converge: s0_O__u450__r1 (failed QC), s0_O__u450__r2 (failed QC)]" (a0main_readout.py:315-330). And src/dft/build_a0main_w3.py:47-49 and :297-299 already gate the whole Ti *OOH A0 grid on the relax converging, with A7.3's denominator shrink registered as the failure path. Procedurally, bcd3cc0 registered and 1c60cc2 launched; a pre-registration cannot be retro-edited without breaking the discipline the finding invokes -- the only legitimate remedy would be a dated addendum, not a "fix before banking".

Verified measurements (probe_decks.parse_input_deck, runs/Ti_slab/): s0_OOH.in 3.166812 A, s0_OOH_r1.in 3.262640 A, s0_OOH_r2.in 3.414495 A, s0_OOH_r3.in 1.781905 A -- the auditor's two numbers are exactly right. The numbers are not the problem; the conclusion drawn from them is.

Out of scope for this finding, noted only so they are not lost: the readout's [spin_state] caveat calls Ti "a d0 closed shell by construction ... no moment to order", which sits awkwardly against tranche 2c's odd-electron *OOH diagnosis; and build_a0main_w2c.py:115-116 ("changing where the walk STARTS cannot bias where it ENDS") is a strong statement about basins of attraction. Neither is what was reported and neither is endorsed here.

----------------------------------------------------------------------
[R7] (registration) "Never improving over the remaining 187 ... periodic spikes to ~5e-3" is contradicted by the output
REFUTED BECAUSE:
REFUTED on all three sub-claims, plus two errors in the auditor's own evidence.

(1) "Never improving" is not loose prose — it is this campaign's REGISTERED quantitative vocabulary. docs/45-error-ledger.md:412-413 defines SLOW = "running min still improving >2x per 150 iterations" and STALLED = "min improved <2x over the last 150 it., magnetization stable". Re-parsing the third SCF block of runs/Ti_slab/s0_OOH_r1.out: running minimum at iteration 50 = 7.575e-05, at iteration 200 = 7.575e-05 — ratio 1.000, i.e. LITERALLY ZERO improvement over the final 150 iterations. Over the whole 187-iteration tail the running min improves only 1.49x, still <2x, i.e. STALLED not SLOW. Magnetization is trivially stable (s0_OOH_r1.in sets no nspin/starting_magnetization -> nspin=1, exactly as the docstring says). The docstring's "never improving over the remaining 187" is the registered STALLED characterization and the data satisfies it. A 1.49x wobble in a running minimum sitting 76x above conv_thr=1.0d-6 is noise inside a limit cycle, which is what the docstring calls it.

(2) The auditor's anchor is MISINDEXED. Iteration 13 (1-indexed — the indexing the docstring's own arithmetic requires, since 200-13=187, a count the auditor concedes is right) = 1.1321e-04. The auditor's "acc[13]=1.222e-04" is the 14th value: they took a 0-indexed value and paired it with a 1-indexed count, hence their internally inconsistent "remaining 186". Under the docstring's real anchor the count below is 62 of 187, not 71 of 186.

(3) Both quoted magnitudes are the measured CENTRAL values. The non-spike plateau (n=174) has median 1.287e-04 and mean 1.380e-04 — the docstring's "~1.2e-4" and the manifest header's "~1.3e-4" (build_a0main_w2c.py:471, :544) bracket it correctly. The spikes (n=13, >=1e-3) have median 5.225e-03 — "periodic spikes to ~5e-3" matches to two significant figures. The auditor read "to" as "up to the maximum"; under the level reading it is exact, and the 1.334e-02 maximum makes the limit cycle worse, strengthening rather than undercutting the registered conclusion.

Additional defects in the finding itself: the WHERE is wrong — the quoted prose is at src/dft/build_a0main_w2c.py:68-70, not 72-75 (72-75 is the DIAGNOSIS paragraph). And "the file that the builder itself re-derives from" overstates the coupling: the builder re-derives nsteps, convergence status and the trajectory geometry blocks from that file (:211, :215, :217, :301); the SCF accuracy trace is narrative diagnosis and is never a registered quantity feeding any verdict, selection rule, manifest field, or readout row. The auditor concedes the limit-cycle conclusion is unaffected, and there is no registration-discipline rule making a descriptive figure a defect when it is in fact the measured median.

----------------------------------------------------------------------
[R8] (registration) The registration commit's own record says tranche 2c was already LAUNCHED; the next commit says nothing had launched yet
REFUTED BECAUSE:
The finding fails on both of its legs: the claimed contradiction is a misreading, and the claimed evidential vacuum is factually wrong.

1) THERE IS NO CONTRADICTION BETWEEN THE TWO COMMITS. The auditor renders 1c60cc2 as "the next commit says nothing had launched yet". It does not. Its actual sentence is scoped to the preflight refusal: "The manifest declared 2 (the concurrency actually wanted) and was refused before anything launched" — i.e. the bad NCONC token was caught by 43's dry preflight and never reached a running job. That the sentence is local to the refusal, not a global statement of the commit's own instant, is settled by the same commit message four lines later enumerating the arrays that HAD by then been submitted ("Launched after the registration was committed and pushed: 20214003 ... 20214014 ..."). Both commits assert the identical ordering — registration committed+pushed, then submit. tasks/todo.md:725's own parenthetical says "(committed before submit)", and bcd3cc0's subject line is "Register tranche 2c before launch". Nothing in the record is inconsistent.

2) THE CENTRAL EVIDENCE CLAIM — "Nothing in the repository independently timestamps the submissions - no sbatch receipt, no slurm log - so the pre-launch property rests entirely on commit prose" — IS FALSE, and it is the load-bearing half of the finding. Every Quantum ESPRESSO output carries a machine-written wall-clock start stamp on line 2, and the .out files ARE the campaign's banked evidence (37 tracked .out files in runs/a0/main/Fe alone). For the very jobs at issue:
   runs/a0/main/Fe/s0_O__u450__r2.out:2  -> "Program PWSCF v.7.5 starts on 29Aug2026 at 15:22:20"
   runs/a0/main/Fe/s0_O__u450__r2b.out:2 -> same, 15:22:20
   against bcd3cc0 committed 2026-08-29T15:18:30-04:00 (pushed 15:18:34 per the auditor's own reflog reading) and 1c60cc2 at 15:21:20-04:00. The jobs started 3 m 46 s after the registration was pushed. Anvil's node clock and the committer's clock agree (both -0400), which the previous round independently confirms: registration 92b8d87 at 02:12:29-04:00, and its repair outputs runs/a0/main/Fe/s0_O__u300__r1.out:2 at 02:19:30, s0_O__u450__r1.out:2 at 02:21:33, runs/Ti_slab/s0_OOH_r1.out:2 at 02:35:31 — all after. So the "registration precedes execution" property is checkable from machine-written evidence banked in the repo, exactly the artifact class the campaign's discipline rests on. The auditor did not open a single .out header.

3) THE docs/59 PASSAGE IS SELF-DISCLOSED AS PROSPECTIVE. The section heading the auditor quotes from is "## 3c. The second escalation round (added 2026-08-29, before it ran)" (docs/59:105). A sentence written under a heading that announces itself as written before the round ran is the disclosure, not a concealed retrospective claim — and it is true on the timestamps above. This is precisely the "already explicitly disclosed as a caveat" exclusion in the audit brief.

4) THE RESIDUE IS A FOUR-MINUTE TENSE ARTIFACT IN A ROLLING TASK LOG, NOT A DEFECT OF RECORD. tasks/todo.md:725 says "Tranche 2c REGISTERED + LAUNCHED 2026-08-29"; at bcd3cc0's instant the submit was ~4 minutes in the future. tasks/todo.md is a working checklist updated across a session, the line carries no array IDs (they arrive in 1c60cc2 and, per the campaign's established pattern for 20183040/20196817/20196856, in the next evidence commit's todo entry), and the line's own parenthetical states the ordering correctly. Note also that the direction of the alleged error is exculpatory-neutral, not incriminating: nothing anywhere suggests a job preceded its registration, and the campaign has demonstrated it issues an explicit correction of record when a launch claim was wrong (tasks/todo.md:698, the "A0-main had never been submitted" entry, checked with sacct).

5) IT IS ALSO OUT OF SCOPE FOR THE GATE. This audit gates banking docs/figs/a0main_readout.json. The item concerns prose in two already-pushed commits that cannot be amended without rewriting published history, and it touches no input, code path, or number that flows into the readout.

Default-to-refute is the correct outcome here: the finding's quotes are accurate but its inference is not, and its decisive premise about missing timestamps is contradicted by the first two lines of the very output files the round produced.

----------------------------------------------------------------------
[R9] (registration) A8.4's threshold has fired at Fe s0_O (25% > 20%) and the mandated consequence appears nowhere in the artifact being banked
REFUTED BECAUSE:
The finding fails on five independent grounds, any one of which sinks it.

(1) THE LABEL DOES TRAVEL INTO THE ARTIFACT. The auditor grepped only for the literal tokens "low-confidence|low_confidence|failure_rate" and concluded there is "no indication" in the banked readout. There is. I ran the readout and dumped its JSON (src/dft/a0main_readout.py:363-366 and :393 write row["repaired"]; :726 dumps it). Both affected Fe rows carry an inline, per-row, A8.4-named note in the JSON that will be banked:
  u=3.0  s0_O: "A6.5(2) REPAIR s0_O__u300__r1, RESTARTED FROM s0_O__u150 DENSITY (original SCF failed at 200 iterations; failed .out retained, A8.4); totmag 22.90"
  u=4.5  s0_O: "A6.5(2) REPAIR s0_O__u450__r2b, RESTARTED FROM s0_O__u300__r1 DENSITY (original SCF failed at 200 iterations; failed .out retained, A8.4); totmag 23.44; did not converge: s0_O__u450__r1 (failed QC), s0_O__u450__r2 (failed QC)"
These print in the stdout table too. A reader sees exactly which points failed, that the failed .out is retained, which density seeded the repair, and that two registered repairs failed — strictly more information than a column-level "low-confidence" flag, and anchored to the two rows that carry the failure instead of smeared over all eight. The code comment at :288-291 states the mechanism explicitly ("The failed .out stays on disk and in the A8.4 failure count"), and :380-383 is the same labels-travel principle the auditor invokes, already applied.

(2) WRONG ARTIFACT. Where A8.4 formally applies (S3), its reported quantity was discharged in a dedicated builder artifact, not in an eta readout: docs/56:20 records the A8.4 table as runs/s3/readout/a84_rates_2026-08-24.{json,md}, and both files exist on disk. The A0 eta readout's docstring (src/dft/a0main_readout.py:3, "The three registered questions this script answers, and nothing else") scopes it to P7 bound / A6.3 ordering / A7.2 pls flips. Demanding the rate table inside this file misidentifies which artifact carries the obligation.

(3) WRONG SCOPE, AND THE AUDITOR CONCEDES IT. docs/43:1599-1601 reads "S3 records a per-metal, per-state convergence-failure rate...". A0 is not S3. The auditor admits the sentence is written for S3, then treats prose cross-references (docs/45:2200, docs/59:95-96) as promoting A0 into A8.4's mandatory path. Those cross-references are the campaign volunteering the number, which is the opposite of a violation.

(4) THE CONSEQUENCE IS REPORT-LEVEL, AND THE ARTIFACT CONTAINS NO RANKING. A8.4 (docs/43:1603-1604) says the state's contribution is "marked low-confidence in the report rather than dropped"; docs/45:102 states the rates "land in the S6 report as A8.4 requires." The banked JSON is a readout, not the report. And it computes no ranking: A6.3 is Ir-vs-Ru (neither has a failure), A7.2 is a per-metal boolean flip census, and Fe's flip bracket is U 7.5->9 with both endpoints clean — neither is a repaired point. There is no ranking contribution in this artifact to mark.

(5) THE TRIGGERING NUMBER IS UNREGISTERED AND NOW STALE. docs/56:329-331 states flatly "Neither numerator nor denominator is registered — both defensible bases tabulated," and docs/56:485-494 lists ten open A8.4 counting questions, including E(1) row-vs-attempt basis and E(6) "whether a row that converges on a round-3 .retry_ms deck stops counting as a ladder failure" — precisely the question that decides whether Fe s0_O is 25% or 0%. docs/45:2200 itself says the pre-repair rate is "reported either way," acknowledging the ambiguity. On the row basis post-tranche-2c both points now stand on converged registered repairs (u300 via r1, u450 via r2b, both visible in the run I did), so the rate is 0/8 = 0% and the 20% line does not fire at all. docs/56 records that choosing the basis is the entrant's call, not the readout's — a script that silently picked one basis and stamped a low-confidence verdict would itself be an unregistered decision, the exact failure mode the prereg discipline exists to prevent.

Nothing was dropped, nothing is concealed, the failure is labelled at row granularity inside the artifact being banked, the rate is disclosed in two dated documents, and the number that supposedly fired the threshold is one unregistered reading that is no longer true post-repair.

----------------------------------------------------------------------
[R10] (arithmetic) Repair-gap message counts NOT-RUN decks as convergence failures, asserting the outcome of jobs still in the queue
REFUTED BECAUSE:
REFUTED as filed. Three independent problems with the finding, two of them fatal.

**1. The evidence is stale; the branch is unreachable on the data on disk.**
The auditor's key evidence is `ls runs/a0/main/Fe/s0_O__u450__r2*` -> "no .out". That is no longer true. Both outputs landed (`git status --short` lists them as untracked; mtime 2026-08-29 16:02):
  runs/a0/main/Fe/s0_O__u450__r2.out   -> 1x "convergence NOT achieved" (genuinely FAILED)
  runs/a0/main/Fe/s0_O__u450__r2b.out  -> 0x, JOB_DONE, plus .lowdin.txt/.projwfc.in (CONVERGED)
The auditor's own whole-grid scan is also stale: re-running it now returns FOUR non-zero files (s0_O__u300, s0_O__u450, __r1, __r2), not three.

I re-ran the readout and emitted the JSON to scratchpad (repo untouched). `won` is now non-empty for Fe s0_O u=4.5, so control takes the `if won:` branch, not `elif cand:`. The banked row reads:
  "A6.5(2) REPAIR s0_O__u450__r2b, RESTARTED FROM s0_O__u300__r1 DENSITY (...); totmag 23.44; did not converge: s0_O__u450__r1 (failed QC), s0_O__u450__r2 (failed QC)"
Both listed decks DID run and DID fail, so the label is correct. A full-text search of the generated JSON for "registered repairs also failed" returns zero hits, and no (metal, state, U) anywhere in the grid currently satisfies qc-fail + registered candidates + zero winners (Ti's s0_OOH gaps take the "absent" path and Ti has no rows in m_a0_repairs*.txt at all). The stated consequence — "docs/figs/a0main_readout.json would bank a false statement about two jobs currently running" — is false: banking now produces a correct artifact.

**2. The arithmetic consequence is contradicted by the auditor's own quoted evidence.**
This finding is filed on the "arithmetic" dimension and claims the message "triples A8.4's convergence-failure count for Fe s0_O (1 failed repair reported as 3)". It does not. src/dft/a0main_readout.py:396-397 computes `n_fail = sum(1 for _, _, ws in gaps if any("qc-fail" in w for w in ws))` — a count over grid POINTS, not over repair decks. `len(cand)` appears only inside a prose why-string. A single Fe gap point yields n_fail = 1 no matter how many stems the string names, which is exactly what the auditor's own quoted stdout says ("1 QC-FAILED"). Their evidence refutes their consequence. A8.4 (docs/43:1594) registers a per-metal, per-state failure RATE against a 20% threshold; nothing derives that rate from this string. `grep -rn gap_why` finds one producer (a0main_readout.py:341) and zero consumers anywhere in src/ or docs/.

**3. The invoked precedent is not analogous.**
The 224-231 control fix the auditor cites mattered because conflating not-run with disagreeing flipped a VERDICT (metal marked UNTRUSTED vs INCOMPLETE — a registered quantity). The `elif cand:` branch changes only prose on a row that is a GAP either way: E[st] stays None, the point is plotted as a hole, and no registered quantity moves.

**Residual (why MINOR, not NOT_A_DEFECT).** There is a small real imprecision in the code: at 333-336 the message formats over `cand` and hard-codes "failed", so a registered-but-not-yet-launched repair stem would be described as failed, and the `lost` reason tuples ("not run" vs "failed QC") built at 305-312 are discarded on that branch. While array 20214003 was in flight that did print one wrong transient stdout line. But it is cosmetic, feeds no counter, is unreachable on the current inputs, and is not a banking gate. Take the one-line hardening when convenient; do not hold the bank for it.

----------------------------------------------------------------------
[R11] (arithmetic) pls-flip brackets and the A7.2 CONFIRMED verdict carry no margin test, while the same script applies the registered 0.20 eV floor to A6.3
REFUTED BECAUSE:
The arithmetic checks out (I reproduced all twelve bracket-edge margins exactly), but the rule the auditor imports does not govern the quantity they apply it to, and the verdict survives their own error model anyway.

(a) SCOPE. docs/43:958-967 registers the 0.20 eV floor inside A5.1(b), "The ranking-claim rule": it gates "a pairwise ordering of METALS" and specifically the "G_max gap between the pair" (Exner 2020 p.12611). docs/43:1264 confirms it binds unchanged as a ranking rule. A6.3 (eta(Ir) vs eta(Ru)) IS a pairwise metal ordering, which is why a0main_readout.py:534 applies the floor there. A pls flip is a within-metal identification of the limiting step -- neither a pairwise metal ordering nor a G_max gap. The 20-line asymmetry the finding highlights is registration scope, not an oversight. Under the pre-registration discipline the finding itself invokes, adding an unregistered margin gate to A7.2 after seeing the grid would be the violation, not the fix.

(b) THE QUANTITY IS REGISTERED AS VANISHING AT A CROSSING. A7.2 (docs/43:1348-1360) registers a binary prediction (">=3 of 6 metals show a pls flip inside the registered A0 grid") and a deliverable reported, per the script docstring (a0main_readout.py:22-23), as "the bracketing grid interval(s)" -- resolution = grid step, with no energy-margin claim attached. A7.3 (docs/43:1364-1367) registers verbatim that "the excess |dG2 - dG3|/2 vanishes identically at a pls crossing, so the ratio is a grid artifact", and A7.2's own non-blind disclosure treats Cr's production U landing "7 meV from the crossing" as the physical content. Measuring the winning-step margin at the two points that straddle a crossing and finding it small is tautological: no true flip on any finite grid can show a large margin at its bracket edges.

(c) THE VERDICT SURVIVES THE AUDITOR'S OWN TEST. Flip EXISTENCE is decided by the best-separated point in each pls regime, not by the bracket edges. Recomputed per-row margins: Cr 1.694 (U=0, pls3) vs 0.748 (U=9, pls2); Ir 0.369 vs 0.443; Mn 0.322 (U=0) vs 0.337 (U=1.5), plus pls-1 at 0.269 (U=9). Three metals clear 0.20 on both sides -> 3 of 6 -> CONFIRMED unchanged. The stated consequences also fail: a 9.5 meV shift at Mn U=6 moves the bracket to [4.5,6] but leaves the flip inside the grid; only Ru 3->2 and Fe 2->1 (each resting on the lone U=9.0 grid-edge point, 44/82 meV) are edge-fragile, and neither is load-bearing.

(d) DISCLOSURE EXISTS. a0main_readout.py:609-612 banks "the Ir bracket is saddle-conditional ...; the EXISTENCE of an Ir flip inside the grid survives the saddle correction, so the CONFIRMED status does not rest on the bracket"; the spin_state caveat names "the Ir flip bracket's low edge"; ir_ooh_basin quantifies the bracket's movement under a rigid -0.291 eV shift. docs/58:115-117 carries the same qualifier into the analysis of record, so "the report inherits an unqualified deliverable" is unsupported.

(e) The finding's own annotation is internally inconsistent: Mn [0,1.5] at 0.3221/0.3373 is called "the only one clearing every error class", but both sit below the 0.36 top of the 1x1 cell/coverage class under the script's own m <= hi test (line 527).

----------------------------------------------------------------------
[R12] (arithmetic) Fe's pls-flip census silently bridges the u450 hole, contradicting "never interpolated across"
REFUTED BECAUSE:
The finding's factual predicate is dead, and its logic does not hold even hypothetically.

1) THE FE HOLE NO LONGER EXISTS. The audit's evidence — "Fe shows rows at 3.00 and 5.30 with a `4.50 GAP (s0_O)` between them" — does not reproduce. `runs/a0/main/Fe/s0_O__u450__r2b.out` (mtime 2026-08-29 16:02:31, untracked, i.e. tranche 2c drained after the auditor's run) contains one "convergence has been achieved", a final `!    total energy = -2515.36930103 Ry`, and JOB DONE. Re-running `PYTHONPATH=src python src/dft/a0main_readout.py` now prints a measured Fe row at U = 4.50 (dG_OH 2.012, dG_O 4.437, dG_OOH 4.839, D 2.426, eta 1.196, pls 2) tagged `[s0_O: A6.5(2) REPAIR s0_O__u450__r2b, RESTARTED FROM s0_O__u300__r1 DENSITY ...; totmag 23.44; did not converge: s0_O__u450__r1 (failed QC), s0_O__u450__r2 (failed QC)]`, and Fe prints no GAPS line at all. Fe's grid is complete; no interval is bridged. Checked every metal in the current run: Cr/Ru/Ir/Mn/Fe have zero gaps, and Ti is all-gap with an empty `seq`, so `zip(seq[:-1], seq[1:])` bridges nothing anywhere. The path the finding describes is unreached on the registered inputs as they now stand.

2) THE AUDITOR CONCEDES NO FLIP WAS EVER HIDDEN, and the measured point confirms it. Their diagnostic used the non-converged estimate E(s0_O) = -2515.36433 Ry and got pls 2; the converged r2b value -2515.36930 Ry also gives pls 2, matching both neighbours. Even in the pre-drain state the Fe census printed exactly one flip (7.5 -> 9), which spans no hole. So there was no arithmetic error in any banked or printed number — the finding is a latent-risk argument dressed as an arithmetic defect.

3) THE "WRONG EDGES" CONSEQUENCE IS LOGICALLY FALSE. If a hole did sit inside a bracket, `zip` over surviving rows prints the two *measured* U values whose pls differ. "pls flipped somewhere between the nearest measured point below and the nearest measured point above" is a true and conservative statement; it invents no data point. docs/43:1276-1279 forbids exactly that invention — "NOT_CONVERGED and plotted as a gap — never interpolated across, never silently dropped. A grid with holes is reportable. A grid with invented points is not." The readout plots the hole (`{u:7.2f}    GAP (states)` at a0main_readout.py:376-378) and reports it (the GAPS line at 389-406, with the A6.5(2) escalation text when it is a qc-fail), and banks it (`gaps=[[u, ms, ws] ...]` in `m_out`). Nothing is silent and nothing is interpolated. A wider-than-necessary bracket is less informative, not wrong.

4) THE DIRECTIONAL CAVEAT ALREADY EXISTS where it matters. A hole can only merge two flips into one bracket or conceal an even-numbered excursion — it can only UNDERCOUNT flips. A7.2's registered prediction is ">=3 of 6 metals show a flip", so an undercount is conservative for the verdict, and the readout prints precisely that: "additional metals or grid points can only add flips, never remove one". The census line is also worded "no pls flip inside the *measured* band", already scoped to measured points. The auditor's reading that line 601's "grid has holes" note is the only hole disclosure ignores the GAP rows and GAPS line printed directly above the census in the same table block.

5) MISQUOTED REGISTRATION SCOPE. A7.2 (docs/43:1348-1356) registers "any row spanning a flip is reported in two pieces, never averaged across it" — a rule about averaging dG rows across a flip, which the readout never does. It does not register a per-bracket hole label.

----------------------------------------------------------------------
[R13] (arithmetic) The banked Fe s0_O u300 repair sits on a magnetic branch no other point in its column occupies, with no CONFOUND or branch label
REFUTED BECAUSE:
Refuted on four independent grounds; I re-ran `PYTHONPATH=src python src/dft/a0main_readout.py` and re-scraped every Fe s0_O .out file.

(1) THE FINDING'S ARITHMETIC IS COMPUTED AGAINST A GAP THAT NO LONGER EXISTS. The auditor's numbers require U=4.5 to be a hole: their "0.647 eV dG_O step" is exactly dG_O(u530) - dG_O(u300) = 4.628 - 3.981, and their "3.0->5.3 slope 0.281" is 0.647/2.3. Tranche 2c has landed. runs/a0/main/Fe/s0_O__u450__r2b.out line 2139 reads "convergence has been achieved in 18 iterations", and the current readout banks U=4.5 at dG_O = 4.437. The actual per-interval dG_O slopes from the rows the readout would bank are: 0->1.5 0.189 | 1.5->3.0 0.226 | 3.0->4.5 0.304 | 4.5->5.3 0.238 | 5.3->6.0 0.143 | 6.0->7.5 0.123 | 7.5->9.0 0.109. The branch-crossing interval (4.5->5.3, 23.44 -> 21.98 uB) has slope 0.238 -- LOWER than the 0.304 in-branch slope immediately preceding it. "The cross-branch slope is 2x the largest in-branch slope" is false against the actual data, and the 0.647 eV step the finding apportions does not appear in the table at all.

(2) THE FINDING'S CENTRAL FACTUAL CLAIM IS FALSE. "The repair banked at U=3.0 is the only grid point on the 22.90 branch." u450 is now banked from __r2b, which was seeded from s0_O__u300__r1's density -- the 22.90 parent -- and converged at 23.44 uB. Two adjacent grid points, not one, and the second exists precisely because the registered protocol ran the branch search from both legal parents. (__r2, seeded from the 21.98 parent, hit "convergence NOT achieved after 200 iterations" at line 3421, so the readout's "did not converge: s0_O__u450__r2 (failed QC)" is honest and no branch-splitting number is owed. docs/59:120-123 pre-registered that outcome as "one converged means that one (labelled with its branch)", and the printed note does label it -- it names both the parent deck and the resulting totmag.)

(3) THE DISCLOSURE THE AUDITOR SAYS IS MISSING IS REGISTERED, PRE-LAUNCH, WITH THE SAME NUMBERS. docs/59-a0-roster-correction-2026-08-28.md:113-125 (committed at bcd3cc0, before either job was submitted) states: "the s0_O ladder runs 18.91, 21.36, 22.90 up to u300 and 21.98, 21.99, 22.00, 21.99 from u530 on, so u450 sits on the crossing between two nearly degenerate branches", and closes with the explicit limit: "the other seven s0_O rungs were not branch-searched, and each row's totmag travels into the readout so a reader can see which branch every point is on." src/dft/a0main_readout.py:135-143 (the _totmag docstring) repeats it and cites docs/59 s3c: "which branch a row landed on is part of the reading, not a footnote. Reported for every spin-polarised row." runs/a0/m_a0_repairs2.txt says it too. And it is implemented: I dumped the banked JSON and every Fe row carries a per-state totmag dict (slab/s0_O/s0_OH/s0_OOH at all 8 U), not just the repaired ones. The auditor concedes this ("the JSON row-level totmag is present") and then re-files the same fact as an undisclosed hazard. The spin_state caveat is the nspin-convention caveat for the arms; it was never the vehicle for the branch disclosure, and docs/59 names the vehicle that was.

(4) A8.3's 0.05 uB THRESHOLD IS A RULE FROM A DIFFERENT CONTRAST AND DOES NOT GOVERN A U-LADDER. docs/43:1560-1590 introduces it as "the magnetic analogue" of the geometry CONFOUND for S3 pairs -- members meant to be the same state differing only in the intended contrast (yaw90/yaw270, magp/magm, a __g1 child re-run at its own relaxed parent's geometry). Its stated effect is to exclude such a pair "from the contrast statistics". In a Hubbard-U ladder the moment is a function of the very variable being swept. Applied to consecutive U rows the rule fires on Fe's u000->u150 step (18.91 -> 21.36 = 2.45 uB, 49x the threshold) between two ORIGINAL, converged, unrepaired points. The "18x the threshold" framing proves far too much.

AND THE CONSEQUENCE DOES NOT LAND. No registered A0 quantity for Fe is a cross-U difference involving s0_O. P7 swing and the apex crossing are gated on `if metal == "Cr"` (a0main_readout.py:424), so there is no "shape-based reading" of Fe's curve in the readout. A6.3 ordering is Ru/Ir only (docs/59:169-171: "Mn/Fe/Ti do not enter it"). A7.3's c_M is `dg["OOH"] - dg["OH"]` -- s0_O does not enter it. Fe's only A7.2 contribution is the pls 2->1 flip between U=7.5 and 9.0, where s0_O sits at 22.00 and 21.99 uB, a 0.01 uB difference that is inside even the threshold the auditor invokes. The branch crossing at U=3-5.3 touches nothing Fe is scored on.

----------------------------------------------------------------------
[R14] (arithmetic) A8.4's registered per-state convergence-failure rate is never computed, and Fe s0_O is over its 20% line
REFUTED BECAUSE:
The finding fails on all three of its load-bearing assertions.

1) SCOPE — the rule is S3's, not A0's. A8.4 lives inside AMENDMENT 8, whose Governs line (docs/43:1483-1487) reads "Governs: S3 — tier_v3 crossed coverage x symmetry x basin over 8 metals; the dy ladder; GATE-1 depth; the CONFOUND rule; P-SYMCOV; the convergence-failure budget; ...". The threshold text itself says "S3 records a per-metal, per-state convergence-failure rate" (docs/43:1600). A0-main is registered under AMENDMENT 6; its own operational rule A6.5(2) (docs/43:1271-1279) registers ONLY the escalation ladder and the NOT_CONVERGED-plotted-as-a-gap rule, and imposes no rate quantity. A8.4 imports A0's ladder into S3 ("The escalation ladder is A6.5's, unchanged"), not the reverse. The registered S3 artifact for that quantity exists and is already banked: runs/s3/readout/a84_rates_2026-08-24.json/.md, indexed at docs/56:20 — it contains the attempt classification (24 scf_nonconvergence / 16 infrastructure_kill), the A8.3-refusal exclusions, and the by-record gaps. So the registered reported quantity is neither missing nor uncomputed; the auditor looked for it in the wrong block's readout.

2) FACTUALLY WRONG — the A0 rate IS computed and reported, in three committed places. src/dft/build_a0main_w2b.py:314-315 writes it into the campaign manifest: runs/a0/main/manifest.json key /tranche_2b/repairs_a652i/a84_rate = "Fe s0_O pre-repair failure rate 2/8 = 25% (> 20% A8.4 line); reported either way, label travels" (verified by loading the JSON). docs/45-error-ledger.md:2200: "A8.4 note: Fe s0_O pre-repair failure rate is 2/8 = 25%, over the 20% line; reported either way, and the failed .outs stay on disk." docs/59-a0-roster-correction-2026-08-28.md:96: "A8.4 reports the 2/8 = 25% pre-repair rate either way." All three files are clean in git status (committed, not working-tree scratch). The finding's consequence sentence — "A registered reported quantity is absent from the artifact that banks the A0 result" — is therefore false: the manifest is that registry, the number is in it, and the >20% comparison is already made explicit.

3) STALE — the conditional the auditor quotes resolved to its OTHER branch. build_a0main_w2b.py:51-55 states the disjunction in full: "(Fe s0_O: 2 of 8 grid points = 25%, over A8.4's 20% line -- so if the repairs do not converge, Fe's s0_O contribution is marked low-confidence in the report; if they do, the repaired points carry the label ... into every artifact)". The auditor checked s0_O__u300.out, s0_O__u450.out and s0_O__u450__r1.out and stopped. Measured on disk now: s0_O__u300__r1.out = "convergence has been achieved" (rung (i) succeeded); s0_O__u450__r1.out and __r2.out = NOT achieved; s0_O__u450__r2b.out = "convergence has been achieved in 18 iterations" (rung (ii), seeded from s0_O__u300__r1). Both failed grid points are repaired, so the branch that fires is the label branch, and the label does travel: the live readout prints, on the Fe U=3.00 and U=4.50 rows, "[s0_O: A6.5(2) REPAIR s0_O__u300__r1, RESTARTED FROM s0_O__u150 DENSITY (original SCF failed at 200 iterations; failed .out retained, A8.4); totmag 22.90]" and "[s0_O: A6.5(2) REPAIR s0_O__u450__r2b, RESTARTED FROM s0_O__u300__r1 DENSITY (... failed .out retained, A8.4); totmag 23.44; did not converge: s0_O__u450__r1 (failed QC), s0_O__u450__r2 (failed QC)]", and a0main_readout.py:289 and :317 carry those notes into row["repaired"] in the banked JSON. Post-ladder, Fe s0_O has 0/8 NOT_CONVERGED — nothing is dropped, nothing is interpolated, and every failure event is named in the artifact by deck. That is precisely the selection effect A8.4's motivating paragraph (Co *OOH x4, Ni *OOH x5 "dropped silently") exists to prevent, and it is not occurring here.

What is literally true is only the narrow observation that a0main_readout.py prints no percentage — it reports failures as classified GAPS (:394-406, with the substring-test fix explicitly annotated "A8.4 makes the failure rate a reported quantity") and as per-row A8.4-tagged repair notes. Against A0's actual registration (A6.5(2)) that is complete, and the numeric rate is banked in the manifest and the ledger regardless. No BLOCKER, no MAJOR, no MINOR: the disclosure the auditor says is missing exists in the manifest, the error ledger, and docs/59, and the mark the auditor says is owed is conditioned on a branch that did not fire.

----------------------------------------------------------------------
[R15] (arithmetic) The Ti *OOH re-anchor at 1.782 A misses the M-O basin every measured *OOH in this campaign occupies, and bypasses the registered PULL_TO multi-start remedy
REFUTED BECAUSE:
REFUTED. The auditor's measurements are all reproducible (I recomputed every distance min-image from the .in files: Ti re-anchor 1.7819 = mean(1.7346, 1.8293); relaxed *OOH at Cr 2.0764 / Mn 2.4803 / Fe 2.5525 / Ru 1.9474 / Ir 1.9120; PULL_TO=(1.70,2.10) at surfaces_rutile.py:171; and the grep for PULL_TO/multi-start/docs/34 in w2c/w3/w2b/readout/docs/45/docs/59 is indeed empty). The inference built on them is what fails, on four independent grounds.

(1) THE CENTRAL MECHANISM IS MEASURED FALSE IN THIS REPO. The finding claims a start at 1.782 A "never samples" the 2.0-2.6 A basin, treating a relaxation's start as its endpoint. results/r3_predict_validation.json is the campaign's own per-start log of the PULL_TO protocol -- start tag, steps, energy AND final M-O -- for s0_OOH on all five measured metals:
  Cr pull1.70 -> m_o 1.951, pull2.10 -> 1.961 (1.1 meV apart)
  Mn pull1.70 -> m_o 2.597 (the WINNER), pull2.10 -> 2.528
  Fe pull1.70 -> m_o 2.541, pull2.10 -> 2.564 (2 meV apart)
  Ru pull1.70 -> m_o 1.880, pull2.10 -> 1.882
  Ir pull1.70 -> m_o 1.916, pull2.10 -> 1.919
A 1.70 A start relaxes INTO the 2.5-2.6 A basin on exactly the two metals (Mn, Fe) whose basins the auditor cites as unreachable, and lands within 1-2 meV of the 2.10 start on the other three. The DFT side agrees: runs/Mn_slab/s0_OOH.in starts at 2.0764 and the A0 deck is 2.4803 (34 ionic steps outward); Fe 2.0764 -> 2.5525 (29 steps); Cr ran 82 steps from a 3.1192 builder start DOWN to 2.0764. There is no minimum inside the bond for the walk to be trapped in -- only repulsion, which drives it outward into the first bound basin, which is the one at issue.

(2) THE DEVIATION IS INVERTED. 1.7819 A sits INSIDE the PULL_TO bracket, 0.08 A above its lower target. The registered remedy's own 1.70 A target underestimates the relaxed *OOH M-O by MORE than the *O/*OH mean does, on all five metals -- by design, since a pull-in deliberately starts inside the bond. The r3 start is the campaign's pull-in idiom applied for the first time in the QE chain, not a bypass of it. (Also: 1.70 vs 2.10 differ by 1-2 meV for *OOH on 5/5 metals, so the "AND 2.10" half of the alleged remedy is measurably worth nothing here.)

(3) PULL_TO DOES NOT GOVERN THIS CODE PATH. adsorbate_starts has exactly one caller in the repo -- src/hea_oer/adsorption.py:193, the MLIP screening path -- and PULL_TO's other use is dft/mlip_predict.py, also MACE. The QE deck builder calls single-start add_oer_adsorbate_at (qe_slab.py:271) and has done so for every metal in the campaign. docs/34's multi-start passage is explicitly about mlip_predict.py; docs/36 s2 registers the remedy for the HEA screening path precisely because that path has "no DFT tier downstream to catch it". docs/43 registers no pull-in multi-start for QE relaxations. So there is no registered rule for w2c to deviate from or disclose; if there were, every DFT relaxation in the campaign would be in violation, not tranche 2c.

(4) TRANCHE 2C ALREADY IS MULTI-START WITH LOWEST-ENERGY SELECTION. r2 (3.4145, outer) + r3 (1.7819, inner) bracket the 1.91-2.55 window from both sides under a pre-declared lowest-converged-energy rule -- structurally the same protocol, and the auditor's own consequence sentence concedes the rule is registered.

Two further factual slips: "every relaxed *OOH in this campaign" occupies 2.0-2.6 A is false for Ru (1.947) and Ir (1.912), which sit within 0.17 A of the r3 start; and the r3 deck is chemically sane (anchor O 1.782 to cus Ti, nearest surface O 2.640, second O 2.899, H 3.847 -- compare the relaxed *O at 1.735 / 2.796), so no bad-contact defect either.

Residue, and it is not the reported finding: build_a0main_w2c.py:115-116 "changing where the walk STARTS cannot bias where it ENDS" is literally false as a general claim -- Cr's *OOH went to a desorbed 3.013 from the builder start and 1.951, 0.088 eV lower, from a pull-in (docs/34). But the same sentence's first clause concedes "a relaxation's answer is its local minimum", and the paragraph four lines later (:120-126) fixes a rule for the case where the two starts land in different basins and calls the gap "the measured basin gap" -- so start-dependence is disclosed in the same docstring, and the sentence's operative claim (the anchor was derived mechanically, not tuned) is true. Loose wording, self-qualified, MINOR at most, and not what the finding is about.

Finally, nothing is at stake yet: the readout prints "Ti NOT YET SCORED" with s0_OOH as a gap and the Ti re-run control marked INCOMPLETE (3/4 states), so the finding's precondition ("a Ti *OOH energy banked from r3 alone") does not obtain and does not gate this banking.

----------------------------------------------------------------------
[R16] (arithmetic) Tranche 2c's pre-launch record of the Ti rung-(ii) trace attributes a force drop spanning two runs to rung (ii), and mis-states the plateau and spike magnitudes
REFUTED BECAUSE:
The finding does not survive. I reconstructed the walk from the raw outputs: geom0 (built, d=3.167, E=-1301.63158987, F=0.281208) was the only geometry the ORIGINAL run converged; its BFGS proposed geom1 (d=3.263) and the SCF there FAILED, so the original run banked ZERO completed ionic steps -- QE's own counter prints "number of bfgs steps = 0" and build_a0main_w3.py:13 already records "zero bfgs steps banked". s0_OOH_r1.in is byte-equal to that geom1 block (verified). Rung (ii) therefore COMPLETED the geom0->geom1 step (converging E=-1301.68764608, F=0.172552) and then the geom1->geom2 step (E=-1301.71065326, F=0.124679, d=3.325), and the third step's SCF (geom3, d=3.414) limit-cycled. So "banked TWO ionic steps ... the total force fell 0.281 -> 0.125 ... and then the third step's SCF limit-cycled" is one coherent whole-walk accounting: two completed steps, force at their start and end, third step failed -- matching the docstring's own distance table (3.167 -> 3.263 -> 3.325 -> 3.414, "3 (then failed)") and w3's explicit disclosure that r1 continues the same walk. The auditor's proposed correction (0.173 -> 0.125) would make a two-step claim span one step. 0.173 was itself first measured BY rung (ii); the last force known before it launched was 0.281.

The plateau and spike charges are affirmatively wrong. Last SCF block, 200 iterations: iteration 13 = 1.1321e-4 and the median of the remaining 187 is 1.31e-4, so "~1.2e-4" is a fair plateau centre. Spikes are genuinely periodic (iters 34, 44, 67, 98, 108, 118, 134, 144, 154, 164, 174, 184, 194) with MEDIAN 5.22e-3 -- "periodic spikes to ~5e-3" describes them correctly; the auditor's "2.7x understatement" substitutes the single maximum (1.334e-2) for the typical spike the sentence is about. "Never improving": the floor after iteration 13 is 7.575e-5, still 76x above conv_thr 1e-6, and the trace ENDS at 1.6041e-4 -- worse than iteration 13 -- so a limit cycle that finishes higher than it started is fairly described as never improving; that clause is not a quantitative claim anyone audits against a threshold.

Residual: the energy clause (0.023 Ry, r1-internal) uses a different span from the force clause (two steps). That is the opposite number from the one flagged, it understates rather than overstates rung (ii)'s progress, both values are true of the named run, and neither enters a threshold, a verdict, or the readout (rederive() machine-checks only the ionic-step count and the totmags). That is a wording-scope ambiguity, which the audit brief excludes.

----------------------------------------------------------------------
[R17] (arithmetic) The extraction control reads energies with no QC while the grid it gates uses strict QC
REFUTED BECAUSE:
The finding is structurally unreachable, and the code already implements the safe behavior the auditor asks for.

1. WHAT `_final_ry` CAN EVER SEE. The comparator set is fully enumerated by the METALS table (src/dft/a0main_readout.py:88-109): 24 files of the form runs/probe/{Cr|Ru|Ir|Mn_audit|Fe_audit|Ti_audit}/<state>__{u0.0|base}.out, plus the ONE override runs/a0/main/Fe/s0_OOH__pilot530_m010.out, compared against runs/a0/main/<metal>/<state>__{u000|u390|u530}.out. I read the matching .in for every one of them: all are `calculation = 'scf'`. The a0 side is emitted by src/dft/build_a0main_w3.py:105 (`calculation="scf"`), and src/dft/probe_decks.py:359 hard-rewrites comparator decks to `calculation = 'scf'`. No entry in the table (or the override table) can name a relax.

2. WHY THAT KILLS THE SCENARIO. The auditor's failure mode -- "a relax that died mid-trajectory but still printed a ! line" -- is the ONLY file class where the last `!` diverges from qe_qc's number (qe_qc.scan uses _RE_FINAL_E `Final energy` to override the last `!`, and that line exists only for a cleanly finished relax). For an `scf` deck pw.x prints `!    total energy` ONLY on SCF convergence. Measured in-repo on the campaign's two real A0 SCF failures: runs/a0/main/Fe/s0_O__u450.out and runs/a0/main/Fe/s0_O__u300.out each contain `convergence NOT achieved` and ZERO `!` lines. So a QC-failing scf comparator makes `_final_ry` return None, which lines 226-232 route to `uncompared`, producing verdict INCOMPLETE (lines 246-247) -- exactly the "FAIL or INCOMPLETE instead of a false OK" the finding says is missing.

3. THE CITED EXAMPLE IS OFF-PATH. runs/Ti_slab/s0_OOH_r1.out really is a `relax` with 2 `!` lines (line 1994: -1301.68764608 Ry; line 2768: -1301.71065326 Ry) and a `convergence NOT achieved`. But runs/Ti_slab/ is the Ti SOURCE relaxation directory -- build_a0main_w2c.py:295-355 writes s0_OOH_r2/_r3 there too -- and nothing in it is reachable from the control, which reads only runs/probe/Ti_audit/<state>__base.out and runs/a0/main/Ti/<state>__u000.out. The auditor took a genuinely poisoned file from one tranche and asserted it could land on a different tranche's code path.

4. NUMERICALLY THERE IS NO GAP TODAY. I ran qe_qc.scan on all 24 reachable comparator/a0 files: every one is TRUSTWORTHY (except Ti s0_OOH, which is MISSING on BOTH sides and is correctly reported as INCOMPLETE 3/4 in the live run), and in every case scan()["energy_ry"] equals the last-`!` value to the last digit. The auditor concedes no live instance exists.

5. THE a0 SIDE IS ALREADY QC'D ANYWAY. Every control token (u000; Mn u390; Fe u530) is itself a registered grid point, so that same file passes through qc.trusted_energy_ev(strict=True) at line 279; a poisoned a0-side control would surface as a GAP in the table independent of the control.

RESIDUAL, and why it still is not the claimed defect: an scf comparator that converged (printed `!`) but was killed before `JOB DONE` would be QC-INCOMPLETE yet still compared. But in a single-point scf the `!` line IS the converged total energy of that geometry, so the drift number would be correct and an "OK" verdict would be a truthful statement about SCF re-run agreement -- not a false OK. That residual is a missing provenance note, not an arithmetic error, and it cannot move a banked number: the control writes only control_ok and rerun_determinism_check_meV, never an energy that enters delta_G or eta.

----------------------------------------------------------------------
[R18] (arithmetic) Fe s0_O's geometry-extraction control is a 100x outlier that the readout points at but never evaluates
REFUTED BECAUSE:
REFUTED on three independent grounds. The auditor's arithmetic reproduces, but every inference drawn from it fails.

(1) "Nothing computes it" is false — the control IS computed, by the registered gating instrument, and it PASSES. `src/dft/probe_eta.py:120-143` is the registered implementation of docs/41 §5 P1 ("extraction control (gating)": |E(base) − E(relaxation final)| ≤ 5 meV, docs/41:313-316). It reads exactly the field the note points at (`probe_eta.py:88-92`, `ref_relax[j["job"]] = j.get("relax_reference_ev")`). Running it prints the auditor's own panel verbatim:
    PYTHONPATH=src python src/dft/probe_eta.py runs/probe/Fe_audit
    s0_O   drift +0.52 meV  OK
    s0_OH  drift +0.00 meV  OK
    s0_OOH drift +276.60 meV DRIFT
    slab   drift +0.00 meV  OK
So the Fe s0_O row is computed, reported, and scored OK against its registered threshold with ~10x margin. The a0cell readout does the same for the 2x1v leg (`a0cell_readout.py:64,130-148`, "tol 5 meV").

(2) The threshold is misapplied — a rule from a different control class. A8.5's 1e-5 Ry (docs/43:1606-1615; docs/52:219 row 30) is the CROSS-MACHINE parity bound for re-running a *banked deck* (same deck, same geometry, Vast vs Anvil). The probe-vs-relax quantity has its own registered tolerance, 5 meV (P1), and `a0main_readout.py:82-84` already names both and says which governs which: "threshold 1e-5 Ry = 0.014 meV -- tighter than the 5 meV extraction tolerance". probe_eta.py's own docstring (:13-16) pre-declares the reason: 5 meV is "comfortably above SCF noise at conv_thr 1e-6". 0.5215 meV is inside the registered bound by 9.6x.

(3) The outlier is not unexplained, and it is not "on the failing state" by coincidence — it tracks the deck, not the metal. The banked Fe s0_O relaxation is the RESTART deck: `runs/Fe_slab/s0_O.out.attempt1` ends "convergence NOT achieved after 200 iterations", and the banked `s0_O.out` was produced from `runs/Fe_slab/s0_O.in.restart`, which differs from the class default at exactly two lines — mixing_beta 0.3 -> 0.1, electron_maxstep 200 -> 500. The probe deck `runs/probe/Fe_audit/s0_O__base.run.in` runs the class default (beta 0.3, maxstep 200). Across the whole Fe/Mn/Ti comparator set, the ONLY two states whose source relaxation used a restart deck are Fe s0_O and Fe s0_OOH (`ls runs/*_slab/*.restart`; Mn_slab and Ti_slab s0_O/s0_OH/slab are all beta 0.3) — and those are precisely the only two non-clean rows. Everything else about the pair is like-for-like and rules out an extraction bug: geometry matches the relax's final ATOMIC_POSITIONS to the 1e-9 A print rounding, K_POINTS 8 4 1 identical, U Fe-3d 5.3 identical, ecut/smearing/nosym/noinv identical, site moments agree to ~5e-3 uB on all 19 atoms (same branch — consistent with the identical 21.98/21.98 totmag the auditor cites). The residual is SCF-path noise between two converged solutions at different tightness: QE's BFGS upscale drives the relax's final SCF to ~1e-8 Ry (accuracy ladder in s0_O.out runs 8.6e-7 -> 8.7e-9 over 38 ionic steps) while the probe's fresh SCF stops at 2.2e-7 Ry against conv_thr 1e-6. That is the exact noise class the 5 meV tolerance was sized for.

(4) The stated consequence does not exist. `relax_reference_ev` is written by `probe_decks.py:424` and consumed only by probe_eta.py's P1 gate; no dG, no eta, and nothing in `a0main_readout.py` uses it. 0.52 meV is also below the readout's own 3-decimal eV print precision.

(5) The "artifact that names it as the control of record" framing misreads the note. `a0main_readout.py:411-420` does not claim to compute the extraction control — it disclaims being one. The printed line reads "re-run agreement check ... NOT a geometry round-trip control", the docstring (:39-42) carries an explicit "CAVEAT (2026-08-28 adversarial review)" making the same distinction, and docs/58:62 states it again ("The genuine geometry-extraction control lives here (2x1v leg, <=5 meV); A0-main's U = 0 'control' is a same-deck re-run determinism check and is named exactly that"). The note is a correct pointer to where the datum lives; the datum is scored elsewhere, by the instrument that registered the rule.

Nothing here blocks banking a0main_readout.json.

----------------------------------------------------------------------
[R19] (ti-geometry) r3 re-anchors to a single short bond (1.782 A) where the campaign's registered remedy is a two-point multi-start, and 1.78 A is on the side that lost 6 of 7 times for *OOH
REFUTED BECAUSE:
REFUTED on four independent grounds; the finding's own citations contradict it.

1) "The registered remedy already exists in the codebase" is false for the DFT side. `surfaces_rutile.adsorbate_starts` / `PULL_TO = (1.70, 2.10)` is a PREDICTOR-side (MACE/ASE) construct, not a deck-building one. Every call site is an MLIP relaxation: C:/Users/frank/sts-electrocatalyst/src/hea_oer/adsorption.py:193 (inside `_predict_rutile_multisite`, relaxing with `.relax.relax` + a MACE calculator), src/dft/mlip_predict.py:74/113 (MACE-MPA-0, ASE BFGS, `starts()` at :103-119), src/scripts/screen_mace.py:43. `grep -rn "adsorbate_starts|PULL_TO" src/` returns NO Quantum ESPRESSO deck builder. There is no registered A0/DFT rule requiring a two-point multi-start; the registered escalation ladder for A0 is A6.5(2) (i) density seed / (ii) halve beta / (iii) NOT_CONVERGED (docs/43:1267), which build_a0main_w2c.py follows and extends under a dated, bounded, disclosed extension (w2c docstring :127-142).

2) The auditor's own key citation says the opposite of what the finding claims. src/dft/build_s3.py:164 is `PULL_TO = 1.70` — a SCALAR, one start, not the (1.70, 2.10) tuple — and the guard at :698-699 asserts the Ti template's binding O sits at exactly z_cus + 1.70. runs/s0/g_tio2_timing/README.md documents that deck as the campaign's only fresh Ti *OOH DFT start (build_s3.py:71: "the PULL_TO idiom the campaign's only fresh *OOH start used"). So the campaign's DFT-side convention for a hand-placed Ti *OOH is a SINGLE ~1.70 Å pull-in. build_ti_r3's 1.7819 Å is within 0.08 Å of that convention and is measured from Ti's own two converged bonds (1.734553 / 1.829256, re-derived on disk by `relaxed_bond`, build_a0main_w2c.py:331-348) rather than a five-metal median. r3 is the campaign's convention applied more carefully, not a departure from it.

3) Tranche 2c is not a single start, and the "keep the lowest" rule is registered. I parsed the decks: runs/Ti_slab/s0_OOH.in anchor-Ti 3.1668 Å, s0_OOH_r1.in 3.2626, s0_OOH_r2.in 3.4145, s0_OOH_r3.in 1.7819. Two arms are in flight bracketing the region (long/desorbed continuation at 3.41 Å; bonded basin at 1.78 Å), and build_a0main_w2c.py:120-126 fixes the selection rule BEFORE launch: "If both converge, the LOWER total energy is the banked s0_OOH geometry (docs/41 s6d, A6.4) and the difference is reported as the measured basin gap." That is exactly the multi-start-with-lowest-energy idiom; the finding's "without the 2.10 A arm ever being run ... a single-start number" ignores r2 and the pre-registered selection rule.

4) The "1.78 is the losing side / 2.55 Å is unreachable" claim imports a different chain, different metals, and reverses the campaign's own datum. docs/36's "winning starts" column is MACE relaxations on 2×2 Vegard HEA slabs over Cr/Co/Ir/Ru/Mn/Ni/Fe — Ti is not in that set, and the A0 Ti chain is a 21-atom 1×1 nspin=1 d0 TiO2 QE cell (runs/Ti_slab/s0_OOH_r3.in:10 nat=21). docs/38-matched-protocol-parity.md:51 measures that entire multi-start as worth "≤3 mV on every tier metal". And the 2.552 Å Fe minimum is not evidence that short starts are trapped: docs/33-r3-mlip-evaluation.md:280-286 records it was reached by walking OUTWARD — "from the transplanted 2.076 Å back out to 2.54" — with the DFT repair at :294 going 3.949 → 2.552 Å from that same ~2.08 Å start. Relaxations in this campaign demonstrably move outward by ~0.5 Å from short starts; "unreachable from 1.78 Å" is asserted, not shown, and the 2.552 Å datum is Fe in the HEA chain, not Ti.

5) It cannot be a BLOCKER on banking. I ran `PYTHONPATH=src python src/dft/a0main_readout.py`: Ti's entire *OOH column is "GAP (s0_OOH)" at all 7 U points, footnoted "7 with no output banked (not yet run; not a convergence event)", and the coverage caveat states "Ti NOT YET SCORED ... whatever stays unscored at freeze is WITHDRAWN-UNSCORED". No r3-derived quantity enters docs/figs/a0main_readout.json, so nothing about r3 gates this bank.

6) Trap 8 (docs/45-error-ledger.md:2253-2257) does not fire against the commit: trap 8 is about a fix already being ON (mixing_mode = 'local-TF' at qe_slab.py:175). `adsorbate_starts` is not on any DFT path in this repo and never was, so there was no already-on fix to grep for.

Residual, non-blocking and NOT what was reported: no intermediate arm (~2.1-2.5 Å) is being run, so if r2 fails as its own diagnosis predicts (nspin=1 cannot describe the desorbed radical) and r3 converges to a short bond, the banked Ti *OOH geometry will rest on a two-point search that never sampled the weak-binding window. That is a coverage/cost judgment on a bounded, disclosed ladder extension, not a defect against any registered rule, and the docstring already frames the outcome as "the measured basin gap" between two named arms with provenance labels rather than as an exhaustive search.

----------------------------------------------------------------------
[R20] (ti-geometry) The "walked DOWN vs walking UP" contrast is not a discriminator: both converged controls also recede for their first 4 and 6 ionic steps
REFUTED BECAUSE:
REFUTED. The auditor's numbers reproduce exactly (I re-parsed every ATOMIC_POSITIONS block with the same cell and min-image convention and got 3.1168/3.1177/3.1257/3.1246/3.1175... for s0_O and 3.1668/3.1802/3.2101/3.2151/3.2167... for s0_OH), but the metric they chose does not measure what they claim it measures.

d(anchor O, nearest Ti) is a distance to a MOVING substrate atom. The rutile(110) cus-Ti is free ("1 1 1" in the deck) and relaxes inward from the first ionic step. Decomposing the same trajectories over steps 0-3:
  s0_O  : adsorbate z 19.3580 -> 19.3428 -> 19.3165 -> 19.3013 (Dz = -0.0567 A); cus-Ti z 16.2412 -> 16.1767 (Dz = -0.0645 A)
  s0_OH : adsorbate z 19.4080 -> 19.3995 -> 19.3841 -> 19.3763 (Dz = -0.0317 A); cus-Ti Dz = -0.0800 A
  s0_OOH: adsorbate z 19.4080 -> 19.4642 -> 19.4870 -> 19.5276 (Dz = +0.1196 A); cus-Ti Dz = -0.1280 A
Both converged controls' adsorbates move TOWARD the surface monotonically from step 1. Their +0.009 A and +0.050 A "recession" is the cus-Ti dropping away faster than the adsorbate descends -- standard 5c-Ti inward rumpling, not adsorbate recession. *OOH's anchor rises in absolute z at every step. At matched step count the adsorbate-motion component is sign-opposite: -0.057 / -0.032 vs +0.120 A. The contrast IS a discriminator.

The forces settle it independently, from the same .out files the auditor parsed:
  s0_O   anchor Fz: -0.02877, -0.02375, -0.02076, -0.01961, -0.01862 Ry/au -- INWARD at every step, including the exact steps the auditor calls recession
  s0_OH  anchor Fz: -0.01598, -0.01359, -0.01324, -0.01150 -- INWARD at every step
  s0_OOH anchor Fz: +0.10613, +0.04313, +0.04917 -- OUTWARD at every step, 4-7x larger in magnitude
So the sign of the first three steps carries information after all; the auditor's coordinate merely hid it.

The adsorbate centroid corroborates: the controls' centroid heights fall monotonically (3.1168->3.0601 and 3.5168->3.4921 over 3 steps) while *OOH's centroid is frozen (4.0835->4.0788, 0.005 A) as its anchor swings up 0.248 A -- precisely the "fixing its internal geometry while its centre drifts" described at docs/45:2246-2248. Even inside the auditor's own metric the excursions are not comparable: +0.009 A (turned at step 2) and +0.050 A (increment collapsed 0.0299 -> 0.0050, at the knee) versus +0.248 A with increments +0.096/+0.063/+0.089 showing no deceleration -- 5x and 31x, with opposite curvature.

Two secondary claims in the finding are also misreads of the source. (1) "the amendment extending mixing_ndim/electron_maxstep that it authorises": build_a0main_w2c.py:101-104 justifies mixing_ndim 8->16 as "more Broyden history, the textbook lever against a limit cycle" and applies it identically to BOTH r2 and r3. It is the MIXER-hypothesis lever, applied under the limit-cycle observation; the geometric diagnosis authorises only r3's re-anchored start. (2) The diagnosis stakes nothing on being right: build_a0main_w2c.py:106-127 runs the plain continuation (r2) and the re-anchored restart (r3) with identical numerics, pre-declares selection by LOWER converged total energy per docs/41 s6d / A6.4, and states "If neither converges, A6.5(2)(iii) stands." A wrong diagnosis costs one extra relaxation, never a banked quantity or a verdict. The consequence the finding asserts -- that the diagnosis "does not distinguish the failing state from the two that converged" -- is false on the repo's own trajectories, and docs/59:136 already discloses the only real limitation (three steps is too short to conclude non-binding).

The single true residual is cosmetic: the docs/45:2244-2249 table and docs/59:131-136 juxtapose built->relaxed endpoints for the controls with a 3-step prefix for *OOH, and in that metric the controls do tick up by 0.009/0.050 A. That is a writing choice, not a defect, and it does not gate banking.

----------------------------------------------------------------------
[R21] (ti-geometry) The net force on the *OOH adsorbate points TOWARD the surface at all three steps and its centre of mass is static; "its centre drifts away" is measurably false
REFUTED BECAUSE:
The auditor's raw numbers reproduce exactly (I re-parsed every force and position block), but the inference does not survive.

1. WRONG FRAME. The centre-of-mass was computed in absolute cell z while the anchoring Ti sinks 0.128 A over the same three steps (z_Ti5: 16.2412 -> 16.2016 -> 16.1619 -> 16.1132). Measured against that Ti -- the reference the whole record and adsorbate_qc.m_o_distances use -- the *OOH centre recedes monotonically at every step: |COM-Ti5| = 3.7046 -> 3.7460 -> 3.7993 -> 3.8551 A (+0.1505 A), with increments GROWING (+0.041, +0.053, +0.056). Same quantity for the two controls over the same three steps: s0_O +0.008 A, s0_OH +0.049 A (both then turn around and descend to 1.735/1.829 A). So "its centre drifts away from the surface" is exactly what the files show, and it is 3x-19x the controls' transient. The "rotation" reading also collapses in that frame: |O_term-Ti5| 4.2130 -> 4.2806 and |H-Ti5| 5.2085 -> 5.2521 -- all three adsorbate atoms recede from the anchor Ti; nothing swings inward. The auditor's "terminal O falls 0.085, H falls 0.049" are lab-frame motions smaller than the 0.128 A the surface itself drops.

2. EVEN IN THE AUDITOR'S OWN FRAME THE SIGN AGREES WITH THE BUILDER. Their table: s0_O COM -0.0567, s0_OH -0.0309, s0_OOH +0.0153. The *OOH is the only one of the three that moves away from the surface. "Measurably false" is the opposite of what their own numbers show; at worst the lab-frame magnitude is small, and the record's quantified claim is never stated in that frame.

3. DIFFERENT CONSTANT THAN THE PIPELINE'S. src/dft/adsorbate_qc.py:37-38 and :89-90 register the campaign's operational definition of the word: M_O_DESORBED_MIN = 3.00 A on adsorbate-atom-to-nearest-metal distance, "unambiguously not interacting with the surface", calibrated on the Mn/Fe s0_OOH precedent the module's own docstring describes as "The *OOH never adsorbed; the run converged because a desorbed molecule has no forces on it." Ti *OOH sits at 3.167 -> 3.414 A: inside that registered tier at every evaluated step and moving deeper into it. The auditor replaced this registered metric+threshold with a private criterion (net fragment-force sign, lab-frame COM monotonicity).

4. THE FORCE SUM DOES NOT ESTABLISH BINDING. The sums are correct (-0.00936, -0.00694, -0.00589 Ry/bohr) but are 3-4x WEAKER than either binding control (s0_O -0.02877 -> -0.01961; s0_OH -0.02610 -> -0.01744) -- consistent with a weak physisorption tail at 3.2-3.4 A, not with chemisorption. And the anchor O, the atom the diagnosis is actually stated about, carries the largest single adsorbate force at step 0 and it points OUTWARD at every evaluated step (+0.10613, +0.04313, +0.04917 Ry/bohr); summing over the fragment cancels it against the terminal O and reports the residual. BFGS steps along the full 3N quasi-Newton direction, not along the fragment's net force, and roughly half the 0.248 A growth in d(anchor O, Ti) comes from the substrate Ti relaxing inward underneath it. Monotonically falling E and |F| are what BFGS does on any descent, toward a desorbed minimum or a bound one, so they are not counter-evidence either.

5. THE CONSEQUENCE FAILS INDEPENDENTLY. The auditor's own alternative -- "a numerics/step-length reading instead, which the ladder had not exhausted" -- is precisely what s0_OOH_r2 IS. diff of runs/Ti_slab/s0_OOH_r1.in vs s0_OOH_r2.in = prefix, mixing_ndim 8->16, electron_maxstep 200->400, geometry continued verbatim; conv_thr/forc_conv_thr/degauss/smearing/nspin/mixing_mode/mixing_beta untouched. Both readings run in the same tranche and the pre-registered rule (build_a0main_w2c.py docstring; docs/41 s6d, A6.4) selects between them by lower converged total energy, so the framing word selects no outcome.

6. ALREADY DISCLOSED. docs/45:2265-2267 (new trap 9) and docs/59:136-137 both state that three ionic steps is far too short a walk to conclude TiO2 does not bind *OOH; build_a0main_w2c.py:139-143 and manifest.json "entrant_decision_open" leave the nspin=2 convention question explicitly open for the entrant rather than asserting the radical mechanism as settled. And the current readout prints "GAP (s0_OOH)" for all seven Ti rungs, so no banked quantity depends on the wording at all.

The auditor's numbers are real; their interpretation rests on a lab-frame COM in a cell whose surface is moving and on a criterion the campaign has not registered. Nothing here should block banking.

----------------------------------------------------------------------
[R22] (ti-geometry) Over half of the reported recession is the Ti_5c relaxing inward, not the adsorbate moving: the distance is measured to a moving reference
REFUTED BECAUSE:
The auditor's arithmetic reproduces exactly (dz(O)=+0.1196, dz(Ti4)=-0.1280 over the window; Ti idx 4 is `1 1 1` free, bottom layer `0 0 0` fixed), and runs/Ti_slab/slab.out confirms the inward Ti_5c motion is a real clean-surface relaxation (16.2412 -> 16.1295 converged, -0.112 A, with no adsorbate present). But the finding as filed does not survive, for six independent reasons.

(1) THE TEXT NEVER TREATS THE Ti AS FIXED. build_a0main_w2c.py:76 labels the column verbatim "built d(anchor O, nearest Ti)". It is a two-body distance and every quoted value is exact (recomputed: 3.1668, 3.2626, 3.3251, 3.4145). No sentence claims the adsorbate moved 0.25 A. The auditor is refuting a claim the file does not make.

(2) THE "CORRECTED" PICTURE IS WRONG -- THE CONTROLS GO THE OTHER WAY. In the auditor's own fixed frame, over the same first-three-step window, the anchor O moves DOWN in both controls (s0_O -0.0567, s0_OH -0.0317) and UP in *OOH (+0.1196). Sign-opposite. So "corrected for the substrate ... ordinary BFGS relaxation" is contradicted by the substrate-corrected data itself: the adsorbate's own absolute motion is qualitatively unlike both controls.

(3) Ti_5c's z IS SIGNAL, NOT A NUISANCE TO SUBTRACT -- as the auditor concedes ("there it later reverses as the adsorbate binds and pulls Ti_5c back out"). Converged Ti_5c displacement: s0_O +0.401, s0_OH +0.307, clean slab -0.112. Outward displacement is the binding signature; *OOH sitting at -0.128 means it is tracking the clean, UNBOUND surface. Subtracting the substrate would delete the evidence that supports the diagnosis, not correct it.

(4) THE "1.4 A" IS A DIFFERENT NUMBER. build_a0main_w2c.py:113 says r3 starts "inside the bonded basin instead of 1.4 A outside it" -- built start 3.167 vs re-anchor target 1.782 = 1.385 A, a statement about where the walk STARTS. No one claimed a 1.4 A departure over three steps; the auditor conflated two figures.

(5) THE SHORT-WINDOW WEAKNESS IS ALREADY EXPLICITLY DISCLOSED, at build_a0main_w2c.py:93-94 and docs/59:136-137: "Three ionic steps is also far too short a walk to conclude that TiO2 does not bind *OOH: *O needed 36 and *OH needed 56."

(6) THE CONSEQUENCE CHAIN IS BROKEN. The unregistered mixing_ndim/electron_maxstep bump is authorised at docs/59:147-154 as a numerics-only dated ladder extension bounded by construction ("can only FILL the gap (iii) would leave"), and motivated at build_a0main_w2c.py:102-103 as "the textbook lever against a limit cycle" -- an SCF symptom, not the geometry. It is applied to r2 (the continuation, the non-geometric arm) as well. The geometric diagnosis authorises r3, whose re-anchor distance is re-derived mechanically by relaxed_bond() from the two converged relaxes and re-verified on disk at lines 424-439. Nothing in the amendment rests on the 3.167->3.414 decomposition. Additionally, a0main_readout.py consumes none of these distances (grep: its "anchor" is the Xu literature anchor), so no banked number or verdict moves.

RESIDUAL, NOT THE FILED FINDING: line 86-87's clause "its centre drifts away from the surface" is genuinely imprecise -- the adsorbate geometric centroid goes 20.3247 -> 20.3200 (-0.005 A) and the mass centroid 19.9008 -> 19.9160 (+0.015 A). The anchor O rises mainly because O-O contracts 1.4213 -> 1.3602 with the distal O (-0.085) and H (-0.049) descending. That is a one-clause wording issue on a sentence the auditor did not cite, at a different location, and it leaves the headline intact (the Ti-O separation opens 0.25 A while both controls close theirs).

----------------------------------------------------------------------
[R23] (ti-geometry) The "three steps" are not one BFGS walk: the Hessian was reset between s0_OOH and r1
REFUTED BECAUSE:
The mechanical sub-claim is true but does not constitute a defect, and the finding's consequence is unsupported and physically inverted.

TRUE PART. runs/Ti_slab/s0_OOH_r1.in carries no restart_mode and changes prefix to 's0_OOH_r1' (line 3), so pw.x wrote a new .bfgs file and rebuilt the BFGS history; s0_OOH_r1.out:2041 prints "number of bfgs steps = 0". Confirmed.

WHY IT IS NOT A DEFECT.

(1) The reset affects exactly ONE of the three moves. s0_OOH.out:1798 also prints "number of bfgs steps = 0" -- every relax's first move is a default-Hessian move, including s0_O.out ("bfgs converged in 36 scf cycles and 34 bfgs steps", :30483) and s0_OH.out ("56 scf cycles and 52 bfgs steps", :44281). The three moves carry 0, 0, 1 Hessian updates versus a single walk's counterfactual 0, 1, 2. The splice costs one BFGS update, on move 2 only.

(2) The composition is explicitly disclosed, in the same docstring and three other registered places. build_a0main_w2c.py:65-69, two paragraphs ABOVE the cited table, states "rung (ii) ran as array 20204308 ... banked TWO ionic steps ... and then the third step's SCF limit-cycled" -- i.e. the "3" is stated as 1 (parent) + 2 (r1) before the reader reaches the table; rederive() at :211-218 machine-asserts r1's trajectory-block count == 2 and dies otherwise. build_a0main_w3.py:37-44 (the builder that MADE r1) documents the mechanism verbatim: same deck, prefix swap, beta 0.3->0.15, "ATOMIC_POSITIONS replaced by the LAST trajectory block of the failed run, spliced VERBATIM (pw.x's own coordinates and its own 0 0 0 constraint flags), so the restart continues from where the walk stopped instead of repeating the completed first step." runs/Ti_slab/m_ti_relax_r1.txt and docs/59:97 both record the parent as "zero bfgs steps banked". And the no-restart property is a REGISTERED campaign-wide convention, not an oversight: build_s3_wave2.py:119 FORBIDDEN_RESTART = startingpot|startingwfc|restart_mode, with the stage-specific policy registered at build_a0main.py:88-93. The guard the auditor cites at build_a0main_w2c.py:313 is that convention being enforced.

(3) The stated consequence is backwards. QE's first BFGS step uses the default (identity/trust-radius) inverse Hessian, so its direction IS the negative gradient -- the most direct possible readout of which way the surface pushes the adsorbate. An accumulated BFGS Hessian rotates the step AWAY from the force. A reset step is therefore more, not less, informative about direction. Moreover the docstring's evidence is measured positions, not optimizer bookkeeping: I recomputed d(anchor O, nearest Ti) from s0_OOH.in and the trajectory blocks and got 3.16681 -> 3.2626 -> 3.3251 -> 3.4145 A, exactly the tabulated chain, with total force falling monotonically 0.281 -> 0.173 -> 0.125 Ry/bohr (s0_OOH.out:1791, s0_OOH_r1.out:2034, :2808). One missing Hessian update cannot flip the sign of a monotone +0.25 A drift.

(4) The only number the consequence rests on is misattributed. "+0.106 Ry/bohr" does not appear in s0_OOH_r1.out (the sole file the evidence block cites); it is s0_OOH.out:1787 (atom 19 Fz = 0.10612916), the PARENT run's first step -- a move that was default-Hessian in every possible world, hence no evidence about the reset. The one move the reset actually touches (r1's first, s0_OOH_r1.out:2030) has anchor-O Fz = +0.0431, and the finding supplies no force data for it.

(5) Nothing reaches a banked quantity. Running the readout, Ti s0_OOH emits seven "GAP (s0_OOH)" rows and A7.2 lists Ti as pending; the "3 steps" text is a diagnosis motivating r2/r3 under a selection rule fixed pre-launch, and if both fail the registered outcome is identical to A6.5(2)(iii).

RESIDUAL. The "steps" column does place 36/56 (single accumulating walks) beside a 3 spanning two runs. But the docstring's only use of that comparison -- "three ionic steps is far too short a walk to conclude that TiO2 does not bind *OOH" -- is strengthened, not weakened, by the reset, and the 1+2 split is disclosed adjacently in the same docstring, in m_ti_relax_r1.txt, and in docs/59:97. No claim is false, nothing is hidden, no banked quantity moves.

----------------------------------------------------------------------
[R24] (ti-geometry) "36 and 56 ionic steps" are SCF cycles; pw.x reports 34 and 52 BFGS steps
REFUTED BECAUSE:
REFUTED. The quoted pw.x line exists verbatim (runs/Ti_slab/s0_O.out:30483 "bfgs converged in  36 scf cycles and  34 bfgs steps"; s0_OH.out:44281 "56 scf cycles and 52 bfgs steps"), but it does not show that "36 and 56 ionic steps" is wrong.

(1) 36/56 IS the ionic-step count. grep -c "^!    total energy" = 36 / 56 and grep -c "^ATOMIC_POSITIONS" = 36 / 56 in the two files. The last ATOMIC_POSITIONS block is the "Begin final coordinates" duplicate, so that is 35/55 NEW geometries plus the input geometry = 36/56 distinct ionic configurations at which the SCF was solved and forces evaluated. The auditor's own parser evidence therefore confirms 36/56 rather than refuting it. QE's bfgs_iter (34/52) counts only ACCEPTED BFGS moves, which is a strictly different and smaller quantity than "number of ionic steps"; QE's own nstep control (src/dft/qe_slab.py:161 nstep=200, :210 nstep=100) bounds the SCF-call counter in a relax run, i.e. the code itself equates ionic steps with the 36/56 counter.

(2) The auditor's stated cause of the 36-vs-34 gap is factually wrong. "grep -ci 'final scf'" returns 0 in both files -- there is no final re-evaluation SCF (that only occurs in vc-relax). The gap is rejected trial steps alone: s0_O.out has 33 "energy_new < energy_old" and 1 "energy_new > energy_old"; s0_OH.out has 51 and 3. So half the auditor's explanation of what 36/56 contains does not exist in these outputs.

(3) The documents' counting convention is internally consistent, and applying the auditor's "fix" would BREAK it. runs/Ti_slab/s0_OOH_r1.out:2812-2813 reports "number of scf cycles = 2 / number of bfgs steps = 1", and src/dft/build_a0main_w2c.py:66-67 describes that same run as having "banked TWO ionic steps", with the table at :77-79 giving *OOH "3 (then failed)". The docs count SCF cycles as ionic steps for *O, *OH and *OOH alike. Rewriting only *O/*OH to 34/52 would substitute the bfgs_iter counter into one half of the "3 vs 36/56" contrast while leaving the scf-cycle counter in the other -- introducing exactly the mixed-counters defect the finding alleges.

No file in the repo labels 36/56 as "BFGS steps"; the phrase used is "ionic steps", which is the standard name for one geometry evaluation (cf. VASP NSW) and is what the 36/56 counter measures. Nothing to fix before banking.

----------------------------------------------------------------------
[R25] (ti-geometry) r3's anchor O starts 0.354 A lower in absolute z than either converged analogue: the bond was measured against a relaxed Ti_5c and applied to an unrelaxed one
REFUTED BECAUSE:
Every number in the finding reproduces exactly (r3 anchor z 18.023087 vs converged 18.376784 / 18.377823; contacts 2.640 vs 2.796 / 2.812; shift (0,0,-1.38490740); d_target 1.781905), but none of it constitutes a defect.

(1) NO CLAIM IS VIOLATED. The only geometric claim made anywhere is about the Ti-O BOND LENGTH, and it is met exactly. build_ti_r3 computes the shift relative to the BUILT deck's own nearest Ti (build_a0main_w2c.py:383-384) and then re-parses the file it wrote and asserts |d - d_target| <= 1e-6 (:425-428). The deck on disk has the anchor O at 1.781905 A from Ti4, exactly mean(1.734553, 1.829256). No docstring, manifest header, or doc claims the ABSOLUTE z matches the converged analogues, so there is no discrepancy against any stated claim.

(2) "NOWHERE DISCLOSED" IS FALSE. That the substrate is the unrelaxed built one is stated in three separate places and enforced by an assert: build_a0main_w2c.py:106 ("The originally built deck with the three adsorbate atoms rigidly translated"); the generated manifest header runs/Ti_slab/m_ti_relax_r2.txt ("restart from a re-anchored geometry: the adsorbate rigidly translated (orientation + internal geometry preserved)"); docs/59-a0-roster-correction-2026-08-28.md:143 ("s0_OOH_r3 restarts from the built deck with the adsorbate rigidly translated"). build_a0main_w2c.py:437-439 dies if any substrate atom differs from the built deck, and :430-436 dies if the adsorbate's internal geometry changes. The reader is told exactly which substrate lies underneath.

(3) THE AUDITOR'S IMPLIED ALTERNATIVE IS WORSE, AND WRONG IN THE DIRECTION BEING REPAIRED. Placing the O at the converged absolute z (18.377) above the UNRELAXED Ti4 (16.2412) yields d(Ti-O) = 2.136 A -- 0.31-0.40 A LONGER than either converged Ti-O bond, i.e. it would start the walk OUTSIDE the bonded basin, up and away from the surface, which is exactly the drift (3.167 -> 3.263 -> 3.325 -> 3.414 A) that killed r1. Bond length is the transferable quantity between a relaxed and an unrelaxed substrate; absolute z is not. So "the bond was measured against a relaxed Ti_5c and applied to an unrelaxed one" argues FOR the code's convention, not against it. The docstring's ":112-113 starts the walk inside the bonded basin instead of 1.4 A outside it" is fully true by the criterion it names (1.782 vs 3.167 A, shift 1.385 A).

(4) THE 0.156 A CONTACT TIGHTENING IS UNRELAXED SUBSTRATE RUMPLING, NOT AN ARTEFACT OF THE SHIFT. Ti4 rises 16.2412 -> 16.6422 (s0_O) / 16.5486 (s0_OH) and the equatorial O16/O17 rise 16.2412 -> 16.3795 / 16.3423 as a RESPONSE to adsorption. In s0_OOH_r3.in:45,57,58 all three carry flags "1 1 1" and are free to do the same during r3's own walk. Every deck qe_slab.py builds starts from this same unrelaxed substrate -- including s0_O and s0_OH themselves, which converged from it over 36 and 56 ionic steps. I verified the min interatomic distance over all 210 min-image pairs of s0_OOH_r3.in is 0.9962 A (the O-H bond), identical to the built s0_OOH deck; the 2.640 A adsorbate-O / lattice-O contact violates no registered criterion (there is none), and the auditor concedes both that no overlap exists and that BFGS should recover.

The finding restates a standard and explicitly disclosed starting-geometry convention as an undisclosed discrepancy against a claim that was never made, and its proposed frame of reference would produce a physically worse start.

----------------------------------------------------------------------
[R26] (readout-code) control_ok reads true for a control that was never fully evaluated; the INCOMPLETE verdict never reaches the JSON
REFUTED BECAUSE:
The code observation is accurate, but the finding's stated consequence is false, and the thing it calls hidden is disclosed twice inside the very artifact being banked.

WHAT IS TRUE. `ok = True` at a0main_readout.py:212; the no-output branch (:226-233) now appends to `uncompared` instead of setting `ok = False` (confirmed against HEAD via `git diff`); `ok` is falsified only by a compared state exceeding tol (:239-240); the three-way `verdict` string (:244-249) is printed but not banked; `m_out` (:409-410) banks `control_ok` and `control_uncompared_states`. Live run reproduces `Ti ... s0_OOH NA  INCOMPLETE (3/4 states compared)` with `control_ok: true`.

WHY IT IS NOT A DEFECT.

1. The consequence is self-refuting. The finding's payload is "Any report generated from the JSON keying on control_ok cannot distinguish 'agreed on all four states' from 'never checked'." But :410 banks `control_uncompared_states` unconditionally, in the same dict, for every metal. I generated the new JSON: Cr/Ru/Ir/Mn/Fe get `[]`, Ti gets `["s0_OOH"]`. The verdict string is exactly derivable from the two banked fields by the same rule as :244-249 — `FAIL if not control_ok else INCOMPLETE if control_uncompared_states else OK`. No information the stdout verdict carries is absent from the JSON. The auditor cites the field at :410 and then asserts the distinction is impossible.

2. The incompleteness is disclosed a second time, at top level, machine-generated. `caveats.coverage_shortfall` (built at :651-669) emits live: "Live status at scoring time: Mn scored complete; Fe scored with 1 hole(s); Ti NOT YET SCORED." That is in the banked JSON, not just stdout.

3. The flag guards nothing for Ti. In the new JSON Ti has `nfull = 0` — all 7 grid points are gaps (s0_OOH missing at every U on both the A0 side, `ls runs/a0/main/Ti | grep OOH` is empty, and the probe side, `runs/probe/Ti_audit/probe_manifest.json` registers only slab/s0_O/s0_OH). There is no eta, dG, D, or pls-flip row for Ti anywhere in the artifact that could inherit a false PASS. The claim "banks PASS for Ti" is banking a trust flag over an empty row set.

4. "The campaign's only per-metal trust flag" is unsupported. `control_ok` appears nowhere in docs/43 (grep for control_ok / "extraction control" over the live registration and all three archives: no hit). It is not a registered gate quantity; docs/58 discusses the control in prose only, exactly as the finding concedes. Nothing consumes it programmatically (grep across .py/.md/.sh/.yml/.json: only the two readouts' own emit sites).

5. The change is deliberate and documented in-source at :227-230 with its rationale ("conflating them marked a metal UNTRUSTED for a state that had simply not been run yet"), and stdout distinguishes all three states with a dedicated INCOMPLETE branch (:246-247) and its own explanatory paragraph (:259-263).

6. The "0/4 compared, vacuous message" scenario is unreachable on the registered roster: every one of the six metals compares at least slab (five at 4/4, Ti at 3/4). It is a hypothetical about inputs that do not exist.

7. Old-vs-new indistinguishability fails on its own terms: the old artifact lacks the `control_uncompared_states` key entirely (verified: `'control_uncompared_states' in old['metals'][m]` is False for Cr/Ru/Ir), so presence of the key is itself the version discriminator — and all three old metals were 4/4 compared, so no banked value gets reinterpreted. Banking replaces the whole file anyway.

RESIDUAL. What genuinely survives is a naming nit: a boolean named `_ok` reads true over an incomplete control, and the docstring's GATES bullet ("The two must land within 5 meV") was not updated to describe the new three-way verdict. With the sibling field and the top-level caveat both present, that is cosmetic, not a banking defect, and not grounds to hold the bank.

----------------------------------------------------------------------
[R27] (readout-code) Repair label cannot record which A6.5(2) rung was reached — incoming rung-(ii) repairs will be banked with a rung-(i) description
REFUTED BECAUSE:
REFUTED. The finding's three load-bearing assertions each fail against the files.

1. "will be banked with a rung-(i) description" — the string does not say rung (i). src/dft/a0main_readout.py:315-318 emits "A6.5(2) REPAIR %s, RESTARTED FROM %s DENSITY (original SCF failed at 200 iterations; failed .out retained, A8.4)". It names the ladder (A6.5(2)), not a rung. Confirmed live: the one repair currently banked prints "s0_O: A6.5(2) REPAIR s0_O__u300__r1, RESTARTED FROM s0_O__u150 DENSITY ...; totmag 22.90" (readout run 2026-08-29; r2/r2b have .in decks but no .out yet, so the rung-(ii) path has not yet emitted anything).

2. Nothing false will be banked when r2/r2b land. Rung (ii) is registered as CUMULATIVE on rung (i)'s seed — runs/a0/m_a0_repairs2.txt header ("Rung (ii) halves the mixing beta, applied CUMULATIVELY on top of the seed") and src/dft/build_a0main_w2c.py:53-58 — and the decks confirm it: runs/a0/main/Fe/s0_O__u450__r2.in and __r2b.in both carry `startingpot = 'file'` alongside `mixing_beta = 0.15`. So "RESTARTED FROM <parent> DENSITY" is literally true of r2/r2b. The "failed at 200 iterations" clause is likewise true on every reachable path: grep confirms `convergence NOT achieved after 200 iterations: stopping` in runs/a0/main/Fe/s0_O__u300.out, s0_O__u450.out and s0_O__u450__r1.out, and the registry (a0main_readout.py:180-193, glob m_a0_repairs*.txt) contains only those two Fe s0_O stems — Ti's repairs live in runs/Ti_slab/ and never enter this code path. The label is incomplete, not wrong.

3. "silently drop the fact that the banked u=4.5 point used a different mixer setting" — not silent. The beta change is recorded in three committed places, including the ledger of record the finding itself cites for A8.4: docs/45-error-ledger.md §"A0 tranche 2c -- the second escalation round (2026-08-29)" states "Rung (ii) (halve beta) now runs from BOTH converged neighbours, with the lower converged energy pre-declared as the banked point"; docs/59-a0-roster-correction-2026-08-28.md:115-123 records the same; runs/a0/m_a0_repairs2.txt's header is titled "A6.5(2)(ii) ESCALATION"; and the deck itself is committed. The finding checked the manifest's row format but not the manifest's header or the ledger — the disclosure it says is missing is exactly there.

Moreover, the fact that actually conditions the u=4.5 number DOES travel into the artifact. Halving mixing_beta is a path parameter, not a physical one: r2/r2b hold conv_thr = 1.0d-6, the same functional, geometry, k-points and electron_maxstep as every other Fe s0_O rung (verified by grep across s0_O__u450.in / __r1.in / __r2.in / __r2b.in — beta is the only difference). What it can change is WHICH near-degenerate magnetic fixed point is reached, and the readout banks precisely that: totmag per row and appended to the repair note (a0main_readout.py:319-321), plus, when both repairs converge, "%d registered repairs converged, LOWER wins by %.1f meV over %s (measured branch splitting)" naming the loser (lines 322-327). That is what build_a0main_w2c.py's disclosed-limit paragraph promised ("the readout carries each row's totmag so a reader can see which branch every point is on").

Finally, no registration requires the rung in the row label. A6.5(2) (docs/43:1276-1281) requires the ordered escalation and NOT_CONVERGED-as-gap; A8.4 (docs/43:1594-1604) makes the per-metal per-state failure RATE a reported quantity (docs/45 records Fe s0_O 2/8 = 25%, over the 20% line) — neither makes the rung a per-row artifact field. The "labels travel" norm the finding leans on (docs/45:2104) has a specific scope: a label that conditions how a number may be QUOTED (PROJECTOR-MISMATCHED, A7.1 fired). A registered, authorized ladder rung producing a converged SCF at the same conv_thr does not condition quotability the way a projector mismatch does. (One secondary point I will not lean on: the r1/r2 suffix is an attempt counter, not a rung code — for Ti, s0_OOH_r1 is rung (ii), per docs/45 — so the job name is not a reliable rung token, but that cuts only against a recoverability argument, not against the disclosure above.)

Residual, genuinely non-blocking: the comment at a0main_readout.py:176 still reads "A6.5(2)(i) repair registry" although the glob now also loads the rung-(ii) manifest. That is a stale comment in source, never banked, and is not the reported defect.

----------------------------------------------------------------------
[R28] (readout-code) The spin_state caveat asserts Ti's nspin=1 is safe "by construction" — the registered tranche-2c diagnosis says it is the cause of the Ti failure
REFUTED BECAUSE:
REFUTED. The finding rests on three misreadings, each contradicted by the files.

(1) It misstates the registration. The auditor says "the registered explanation for why Ti has no *OOH column at all is that this convention cannot describe the state." That is not what was registered. src/dft/build_a0main_w2c.py's own section heading is "2. Ti s0_OOH -- rungs (i) and (ii) exhausted; the failure is GEOMETRIC", and its DIAGNOSIS paragraph concludes: "So the SCF failure is a SYMPTOM of a bad starting geometry, not a mixing problem to be patched." docs/59 s3c repeats it verbatim ("Ti s0_OOH -- the ladder is exhausted, and the cause is geometric ... The SCF failure is a symptom of the starting geometry"), and commit bcd3cc0's message reads "the Ti *OOH failure diagnosed as GEOMETRIC (adsorbate desorbing, not a mixer problem)". The nspin=1 clause the auditor quotes is a subordinate mechanism for why the SCF limit-cycles AFTER the walk has left the bonded basin (3.167 -> 3.414 A), not the registered cause. The registered remedy is correspondingly geometric: s0_OOH_r2 (continue the walk) and s0_OOH_r3 (re-anchor to the mean of Ti's own converged 1.734553/1.829256 A bonds), with nspin, mixing_mode, mixing_beta, conv_thr, forc_conv_thr, degauss and smearing all explicitly UNTOUCHED (build_a0main_w2c.py, "Numerics-only additions"). The convention change to nspin=2 is explicitly declined and parked as an entrant question. So the caveat and the registration do not contradict; they agree that the banked Ti chain is the d0 closed-shell convention and that the failure was a geometry the campaign is discarding.

(2) It misreads "by construction" as a safety claim. "Safe by construction" and "unconditionally correct" are the auditor's words, not the file's. The same caveat's opening sentence uses the identical idiom for Ru/Ir -- "the Ru/Ir columns are nspin=1 nonmagnetic by construction" -- and then immediately reports that this convention was MEASURED WRONG ("gate (h) measured 4/4 ADOPT_AFM ... AFM re-run owed"). Within this block "by construction" plainly means "as configured/by convention", which is exactly what src/dft/qe_slab.py encodes for Ti (line 38 comment "TiO2: d0 closed-shell control metal", ELEMENTS["Ti"] mag=0.0 -> nspin emitted as 1). The caveat labels it "production convention", not a validated result.

(3) The physical disanalogy it calls false is true. Gate (h) is about ORDERING the RuO2 anchors' Ru local moments (AFM vs NM), and Ti(IV) in TiO2 is d0 -- there is no metal-site moment to order. The w2c radical is a half-occupied state on a DESORBING *OOH molecule ~3.4 A off the surface, not a moment on Ti. Two different physical claims; "no moment to order" is not refuted by the existence of a molecular open-shell state in an off-manifold geometry.

Additionally, the caveat mis-conditions no banked number: I re-ran `PYTHONPATH=src python src/dft/a0main_readout.py` and Ti prints 7/7 `GAP (s0_OOH)` with no eta row at all, so the only Ti states behind the artifact are the converged bonded slab/s0_O/s0_OH -- precisely the closed-shell regime the caveat describes. And the artifact is not silent about where the diagnosis lives: every Ti table is labelled "blind-metal arm (A7.2/A7.3; tranche 2/3, docs/59)" (a0main_readout.py:198), caveats.coverage_shortfall names docs/59 as "the dated correction of record" (:656), and a0main_readout.py:139 already cites "docs/59 s3c" -- the very section carrying the geometric diagnosis and the open nspin=2 question. The disclosure exists and the artifact points at it.

(One genuinely questionable Ti line does exist -- the readout labels the 7 Ti holes "no output banked (not yet run; not a convergence event)" when the s0_OOH relax in fact failed twice -- but that is a gap-label issue in a different code path, not the spin_state caveat this finding indicts, and the finding does not claim it.)

----------------------------------------------------------------------
[R29] (readout-code) "not a convergence event" is asserted over Ti's seven holes, whose cause is two recorded convergence failures
REFUTED BECAUSE:
Refuted on all three load-bearing claims.

(1) "The artifact positively denies a convergence event" is false. I regenerated the JSON: src/dft/a0main_readout.py:420-423 banks gaps as [u, states, why-strings], and every Ti entry is gap_why: ["absent"]. The phrase "not a convergence event" is built at :399-401 and only printed to stdout; it never enters docs/figs/a0main_readout.json, which is the artifact this audit gates. No CI job, gate, or doc consumes the stdout (nothing in .github/ or anvil/ references the script; docs/58 quotes the JSON).

(2) The A8.4 consequence is wrong twice over. a0main_readout.py computes no A8.4 rate at all — no numerator, no denominator, no table; the only A8.4 mentions are comments at :289, :317, :394. A8.4 is registered at docs/43:1594-1604 as an S3 deliverable whose rate table is a separate artifact (runs/s3/readout/a84_rates_2026-08-24.json, docs/56:20), and the A0-side rate that exists was computed in ledger prose (docs/45:2200, "Fe s0_O pre-repair failure rate is 2/8 = 25%"). So this readout is not "the readout that is supposed to report it." Further, the Ti event is a *relaxation* failure in runs/Ti_slab/, not an A0-main grid SCF: the A0-main Ti s0_OOH column has zero attempted SCFs and is not in that denominator. Consistently, runs/a0/m_a0_repairs.txt and m_a0_repairs2.txt contain only Fe rows; the Ti repairs live in runs/Ti_slab/m_ti_relax_r1.txt / _r2.txt, a different manifest namespace, precisely because it is a geometry-chain event.

(3) The cause is explicitly disclosed in the manifest the readout is scored against, so the A8.4 "silently dropped failure" pathology does not apply: runs/a0/main/manifest.json tranche_3.stage_1.s0_OOH ("FAILED -- SCF after first ionic step hit 200 iterations at accuracy 0.0097 Ry ... failed .out retained per A8.4"), tranche_3.s0_OOH_gate ("base + 7 rungs built only after s0_OOH_r1 reaches bfgs converged; if r1 fails, A6.5(2)(iii)..."), and tranche_2c.ti_s0_OOH (rungs_exhausted, geometric diagnosis, r2/r3 decks). docs/45:2204-2211 records the failure and states the registered disposition verbatim: "the readout shrinks instead of blocking."

The line is also literally true in the readout's registered scope: runs/a0/main/Ti/ holds 84 files and zero s0_OOH decks (not even .in), so no A0-main SCF was ever attempted at those seven points and there is no A0-main convergence event to count. The absent-vs-qc-fail split is the correct A6.5(2) taxonomy and is what makes the Fe u450 row correctly print as QC-FAILED. And the readout scores nothing for Ti: control INCOMPLETE (3/4 states), caveats.coverage_shortfall "Ti NOT YET SCORED" under A7.7's WITHDRAWN-UNSCORED rule, a7_2.pending_data ["Ti"].

Residue: for a gap blocked upstream, the stdout parenthetical "not yet run" reads as pending compute rather than gate-blocked — but it is genuinely pending (r2/r3 in flight under tranche 2c), it is stdout-only, and it is a wording nicety, not a banking blocker.

----------------------------------------------------------------------
[R30] (readout-code) Registered repairs are only consulted when the failed point's own .out exists
REFUTED BECAUSE:
The auditor's code reading is literally accurate — `src/dft/a0main_readout.py:275-278` sets why="absent" and `continue`s, and the repair lookup at :290-292 is nested under `if E[st] is None:` at :284, reachable only after a file was found and QC-rejected. But the finding fails on every downstream link of its own causal chain.

1. THE POSITED STATE IS BLOCKED AT REGISTRATION. A repair cannot enter the registry unless the failed point's `.out` is on disk. `src/dft/build_a0main_w2b.py:189-198` computes `bad_out = FE_DIR/s0_O__<tok>.out`, then `if not os.path.exists(p): W.die("%s missing -- pull tranche 2 before building")` over `(bad_in, bad_out, parent_out)`, and then `if "convergence NOT achieved" not in bt: W.die("... a repair for a converged point is a replacement, and A8.8 forbids those")`. The rung-(ii) builder is the same: `build_a0main_w2c.py:225-230` reads `s0_O__u450__r1.in` and dies if it lost its seed. So "registered repair whose parent produced no .out" is not a state the builders can emit — the failed `.out` is the *precondition* of registration, not an incidental artifact. The readout comment at :288-289 states the same policy ("The failed .out stays on disk and in the A8.4 failure count"), and A8.4's failure-rate number is computed from those retained files, so purging them would break a reported quantity, not just this lookup.

2. BOTH REGISTERED PARENTS EXIST ON DISK. `runs/a0/main/Fe/` contains `s0_O__u300.out` and `s0_O__u450.out` — the only two points any registry row (`m_a0_repairs.txt`, `m_a0_repairs2.txt`; four rows total, all `a0/main/Fe s0_O__u{300,450}__r*`) repairs. The branch is unreachable for the entire registered input set. The live run confirms it: U=3.00 scores with "[s0_O: A6.5(2) REPAIR s0_O__u300__r1 ... totmag 22.90]" and U=4.50 reports `GAP (s0_O)` classified as "1 QC-FAILED", not "absent".

3. THE "UNMATCHED STEM" HALF IS BLOCKED TWICE. The manifests are machine-generated, not typed: `build_a0main_w2b.py:275-278` and `build_a0main_w2c.py:511-513` format rows from the stem lists the builders just wrote decks for, and those stems are derived (`stem = "s0_O__%s__r1" % tok_bad`) from tokens validated against on-disk decks. A stem that names no deck therefore cannot be written. And even if one were hand-inserted, `anvil/49_submit_repairs.sh:60-61` refuses to spend an SU: `deck="$RUNS/$d/${job}${suf}"; [ -f "$deck" ] || { echo "REFUSE: missing deck $deck"; exit 2; }`, plus checks for `startingpot = 'file'`, an on-disk parent `.save` with a charge density, and a stale JOB DONE. So the finding's scenario — "a repair can be registered, run, converge, and never appear" — requires a hand-edited manifest that also happens to name a real convergeable deck, i.e. a bypass of the whole registered flow.

4. THE CONSEQUENCE WORD "SILENTLY" IS WRONG. Even granting the unreachable state, the readout would not emit a wrong number: it would emit a GAP. Gaps are the loudest object in the artifact — a `GAP (s0_O)` table row, a `GAPS: N point(s)` summary line, and a live hole count inside the `[coverage_shortfall]` caveat ("Fe scored with 1 hole(s)"), never interpolated. The mislabel would be confined to the gap's *reason* string ("absent" vs "qc-fail"), on a point the readout is already withholding. The registry-integrity guard the author did write covers the case that actually can corrupt a number: :189-192 raises `SystemExit("REFUSING TO SCORE: ... registers %s twice with different parents")`.

5. NOT AN INSTANCE VIA Ti. The Ti s0_OOH repairs (`s0_OOH_r2`/`_r3`) live in `runs/Ti_slab/m_ti_relax_r2.txt` and are outside the `runs/a0/m_a0_repairs*.txt` glob — correctly, because they repair a *relaxation*, not an a0 grid point. `runs/a0/main/Ti/` holds exactly 84 files = 3 states x 7 U x 4, with no `s0_OOH__u*` deck built at all, so the seven `GAP (s0_OOH)` rows labelled "no output banked (not yet run)" are the accurate label, not a suppressed repair.

A "was every registered key consumed?" pass would be reasonable belt-and-braces, but it guards a state that four independent gates already make unconstructible and whose worst outcome is a withheld point. That is a hardening idea, not a substantiated defect, and it does not gate banking.

----------------------------------------------------------------------
[R31] (readout-code) n_abs is derived by subtraction and undercounts whenever one gap point mixes an absent state with a qc-failed one
REFUTED BECAUSE:
REFUTED on three independent grounds.

(1) The two counters are point counts, and they partition points exactly. `src/dft/a0main_readout.py:399` opens the sentence with `f"GAPS: {len(gaps)} point(s)"`, and `n_fail + n_abs == len(gaps)` by construction. `gaps` entries are `(u, missing_states, why_strings)` — one entry per U point (built at :338). So the split is a partition of POINTS, not of outputs, and nothing is undercounted: a mixed point is counted once, on the side that is correct. Classifying it as QC-FAILED is the only defensible assignment — the alternative label reads "(not yet run; not a convergence event)", which would be factually FALSE for a point that did have a convergence event and does owe A6.5(2) escalation. The auditor's proposed semantics ("measure the complement instead of subtracting") would move the mixed point into a bucket whose parenthetical explicitly denies a convergence event happened there.

(2) The stated consequence — "no line of output will name the absent state" — is false, twice over. The per-row print at :375 is `f"{r['u']:7.2f}    GAP ({', '.join(r['gap'])})"`, and `r['gap']` is the FULL list of missing states at that point, so a mixed point prints `GAP (s0_O, s0_OOH)`. The banked artifact carries per-state reasons in both places: I ran the readout with `--json` to a scratch path and Fe's gap banks `gap_why: ["qc-fail (all 3 registered repairs also failed: ...)"]` and Ti's seven bank `gap_why: ["absent"]`, in `metals.<M>.rows[].gap_why` and in `metals.<M>.gaps[i][2]`, index-aligned with the state names. A mixed point would bank `gap: ["s0_O","s0_OOH"]`, `gap_why: ["qc-fail (...)","absent"]`. No information about the absent state is lost anywhere in stdout or in the artifact this audit gates.

(3) The finding mis-identifies this line as "the printed A8.4 split". A8.4 (docs/43:1600-1604) registers a "per-metal, per-state convergence-failure rate", which is computed and reported per state elsewhere, not from this counter: docs/45:2200 records "Fe s0_O pre-repair failure rate is 2/8 = 25%" (per state, over U runs), and docs/56:20 routes the A8.4 rate table through a dedicated builder writing `runs/s3/readout/a84_rates_*.json`. `n_abs`/`n_fail` appear nowhere else in the repo (grep: only :396-403), are never written into `m_out`/`result`, and gate no verdict — they are stdout gap triage only. So no registered quantity inherits the alleged mislabel.

Reachability: conceded not live by the auditor and confirmed — Fe has one gap point (u=4.5, s0_O only) and Ti seven (s0_OOH only). The R1 dependency is also inert here: Fe's u450 point carries a genuine qc-fail on the original `s0_O__u450.out` regardless of whether the in-flight `__r2`/`__r2b` repairs (`.in` present, `.out` absent) are mislabelled as "failed", so its n_fail classification is correct either way.

At most this is a wording nicety about a print-only triage line whose per-state ground truth is printed and banked one line above. Below the reporting bar, and not a reason to withhold banking.

----------------------------------------------------------------------
[R32] (docs-vs-evidence) "local-TF is on for every slab deck in the campaign" is false — three Cr adslab decks lack it
REFUTED BECAUSE:
The raw grep is reproducible — runs/Cr_slab/s0_{O,OH,OOH}.in carry no mixing_mode, and the QE outputs confirm they actually ran with `plain     mixing` while slab.out ran `local-TF  mixing`. But every load-bearing part of the finding fails.

(1) ALREADY EXPLICITLY DISCLOSED, which the brief excludes. The pre-registration itself says it: docs/43-prereg-week1-factorial.md:692-693, review finding N11 — "`write_probe` hardcodes `mixing_mode = 'local-TF'`, and Cr's production **adslab** decks carry no `mixing_mode` at all" — and registers a fix (the 1x1 mirror rows are re-emitted as cellsym decks rather than reused from production, precisely so the pair does not differ in mixing scheme). docs/54-s3-deck-matrix-2026-08-23.md:71-74 names all three files verbatim: "the three Cr production adsorbate decks — runs/Cr_slab/s0_{O,OH,OOH}.in — default to 'plain'; runs/Cr_slab/slab.in and every other metal's production decks carry local-TF; the 2x1v/S0 generation is canonically local-TF — verifier-corrected 2026-08-23". A prior verifier already caught this exact over-generalization and the correction is banked. The exception is not hidden; it is named by path in two places.

(2) MISIDENTIFIED DOCUMENT CLASS. docs/59:3 reads "Status: DRAFT for the entrant. supporting infrastructure ... the entrant re-authors and deposits." It is a correction-of-record draft, not a pre-registration. The actual pre-registration states the opposite of the universal, so "a universal asserted in a pre-registration document" is wrong.

(3) THE CONSEQUENCE IS FALSE. All 232 mixing_mode lines under runs/a0/main/ are 'local-TF'; zero non-projwfc A0 decks lack it (the 227 hits without it are all .projwfc.in, which have no &ELECTRONS block). runs/a0/m_a0_repairs2.txt targets only a0/main/Fe rows, and grep -n "Cr_slab" over build_a0main_w2c.py, build_a0main_w3.py, build_a0main_w2b.py and anvil/48_a0_repair.slurm returns nothing. The A6.5(2) escalation ladder has no code path to runs/Cr_slab; those are completed, converged June-2026 relaxations that serve as a geometry source. A future Cr SCF escalation would run on an a0/main/Cr deck, which does carry local-TF — so "local-TF would still be available, contrary to what the record says" is doubly wrong: it would not be available, and the record already says the legacy decks are 'plain'.

(4) THE GIT EVIDENCE DOES NOT SHOW WHAT IS CLAIMED. `git log -S"mixing_mode = 'local-TF'" -- src/dft/qe_slab.py` returns exactly one commit, 7e06fdb (2026-06-29). `git log -- runs/Cr_slab/s0_O.in` returns that SAME commit as the file's introduction. The three decks were committed in 7e06fdb, not before it; "dates the line to 7e06fdb ... after those three decks were written" is an inference the cited command does not support.

(5) THE OPERATIVE CLAIM IS TRUE WHERE IT IS USED. docs/59 §3c and build_a0main_w2c.py:57-58 invoke the sentence to justify that mixer-switching is not an available rung for the Ti s0_OOH relaxation. Every Ti deck with ATOMIC_POSITIONS (runs/Ti_slab, runs/a0/main/Ti, runs/probe/Ti_audit) carries local-TF except the two gas references H2.in/H2O.in. The escalation decision, the geometric diagnosis, and the banked numbers are all untouched.

What actually remains is a literal over-reach in two prose sentences ("every slab deck in the campaign", "or anywhere else in this project") whose exception is documented in the registration and in the deck matrix. It gates nothing, changes no number, flips no verdict, and cannot block banking of docs/figs/a0main_readout.json. Not MAJOR, and under the brief's disclosure rule not reportable.

----------------------------------------------------------------------
[R33] (docs-vs-evidence) The central Ti diagnosis's "walked UP vs walked DOWN" contrast does not survive matched step counts — *OH also walks up
REFUTED BECAUSE:
The auditor's per-step numbers reproduce exactly, but the metric they chose conflates two independent motions and they attributed all of it to the adsorbate.

WHAT I RE-RAN. Parsing every ATOMIC_POSITIONS block from runs/Ti_slab/s0_O.out, s0_OH.out, s0_OOH.out + s0_OOH_r1.out and computing minimum-image d(anchor O, nearest Ti) with the deck's own cell (2.95870, 6.49647284, 25.98589137) reproduces the auditor's series to the digit: s0_O 3.1168 -> 3.1177 -> 3.1257 -> 3.1246 (1.7346 at step 36); s0_OH 3.1668 -> 3.1802 -> 3.2101 -> 3.2151, peak 3.2167 at step 4 (1.8293 at step 56); s0_OOH 3.1668 -> 3.2626 -> 3.3251 -> 3.4145. The docs' quoted figures (3.117 / 3.167 / 1.735 / 1.829 / 36 / 56, the 3.167->3.263->3.325->3.414 chain, and the r3 re-anchor mean 1.781905 A) all reproduce. So the arithmetic is not the issue.

WHY THE FINDING FAILS. d(anchor O, nearest Ti) is a DIFFERENCE of two moving coordinates. The bottom two Ti layers and four O carry '0 0 0' constraint flags (runs/Ti_slab/s0_OH.in:40-41; 8 pinned rows), so absolute z is a pinned frame with no rigid-body drift, and the two contributions can be separated. Tracking Ti index 4 -- the atom directly beneath the anchor O (dx=dy=0; its distance equals the nearest-Ti distance at every step) -- gives, by step 3:

  state    d(anchor-O z)      d(surface-Ti z)   d(O,Ti)
  s0_O     -0.0567  DOWN      -0.0645           +0.0078
  s0_OH    -0.0317  DOWN      -0.0800           +0.0483
  s0_OOH   +0.1196  UP        -0.1280           +0.2477

Both converged controls' anchor O moves DOWN, monotonically, from step 1. s0_OH's anchor O goes 19.4080 -> 19.3995 -> 19.3841 -> 19.3763 -> 19.3668 -- still descending at step 4, i.e. still descending at the very step where the auditor's distance metric peaks (3.2167). The distance rose only because the free surface Ti relaxed INWARD faster than the O descended (-0.0800 vs -0.0317). *OOH is the only state whose anchor O moves up in the pinned frame: 19.4080 -> 19.4642 -> 19.4870 -> 19.5276.

So at matched step count the sign of the ADSORBATE's motion is NOT the same for all three states -- it is opposite for *OOH versus both controls, which is exactly the discriminator the documents assert. The finding's consequence ('Direction of travel does not discriminate; only rate does') is the reverse of what the trajectory shows once substrate relaxation is separated out.

THE DOCS' SUBJECT IS THE ADSORBATE, NOT THE DIFFERENCE COORDINATE. build_a0main_w2c.py:96-99: 'the molecule is fixing its internal geometry while its centre drifts away from the surface.' docs/45:2245-2247 and docs/59:131-134: '*O and *OH walked DOWN into ... bonds ... while *OOH walked UP ... into the desorbed-radical region.' On the adsorbate coordinate every one of those statements holds from step 1 onward. Trap 9's sentence 'the direction of travel over those three steps was the whole diagnosis' (docs/45:2266) is therefore correct as phrased, and the finding's claim that 'trap 9's lesson is wrong as phrased' does not hold.

TWO INDEPENDENT WEAKENINGS. (1) The window mismatch the finding objects to is disclosed at every site the claim appears, not hidden: the 36 / 56 vs 3 step counts are printed in the same table (build_a0main_w2c.py:76-81), and each site adds the explicit caveat 'three steps is far too short a walk to conclude that TiO2 does not bind *OOH: *O needed 36 and *OH needed 56' (build_a0main_w2c.py:104-105, docs/45:2264-2266, docs/59:136, runs/Ti_slab/m_ti_relax_r2.txt:7). The campaign never draws a binding conclusion from the 3 steps. (2) Even on the auditor's own conflated metric the contrast survives: the controls turn around within 2-4 steps at <= +0.05 A total excursion, while *OOH never turns and reaches +0.25 A -- a 5x rate difference plus a qualitative turning-point difference.

NOTHING IS AT RISK EITHER WAY. The claim is narrative; it is consumed by no computation. src/dft/a0main_readout.py contains no Ti geometry term (grep for anchor/3.1/3.4/geometr returns only unrelated A6.4 and projector text), so banking docs/figs/a0main_readout.json is untouched by this. The registered selection rules (lower converged energy is the banked geometry; else A6.5(2)(iii) NOT_CONVERGED) and the r3 re-anchor distance (mean of Ti's own two converged Ti-O bond lengths, computed by the builder) are all derived from endpoints, which the finding itself concedes are correct.

Verified read-only: no files modified, no state-changing git commands run.

----------------------------------------------------------------------
[R34] (docs-vs-evidence) Banked artifact labels Ti's seven holes "not a convergence event" while two A6.5(2) failures sit on disk
REFUTED BECAUSE:
The finding does not survive contact with the files. Four independent refutations, any one of which sinks it:

**1. The string is never banked. The finding's title, claim, and consequence are all about the banked artifact, and the string is print-only.**
`src/dft/a0main_readout.py:399-406`: `msg` is a local built at :399, appended at :401/:403, and consumed by a single `print(msg + ".")` at :406. `grep -n "msg"` over the whole file returns exactly those four lines — it is never written into `m_out` (:409) or `result`, which are what `json.dump` writes at :726. I verified empirically: `grep -c "convergence event"` returns **0** for both the currently banked `docs/figs/a0main_readout.json` and a fresh run I banked to scratchpad. The banked why-token for all seven Ti rows is the neutral machine token `"absent"` (`gap_why: ["absent"]`, and `gaps: [[0.0, ["s0_OOH"], ["absent"]], ...]`). So the premise "the readout prints **and banks**" is half false, and the stated consequence — "a reader of the banked JSON would conclude Ti simply had not been scheduled" — rests on a sentence that is not in the banked JSON. The place the causal gloss is asserted (stdout) and the place nothing discloses (JSON) are different places; the pairing that would make this MAJOR does not exist.

**2. The disclosure the finding says is missing exists in the manifest — one of the exact exemptions the brief names.**
`runs/a0/main/manifest.json` discloses it three times: `tranche_3.stage_1.s0_OOH` = *"FAILED -- SCF after first ionic step hit 200 iterations at accuracy 0.0097 Ry (conv_thr 1e-6), still falling; A6.5(2)(ii) repair s0_OOH_r1 ... failed .out retained per A8.4"*; `tranche_3.s0_OOH_gate` = *"base + 7 rungs built only after s0_OOH_r1 reaches bfgs converged; if r1 fails, A6.5(2)(iii): NOT_CONVERGED, A7.3's denominator shrinks per its own registered conditioning"*; and `tranche_2c.ti_s0_OOH` with `rungs_exhausted`, the geometric diagnosis, and the r2/r3 decks. It is also in `docs/45-error-ledger.md:2203-2213` (**"Ti s0_OOH relax failed"**, with the A7.3-denominator sentence verbatim) and again at `:2243-2251`, in `runs/Ti_slab/m_ti_relax_r1.txt` and `m_ti_relax_r2.txt`, and in the `build_a0main_w3.py` docstring. The `s0_OOH_gate` line is the registered reason the decks do not exist.

**3. The statement is true about the object it describes.** `runs/a0/main/Ti/` contains no `s0_OOH__u*` files at all — not even `.in`. Zero Ti A0 s0_OOH SCFs were ever built or submitted, so zero A0 convergence events for that state exist; `why[st] = "absent"` (:275) is reached because `os.path.exists(p)` is False, and the gap really is "not yet run". The failures the auditor points to are in `runs/Ti_slab/` — the geometry chain, a **relaxation**, in a tree this readout has never walked for any metal (it only globs `runs/a0/main/<metal>/<state>__<utok>.out`, :272). It does not count Cr_slab or Ru_anchor relaxation failures either. The finding asks the A0 grid scorer to report an event from a directory outside its registered scope.

**4. Both downstream consequences are wrong.** (a) A8.4: this readout is not the A8.4 register — the dedicated artifact is `runs/s3/readout/a84_rates_2026-08-24.{json,md}` (docs/56:20), and the A0-side rate that exists (Fe s0_O 2/8 = 25%) is recorded in `docs/45:2200` and `manifest.json tranche_2b.repairs_a652i.a84_rate`, not here. The code comment at :390-395 shows the n_fail counter's scope explicitly: it was fixed so that a *repaired-and-still-failed A0 point* is not mislabeled — Ti has no such point. (b) A7.3: `grep -n "A7.3\|span\|c_M"` shows the readout never computes span(c_M)/2; A7.3 appears only in an arm-label string (:198) and inside the coverage_shortfall caveat text (:653). The readout cannot mis-state a denominator it does not compute — and A7.3's registered text (docs/43:1372) already self-conditions on "metals **with a converged *OOH geometry**".

The nearest thing to a genuine residue is that `build_a0main_w3.py`'s docstring claims the failure "is disclosed in the manifest and the readout" — true of the manifest, not of the readout. That is a stale sentence in a builder docstring, not a defect in a banked artifact, and it is not what was reported.

----------------------------------------------------------------------
[R35] (docs-vs-evidence) "converged in 202 s" is not the time any file records
REFUTED BECAUSE:
REFUTED. The auditor compared two different quantities and called the difference an unreproducible number.

1. The campaign has a committed, standing job-wall convention that is NOT pw.x's internal timer. `anvil/48_a0_repair.slurm:88-92` wraps the MPI launch in its own integer-second clock and prints it:
   t0=$(date +%s) / mpirun --oversubscribe -np "$NP" pw.x ... / t1=$(date +%s)
   echo "TASK ... rc=$rc wall=$((t1-t0))s bfgs_conv=$conv scf_fail=$scffail job_done=$done_ ..."
   The identical block appears in `anvil/46_a0.slurm:79-89` and `anvil/42_s3_wave1.slurm:74-86`, i.e. it is the runner every A0 array uses. Quoting a runner/job wall in prose is also the established habit (docs/41-prereg-anchor-offset-diagnosis.md:780, "`slab__base` converged in 4.4 h"). "202 s" is an integer, exactly the shape `wall=$((t1-t0))s` emits; 199.29 s is not.

2. The banked .out does not contradict 202 s — it PREDICTS it. `runs/a0/main/Fe/s0_O__u300__r1.out:2` "starts on 29Aug2026 at 2:19:30" and :2253 "terminated on: 2:22:50" span exactly 200 s, already 0.71 s more than the 199.29 s PWSCF WALL the auditor offers as the true value. The runner's clock brackets the QE process, so it must exceed 200 s by MPI launch + MPI_Finalize/teardown on 128 ranks; 202 s implies ~2 s of that, which is if anything low. So 202 s sits in the narrow window only the runner's wall can occupy.

3. This kills the misread hypothesis outright. 202 is LARGER than every timing number in the file (max WALL entries: 199.29 PWSCF, 187.86 electrons, 155.56 c_bands) and larger than the 200 s stamp span. Any transcription error or rounding from the .out timing block would have produced a value <= 199.29, not 202. The number cannot have come from misreading the file the auditor points at.

4. The pre-registration argument does not attach. `src/dft/build_a0main_w2c.py:6` says it "re-derives every fact it depends on from the outputs on disk," and `rederive()` (line 192) does exactly that for the facts the build actually depends on — convergence status and totmag, with a hard tolerance assert at line 208 (`abs(fe_u300_r1_totmag - 22.90) > 0.02`). No assert, branch, deck parameter, selection rule, or banked value in the builder depends on the wall time. Under docs/43, verdicts may only come from registered quantities; the wall clock is not one, and no gate or verdict rests on it. The physics claim in that sentence — "converged ... and landed at totmag 22.90" — is re-derived from disk and is confirmed by the .out (:2167 "convergence has been achieved in 20 iterations").

5. The one true residue is trivially weak: no Anvil slurm log is committed. But that is campaign-wide policy, not a tranche-2c regression — `runs/logs/` holds only five 2026-08-11/13 Vast box logs, and zero Anvil array logs exist for ANY wave (46_a0, 42_s3, 44_chain). The auditor's own note that "no slurm log ... is committed" is therefore an observation about the standing artifact policy, not a defect introduced here, and it does not make the quoted number wrong.

The finding's CLAIM ("is not the time any file records") is true only of committed files, and its EVIDENCE (199.29 s from the .out) does not show the doc is wrong — it shows a different, smaller, inner quantity that is fully consistent with the outer one. Not a defect.

----------------------------------------------------------------------
[R36] (docs-vs-evidence) Limit-cycle description: it does improve after iteration 13, and the spikes are 2.7x larger than stated
REFUTED BECAUSE:
I reproduced the auditor's extraction exactly and then read the trace it came from, and the finding's headline half inverts under its own evidence.

Source confirmed. `runs/Ti_slab/s0_OOH_r1.out` is the rung-(ii) run (three SCF sections: converged in 118 iters at line 2006, converged in 18 at 2780, "convergence NOT achieved after 200 iterations" at 5067; E fell -1301.68764608 -> -1301.71065326 = 0.0230 Ry, matching the docstring). The auditor picked the right file and the right (third) section. Line refs 69-70 are correct. Iteration 13 = 1.1321e-04, and 200-13 = 187. So the setup is right.

HALF A ("it does improve after iteration 13") — REFUTED. The auditor's 7.575e-05 at iteration 45 is a single-iteration dip inside an oscillating band, not a state the run reached and held: the very next spike (5.22e-03 at iteration 67) throws it back two orders of magnitude. The correct measure of an oscillating SCF's progress is the plateau level, not its running minimum, and the plateau does not move. Excluding the 11 spikes, the residual over iterations 14-200 has median 1.29e-04, range 7.6e-05 to 2.8e-04, and the run's FINAL value is 1.60e-04 — worse than iteration 13's 1.1321e-04. The auditor's own second statistic makes the point: 62 of the remaining 187 iterations sit below the iteration-13 value, i.e. one third below and two thirds above — a band centred on the quoted level, which is the definition of not improving. The docstring's "~1.2e-4" is in fact a better description of the whole 187-iteration plateau (median 1.29e-04) than of iteration 13 alone.

The document's idiom also settles this. Six lines earlier the same docstring writes of Fe u450 rung (i) that it "plateaued at ~1.5e-5 Ry ... for all 200 iterations"; I checked `runs/a0/main/Fe/s0_O__u450__r1.out` and that trace's own tail wobbles 1.23e-05 to 2.84e-05 (2.3x) with earlier excursions to 7.9e-02. docs/45-error-ledger.md:2249 names the Ti behaviour "plateau-with-spikes limit cycle". "Never improving" in this document means "the plateau level never descends toward conv_thr", not "no iteration is ever numerically lower". Under the author's consistently applied idiom the sentence is accurate; the auditor imposed a monotone-bound literalism the document does not use anywhere.

HALF B ("spikes are 2.7x larger than stated") — TRUE ARITHMETIC, NOT A DEFECT. The 11 post-13 spikes are 2.84e-03, 4.29e-03, 4.77e-03, 4.83e-03, 5.22e-03, 6.69e-03, 9.01e-03, 9.74e-03, 1.104e-02, 1.137e-02, 1.334e-02: median 6.69e-03, max 1.334e-02. "~5e-3" is 1.34x below the median and 2.7x below the max, but it is inside the observed range (five of eleven spikes are <=5.22e-03, one is 5.22e-03) and every spike lies in the same 3e-3 to 1.3e-2 decade. That is a tilde-qualified characteristic value in a descriptive clause, low by a factor under 3 within one order of magnitude.

CONSEQUENCE — BACKWARDS. The auditor argues it matters because the sentence is the evidence for "a clean limit cycle rather than a divergence". The corrected numbers strengthen that reading, they do not weaken it: the spikes occur at iterations 98, 108, 118, 134, 144, 154, 164, 174, 184, 194 — the last eight at exact period 10 — they are bounded, they do not grow, and the band between them is flat. Bounded non-growing periodic excursions around a flat plateau is precisely a limit cycle and is precisely not a divergence. Nothing downstream keys off the spike height either: the registered decision (rung (ii) exhausted -> geometric re-anchor plus mixing_ndim 8->16 and electron_maxstep 200->400) rests on the bond-distance table (3.117 -> 1.735 A over 36 steps, 3.167 -> 1.829 A over 56, versus *OOH walking up 3.167 -> 3.263 -> 3.325 -> 3.414 A), not on the residual trace. The builder's own `rederive()` re-checks the load-bearing facts (Fe moments, the two banked ionic steps, absence of "bfgs converged") from disk; the SCF-trace sentence is narrative colour around a diagnosis carried elsewhere.

So the finding's title asserts two things, the primary one is false on the evidence it cites, and the surviving one is a tilde value low by <3x in a non-load-bearing descriptive clause — a wording tightening, which the audit brief excludes as a style preference.

----------------------------------------------------------------------
[R37] (docs-vs-evidence) "an odd electron count" does not distinguish *OOH from the state that converged
REFUTED BECAUSE:
REFUTED on three independent grounds.

(a) WRONG LOCATIONS. `grep -rn "odd electron"` over the repo returns exactly ONE hit: src/dft/build_a0main_w2c.py:88. The other two cited sites do not make the claim. docs/59-a0-roster-correction-2026-08-28.md:134-135 reads "...*OOH is walking UP (3.167 -> 3.263 -> 3.325 -> 3.414 A) into the desorbed-radical region, where the chain's nspin = 1 convention cannot spin-split the resulting half-occupied state." docs/45-error-ledger.md:2248-2249 is the same sentence. No parity claim in either. The banked artifact is likewise clean: runs/a0/main/manifest.json's diagnosis field reads "geometric: built 3.167 A off the nearest Ti and walking UP (3.167 -> 3.414 A over 3 steps) into the desorbed-radical region; *O and *OH walked DOWN to 1.735 / 1.829 A over 36 / 56 steps". So the stated CONSEQUENCE -- that this "weakens a claim the entrant is being asked to countersign" -- has no referent: neither countersigned doc nor the manifest mentions electron parity at all. It exists only in a builder docstring.

(b) THE ONE OCCURRENCE IS CONDITIONED ON THE GEOMETRY. build_a0main_w2c.py:87-89 reads "In that desorbed region *OOH is a radical with an odd electron count, and the Ti chain's nspin = 1 convention cannot spin-split the resulting half-occupied state". The governing clause is "In that desorbed region". *OH does not satisfy that antecedent -- the table nine lines above in the SAME docstring records s0_OH relaxing 3.167 -> 1.829 A over 56 steps, i.e. bonded, with its unpaired electron paired into a Ti-O bond. The mechanism therefore does not predict the same pathology for *OH; the auditor's inference is obtained by deleting the conditional. The section is titled "rungs (i) and (ii) exhausted; the failure is GEOMETRIC" and concludes "the SCF failure is a SYMPTOM of a bad starting geometry, not a mixing problem to be patched" -- the discriminator is stated as bonded-vs-desorbed throughout, not parity.

(c) THE RUN DATA AFFIRMS THE DOC AND REFUTES THE AUDITOR. In runs/Ti_slab/s0_OOH_r1.out the electron count is fixed at 157.00 and nspin is unset (=1) for every ionic step, yet grep -c gives 2 x "convergence has been achieved" and 1 x "convergence NOT achieved" while the anchor height went 3.167 -> 3.263 -> 3.325 -> 3.414 A. A quantity that is constant within the run cannot be the variable the doc is blaming for a within-run change of outcome; the height is the only thing that moved. This is direct evidence that parity is stated as a co-property of the desorbed fragment, not as the cause.

(d) NOT LOAD-BEARING, AND THE ROOT-CAUSE ALTERNATIVE IS ALREADY DISCLOSED. The registered content of tranche 2c -- decks s0_OOH_r2 (continuation) and s0_OOH_r3 (re-anchor to mean(1.734553, 1.829256) = 1.781905 A), the mixing_ndim 8->16 / electron_maxstep 200->400 extension, and the pre-declared lower-energy selection rule -- is entirely geometric/numeric. Striking the parity clause changes no deck, no threshold, no recorded outcome. And docs/59 section 3c already carries the exact item the auditor says this "feeds": "An open question for the entrant, deliberately not settled here... Running it nspin = 2 throughout would be strictly more general... and would remove the radical-state pathology at its root rather than routing around it. It is not done here because it is a CONVENTION change across all four Ti states and 24 already-banked SCFs, and conventions are the entrant's to set, not the assistant's." That is an explicit disclosure, which the task instructions exclude from reporting.

The auditor's measured electron counts are correct (slab 144.00, s0_O 150.00, s0_OH 151.00, s0_OOH 157.00, all reproduced), and s0_OH did converge ("bfgs converged" present) under nspin=1 with odd parity. But those facts do not show what the finding claims they show, because the sentence they are aimed at never asserts parity as the distinguishing property. What remains is at most a wording preference in a non-banked docstring, which is out of scope.

----------------------------------------------------------------------
[R38] (docs-vs-evidence) "matching every other manifest in the campaign" is wrong; the real invariant is narrower
REFUTED BECAUSE:
The auditor's raw facts are accurate but the finding does not survive, because the narrow invariant it says is missing was added by the very commit it faults, in the file a reader actually reads.

VERIFIED AS CLAIMED: runs/a0/m_a0_repairs2.txt:17 = "# NP=128 NCONC=2" (committed bcd3cc0, untouched by 1c60cc2, whose diff is only docs/45, runs/Ti_slab/m_ti_relax_r2.txt, src/dft/build_a0main_w2c.py). runs/probe/m_cellsym_a_np4.txt:1 = "# NP=4 NCONC=5". src/dft/queue_r1.sh:102-104 is exactly the want_nconc != NCONC refusal. anvil/43_submit_s3_wave1.sh:68 runs PREFLIGHT_ONLY=1 LOG=/dev/stdout bash "$DRIVER" "$MANIFEST" "$NP" 1. anvil/49_submit_repairs.sh never invokes the driver; its only concurrency is CONC=${2:-2} at line 17, used solely for sbatch --array=1-$N%$CONC. So the commit message's universal quantifier is literally loose.

WHY IT IS STILL NOT A DEFECT:

(1) ALREADY DISCLOSED, IN THE MANIFEST, IN THE SAME COMMIT. The block 1c60cc2 inserted into runs/Ti_slab/m_ti_relax_r2.txt reads: "NCONC below is the token 43's DRY PREFLIGHT checks, not the array cap: 43 invokes the driver with concurrency 1 unconditionally, so every manifest routed through it declares 1. The array itself is submitted at whatever %cap the caller passes (here 2 ...)". "every manifest ROUTED THROUGH IT declares 1" is verbatim the scoped invariant the auditor says the next reader needs. It is present, tracked, and in the same commit.

(2) THE COMMIT MESSAGE SCOPES ITSELF. Its first sentence names 43_submit_s3_wave1.sh as the checker; its own launch table three lines below records the Fe repairs as going "via 49 -> 48", a different launcher. Both disambiguating facts are inside the message.

(3) THE PREDICTED CONSEQUENCE CANNOT OCCUR BY THAT ROUTE. git blame on m_a0_repairs2.txt:17 points at bcd3cc0, not 1c60cc2. Nothing on the 49 -> 48 path reads the token, so there is no refusal or warning that would send anyone there. No registered quantity, deck, verdict, or number depends on the clause.

(4) THE PROPOSED REPLACEMENT IS ITSELF LESS ACCURATE THAN WHAT IS ON DISK. The rule is not 43-specific: anvil/41_submit_wave.sh:50 and anvil/47_submit_a0.sh:72 hard-code preflight concurrency to 1 identically, while 45_submit_chains.sh and 49 bypass the driver entirely. "Manifests routed through 43 must declare 1" under-describes three launchers; the file's own "every manifest routed through it" generalizes correctly.

(5) NO GATE, NO FIX SURFACE. This has zero bearing on banking docs/figs/a0main_readout.json, which is what the audit gates. A commit message is immutable; the only available action is a redundant erratum in docs/45 trap 11, which already states the accurate NON-universal version ("declaring 1 and submitting at %2 is the campaign's established (if unobvious) pattern") that the auditor concedes is correct.

Every load-bearing claim in the commit message -- 43's preflight hard-codes 1, the driver refuses mismatches, the array still went out at %2 -- is verified true. One imprecise quantifier in an explanatory aside, with the precise rule registered alongside it in the same commit, is prose looseness in non-load-bearing text, not a docs-vs-evidence defect.
