A0 wave-2 verdict verification, 2026-08-28
==========================================
Four independent adversarial verifiers (fresh contexts, own parsers) run over the
A0-cell and A0-main readouts BEFORE banking, per campaign norm. Verdict: zero
BLOCKERs -- every quoted number reproduced from raw pw.x outputs to 1e-10 eV or
better -- and 10 MAJOR findings (5 distinct issues), ALL FIXED the same day in
src/dft/a0cell_readout.py + src/dft/a0main_readout.py before the JSONs were
regenerated and banked. The findings below are the verifiers' verbatim reports.

==========================================================================================
verify:rederive
SUMMARY: Independent re-derivation from the raw pw.x outputs (own parser: '!' total-energy grep, RY_EV=13.605693122994, re-implemented CHE ladder and eta/pls) confirms every number about to be banked: the full Cr 19-row table matches a0main_readout.json to 3.4e-10 eV with all pls equal; swing 1.17736 V (U=9.0 max, U=3.5 min); apex crossing 3.8655 eV bracket [3.5,4.0]; pls flip 3->2 in [3.5,4.0]; cell legs match to 2e-10 eV with I_U=-0.20129 (inconclusive bin, thresholds registered verbatim at docs/43 A6.2), crossings 3.8646/5.3326, shift 1.4680 > the registered 1.0 eV threshold; both extraction controls recomputed at <=0.0062 meV; Ru 15/32 / Ir 0/32 absences verified as not-run rather than convergence failures; pproj legs and the 0.487 V A7.1 FIRES verdict reproduced. No BLOCKERs. Two MAJORs: (1) the current a0main_readout.py crashes on its own gap bookkeeping (line 217 unpacks the 3-tuples appended at line 171), so the script named as the artifact's source cannot regenerate the banked JSON — the JSON demonstrably came from an earlier revision (its gap rows lack the gap_why field the current code always writes); (2) the banked a0cell JSON carries no PROJECTOR-MISMATCHED label on its U=7.15 entries despite A7.1's labelled-before-any-result-exists requirement and both verdicts depending numerically on that rung (though I verified both verdicts survive with the rung excluded: I_U=-0.155 still inconclusive, shift still >1.13 eV). Minor items: the <=0.01 meV cell-control claim is true but unrecorded in the artifact, the cell manifest still says u715 was never built, and the prereg's '5.00 eV' is the 4.995 eV u1.35 rung.

[MAJOR] The current src/dft/a0main_readout.py cannot regenerate the banked docs/figs/a0main_readout.json: it crashes with ValueError on any metal that has grid gaps (Ru and Ir both do), so the JSON about to be banked was produced by an earlier revision of the script named as its source.
   EVIDENCE: Line 171 appends 3-tuples gaps.append((u, missing, [why[st] for st in missing])); line 217 unpacks them as 2-tuples: gaps=[[u, ms] for u, ms in gaps] -> ValueError: too many values to unpack (reproduced on the exact pattern). Independently, the banked JSON's Ru/Ir gap rows contain only {u, gap} with no gap_why key, which the current script writes unconditionally (line 173) — proof the JSON predates the current code. All numbers in the JSON are nonetheless correct (verified from raw .out files, max deviation 3.4e-10 eV), so this is a provenance/reproducibility defect, not a numeric error.
   FIX: Fix the unpack (gaps=[[u, ms] for u, ms, _ in gaps] or store dicts), re-run the readout, and bank the regenerated JSON so the artifact matches the named script; record the revision that produced the banked file.

[MAJOR] The banked docs/figs/a0cell_readout.json carries no PROJECTOR-MISMATCHED label on its U=7.15 entries, although A7.1 (docs/43:1345 'the fifth grid point is labelled PROJECTOR-MISMATCHED before any result exists') fired at 0.487 V and both banked verdicts numerically depend on that rung.
   EVIDENCE: a0cell_readout.json D/eta objects under key "7.15" and the crossings/span/I_U fields have no label field of any kind; the label exists only in the script's stdout table header and docstring and in the separate pproj_readout.json. Dependence is real: both legs' span maxima are the U=7.15 D values (2.3854 / 1.9932), and the 2x1v crossing 5.33 eV is interpolated inside bracket [4.995, 7.15]. Mitigation I verified: dropping the 7.15 rung entirely still gives I_U = -0.155 (still the inconclusive bin) and a crossing shift >= 4.995-3.865 = 1.13 eV > 1.0, so both verdicts survive without the mismatched point — but the banked artifact must say the label, not rely on a reader also opening pproj_readout.json.
   FIX: Add an explicit projector_mismatched marker (e.g. a labels field or per-row flag on the 7.15 entries, plus a note that both verdicts are robust to excluding the rung) to the JSON before banking.

[MINOR] The A6.2 claim 'extraction control drift <=0.01 meV all 4 states' is true but is not evidenced by the banked cell artifact: a0cell_readout.json records no extraction-control values (they are stdout-only), and the script's registered gate is 5 meV, not 0.01 meV.
   EVIDENCE: Recomputed from raw Ry energies (base SCF vs source relaxation): slab +0.0011, s0_O +0.0031, s0_OH +0.0049, s0_OOH +0.0062 meV — all <= 0.01 meV, so the claim survives. But the JSON has no extraction_control field (unlike a0main_readout.json, which banks its drifts: slab +0.00381, s0_O +0.00340, s0_OH +0.00245, s0_OOH -0.00367 meV, all matching my recomputation to 1e-6 meV).
   FIX: Bank the four cell-leg drifts in the JSON (as a0main already does) so the <=0.01 meV statement is checkable from the artifact.

[MINOR] runs/a0/cell/manifest.json is stale: it says 'built 16, registered_total 20' and 'U = 7.15 eV not built here', but all 20 outputs including the four *__u715.* files exist in that directory and are consumed by the readout.
   EVIDENCE: Directory listing shows ref__2x1v__u715.out, s0_O__2x1v_mir__u715.out, s0_OH__2x1v_mir__u715.out, s0_OOH__2x1v_escape__u715.out present alongside the 16 originally-built decks; the manifest's variants list stops at u1.35.
   FIX: Update the manifest to record the u715 build (and its A7.1 gate outcome) before banking.

[NOTE] Prereg A6.1(b) registers the fourth shared U point as 'u1.35 = 5.00 eV' while every readout uses 4.995 eV; same deck (1.35 x 3.70 = 4.995), the prereg text rounded.
   EVIDENCE: docs/43-prereg-week1-factorial.md:1200 ('u1.35 = 5.00 eV') vs SUFFIX maps in both readout scripts and both JSONs (4.995). No numeric consequence; worth one sentence when quoting the registered points.
   FIX: State once that the registered '5.00' is the u1.35 ladder rung, which is exactly 4.995 eV.

[NOTE] The three metals' gas references are byte-identical copies of one calculation, so 'gas references per metal from each probe's own source run' is satisfied only nominally.
   EVIDENCE: md5(runs/Cr_slab/H2O.out) = md5(runs/Ru_anchor/H2O.out) = md5(runs/Ir_anchor/H2O.out) = 734ea527...; same for H2.out. Physically harmless (the molecules are metal-independent and the calculator matches), and irrelevant to the Cr-only verdicts being banked now, but the A6.3 ordering readout should disclose the shared reference when it ships.
   FIX: Note the shared gas reference in the eventual Ru/Ir readout rather than implying independent per-metal gas runs.

[NOTE] Everything quoted for banking that I could re-derive is correct. Full re-derivation from raw pw.x outputs with an independently written parser (own '!' total-energy regex, RY_EV = 13.605693122994, own CHE ladder and eta/pls logic re-implemented from the documented formulas) reproduced every banked number.
   EVIDENCE: (1) Cr 19-row table: all dG_OH/dG_O/dG_OOH/D/eta match a0main_readout.json to max |dev| 3.4e-10 eV; all pls match; all 76 Cr outputs have JOB DONE, zero 'convergence NOT achieved'. (2) Swing = 1.17736 V (max eta 1.55805 at U=9.0, min 0.38070 at U=3.5); quoted 1.177 correct; withdrawn-headline figure 1.122 V confirmed at docs/43:1187. (3) Apex crossing U = 3.8655 eV, bracket [3.5, 4.0] (+/-0.25 = half the registered 0.5 eV step); pls flip 3->2 between 3.5 and 4.0 — both match. (4) Cell readout: both D(U) legs at {0, 1.85, 3.70, 4.995, 7.15} match to 2e-10 eV; span(1x1)=1.62400, span(2x1v)=1.42271, I_U=-0.20129 (quoted -0.201, correctly the inconclusive bin per the A6.2 table at docs/43:1220, bins <0.05 / >=0.30 registered verbatim); crossings 3.8646 [3.7,4.995] and 5.3326 [4.995,7.15]; shift 1.4680 (quoted 1.47) > the registered 1.0 eV threshold (docs/43:1237) -> CELL-CONDITIONAL correct. (5) Both extraction controls recomputed at the Ry level and <= 0.0062 meV. (6) Ru 15/32 and Ir 0/32 confirmed by file count; all 15 present Ru outputs converged (0 SCF failures) and the 49 missing files are absent, so 'queued, not convergence failures' is accurate; Ru control drifts (+0.00476, -0.00571 meV, others NA) match the JSON; ordering verdict correctly withheld (shared_points=[], complete=false). (7) pproj: both legs re-derived (atomic eta 1.15540 pls 2, ortho 1.64226 pls 1), |d_eta| = 0.48686 V — A7.1 FIRES verdict (>0.10 V, docs/43:1341) correct. (8) A7.2 closed-form identity devs <= 2.2e-16 as banked. (9) Cross-validation across independent runs: probe 1x1 D(3.7)=1.56028 vs main-grid linear interpolation 1.56037 — consistent; the U=0 eta difference between the two campaigns (7.1e-6 V) is exactly what the ~0.003 meV extraction drifts predict.

[NOTE] runs/probe/Cr/CRASH is a leftover mkdir failure from an earlier attempt of s0_OOH__u0.0 and does not taint the 1x1 leg; likewise the cell leg's OOM first attempt is properly quarantined.
   EVIDENCE: CRASH reads 'unable to create directory ./tmp_s0_OOH__u0.0/...'; the surviving s0_OOH__u0.0.out has JOB DONE, zero SCF failures, and a final energy, and its value feeds the U=0 cell-leg point that matches the banked JSON. s0_OH__2x1v_mir__u0.0.out.oom_attempt1 has a non-.out suffix so no scanner picks it up as primary; the retried .out passes.
   FIX: Optionally delete or annotate the stray CRASH file so a later audit does not have to re-establish this.

==========================================================================================
verify:registration
SUMMARY: Registration conformance audit of the two A0 readouts against docs/43 A6.0-A6.7 and A7.1-A7.2. Every quoted NUMBER survives: the banked a0cell JSON is byte-identical to a fresh rerun (I_U = -0.201 eV in the verbatim-registered inconclusive bin, defined on D not eta, right sign, right pair of crossings, shift 1.468 > 1.0), the Cr grid numbers all reproduce (swing 1.177 V, crossing 3.87 [3.5,4.0], flip 3->2, controls <= 0.01 meV), and A7.2's closed form holds to 2e-16. Three MAJORs: (1) the CELL-CONDITIONAL verdict rests ENTIRELY on the PROJECTOR-MISMATCHED U=7.15 rung — without it the 2x1v leg never reaches D=1.6 and no crossing exists — yet the banked JSON and verdict lines carry no label, violating A7.1's fired consequence and the script's own 'label travels with every number derived from that rung' commitment (I_U's bin, by contrast, is robust: -0.155 without the rung); (2) a0main's 'located to +/-0.25 eV' claim lacks the caveat A6.2 makes mandatory once the shift fired ('must be reported as such'); (3) the working-tree a0main_readout.py crashes at line 217 on any metal with gaps (verified: dies at Ru), so the script on disk cannot regenerate the banked JSON, which came from an uncommitted intermediate revision — one-line unpack fix, then regenerate and commit together. Minors: vacuous A6.3 'stable on all 0 points' line instead of an explicit WITHHELD; 4.995 vs registered '5.00 eV' wording; the 2x1v *OOH escape-geometry substitution (OOH-free for the A6.2 quantities, but the banked eta/pls rows inherit it unlabelled); a0cell's extraction control absent from its banked JSON. The P7-bound formula (max-min over grid) is not verbatim-registered but was frozen pre-data in commit b746774 and reproduces P7's own 1.122 V construction exactly — quote it citing that commit, not as registered text. Deposit gates, one-projector rule, Lowdin retention, and the escalation ladder's non-trigger all verified clean.

[MAJOR] The A6.2 CELL-CONDITIONAL verdict rests ENTIRELY on the PROJECTOR-MISMATCHED U=7.15 rung, and the banked artifact carries no label saying so. Without U=7.15 the 2x1v leg never reaches the apex (max D = 1.527 eV at U=4.995 < 1.6), so there is NO 2x1v crossing and no shift to compute; the 5.33 eV crossing is interpolated into the mismatched rung (bracket [4.995, 7.15]).
   EVIDENCE: Registered A7.1 (docs/43:1338): 'If it fires: the fifth grid point is labelled PROJECTOR-MISMATCHED before any result exists'. A7.1 FIRED at |d-eta| = 0.487 V (docs/figs/pproj_readout.json, verdict FIRES). src/dft/a0cell_readout.py's own docstring (:28-30) commits 'the label travels with every number derived from that rung, including this test's fifth column' — but the implementation prints the label only once, in the stdout D-table header (:170); the verdict lines (I_U, crossing shift, CELL-CONDITIONAL) and the entire banked JSON docs/figs/a0cell_readout.json contain no PROJECTOR-MISMATCHED field anywhere. Sensitivity computed from the banked D values: dropping 7.15 leaves 2x1v max D 1.527 < 1.6 (no crossing possible); I_U without 7.15 = -0.155 eV (still inconclusive, so THAT bin is robust).
   FIX: Add a projector_mismatched marker to the JSON on the 7.15 D/eta entries, on both crossings whose value derives from that rung, and on the crossing_shift_eV/CELL-CONDITIONAL verdict; state in the verdict line that the 2x1v crossing exists inside the five-point band only through the mismatched rung.

[MAJOR] a0main's located-crossing claim ('apex crossing U = 3.87 eV, bracket [3.5, 4.0] -> located to +/-0.25 eV') lacks the CELL-CONDITIONAL caveat that A6.2 makes mandatory now that the crossing shift (1.468 eV) exceeded 1.0 eV.
   EVIDENCE: Registered A6.2 (docs/43:1231-1234), verbatim: 'If the two cells place the crossing at U values differing by more than 1.0 eV, then A0's central claim — that the crossing is located rather than bracketed — is cell-conditional, and must be reported as such.' The a0cell readout fired this (shift 1.468 > 1.0), but src/dft/a0main_readout.py prints 'located to +/-0.25 eV' (:228-229) and docs/figs/a0main_readout.json banks crossings [[3.8655, [3.5, 4.0]]] with no cell-conditional flag. The two readouts are being banked together; the located-crossing quantity is exactly the claim A6.2 says must carry the label.
   FIX: Attach CELL-CONDITIONAL to the a0main crossing wherever it is quoted (stdout line and a JSON flag), citing a0cell's shift = 1.47 eV; the m_a0main.txt header itself points at 'A6.2's 1.0 eV threshold' as the governing test.

[MAJOR] The working-tree src/dft/a0main_readout.py CRASHES (ValueError, line 217: 'too many values to unpack') on any metal with gaps — i.e., on the current data it dies at Ru — so the script on disk cannot regenerate the banked docs/figs/a0main_readout.json, which was produced by an intermediate, now-nonexistent revision (banked Ru rows lack the gap_why field the current script writes; the committed HEAD version b746774 has a different extraction control that would FAIL Cr on a u370 file that cannot exist).
   EVIDENCE: Reproduced: 'PYTHONPATH=src python src/dft/a0main_readout.py' prints the full correct Cr block (all quoted Cr numbers verified: swing 1.177 V, crossing 3.87 [3.5,4], flip 3->2, control <=0.01 meV) then raises ValueError at m_out gaps=[[u, ms] for u, ms in gaps] (:217) because :171 now appends 3-tuples (u, missing, whys). git status: src/dft/a0main_readout.py Modified/uncommitted (31 insertions), both readout JSONs untracked. The banked JSON's Ru row {'u': 0.0, 'gap': [...]} has no gap_why, so the banked artifact also does not itself record the 'absent, NOT convergence failures' distinction being quoted (that claim is nonetheless TRUE — verified all 16 Ru/Ir gap points are file-absent, zero qc-fails).
   FIX: One-line fix: unpack the 3-tuple (gaps=[[u, ms, ws] ...] or [[u, ms] for u, ms, _ in gaps]) and persist gap_why into the JSON; regenerate the JSON from the fixed script, verify it reproduces, and commit script + JSON together before banking.

[MINOR] The A6.3 verdict line is vacuous when zero shared points exist: the script prints 'A6.3 VERDICT: ordering Ir < Ru stable on all 0 measured shared points (grid incomplete)' — asserting the registered prediction held on no evidence — and the banked JSON has no explicit withheld marker; the quoted 'A6.3 ordering verdict withheld' is an interpretation of ordering.complete=false, not something the artifact says.
   EVIDENCE: src/dft/a0main_readout.py:269-271 (else branch runs whenever inversions==[], including shared==[]); docs/figs/a0main_readout.json ordering = {shared_points: [], inversions: [], complete: false} with no verdict field. Withholding is the registered-conservative behavior (A6.3 predicts stability 'across U in [0,9]', unscoreable with 0 shared points), so the summary's wording is right and the artifact's is wrong.
   FIX: Guard the verdict: if not shared, print 'A6.3 VERDICT WITHHELD: no shared measured points' and write ordering.verdict='WITHHELD'.

[MINOR] The fifth shared U point is implemented and quoted as 4.995 eV where A6.1(b) registers 'u1.35 = 5.00 eV' — the registered value is a rounding of the ladder's actual 1.35 x 3.70 = 4.9950 eV; identity is by ladder token ('the four already on the ladder'), so this is a wording mismatch, not a wrong point, but it should be stated once.
   EVIDENCE: docs/43:1200-1201 vs runs/a0/m_a0cell.txt (:53 'u1.35, U=4.9950') and U_SHARED=[...4.995...] in src/dft/a0cell_readout.py:57. The task's claim text quotes {0, 1.85, 3.70, 4.995, 7.15}.
   FIX: One sentence wherever the five points are listed: 'u1.35 = 4.995 eV (registered as 5.00 eV; same ladder deck, 1.35 x U_base)'.

[MINOR] The 2x1v *OOH leg uses the S3 escape-minimum geometry (runs/s3), not 'block 1A's already-relaxed 2x1v Cr geometries' as A6.1(b) registers verbatim; this provably cannot move I_U or the crossings (D = dG_O - dG_OH uses only slab/O/OH — verified in the code), but the banked 2x1v eta/pls rows (pls = 1 at U = 4.995 and 7.15) DO inherit it and the JSON carries no geometry-provenance note.
   EVIDENCE: A6.1(b) docs/43:1194-1198 ('run on block 1A's already-relaxed 2x1v Cr geometries... not re-pick them'); runs/a0/m_a0cell.txt:25-30 discloses the substitution and its docs/45 energy-of-record designation (mir relax = saddle 150.8 meV above escape); a0cell_readout.py CONTROLS_2X1V s0_OOH -> runs/s3/Cr/s0_OOH__2x1v_escape.out; D computation (:174-180) excludes OOH; eta computation (:193-199) includes it.
   FIX: Carry the m_a0cell.txt disclosure into the readout: a one-line note on the 2x1v eta rows that *OOH is the docs/45 escape-minimum geometry, and that the A6.2 verdict quantities are OOH-free.

[MINOR] The a0cell extraction-control result being quoted ('drift <= 0.01 meV all 4 states') exists only in stdout — the banked docs/figs/a0cell_readout.json omits the control drifts entirely (a0main's JSON does include them); additionally the 5 meV control tolerance is a script-level QC gate, not a registered threshold, on both readouts.
   EVIDENCE: Rerun stdout: slab +0.00, s0_O +0.00, s0_OH +0.00, s0_OOH +0.01 meV, all OK (verified; the claim's numbers are true). json.load of docs/figs/a0cell_readout.json: keys are gas/D/eta/gaps/span/I_U/verdict/crossing_shift_eV/crossings — no extraction_control field. Registration A6.5 contains no extraction-control clause; the gate is defined only in the scripts' docstrings.
   FIX: Write the four drifts and ctrl_ok into the a0cell JSON; quote the control as an infrastructure QC observation, not as a registered verdict.

[NOTE] SURVIVES — the A6.2 I_U verdict is exactly the registered quantity in the registered bins: I_U is defined on D = dG_O - dG_OH (the descriptor P7 measured), NOT on eta; the sign convention span(2x1v) - span(1x1) matches; the bins <0.05 additive / >=0.30 not separable / 0.05-0.30 inconclusive are implemented verbatim ('not rounded toward either' respected: |I_U| = 0.201 -> INCONCLUSIVE); and the crossing-shift threshold is applied to the right pair (both cells' crossings from the same five shared points, not the 19-point grid).
   EVIDENCE: Checked docs/43:1205-1234 word-by-word against a0cell_readout.py:218-243 and the banked JSON (I_U = -0.20129, span 1.624/1.423, crossings 3.8646 [3.7,4.995] / 5.3326 [4.995,7.15], shift 1.4680). Banked JSON is byte-identical to a fresh rerun (dict equality True). The a0main 19-point 1x1 crossing (3.8655) agrees with the a0cell five-point 1x1 crossing (3.8646) to ~1 meV in U — internal consistency. INCONCLUSIVE is additionally robust to dropping the mismatched 7.15 rung (I_U = -0.155, same bin) — worth stating when banking.

[NOTE] The P7-bound formula ('eta swing = max - min over the measured grid') is NOT verbatim in docs/43 — A6.1(a) registers only 'A0's registered job is to bound THAT claim' — so quote 1.177 V as an inferred-but-pre-frozen quantity citing commit b746774, not as a verbatim-registered one. It survives on two grounds: the formula was committed in the readout script BEFORE any A0 number existed (b746774, 'Verified on the empty grid'), and it is P7's own construction — the banked 1x1 five-point eta reproduces the withdrawn 1.122 V headline exactly as max-min (computed 1.12199 V). A7.3's 'never max-minus-min over a grid' stricture is registered for span(c_M) only and does not govern this quantity.
   EVIDENCE: docs/43:1187-1191 (A6.1a), :1370 (A7.3, fixed-endpoints rule scoped to c_M); git log b746774 predates all A0 outputs (059000d records A0-main had zero .out at launch); max-min over banked five-point 1x1 etas = 1.12199 V vs registered headline 1.122 V; banked swing_V = 1.17736 over the 19-point grid, quoted 1.177 correctly. The +/-0.25 eV location precision (half the 0.5 eV step) is likewise declared pre-data in runs/a0/m_a0main.txt:21-22.

[NOTE] A7.1's other fired consequences check out: the whole eta(U) grid runs in ONE projector — HUBBARD (atomic) verified in the A0-cell u715 decks and A0-main decks — and the fifth cell rung was built only AFTER P-PROJ scored, satisfying 'Before any A0 deck is built on the fifth grid point'. Still pending: 'the projector delta becomes its own labelled sub-row' exists only in docs/figs/pproj_readout.json (0.487 V), not as a labelled sub-row in either A0 readout.
   EVIDENCE: grep 'HUBBARD (atomic)' in runs/a0/cell/ref__2x1v__u715.in, s0_O__2x1v_mir__u715.in, runs/a0/main/Cr/slab__u350.in; commit 059000d ('A0-cell fifth rung built AFTER P-PROJ scored, in the ladder's own atomic projector'); a0main labels the Ru/Ir Xu-anchor rows 'XU-ANCHOR [PROJECTOR-MISMATCHED]' as A6.3 requires ('marked as declared anchor points').

[NOTE] A6.5 compliance verified: Lowdin retention holds (projwfc ran inline; .lowdin.txt extracts present for 76/76 Cr A0-main points, 20/20 A0-cell points, and all 15 banked Ru points); the escalation ladder was correctly NOT triggered — zero qc-fails, all 16 Ru/Ir gap points are file-absent (queued), and treating absence as 'not a convergence event' is the right reading of A6.5(2), which governs non-CONVERGENT points; gaps are reported, never interpolated (the a0cell verdict refuses on partial legs, per the a6c71fc wave-1 withhold). The A6/A7 deposit launch gate was discharged (combined A1-A7 record 10.5281/zenodo.21963144, docs/45 ledger 'done 2026-08-16').
   EVIDENCE: File counts via ls/grep -c under runs/a0/{main/Cr,main/Ru,cell}; rerun stdout '8 with no output banked (not yet run; not a convergence event)' x2; docs/45-error-ledger.md:48-49; a0cell_readout.py:212-216 (VERDICT WITHHELD branch on incomplete legs). The 'UNTRUSTED until reconciled' marking on control-incomplete Ru/Ir is an unregistered but strictly conservative script-level label.

[NOTE] A7.2's closed form is implemented exactly as registered — eta = (c_M/2 - 1.23) + |dG2 - dG3|/2 for pls in {2,3} only, identity not applied at pls 1/4 — and holds to machine precision on every applicable banked row (max deviation 2.2e-16). The measured Cr flip bracket [3.5, 4.0] is consistent with the A7.2 disclosed non-blind 'flips 3->2 between 1.85 and 3.70' on their overlap (the a0cell ladder shows pls=2 at U=3.70, so the flip sits in (3.5, 3.70]); the >=3-of-6-metals flip prediction is correctly left unscored with only Cr measured.
   EVIDENCE: docs/43:1350-1359 vs a0main_readout.py:179-183; banked Cr rows' closed_form_dev; banked pls_flips [[3.5, 4.0, 3, 2]]; a0cell eta table pls column (3,3,2,2,2 on 1x1). No row is averaged across a flip — the swing is a range and the apex crossing interpolates D (continuous through the flip), which is the registered deliverable itself.

==========================================================================================
verify:qc-mag
SUMMARY: QC + magnetization sweep over the 96 newly banked A0 outputs (76 Cr grid, 15 Ru, 5 new cell files): everything the task quotes from the readouts survives. 96/96 pass qe_qc trusted_energy_ev(strict=True); zero SCF failures, zero 'Error in routine', all JOB DONE; Ru/Ir gaps are genuinely absent runs (Ir 0/32 = decks staged, no outputs), not failures. Cr magnetization is integer-pinned per state (12/10/11/11) with smoothly monotonic absolute magnetization — no branch hops, supporting the smooth-eta claim; cell ladder holds the wave-1 record (24/22/23/23) on all 5 U points including the four u715 rows and the OOM retry, and the retry is provably the same SCF branch as the killed attempt (it-1 energy identical to 2e-8 Ry, identical mag trajectory). conv_thr (1e-6) and U-token echo checks pass 10/10 and 6/6. One MAJOR: the Ru A0 grid inherits the production non-spin-polarized (nspin=1) decks byte-for-byte, while banked S0 finding (h) ruled the production RuO2 magnetic state wrong (ADOPT AFM) — the UNTRUSTED/incomplete label on Ru cites only completeness, and completing the queue will not cure the magnetic-state conflict; the withheld A6.3 verdict needs that caveat wired in. No BLOCKERs.

[MAJOR] The Ru A0-main grid is run non-spin-polarized (nspin=1, no magnetization printed at all), which collides with the banked S0 finding (h) that production RuO2 magnetic state was wrong (unanimous ADOPT AFM, re-run owed). The readout's UNTRUSTED label cites only incompleteness (15/32) — completing the Slurm queue will not cure this, so the withheld A6.3 ordering verdict needs a magnetic-state gate, not just a completeness gate.
   EVIDENCE: runs/a0/main/Ru/*.in contain no nspin or starting_magnetization keys (QE default nspin=1); the 15 existing runs/a0/main/Ru/*.out print zero 'total magnetization' lines. The species are Ru + O (an oxide slab, Ru_ONCV_PBE + O kjpaw). src/dft/build_a0main.py:79-80 states nspin is 'inherited byte-for-byte' from the production probe decks — i.e., the inheritance is registered, but it inherits exactly the production magnetic state S0(h) ruled against for RuO2. Cr, by contrast, is nspin=2 FM.
   FIX: When the Ru queue completes, do not auto-promote: add to a0main_readout.json a per-metal magnetic-state field and require the A6.3 verdict to either (a) show the Ru slab NM choice is registered as correct independently of the RuO2-anchor AFM decision, or (b) inherit a WRONG-MAGNETIC-STATE caveat parallel to the RuO2 anchor re-run.

[NOTE] QC sweep is clean: 96/96 banked outputs (76 Cr + 15 Ru + 5 new cell files) pass trusted_energy_ev(strict=True); zero 'convergence NOT achieved', zero 'Error in routine', 'JOB DONE' present in all 96. The readout claims 'all converged' (Cr) and 'absent, NOT convergence failures' (Ru/Ir) both SURVIVE.
   EVIDENCE: Ran src/dft/qe_qc.py scan + trusted_energy_ev over every file with matching .in: all verdicts TRUSTWORTHY (calculation='scf' single points, so the force criterion correctly does not apply). Ru has 15/32 outputs (s0_O 7 of 8 — u673 deck staged but not run — slab 8 of 8; s0_OH/s0_OOH decks staged, no outputs); runs/a0/main/Ir has 32 .in and 0 .out — pure absence, no failed runs anywhere.

[NOTE] Cr grid magnetization: no branch hops. Total magnetization is pinned at exact integers across all 19 U points in every state (slab 12.00, s0_O 10.00, s0_OH 11.00, s0_OOH 11.00 Bohr mag; zero jumps, threshold 1.5), consistent with a half-metallic gap — this supports the smooth-eta claim on the magnetic axis. Caveat: an integer-pinned total moment has no discriminating power against a branch hop within the same integer plateau, so I also checked absolute magnetization, which rises smoothly and monotonically with U in all 4 states (e.g. slab 14.23 -> 24.96, max adjacent step ~0.8, no discontinuity). No fixed-moment constraint is imposed (no tot_magnetization in any deck; smearing 'mv', degauss 0.01) — the integers are self-consistent.
   EVIDENCE: Final 'total magnetization' and 'absolute magnetization' extracted from all 76 runs/a0/main/Cr/*.out; decks runs/a0/main/Cr/*.in show nspin=2, starting_magnetization(1)=0.6, no tot_magnetization.

[NOTE] Cell-ladder magnetizations match the wave-1 record on every row including the new ones: ref 24.00, s0_O 22.00, s0_OH 23.00, s0_OOH 23.00 Bohr mag at all 5 U points (u0.0, u0.5, u1.35, base, u715) — the four new *__u715 rows and the s0_OH u0.0 retry sit exactly on their state's plateau. No branch change at the projector-mismatched U=7.15 point.
   EVIDENCE: Final 'total magnetization' from all 20 runs/a0/cell/*.out.

[NOTE] The OOM retry runs/a0/cell/s0_OH__2x1v_mir__u0.0.out is the SAME SCF branch as the killed attempt: iteration-1 energy identical to 2e-8 Ry (-3146.19467113 vs -3146.19467115) with byte-identical estimated accuracy (64.27826770), and the magnetization trajectory through iteration 4 is identical (23.00, 23.44, 22.99, 23.00). Iterations 2-4 differ by 1e-4 to 8e-3 Ry — the expected signature of a different process/diagonalization grid, not a different basin. Retry converged in 26 iterations to -3150.06050818 Ry, final accuracy 7.4e-7 < conv_thr 1e-6, final magnetization 23.00 matching the wave-1 s0_OH record.
   EVIDENCE: SCF trajectories parsed from runs/a0/cell/s0_OH__2x1v_mir__u0.0.out and .out.oom_attempt1 (killed after iteration 5, last printed estimate at it4).

[NOTE] conv_thr and parameter-echo spot checks pass. 10-file sample (5 Cr states/U extremes, 2 Ru, 3 cell incl. both u715 and the retry): final 'estimated scf accuracy' spans 3.0e-7 to 8.9e-7 Ry, every one below the uniform deck conv_thr = 1.0e-6 (first regex pass falsely matched forc_conv_thr=2e-3; corrected with an anchored match). U echo 6/6 decks: main-grid token = U x 100 (u050->0.5000, u350->3.5000, u900->9.0000) and ladder tokens = multipliers of 3.70 (u0.5->1.8500, u1.35 implied 4.995, base->3.7000) plus absolute u715->7.1500, each confirmed identical in the .in HUBBARD card and the .out U(Cr-3d) echo — exactly the dual convention build_a0main.py registers at :66-68.
   EVIDENCE: runs/a0/main/Cr/{s0_O__u050,s0_OH__u350,s0_OOH__u900}, runs/a0/cell/{s0_OH__2x1v_mir__u715,ref__2x1v__u0.5,ref__2x1v__base and the other 3 base decks}; U=0 decks (Cr u000, Ru u000, cell u0.0) correctly carry NO HUBBARD card at all, per the builder's registered '3 lines differ / card removed' rule — the absent echo is by design, not a mismatch.

==========================================================================================
verify:refute
SUMMARY: Adversarial re-derivation of both A0 readouts from raw outputs (all Cr numbers reproduced to 1e-12; strict QC re-run on every file used). Four MAJORs: (1) the 1.177 vs 1.122 V P7-bound comparison spans different U windows — within P7's own [0,7.15] window the grid measures 1.072 V, BELOW the headline (I reconstructed 1.12199 V exactly from the five probe points), and 1.177 only exceeds it via the U>7.15 extension with eta still rising at the U=9 grid edge; (2) the 2x1v crossing (5.33 eV) and the 1.47 eV shift exist only through the PROJECTOR-MISMATCHED U=7.15 rung (clean-point D(2x1v) tops out at 1.527 < 1.6) and carry no label, though CELL-CONDITIONAL itself survives on clean points alone (shift >= 1.13 eV > 1.0); (3) the A0-main Cr 'extraction control' compares byte-identical decks (prefix line only) so it tests only SCF determinism, unstated — the a0cell 2x1v controls, by contrast, are genuine and their <=0.01 meV numbers check out; (4) src/dft/a0main_readout.py crashes on Ru's gap rows (line 217 unpack bug) and cannot regenerate the banked JSON, which came from an older revision. Claims that survived attack, with checks stated: apex crossing 3.87 in [3.5,4.0] (quadratic refits move it 0.0006 eV; D strictly monotone, single crossing), I_U bin INCONCLUSIVE robust to dropping the mismatched rung (−0.155 on 4 clean points), pls flip 3->2 in [3.5,4.0], gas refs clean, cell-leg QC all-pass, Ru/Ir absences verified as not-run rather than convergence failures (though the banked JSON's older schema can't itself show that). Two MINORs: '3.87 +/- 0.25' conflates interpolant with bracket center; the 'all 4 states' control claim covers only the 2x1v leg.

[MAJOR] (a) 'P7 bound: swing 1.177 V >= withdrawn 1.122 V' compares different U windows and is not a like-for-like confirmation. The 1.122 V headline lives on U in [0,7.15]; restricted to that window the dense grid measures only 1.072 V — BELOW the headline.
   EVIDENCE: Reconstructed the withdrawn headline exactly from the a0cell 1x1 leg: eta at the probe's five points {0,1.85,3.70,4.995,7.15} = {1.4523, 0.9040, 0.3303, 0.6428, 1.1554}, swing = 1.12199 V — so P7's window was [0,7.15]. The 19-point grid's swing restricted to U<=7.15 is max 1.4523 (U=0) − min 0.3807 (U=3.5) = 1.0716 V < 1.122 (the 0.5-step grid misses the eta minimum near the apex crossing at 3.865, where the probe's 3.70 point reads 0.3303). The banked 1.177 V exceeds 1.122 only via the U∈(7.15,9] extension (max eta 1.5581 at U=9.0, the grid EDGE, with eta still rising 1.4538→1.5581 from 8.5→9.0 — so 1.177 is also edge-truncated, not a plateau). Files: docs/figs/a0main_readout.json, docs/figs/a0cell_readout.json; all etas re-derived by me from runs/a0/main/Cr/*.out via qe_qc.trusted_energy_ev strict + delta_G, matching to 1e-12.
   FIX: Bank the swing as: 'grid swing 1.177 V over U∈[0,9] (edge-limited, eta still rising at U=9); over P7's own window [0,7.15] the 0.5-step grid reads 1.072 V, below the 1.122 V five-point headline because the grid straddles the eta minimum'. Do not phrase 1.177>=1.122 as the bound confirming the headline without the windows-differ statement.

[MAJOR] (c) The 2x1v crossing U=5.33 eV — and therefore the quoted shift 1.47 eV — exists ONLY through the PROJECTOR-MISMATCHED U=7.15 rung, and the banked JSON/verdict line carries no label on either number, violating the registration's own 'the label travels with every number derived from that rung' rule. The CELL-CONDITIONAL verdict itself survives, with a mandatory caveat.
   EVIDENCE: D(2x1v) on the four clean points {0,1.85,3.70,4.995} = {0.5705, 0.9001, 1.2564, 1.5270}: max 1.527 < 1.6, so D=1.6 is never crossed on [0,4.995] — the 5.33 eV crossing is interpolated across [4.995,7.15] using the mismatched point. The 1x1 leg is safe: its crossing 3.8646 sits in the clean bracket [3.7,4.995] (D 1.5603→1.8728), no 7.15 dependence. Verdict survival: with D(2x1v) monotone increasing and <1.6 through 4.995, the 2x1v crossing is >4.995, so shift >= 4.995−3.865 = 1.13 eV > the registered 1.0 eV threshold on clean points alone. But docs/figs/a0cell_readout.json's crossings/crossing_shift_eV fields and the printed 'crossing shift = 1.47 eV -> CELL-CONDITIONAL' line carry no PROJECTOR-MISMATCHED tag (src/dft/a0cell_readout.py:233-247), while the script's own docstring (line ~30) registers that the label must travel with every derived number.
   FIX: Bank as: 'CELL-CONDITIONAL, shift >1.13 eV established on clean points alone (2x1v does not reach D=1.6 by U=4.995); shift = 1.47 eV and crossing U=5.33 only if the PROJECTOR-MISMATCHED U=7.15 rung is admitted'. Add the label field to the crossings/shift entries in the JSON.

[MAJOR] (d) The A0-main Cr 'extraction control <=0.01 meV' is circular: the a0 u000 decks and probe u0.0 decks are byte-identical except the prefix= line, so the ~0.003 meV drift measures only SCF/scheduler determinism, not geometry extraction — and the readout does not say so.
   EVIDENCE: diff runs/a0/main/Cr/{slab,s0_O,s0_OH,s0_OOH}__u000.in vs runs/probe/Cr/*__u0.0.in: every pair differs ONLY in line 3 (prefix token 'u000' vs 'u0.0'); geometry, cutoffs, everything else identical. Drifts 0.0024–0.0038 meV are re-run noise of the same deck. The script's docstring admits it 're-runs an SCF the probe campaign already banked' but the banked claim line ('extraction control ... OK', a0main_readout.py:157-160, control_ok=true in the JSON) presents it as an extraction control without the determinism-only caveat. Contrast: the a0cell 2x1v controls ARE genuine — base SCF vs the SOURCE RELAXATION's final energy (calculation='relax' in runs/probe/Cr_cellsym/ref__2x1v.in vs 'scf' in runs/a0/cell/ref__2x1v__base.in); I recomputed those drifts myself: slab +0.0011, s0_O +0.0031, s0_OH +0.0049, s0_OOH +0.0061 meV — the '<=0.01 meV all 4 states' numbers for the CELL readout are real and check out.
   FIX: Relabel the A0-main Cr control as a determinism/re-run check (identical decks), or make it a real extraction control by re-deriving the deck from the source relaxation; either way the banked text must state which it is.

[MAJOR] The banked docs/figs/a0main_readout.json cannot be regenerated by the script it cites: src/dft/a0main_readout.py crashes on any metal with gaps (Ru/Ir) before writing JSON — the artifact is from an older script revision (version skew).
   EVIDENCE: Ran `PYTHONPATH=src python src/dft/a0main_readout.py` (no --json): ValueError at line 217, `gaps=[[u, ms] for u, ms in gaps]` — gaps holds 3-tuples (u, missing, why) since the gap_why feature was added, so the comprehension's 2-way unpack dies on Ru's 8 gap rows and the script never reaches the A6.3 section or the json.dump. Corroborating skew: the banked JSON's Ru/Ir gap rows lack the gap_why field the current code writes. The Cr numbers themselves are correct (I re-derived D/eta at u000/u350/u400/u900 from raw outputs, exact match), but a registered result must be reproducible by its named script.
   FIX: Fix the unpack (`for u, ms, *_ in gaps` or store 2-tuples), re-run, and re-bank; diff the regenerated JSON against the current one before banking.

[MINOR] 'apex crossing at U=3.87 +/- 0.25 eV' conflates the interpolated crossing with the bracket half-width: the grid guarantees U in [3.5,4.0] = 3.75 +/- 0.25, whereas 3.87 +/- 0.25 = [3.62,4.12] extends 0.12 eV past the guaranteed bracket and drops [3.5,3.62].
   EVIDENCE: a0main_readout.py:243 prints 'U = 3.87 eV, bracket [3.5, 4] -> located to +/-0.25 eV'. +/-0.25 is correctly half the 0.5 eV step, but it is the half-width about the bracket CENTER (3.75), not about the interpolated 3.87.
   FIX: Quote 'crossing inside [3.5, 4.0] (0.5 eV bracket); linear interpolation 3.87 eV' rather than 3.87 +/- 0.25.

[MINOR] The a0cell claim 'extraction control drift <=0.01 meV all 4 states' covers only the 2x1v leg; the 1x1 leg (including the P-PROJ fifth point) has no in-script control — it is delegated to probe-time GATE 1 and a build-time byte-identity assertion the readout does not re-verify.
   EVIDENCE: src/dft/a0cell_readout.py CONTROLS_2X1V covers only the 2x1v files; lines 152-154 are a comment, not a check. The banked JSON has no 1x1 control fields at all.
   FIX: State 'controls: 2x1v leg (4 states, <=0.01 meV); 1x1 leg inherited from probe GATE 1' in the banked text.

[NOTE] (b) The apex-crossing interpolation SURVIVES adversarial attack: curvature is negligible and D is strictly monotone.
   EVIDENCE: Checked: quadratic fits through (3.0,3.5,4.0) and (3.5,4.0,4.5) place the crossing at 3.8661 and 3.8660 eV vs linear 3.8655 — a 0.0006 eV move, deep inside [3.5,4.0] (second difference of D at the bracket is 0.0015 eV). D increases strictly across all 19 grid points (0.7614→2.7881), exactly one crossing of 1.6. Same for the a0cell 1x1 crossing 3.8646 in [3.7,4.995]. The pls flip 3→2 between 3.5 and 4.0 is confirmed from the rows and does not inflate the swing (eta = max(dG)−1.23 is continuous through a flip; the swing endpoints U=3.5 and U=9.0 are both flip-free).

[NOTE] (c) The I_U INCONCLUSIVE bin SURVIVES removal of the PROJECTOR-MISMATCHED U=7.15 point.
   EVIDENCE: On the four clean points: span(1x1) = 1.8728−0.7614 = 1.1114, span(2x1v) = 1.5270−0.5705 = 0.9565, I_U = −0.1549 eV, |I_U| = 0.155 ∈ [0.05,0.30) → still inconclusive, same bin as the registered 5-point I_U = −0.2013 (arithmetic verified: spans 1.6240/1.4227, sign convention span(2x1v)−span(1x1) as registered). Worth one sentence in the banked text: 'bin robust to dropping the mismatched rung'.

[NOTE] Gas references are clean — no projector/U-leg mixing.
   EVIDENCE: Both readouts use runs/Cr_slab/{H2O,H2}.out (−599.2110126, −31.7453226 eV), which pass qe_qc.trusted_energy_ev strict; same ecutwfc 80 / ecutrho 640 and pseudo_dir as the slab decks; gas species carry no Hubbard card, so Xu's U-provenance issue cannot touch them. Also verified: all 20 runs/a0/cell/*.out (including the OOM-retried s0_OH__2x1v_mir__u0.0.out) and all probe/p_proj/e_proj leg files pass strict QC; a0cell 'gaps: []' is honest.

[NOTE] 'Ru 15/32, Ir 0/32, absent NOT convergence failures' is true on disk, but the banked JSON cannot substantiate the 'not convergence failures' part because its gap rows predate the absent-vs-qc-fail schema.
   EVIDENCE: Disk: exactly 15 runs/a0/main/Ru/*.out (slab + s0_O only; s0_OH/s0_OOH outputs do not exist), zero occurrences of 'convergence NOT achieved'; runs/a0/main/Ir has 0 .out against 32 staged decks. But the banked JSON's Ru/Ir gap rows carry only {u, gap} with no gap_why field — the absent/qc-fail distinction exists only in the (currently crashing) script revision. Re-banking with the fixed script (see the version-skew finding) resolves this automatically.

