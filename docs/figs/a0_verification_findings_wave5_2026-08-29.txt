A0 WAVE-5 ADVERSARIAL AUDIT -- Ti stage 3 landing, A7.2/A7.3 verdicts
2026-08-29 | 47 agents | 38 raised / 26 refuted / 7 survived / 7 critic

METHOD: 8 independent finder dimensions (a73-scoring, a72-census, run-qc,
reanchor-bias, caveat-accuracy, staleness, prereg-discipline, evidence-integrity),
each finding then handed to a refute-by-default verifier, then a completeness critic
asked what nobody checked. Run BEFORE banking, against the uncommitted tree.

IMPORTANT -- FIVE VERIFIERS DIED on API safeguards (verify:staleness x3,
verify:a72-census x1, verify:reanchor-bias x1). Their findings received NO verdict.
They were re-checked BY HAND rather than counted as refuted; that is why the
survived/refuted totals do not sum to 38.

==============================================================================
SURVIVED (7)
==============================================================================

[1] [a72-census] [MINOR] The A7.2 census banks flip brackets with no pls margin anywhere, though the scorer already computes margin-vs-error-class credibility for A6.3 — so three of the five claimed flips are indistinguishable in the artifact from the robust ones
    WHERE: src/dft/a0main_readout.py:583
    CLAIM: A7.2 banks flip brackets with no margin or conditionality field (src/dft/a0main_readout.py:583, :595, :719), while the same script computes error-class margin credibility for A6.3 fifty lines later (:642-668). The census-relevant consequence is narrower than "five brackets of differing robustness": A7.2 counts metals, and per-metal flip existence is fixed by the grid ENDPOINTS. Cr (endpoint margins 1694/748 meV), Mn (322/268) and Ir (369/443) are robust against every error class the campaign has measured, so the CONFIRMED verdict at >=3 of 6 is safe. But Ru and Fe each rest on ONE endpoint row: every other Ru row is pls 3 and every other Fe row is pls 2, so if U=9.0 reverts, that metal leaves the census entirely. Ru's U=9.0 pls margin is 44.3 meV (dG2 1.5199 vs dG3 1.4757) — inside the campaign's own measured, still-open NM-vs-AFM adsorption class (0.033-0.064 eV, gate (h), AFM re-run owed), against an A0 Ru column that is nspin=1. Fe's is 81.7 meV (dG1 2.578792 vs dG2 2.497067), closed by a 40.9 meV shift in dG_OH, inside the measured 47.77 meV GATE-1 child-above-parent basin drift for nspin=2 3d columns (docs/45 §A row 2). The specific, pointable defect is not the missing field alone but that caveats.spin_state at :838-840 DOES enumerate which margins are conditional on that class — "the U <= 4.5 ordering rows and the Ir flip bracket's low edge" — and (a) omits Ru's flip-carrying 44.3 meV margin, and (b) names the wrong Ir edge: the low edge (U=3.0) is 93.7 meV, above the 64 meV class top, while the edge actually inside the class is the high edge (U=4.5, 44.6 meV). A reader treating that enumeration as complete concludes Ru's flip is unconditional when it is the most exposed of the five. This matters now because Ti closes the census in this change set (metals_measured 5 -> 6, pending_data -> []), so the monotonicity defense of docs/59:239 and :252 ("more metals can only add flips") no longer protects the reported 5-of-6 strength; docs/58:118-120 already reports the census as Cr + Ir + Ru, resting on one of the two fragile legs. Minimal fix: bank a per-bracket (and per-metal endpoint) pls margin plus the ERROR_CLASSES test already implemented at :642-658, and correct the :838-840 enumeration to name Ru U=9.0 and the Ir U=4.5 edge. Two claims in the original finding should be dropped: the brackets span 9.5-337 meV, not "1.7 eV", and the artifact is not opaque — every row's dG_OH/dG_O/dG_OOH is banked, so the margins are derivable from the JSON alone; what is missing is the derived label and the completeness of the caveat, not the data.
    VERIFIER: I tried to refute this and could not fully, though the finding is mis-stated in several ways and its general framing (per-bracket margins) is the wrong quantity.
              
              What checks out exactly. The line references are right (flip tuple built at src/dft/a0main_readout.py:583, banked at :595 and :719; the A6.3 credibility machinery at :642-668, applied only under `if inversions:`). Recomputing the four step energies from the banked rows (dG1=dG_OH, dG2=dG_O-dG_OH, dG3=dG_OOH-dG_O, dG4=4.92-dG_OOH) reproduces every margin the finding quotes to 0.1 meV: Cr [3.5,4.0] 98.1/76.0; Mn [0.0,1.5] 322.1/337.3; Ru [7.5,9.0] 153.6/44.3; Mn [6.0,7.5] 9.5/135.9; Fe [7.5,9.0] 68.3/81.7. The Fe scenario math is right: at U=9.0, dG1=2.578792 vs dG2=2.497067, so a 40.9 meV drop in dG_OH restores pls 2, and 47.77 meV is a measured GATE-1 child-above-parent drift for nspin=2 3d columns (docs/45 §A row 2). No margin, error-class, or conditionality field attaches to any bracket in the a7_2 block (:715-724).
              
              Where the finding is wrong or overstated. (1) "Five flip brackets span 1.7 eV of robustness" is false — the margins span 9.5 to 337 meV, about 0.33 eV. (2) "No way to tell them apart" is false as stated: every row's dG_OH/dG_O/dG_OOH is banked in the same JSON, so the margins are derivable in ten lines (I derived them from the artifact alone). The gap is a missing derived label, not missing data. (3) The per-bracket framing is the wrong quantity for this census: A7.2 counts METALS with a flip, and per-metal flip existence is fixed by the grid endpoints, not by mid-grid brackets. Computing endpoint margins: Cr 1694/748, Mn 322/268, Ir 369/443 meV — all far above the top of every measured error class, so three metals carry >=3 of 6 robustly and the CONFIRMED verdict cannot be moved by any class the campaign has measured. Mn's 9.5 meV bracket is not a census risk because Mn's membership is carried by its 322/337 meV bracket. (4) Partial disclosure exists that the finding does not credit: caveats.ir_ooh_basin conditions the Ir bracket, and caveats.spin_state (:838-840) already states the exact principle — "Margins smaller than that class ... are spin-state-conditional" — and applies it to a flip bracket.
              
              Why it nonetheless survives, in narrowed form. Two of the five claimed flips rest entirely on a single grid-endpoint row: every other Ru row is pls 3 and every other Fe row is pls 2, so Ru (44.3 meV at U=9.0) and Fe (81.7 meV at U=9.0) each leave the census outright if that one endpoint reverts. Ru's 44.3 meV sits INSIDE the campaign's own measured, still-open NM-vs-AFM adsorption class (0.033-0.064 eV, gate (h), AFM re-run explicitly owed) and the Ru A0 column is nspin=1 — and the caveat sentence at :838-840 that enumerates which margins are spin-state-conditional lists "the U <= 4.5 ordering rows and the Ir flip bracket's low edge" and omits it. That enumeration is also wrong about Ir: the low edge it names (U=3.0) is 93.7 meV, above the 64 meV class top, while the Ir edge that is inside the class (U=4.5, 44.6 meV) is not named. A reader taking that enumeration as complete concludes Ru's flip is unconditional when it is the most exposed of the five. This is not taste: the campaign has already litigated exactly this failure mode for exactly this field (docs/45 trap 14 — pls_flips: [] indistinguishable from an unmeasured grid, fixed by adding pls_flips_measurable), which sets the standard that a consumer-ambiguous derived field is a defect.
              
              The change set also removes the standing defense. docs/59:239 and :252 protect A7.2 with "more metals can only add flips"; Ti closes the census in this very change set (metals_measured goes 5 -> 6, pending_data -> []), so the 5-of-6 headline is now final and the monotonicity argument no longer covers its strength. docs/58:118-120 still reports the pre-Mn/Fe/Ti census as Cr + Ir + Ru, i.e. resting on Ru, one of the two fragile legs.
              
              Verdict unaffected either way; the reported strength is what is exposed. Severity minor, as claimed.

[2] [run-qc] [MAJOR] s0_OOH never added to Ti_audit/probe_manifest.json, so the state that decides A7.3 is invisible to probe_eta.py and escapes GATE 1
    WHERE: src/dft/build_a0main_w3b.py:189
    CLAIM: s0_OOH was never appended to runs/probe/Ti_audit/probe_manifest.json, so the state that decides A7.3 is invisible to probe_eta.py, escapes GATE 1, and is contradicted by two repo artifacts.
           
           build_a0main_w3b.build_base() writes runs/probe/Ti_audit/s0_OOH__base.in via P.write_probe (src/dft/build_a0main_w3b.py:172) + W.write (:189), deliberately bypassing P.cmd_build — the only code path that appends a job record to probe_manifest.json (src/dft/probe_decks.py:450). The builder compensates only in runs/a0/main/manifest.json["tranche_3b"], never in the probe manifest. runs/probe/Ti_audit/probe_manifest.json therefore still lists three jobs (slab, s0_OH, s0_O) while four primary decks sit on disk. Counting only primary decks (excluding `.run.in` runtime copies), Ti_audit is the only probe directory in the repo whose manifest disagrees with its contents.
           
           Two concrete consequences, both reproducible today at HEAD+working-tree:
           
           (1) A live script now asserts the opposite of the tranche's headline. probe_eta.py enumerates work exclusively from man["jobs"] (src/dft/probe_eta.py:89, :123), and src/dft/collect_probes.sh:57-59 runs it over every probe directory with a manifest (a purely local path, no SSH). `PYTHONPATH=src python src/dft/probe_eta.py runs/probe/Ti_audit` prints GATE 1 over only s0_O, s0_OH and slab, concludes "-> control reproduces the relaxation; extraction is faithful.", then prints "No variant has a complete slab + OH + O + OOH set yet." (probe_eta.py:192) while a converged s0_OOH__base.out sits in that directory. Before this tranche that line was true for Ti_audit; the change set makes it false.
           
           (2) The banked artifact points at a control that does not exist. src/dft/a0main_readout.py:518-525 writes control_note into docs/figs/a0main_readout.json stating that the genuine extraction control (SCF vs source relaxation) "lives in the a0cell readout for Cr/Ru/Ir and in probe_manifest relax_reference_ev for Mn/Fe/Ti". For Ti's s0_OOH there is no probe_manifest record and hence no relax_reference_ev, so for the single state whose re-anchoring provenance is the tranche's headline, the named control is absent. The a0 readout's own Ti check is only the byte-identical-deck determinism drift (banked s0_OOH: 0.005578 meV), which src/dft/a0main_readout.py:38-42 expressly disclaims as not a geometry round-trip. Nothing in the artifact's seven caveat blocks, and nothing in docs/59, discloses either gap; no CI workflow gates manifest/disk consistency.
           
           Materiality: A7.3's registered quantity is span(c_M)/2 at fixed endpoints U = 0 and U = 9 with c_M = dG_OOH - dG_OH, so Ti's s0_OOH column supplies dG_OOH at both endpoints (c_M = 3.45694 and 3.36943 -> 0.04376, the banked 0.0438).
           
           Severity is discipline and artifact integrity, not arithmetic: no banked number changes. Verified independently — qe_qc.trusted_energy_ev('runs/probe/Ti_audit/s0_OOH__base.out', strict=True) = -17712.319505409876 eV vs probe_decks.relax_final_energy_ev('runs/Ti_slab/s0_OOH_r3.out') = -17712.319507586788 eV, drift +0.0022 meV. Patching a scratch copy of the directory with the single missing job record (job s0_OOH, variant base, file s0_OOH__base.in, geometry_provenance "final", relax_reference_ev -17712.319507586788) makes GATE 1 pass 4/4 at +0.00 meV and makes probe_eta emit a complete Ti row (eta 1.7407 V, pls 2), matching the readout's banked Ti u=0 row (1.74065). The fix is that one manifest record; the defect is that the registered control was never applied to the deciding state and two artifacts currently misstate the situation.
    VERIFIER: Every factual element checks out against the files, and the attempted refutations all fail.
              
              (1) Manifest vs disk: verified programmatically across all 15 probe dirs. runs/probe/Ti_audit/probe_manifest.json holds 3 jobs; 4 primary decks are on disk. Ti_audit is the ONLY directory with an unmanifested primary deck — every other apparent extra is a `.run.in` runtime copy, so the uniqueness claim survives with the counting corrected.
              
              (2) Bypass verified at exact lines: build_a0main_w3b.py:172 calls P.write_probe and :189 calls W.write; probe_decks.py:450 is the sole writer of probe_manifest.json; the builder records the deck only in runs/a0/main/manifest.json["tranche_3b"]["decks"]["base"], never in the probe manifest.
              
              (3) Consequence reproduced verbatim: `PYTHONPATH=src python src/dft/probe_eta.py runs/probe/Ti_audit` prints GATE 1 over s0_O/s0_OH/slab only, "-> control reproduces the relaxation; extraction is faithful.", then "No variant has a complete slab + OH + O + OOH set yet." probe_eta.py:89/:123 enumerate strictly from man["jobs"], and collect_probes.sh:57-59 is a live local path with no SSH dependency. This is a NEW regression: before this tranche the "no complete set" line was true for Ti_audit.
              
              (4) Numbers re-derived independently, not trusted from the finding: base -17712.319505409876 eV vs relax_final_energy_ev(runs/Ti_slab/s0_OOH_r3.out) -17712.319507586788 eV, drift +0.0022 meV. I then patched a scratch copy of the directory with the one missing job record and re-ran probe_eta: GATE 1 passes 4/4 and a full Ti row appears (eta 1.7407 V, pls 2), matching the banked readout's Ti u=0 row (1.74065). So no banked number changes — the finding is honest about this.
              
              Refutation attempts that failed: (a) "already disclosed" — I read all seven caveat blocks in docs/figs/a0main_readout.json and grepped docs/59; neither mentions the manifest omission or a missing extraction control for Ti s0_OOH. (b) "dead code path" — collect_probes.sh score is local and live. (c) "a0main_readout covers it" — it reads runs/probe/Ti_audit/s0_OOH__base.out by filename (its determinism drift 0.005578 meV is banked for s0_OOH), so the file is load-bearing there, but that check is byte-identical-deck determinism, which the script itself disclaims as not a geometry round-trip. (d) "no CI gate is bypassed" — there is no manifest-consistency gate anywhere, which is why nothing caught it.
              
              The finding is in fact understated in one respect and mis-attributed in another. It cites a0main_readout.py:38-42, which names the a0cell readout as the genuine control. The stronger text is a0main_readout.py:518-525, which writes control_note into the COMMITTED artifact docs/figs/a0main_readout.json asserting that for Ti the genuine extraction control "lives in probe_manifest relax_reference_ev" — a locator that is false for s0_OOH. Materiality is also real: A7.3's registered quantity is span(c_M)/2 with c_M = dG_OOH - dG_OH at U=0 and U=9, so Ti's s0_OOH column supplies both endpoints (3.45694, 3.36943 -> 0.04376 = the banked 0.0438).
              
              Severity is the one thing I would restate: this is an artifact-integrity and registered-control defect, not a numerical one, with a one-record fix.

[3] [caveat-accuracy] [MAJOR] Caveat attributes the Ti adsorbate starting geometry to gen_rutile.py, which emits 6-atom bulk SCF cells and contains no slab or adsorbate code at all
    WHERE: C:/Users/frank/sts-electrocatalyst/src/dft/a0main_readout.py:813
    CLAIM: src/dft/a0main_readout.py:813 (uncommitted, and already frozen into docs/figs/a0main_readout.json:1293) attributes the Ti adsorbate starting geometry to the wrong module: "it is the only geometry in this readout whose starting structure was not emitted by gen_rutile.py. That default placement puts every Ti adsorbate ~3.2 A off the nearest Ti". gen_rutile.py emits only the 6-atom experimental rutile BULK scf cell — calculation='scf' (:115), ibrav=6 (:124), nat=6 (:127), ATOMIC_POSITIONS = 2 M at 2a + 4 O at 4f (:140-146) — and contains no slab, adsorbate, or relax code (zero grep hits for slab|adsorb|OOH|OH|surface|110 across all 179 lines). The readout scores only slabs and adslabs (a0main_readout.py:73, STATES = slab/s0_O/s0_OH/s0_OOH), so NO geometry in it was emitted by gen_rutile.py and the uniqueness test is vacuously true of every row — the one sentence carrying the Ti *OOH provenance exception isolates nothing. The module that emits these starting structures is qe_slab.py ("Rutile(110) slab + adsorbate DFT", :2; imports build_rutile110_hea / cus_site_xy / add_oer_adsorbate_at at :249; calls add_oer_adsorbate_at at :271), with the ~3.2 A default itself arising in hea_oer/surfaces_rutile.py:190-200, where _adsorbate sets the height above the slab's topmost atoms (the bridging-O rows) so the adsorbate "lands 3.07-3.13 A off the cus metal". Every registered record names qe_slab.py (docs/59 s3 :75 and s3c :137, build_a0main_w2c.py:69, runs/Ti_slab/m_ti_relax_r2.txt). The name appears once at :813, with the following sentence's "That default placement" inheriting the wrong referent. This is a regression introduced by the hand rewrite: the committed HEAD text of the fixed_geometry caveat named no module (gen_rutile.py appears at HEAD only in spin_state :828 and mn_afm :841, where it is correct). qe_slab.py appears nowhere in the artifact (0 hits in docs/figs/a0main_readout.json), so an auditor following the caveat opens a bulk scf generator and can reproduce neither the placement nor the uniqueness claim. Fix: replace "gen_rutile.py" with "qe_slab.py (placement in hea_oer.surfaces_rutile)" and regenerate the JSON before committing.
    VERIFIER: Could not refute; every element checks out against the files.
              
              (1) gen_rutile.py really is bulk-only. Its docstring is "Generate a Quantum ESPRESSO pw.x SCF input for a rutile MO2 endmember"; it emits calculation='scf' (:115), ibrav=6 (:124), nat=6 (:127), and ATOMIC_POSITIONS (:140-146) is exactly the 6-atom P4_2/mnm cell (2 M at 2a, 4 O at 4f). `grep -niE 'slab|adsorb|OOH|\bOH\b|surface|110' src/dft/gen_rutile.py` returns ZERO hits over all 179 lines. There is no adsorbate placement and no relax path in it.
              
              (2) The uniqueness test is vacuous, exactly as claimed. a0main_readout.py:73 sets STATES = ("slab","s0_O","s0_OH","s0_OOH") — every scored point is a (110) slab or adslab, so NO geometry in this readout was emitted by gen_rutile.py. "the only geometry ... whose starting structure was not emitted by gen_rutile.py" is true of all 24 of them, so the sentence that is supposed to isolate Ti's *OOH provenance isolates nothing.
              
              (3) The real emitter is qe_slab.py: :2 "Rutile(110) slab + adsorbate DFT", :249 imports build_rutile110_hea/cus_site_xy/add_oer_adsorbate_at from hea_oer.surfaces_rutile, :271 calls add_oer_adsorbate_at. The ~3.2 A default is documented at src/hea_oer/surfaces_rutile.py:190-200 (adsorbate_starts docstring): _adsorbate sets the height above the slab's TOPMOST atoms, which on rutile(110) are the bridging-O rows, so "the adsorbate therefore lands 3.07-3.13 A off the cus metal". That is the placement the caveat is describing, and it is not in gen_rutile.py.
              
              (4) Every registered record names qe_slab.py: docs/59:75 "Chain: `qe_slab.py build Ti --supercell 1` (production builder)"; docs/59:137 "`qe_slab.py` starts every Ti adsorbate ~3.1-3.2 A off the nearest Ti"; build_a0main_w2c.py:69 identical.
              
              (5) It IS a regression from the hand rewrite. `git show HEAD:src/dft/a0main_readout.py | grep -n gen_rutile` returns only :828 (spin_state) and :841 (mn_afm) — the committed fixed_geometry caveat named no module. git diff shows the entire gen_rutile.py sentence added in the uncommitted block, and it is already frozen into docs/figs/a0main_readout.json:1293.
              
              (6) The correct module is disclosed nowhere in the artifact: `grep -c qe_slab docs/figs/a0main_readout.json` = 0, and zero hits in a0main_readout.py. So an auditor reading the artifact has no in-artifact route to the right file — the "already disclosed elsewhere" escape does not apply to the object that travels with the numbers.
              
              Refutation attempts that failed: (a) I checked whether surfaces_rutile derives its cell from gen_rutile — RUTILE_AC:33-35 does comment that Ti's constants are "gen_rutile.py's constants", but that lineage is shared by Cr/Mn/Fe/Ru/Ir too, so it cannot make the uniqueness claim true, and gen_rutile.py still performs no placement. (b) I checked whether "gen_rutile.py" is a campaign shorthand for convention tables — it is used that way legitimately in spin_state and mn_afm (mag/U conventions, which gen_rutile.py genuinely holds), but the fixed_geometry sentence is about an emitted starting STRUCTURE and a default adsorbate PLACEMENT, neither of which exists in that module.
              
              Only wording correction: the module is named once (:813), not twice — "That default placement" is an anaphoric reference inheriting the wrong attribution rather than a second naming. Also the placement code proper lives in hea_oer/surfaces_rutile.py + surfaces.py; qe_slab.py is the production entry point that invokes it and is the name the registered records use.

[4] [staleness] [MINOR] docs/45's correction-of-record entry still cites docs/43:279 as the scope-change rule, a citation docs/59 explicitly retracts
    WHERE: C:/Users/frank/sts-electrocatalyst/docs/45-error-ledger.md:2145
    CLAIM: docs/45-error-ledger.md:2143-2146 (wave-3 A0 entry, committed 904a40b 2026-08-28) still reads "against docs/43's own scope-change rule (:279)". docs/43-prereg-week1-factorial.md:279 is the fourth row of the 1B hp.x internal-gate table ("symmetry-equivalent perturbed atoms | agree within 0.05 eV | an inconsistent projector or k-set"), inside the block headed "Internal part (the real gate; no citation required)" at :272-279 and followed at :281 by "### Declared consequence of NO-GO"; it says nothing about scope. The campaign has already retracted this pointer: docs/59-a0-roster-correction-2026-08-28.md:11-14 records "(An earlier draft of this document cited docs/43:279, which is a row of the 1B hp.x internal-gate table and says nothing about scope -- corrected 2026-08-29 after the wave-4 audit.)" The retraction was banked in commit bae6cd2 (2026-08-29), the SAME commit that added 73 lines to docs/45 — so the correction was propagated to docs/59 and not to the ledger entry where the claim was first banked. It is not drift: docs/43 was last touched 2026-08-23 (6f258d1), before the ledger entry existed, and all three docs/43 archives also carry non-scope text at line 279. Nothing in docs/45 discloses the problem: grep finds no "932", no "Amendment 5", and no docs/59 pointer anywhere near :2145 (the ledger's only docs/59 reference, :2352, is about trap 17's defence wording), and the wave-4 ledger section :2286-2352 never mentions it. The wave-4 audit's own finding [5] (docs/figs/a0_verification_findings_2026-08-29.txt:131-150) was scoped "WHERE: docs/59-...:11" only, so the second copy in the ledger was never looked for. ONE CORRECTION to the finding as filed: it names "Amendment 5 at docs/43:932" as the real rule, but wave-4 finding [5] holds that 932 is amendment 5's sentence governing the five DEFERRED literature items (D3/D5/D7/D9/D10) and "is not a global scope rule", and its FIX AS PROPOSED (:148) cites docs/43:4-5 ("Nothing in this document may be edited after that deposit; corrections go in a dated addendum at the bottom with the reason") with 932 demoted to a parenthetical — docs/59's applied edit cites 932 alone, diverging from the proposed fix. The repair is therefore a dated "### CORRECTION" entry in docs/45 (the ledger's established convention at :329, :1516, :1803) noting that :279 is retracted and that the governing rule is docs/43:4-5, NOT a verbatim copy of docs/59's wording. Severity minor: the underlying MAJOR finding (the Cr 19 / Ru 7+1 / Ir 7+1 allocation was chosen with no dated amendment) stands on its merits; only the registration pointer is wrong, and an external auditor following it lands on an hp.x convergence-gate table.
    VERIFIER: Every refutation route failed. (1) The pointer is factually wrong in the live file: docs/43-prereg-week1-factorial.md:279 is the fourth row of the 1B hp.x internal-gate table ("symmetry-equivalent perturbed atoms | agree within 0.05 eV | an inconsistent projector or k-set"), inside the block 274-279 headed "Internal part (the real gate; no citation required)" and followed at 281 by "### Declared consequence of NO-GO". (2) Not line drift: docs/43 was last modified 2026-08-23 (6f258d1), before the ledger entry was committed 2026-08-28 (904a40b); all three archived docs/43 versions also have non-scope text at line 279; and grep for "only through its own dated amendment" returns a single hit at 932. The wave-4 findings file states independently (line 139) that the number "was wrong at authoring time too, not a later drift." (3) Not disclosed in the ledger: grep over docs/45 for "932", "Amendment 5", "scope-change" and "docs/59" finds nothing near :2145 — the sole docs/59 mention is :2352, about trap 17's defence wording — and the wave-4 ledger section (2286-2352) records the BLOCKER and traps 12-17 without mentioning the miscitation. (4) The gap is live and inside the current work stream: the docs/59 retraction was banked in bae6cd2 (2026-08-29), and that same commit also edited docs/45 (+73 lines); the correction was applied to one file and not the other 140 lines away. (5) The "a dated ledger entry is a frozen historical record" defence fails: docs/45 already carries in-place dated corrections at :329, :1516 and :1803, and wave-4 finding [5]'s sibling fix (findings file :126) explicitly prescribes that convention. The finding is real. It is mis-stated in one respect, so I have corrected it: wave-4 finding [5] (findings file :136, :148) holds that docs/43:932 is amendment 5's rule for the deferred literature items D3/D5/D7/D9/D10 and "is not a global scope rule", and proposes docs/43:4-5 instead; docs/59's applied edit cites 932 anyway. So the ledger's replacement pointer is itself contested, and copying docs/59's sentence verbatim would import that dispute.

[5] [prereg-discipline] [MAJOR] The hand-rewritten Ti provenance caveat attributes adsorbate placement to gen_rutile.py, which emits no slab or adsorbate geometry for any row in the readout
    WHERE: src/dft/a0main_readout.py:813
    CLAIM: The hand-rewritten caveats.fixed_geometry names the wrong script as the source of the campaign's default adsorbate placement. src/dft/a0main_readout.py:812-813 (uncommitted; banked into docs/figs/a0main_readout.json:1293) reads "it is the only geometry in this readout whose starting structure was not emitted by gen_rutile.py. That default placement puts every Ti adsorbate ~3.2 A off the nearest Ti". gen_rutile.py is a bulk rutile MO2 SCF generator — docstring "held FIXED (scf, no relax)", single builder make_input (:76), no slab/vacuum/adsorbate code, and a RUTILE dict (:63-72) with no Ru or Ir key — so it emitted NONE of the 24 starting structures in this readout, not merely all-but-one, and it could not have emitted the Ru or Ir rows under any reading. Default placement is qe_slab.py cmd_build -> hea_oer.surfaces_rutile.add_oer_adsorbate_at (qe_slab.py:249, :271; surfaces_rutile.py:160-164), which is how build_a0main_w3b.py:31 and docs/59:137 correctly attribute it. Only the script name is wrong: I re-derived the built starts from the decks and the numeric claim holds — anchor-O to nearest Ti = 3.117 A (runs/Ti_slab/s0_O.in), 3.167 A (s0_OH.in), 3.167 A (s0_OOH.in), 1.782 A after the w2c re-anchor (s0_OOH_r3.in) — so the "~3.2 A" half is true and only its antecedent misdirects. The concrete failure: a provenance auditor following the artifact's pointer for the metal that decides A7.3 lands on a 179-line bulk-cell script with no adsorbate code and no Ru/Ir rows, and either reads the disclosure as fabricated or accepts the false distinguishing property and reports that Cr/Mn/Fe/Ru/Ir starting structures share a gen_rutile provenance none of them have. Fix is one substitution (gen_rutile.py -> qe_slab.py / add_oer_adsorbate_at) plus rescoring the artifact. Severity minor-to-moderate, not major: no number, threshold or verdict moves, and the genuine provenance difference (the w2c rigid re-anchor to 1.781905 A) is already stated correctly three sentences later in the same caveat.
    VERIFIER: I tried four refutation routes and all failed on the central point.
              
              (1) Does gen_rutile.py emit any geometry in this readout? No. src/dft/gen_rutile.py is a bulk rutile MO2 SCF-input generator (docstring :2-16, "geometry ... held FIXED (scf, no relax)"), its only builder is make_input (:76), and it contains no slab, no vacuum, no adsorbate code. `git log -- src/dft/gen_rutile.py` returns two commits (97441fe, c40b68e), neither adding adsorbate code, so no historical version did either. The readout itself (docs/figs/a0main_readout.json) has metals = Cr/Ru/Ir/Mn/Fe/Ti with slab/*O/*OH/*OOH rows and no bulk row, so gen_rutile.py emitted zero of the 24 starting structures the caveat is quantifying over.
              
              (2) Could "emitted by" be read loosely as "supplied the lattice constants"? Even then it fails, and fails hardest exactly where the sentence needs it: surfaces_rutile.py RUTILE_AC (:33-43) hardcodes its own table, credits gen_rutile only in a comment for Ti (:34), and takes Ru/Ir from Bolzan et al. (:41-43) — species gen_rutile.py has no entry for at all (RUTILE :63-72 = TiO2/CrO2/MnO2/FeO2/CoO2/NiO2/CuO2). And under the loose reading Ti's slab/*O/*OH would inherit the same constants as its *OOH r3 deck (a rigid translation of the built deck), so the sentence still would not single out *OOH.
              
              (3) Is the attribution merely stylistic / a harmless name slip? No. The named script is the disclosure's only pointer, and it points to a 179-line bulk script. Placement actually comes from qe_slab.py cmd_build -> hea_oer.surfaces_rutile.add_oer_adsorbate_at (qe_slab.py:249 import, :271 call; surfaces_rutile.py:160-164 sets the height via _adsorbate() + ASE add_adsorbate). qe_slab.py never imports gen_rutile — it mentions it once in a comment at :39.
              
              (4) Is it already correctly disclosed? Correctly elsewhere (build_a0main_w3b.py:31 "qe_slab.py starts every Ti adsorbate ~3.2 A out"; docs/59:137 "`qe_slab.py` starts every Ti adsorbate ~3.1-3.2 A off the nearest Ti"; docs/59:75 records TiO2 had "only bulk hp.x work"), but NOT in the banked artifact, which is the travelling label. Being right in the docs does not make a false sentence in the artifact true.
              
              WHERE THE FINDING IS WRONG, and why I am correcting rather than accepting it verbatim:
              - "Both halves are false" is an overstatement. I re-derived the start geometries from the built decks (nearest-Ti distance to the anchor O): runs/Ti_slab/s0_O.in 3.117 A, s0_OH.in 3.167 A, s0_OOH.in 3.167 A, and the re-anchored s0_OOH_r3.in 1.782 A. So "puts every Ti adsorbate ~3.2 A off the nearest Ti" is substantively TRUE (matching docs/59's "~3.1-3.2 A"); it is defective only because its antecedent "That" points at gen_rutile.py. One half is false; the other is true-but-misattributed.
              - "it is exactly what the sentence fails to say" overreaches: the genuine provenance difference IS stated three sentences later in the same caveat ("The banked geometry comes from s0_OOH_r3, RE-ANCHORED by build_a0main_w2c.py to the mean of Ti's own two converged Ti-O bond lengths (1.781905 A)"). The defect is a false sentence sitting inside an otherwise correct disclosure, not an absent disclosure.
              - Severity: the error moves no number, no threshold and no verdict (A7.2 CONFIRMED / A7.3 NOT MET are untouched), and the true provenance is recoverable from the same caveat. "Major" is too strong; this is a minor-to-moderate factual error in a banked artifact's provenance text.
              
              It survives because the sentence, as written into docs/figs/a0main_readout.json, asserts a false property of the deciding metal's row and a false property of the other five metals' rows, in the campaign's own travelling label, on uncommitted work that this audit gates.

[6] [prereg-discipline] [MAJOR] A6.6's zero-relaxation scope breach and its ungranted licence never travel into the artifact whose verdict the unlicensed compute decides
    WHERE: src/dft/a0main_readout.py:800
    CLAIM: A6.6's zero-relaxation scope conflict is disclosed in docs/59 but its licence status and verdict-contingency never reach the artifact whose A7.3 status the unlicensed compute decides.
           
           src/dft/a0main_readout.py:800 builds the seven caveats banked into docs/figs/a0main_readout.json. Two comparable scope breaches are carried into that file with their registrations quoted: caveats.mn_afm quotes A7.5's unmet AFM condition verbatim, and caveats.coverage_shortfall carries the undated A6.3 roster allocation. A6.6 does not appear at all -- "A6.6" occurs 0 times in the artifact, "licen" 0 times, "zero relaxation" 0 times.
           
           Narrowing the original claim: the artifact is not silent about the Ti relaxations themselves. caveats.fixed_geometry states that Ti's "slab, *O, *OH AND *OOH geometries were all built and relaxed inside tranche 3 (2026-08-28/29, docs/59 s3/s3c)", and caveats.coverage_shortfall states that docs/59 is "drafted for the entrant to countersign and deposit" and quotes A7.7's WITHDRAWN-UNSCORED disposition rule. What does not travel is the three load-bearing facts recorded at docs/59:174-195: (i) that those seven relaxations are "new compute outside A6.6's declared footprint" -- A6.6 at docs/43:1285 registers block 6A as "~160 fixed-geometry SCFs and zero relaxations" and at :1288-1289 states it "does not license ... any relaxation in any cell", and A7.4 gate (g) licenses exactly one TiO2 relaxation in the 2x1v cell, not these; (ii) that "the licence is the entrant's to grant or withhold when countersigning this document", which has not happened -- docs/59:3 is still "Status: DRAFT for the entrant"; and (iii) that if withheld, "A7.3's denominator shrinks and the Ti rows are WITHDRAWN-UNSCORED under A7.7".
           
           The contingency is not hypothetical and its size is fixed by the scorer's own branch order. a0main_readout.py:741-744 appends any metal lacking a U=0 or U=U_max row to a73_pending; :755-756 routes any non-empty a73_pending to "NOT YET MET -- UNDECIDED (pending metals could still reach it)" before the "NOT MET" branch at :758 can be reached. So withdrawing the Ti rows changes a7_3.denominator from 6 to 5, moves Ti into a7_3.pending_no_endpoints, and changes a7_3.status from "NOT MET" to "NOT YET MET -- UNDECIDED".
           
           Failure scenario: the artifact is consumed as the standalone record while docs/59 is still undeposited. A downstream figure or report script reads a7_3.status = "NOT MET", denominator = 6, and per_metal.Ti.span_over_2_V = 0.0438 with no field marking the verdict as licence-contingent. If the entrant withholds the licence at countersignature, two fields of the banked JSON become wrong at once and nothing in the file flags that they were ever provisional. Remedy: add a caveats.a66_relaxation_licence key on the model of mn_afm -- quoting docs/43:1285 and :1288-1289, stating the seven 1x1 relaxations, the ungranted licence, and the registered denominator/status consequence -- and mirror it as a per-row flag on metals.Ti and in a7_3.note so the contingency travels with the deciding row, not only with the document that awaits signature.
    VERIFIER: Every mechanical assertion verifies. docs/figs/a0main_readout.json contains "A6.6" 0 times, "licen" 0 times, "zero relaxation" 0 times; its caveat keys are exactly the seven listed; src/dft/a0main_readout.py:800 is `caveats = dict(`. docs/43:1283-1289 registers block 6A as "~160 fixed-geometry SCFs and zero relaxations" and says it "does not license ... any relaxation in any cell". docs/59:174-195 concedes seven Ti relaxations as "new compute outside A6.6's declared footprint", states "the licence is the entrant's to grant or withhold when countersigning this document", and registers the consequence if withheld ("A7.3's denominator shrinks and the Ti rows are WITHDRAWN-UNSCORED under A7.7"); docs/59:3 is still "Status: DRAFT for the entrant". The asymmetry the finding points at is real: caveats.mn_afm quotes A7.5 verbatim and caveats.coverage_shortfall carries the undated A6.3 allocation, while A6.6 -- the one attached to the metal that decides A7.3 -- carries nothing.
              
              My strongest refutation attempt was to show the claimed field-flip is impossible, i.e. that withdrawing Ti leaves "NOT MET" intact because 3 of 5 measured metals can never reach the registered >=4. It fails against the scorer's actual code. At a0main_readout.py:741-744 a metal with no U=0 / U=U_max row is appended to a73_pending, and at :755-756 a non-empty a73_pending routes to "NOT YET MET -- UNDECIDED (pending metals could still reach it)" ahead of the "NOT MET" branch at :758. So withdrawing the Ti rows produces exactly the two-field change the finding describes: denominator 6 -> 5 and status "NOT MET" -> "NOT YET MET -- UNDECIDED". The failure scenario is mechanically correct, not speculative.
              
              The second refutation attempt -- that it is already disclosed inside the artifact -- succeeds only in part, which is why the claim needs narrowing rather than dropping. caveats.fixed_geometry does say Ti's "slab, *O, *OH AND *OOH geometries were all built and relaxed inside tranche 3 (2026-08-28/29, docs/59 s3/s3c)", and caveats.coverage_shortfall does say docs/59 is "drafted for the entrant to countersign and deposit" and quotes A7.7's WITHDRAWN-UNSCORED disposition rule. So the existence of the Ti relaxations, a pointer to the exact section that discusses them, and the undeposited status of that document all do travel. The finding's "None of that reaches docs/figs/a0main_readout.json" is therefore too strong. What genuinely does not travel is the three load-bearing pieces: that those seven relaxations fall outside A6.6's registered footprint, that the licence for them is not yet granted, and that A7.3's status and denominator are consequently contingent on a countersignature that has not happened.
              
              Third attempt -- that the relaxations are not really a breach because A7.2/A7.3's six-metal census warrants them -- fails on the repo's own record: docs/59:190-193 explicitly calls that "an explanation of why they were run, not a licence", and A7.4 gate (g) licenses one TiO2 relaxation in the 2x1v cell for timing, not these seven in the 1x1.
              
              Fourth attempt -- that this is a pre-existing condition, not part of the change under review -- is true of the code (git diff shows the committed fixed_geometry text also lacked any A6.6 note) but not exculpatory: this is the first artifact in which a Ti row is scored, so it is the first artifact whose verdict is licence-contingent. The omission only becomes load-bearing with this uncommitted change.
              
              Not taste, not a wish: the campaign's standing rule is that labels travel into every artifact, and this artifact demonstrably inlines two comparable registrations verbatim while omitting the one attached to the deciding row.

[7] [prereg-discipline] [MAJOR] The committed tranche-2c registration docstring was corrected with a botched string edit and now attributes the u530 branch's non-convergence to the sibling that converged
    WHERE: src/dft/build_a0main_w2c.py:18
    CLAIM: The tranche-2c registration docstring is corrupted by a botched string edit and now attributes the u530-seeded rung-(i) failure to the sibling that converged. src/dft/build_a0main_w2c.py:17-19 at HEAD (bb17152) reads: "Rung (i) ran as array 20204306 task 2: seeded from dens/s0_O__u530.save, it / O. Its sibling, u300 seeded from u150, converged in 202 s andscillated between ~1e-4 and ~8e-2 Ry for most of its 200 iterations and only settled to ~1.2-4e-5 Ry over the final ~17, never reaching conv_thr 1e-6, with totmag pinned at 21.98 throughout / landed at totmag 22.90." The rung-(i) clause is amputated to "it / O." with no predicate, and the oscillation / never-reached-conv_thr / totmag-21.98 description is syntactically attached to u300, which the same sentence also says converged in 202 s at totmag 22.90. Introduced by commit bae6cd2 applying wave-4 trap 16 (docs/45-error-ledger.md:2337-2342); `git diff 903f15f bae6cd2 -- src/dft/build_a0main_w2c.py` shows the two-line replacement collapsing into the malformed "converged in 202 s andscillated". Raw evidence inverts it: runs/a0/main/Fe/s0_O__u300__r1.out has zero "convergence NOT achieved", ends "convergence has been achieved in 20 iterations" at final totmag 22.90 (WALL 3m19.29s); runs/a0/main/Fe/s0_O__u450__r1.out has "convergence NOT achieved" at final totmag 21.98. This matters because docs/59-a0-roster-correction-2026-08-28.md:110-112 designates this file as the registration of record ("the response to each is registered in `src/dft/build_a0main_w2c.py` -- committed and pushed before either job was submitted, which is again the property that matters"), so the document the campaign cites for pre-registration authority now states the opposite of what happened. SCOPE CORRECTION to the original finding: this is NOT the only place in the committed tree where the rung-(i) failure mode is stated, and the escalation narrative is NOT rendered unverifiable. HDR_FE at src/dft/build_a0main_w2c.py:451-455 carries the correct wording in the same file (and is the text written into the deck headers), runs/a0/main/manifest.json:207-231 independently records fe_u450_r1_failed=true, fe_u450_r1_totmag=21.98, fe_u300_r1_totmag=22.9 and the r2<-u530 / r2b<-u300__r1 seeding, and docs/59:114-119 has the correct prose. No number, deck, verdict, or banked result is affected. Severity is therefore minor-to-moderate, not major: a documentation-integrity defect inside the registration of record, whose fix is restoring the two-sentence split ("...seeded from dens/s0_O__u530.save, it oscillated between ~1e-4 and ~8e-2 Ry ... with totmag pinned at 21.98 throughout. Its sibling, u300 seeded from u150, converged in 202 s and landed at totmag 22.90.") in a follow-up commit, ideally with a new ledger trap for "verify the replacement text after applying a correction."
    VERIFIER: Could not refute. Verified at every step. (1) The corruption exists verbatim at src/dft/build_a0main_w2c.py:17-19 at HEAD bb17152, and `git diff 903f15f bae6cd2` shows it was introduced by the wave-4 trap-16 correction collapsing two lines into one malformed line containing "converged in 202 s andscillated". Working tree matches HEAD (the file is not in git status). (2) The attribution is genuinely inverted against raw evidence: runs/a0/main/Fe/s0_O__u300__r1.out has zero "convergence NOT achieved", ends "convergence has been achieved in 20 iterations", final totmag 22.90, WALL 3m19.29s; runs/a0/main/Fe/s0_O__u450__r1.out (the u530-seeded rung (i)) has one "convergence NOT achieved" and final totmag 21.98, and its estimated-scf-accuracy trace does oscillate in the 1e-3 range before settling to ~1.2-4e-5 in the tail -- i.e. the corrected description is factually right but was attached to the wrong run. (3) Not already disclosed: docs/45-error-ledger.md:2337-2342 (trap 16) discloses the ORIGINAL plateau misstatement and its correction, not that the fix was botched. (4) The file's authority is asserted: docs/59:110-112 names src/dft/build_a0main_w2c.py as the registration of record, "committed and pushed before either job was submitted, which is again the property that matters." (5) Independent records contradict the corrupted sentence: runs/a0/main/manifest.json:207-231 records fe_u450_r1_failed true, fe_u450_r1_totmag 21.98, fe_u300_r1_totmag 22.9, decks r2 <- s0_O__u530 and r2b <- s0_O__u300__r1. Not style, not taste, not a wish. WHERE THE FINDING IS MIS-STATED: its failure scenario claims the corrupted sentence is "the only place in the committed tree where the rung-(i) failure mode is stated." That is false and is contradicted by the finding's own evidence line -- HDR_FE at build_a0main_w2c.py:451-455 states it correctly in the same file (and that header is what is written into the deck files), manifest.json encodes the facts correctly, and docs/59:114-119 has the correct prose. Three independent correct records survive, so no number, deck, verdict, or banked result is affected; severity is over-claimed at "major" and is better recorded as minor/moderate. Also noted but not defect-worthy: "202 s" is a rounding of the measured 199.29 s WALL / 194.32 s CPU and predates the botched edit.

==============================================================================
COMPLETENESS CRITIC (7)
==============================================================================

[C1] [MAJOR] A5.1(b)'s registered 0.20 eV floor is scored on eta margins, not the G_max gaps it is defined on, and leg 1 was never evaluated at all
    WHERE: src/dft/a0main_readout.py:647
    CLAIM: `DISTINGUISH_FLOOR = 0.20   # A5.1(b), registered (Exner 2020)` (:647) is applied at :670-671 to `margins[u] = eta(Ir) - eta(Ru)`, an eta_TD difference, and banked as `ordering.distinguishability_floor_eV` / `holds_below_distinguishability_floor` (:686-687). A5.1(b) does not define that floor on eta. docs/43:961-963 registers three legs, verbatim: "1. the ordering is the same under eta_TD and under G_max(eta = 0.3 V); 2. the G_max gap between the pair is >= 0.20 eV (Exner 2020's stated sensitivity floor, p. 12611)". The 0.20 eV floor is leg 2 and it is a G_max gap. Leg 1 -- agreement between the two metrics -- is not computed anywhere in the readout, in any banked artifact, or in docs/58, even though A6.4 at docs/43:1264 states "A5.1(b)'s ranking-claim rule continues to bind unchanged ... It relaxes none of the three legs", and the campaign's own `g_max()` already exists at src/dft/volcano_r1.py:37 with the exact convention A5.1(b) names (`g_max(steps, OER_EQUILIBRIUM_V + 0.3)`, volcano_r1.py:63). A5.1(c) -- "G_max across the A0 grid ... a pure function of the same four dG values, zero new DFT" (docs/43:972-976) -- is the readout that would supply it, and it is still on the unscored list at docs/58:171.
    FAILURE: A report writer follows docs/58 §7 line 162 and writes the MAY-NOT verbatim: "claim Ir < Ru holds anywhere (below the registered floor everywhere)". Computing the registered quantity from the banked dG rows with the campaign's own g_max at eta = 0.3 V, the G_max gap (Ir minus Ru) is +0.106 / +0.141 / +0.194 / +0.260 / +0.337 / +0.424 / +0.465 eV at U = 0 / 1.5 / 3.0 / 4.5 / 6.0 / 7.5 / 9.0. Four of the seven clear the registered 0.20 eV floor, so "below the registered floor everywhere" is false on the quantity the floor is registered on -- the pair IS resolved at U >= 4.5, in the direction Ir worse. Worse for leg 1: G_max puts Ir above Ru at ALL seven U, including production U = 0, so at the three points the readout calls "holds" (U = 0, 1.5, 3.0, where eta says Ir < Ru by 0.006 / 0.027 / 0.031 V) the two registered metrics disagree in SIGN. Under A5.1(b) that makes the pair "not distinguishable at this protocol's resolution" for a reason no artifact states, and it means the strength of the A6.3 INVERTED verdict (docs/58:97-99, "carried outright by U = 9.0 alone") was assessed only on the metric A5.1(b) treats as insufficient on its own.
    EVIDENCE: Registration: docs/43-prereg-week1-factorial.md:959-965 (legs 1-3), :972-976 (A5.1(c)), :1264 (A6.4, "relaxes none of the three legs"). Code: src/dft/a0main_readout.py:647, :669-678, :686-687. Implementation available: src/dft/volcano_r1.py:37 `def g_max(steps, U)`, used at :63 as `g_max(steps, OER_EQUILIBRIUM_V + 0.3)`. Recomputed by me from docs/figs/a0main_readout.json metals.Ru/Ir rows through hea_oer.descriptors.oer_steps + that same g_max; spot-checked by hand at U = 0 (Ir steps -0.0005/1.6416/2.0107/1.2682 -> G_max 0.592; Ru 0.5291/1.1633/2.0168/1.2108 -> G_max 0.487; gap +0.106). Zero hits for `g_max|G_max|gmax` in src/dft/a0main_readout.py and in docs/figs/a0main_readout.json.

[C2] [MAJOR] A7.3's 3-over / 3-under split is exactly the nspin=2 / nspin=1 partition, and the deciding metal is 15.5 meV from flipping the verdict -- inside a measured error class whose re-run is owed
    WHERE: src/dft/a0main_readout.py:786
    CLAIM: `result["a7_3"]` (:786-797) banks status, per_metal, denominator and a note about Cr's interval, and carries no conditionality field of any kind -- no spin-treatment flag, and no application of the ERROR_CLASSES margin test the same script implements 140 lines earlier for A6.3 (:642-668). The three metals over the 0.10 V floor (Cr 0.3435, Mn 0.6307, Fe 0.6102) are exactly the three whose A0 decks carry `nspin = 2`; the three under it (Ru 0.0922, Ir 0.0637, Ti 0.0438) are exactly the three whose decks carry no nspin key and run nonmagnetic. The verdict is therefore perfectly confounded with the spin treatment, and nothing says so: `caveats.spin_state` (:838-857) enumerates which quantities are spin-conditional -- "the U <= 4.5 ordering rows and the Ir flip bracket's low edge", plus every Ti row -- and never mentions A7.3, while `caveats.mn_afm` (:858-871) names "A7.3's span" only to license the FM-initialised Mn column. The registered threshold is >= 4 of 6, so ONE metal moving flips NOT MET to CONFIRMED, and the nearest is Ru: |c_M(0) - c_M(9)| = 0.1845 eV against the 0.20 eV needed, a shortfall of 15.5 meV.
    FAILURE: S0 gate (h) measured 4/4 ADOPT_AFM on the RuO2 anchors with 0.033-0.064 eV of adsorption-energy movement and the AFM re-run is still owed (the readout's own ERROR_CLASSES entry, :644, is labelled "re-run owed"); docs/59 s3c leaves nspin=2 for the Ti arm OPEN for the entrant. When either lands, Ru needs only a 15.5 meV differential change in c_M = dG_OOH - dG_OH between U = 0 and U = 9 -- two to four times SMALLER than the class already measured on those very anchors -- to cross the floor and turn A7.3 from NOT MET (3 of 6) into CONFIRMED (4 of 6). A downstream consumer reading a7_3.status = "NOT MET" with denominator 6 and no conditionality field records a failed registered prediction as settled, when it is one owed re-run inside an open error class away from passing, and when the entire over/under split tracks the spin convention rather than the chemistry.
    EVIDENCE: Deck check: `grep -i nspin runs/a0/main/<M>/s0_OOH__u900.in` returns `nspin = 2` for Cr, Mn, Fe and nothing for Ru, Ir, Ti. Distances to the floor computed from docs/figs/a0main_readout.json a7_3.per_metal: Ru needs +15.5 meV, Ir +72.6 meV, Ti +112.5 meV of additional |dc_M|. Error class: src/dft/a0main_readout.py:644 `("NM-vs-AFM adsorption sensitivity, gate (h) (re-run owed)", 0.033, 0.064)`. Registration: docs/43:1370-1373 (>= 4 of 6). The a7_3 dict at :786-797 has no field naming spin, error class, or conditionality; `ordering` at :679-692 has three.

[C3] [MAJOR] docs/58, the A0 analysis of record titled "A0 closed", still reports the passing sibling and files the failing one as deferred future work
    WHERE: docs/58-a0-analysis-2026-08-28.md:169
    CLAIM: docs/58 is unmodified in this change set (it is not in `git status`), and it is the only human-readable record of A0 -- docs/59 is still "Status: DRAFT for the entrant" and, at :239-244, reports the pre-landing 3-of-5 state. After this landing docs/58 contradicts the artifact it declares itself a quotation of ("every number below is quoted from a committed JSON", :3-5) in four places: :115-117 reports "A7.2: CONFIRMED ... Cr, Ir, Ru" and "The unrun blind metals (Mn, Fe, Ti) can only add flips" when the banked census is now Cr/Fe/Ir/Mn/Ru with Ti flat and closed; :134 says "Mn/Fe/Ti were never built"; :162 says the ordering is "below the registered floor everywhere"; and :169-171 lists "A7.3 span(c_M)/2" among "Deferred readouts of this same grid (zero new DFT)" that are still owed. The net effect is that the repo's prose record reports A7.2 CONFIRMED and is silent on its sibling A7.3 NOT MET -- the exact selectivity the wave-4 audit named as "the exact failure pre-registration exists to prevent" (docs/45:2303-2310) and fixed inside the script. The fix was applied one layer down and not one layer up. tasks/todo.md:728 pre-committed the sequence for this landing: "re-score ... Then docs/58 update + adversarial verify + bank".
    FAILURE: The change set is committed as-is. Anyone reading the repo for A0's outcome -- the report writer at freeze, an external auditor following docs/45's "Addressed in: S4 (A0)" pointer, or the entrant assembling the six-row body ledger -- opens docs/58 ("A0 closed: the eta(U) grid, end to end"), finds A7.2 CONFIRMED presented as the A7 result, finds A7.3 filed under "Open items" as not yet done, and never learns that the registered prediction was scored and failed at 3 of 6 with Ti deciding. The failure exists only in a machine artifact nobody is directed to read for verdicts.
    EVIDENCE: docs/58-a0-analysis-2026-08-28.md:115-117, :134, :162, :169-171 (verified verbatim). `git status --short` lists only .github/ci/run_oc20.py, .github/workflows/s1-controls.yml, docs/figs/a0main_readout.json, src/dft/a0main_readout.py plus untracked run outputs -- docs/58, docs/59, docs/45 and tasks/todo.md are all untouched. Banked contradiction: docs/figs/a0main_readout.json a7_2.metals_with_flip = [Cr, Fe, Ir, Mn, Ru], a7_2.pending_data = [], a7_3.status = "NOT MET", a7_3.denominator = 6. docs/59-a0-roster-correction-2026-08-28.md:3 ("Status: DRAFT"), :239-244 (the pre-landing 3-of-5 snapshot). tasks/todo.md:728.

[C4] [MINOR] A5.1(d)'s intercept test is filed as unscored future work although its number is already banked inside a7_3.per_metal, and read across metals it contradicts the registered hypothesis
    WHERE: tasks/todo.md:730
    CLAIM: tasks/todo.md:730 and docs/58:170-171 both list "A5.1(d) intercept test" as a registered zero-DFT readout still to be done. It is not pending: A5.1(d) asks (docs/43:978-979) "does the 3.2 eV scaling intercept stay U-robust while the descriptor axis is U-fragile?", and the scaling intercept IS c_M = dG_OOH - dG_OH, the very quantity a7_3.per_metal banks at U = 0 and U = 9 for all six metals in this change set. Read down the column instead of across it, the answer is already determined and it goes against the registered motivating prior: the cross-metal mean intercept moves 3.478 -> 2.883 eV (-0.595 eV) and its cross-metal spread nearly triples (sd 0.154 -> 0.465 eV, range 0.472 -> 1.227 eV), while the descriptor axis D = dG_O - dG_OH moves +0.246 to +2.027 eV per metal. Per-metal the intercept moves Cr -0.687, Mn -1.261, Fe -1.220, Ru -0.184, Ir -0.127, Ti -0.088 eV -- i.e. A7.3's span is literally the intercept test's per-metal half, doubled and unsigned. The same is true of A5.1(a): the four Ti *OOH `.lowdin.txt` extracts landed in this change set complete the Loewdin coverage, and no script in the repo reads a `.lowdin.txt` at all (zero hits for `lowdin` across src/, .github/ except the builder that writes the projwfc decks), while for Ru/Ir/Ti the nspin=1 decks make the moment-based primary tracker structurally unavailable, so Loewdin is the only valence tracker those three can ever have -- and those three are exactly A7.3's under-the-floor set.
    FAILURE: The intercept test is left on an owed-work list until after the Oct 15 freeze and is then marked WITHDRAWN-UNSCORED under A7.7 (docs/43:1437-1438), even though its answer was fully determined by the numbers committed in this change set. Worse, an unscored A5.1(d) is scored later, after A7.3's failure is known -- and it is the same quantity, so it can be framed to explain the A7.3 result rather than tested against it, which is precisely the post-hoc freedom the registration's blind-metal list exists to remove.
    EVIDENCE: docs/43:978-981 (A5.1(d), with the Tripkovic prior "dE(*OOH)-dE(*OH) moves 2.94->2.93 eV over U = 0-5 while dE(*O)-dE(*OH) moves +1.06 eV"); docs/43:948-953 (A5.1(a), Loewdin supplement). Filed as pending at tasks/todo.md:730 and docs/58:170-171. Numbers recomputed by me from docs/figs/a0main_readout.json metals.*.rows only, zero new DFT. `grep -rn lowdin src/ .github/` returns one hit, src/dft/build_a0main_w2.py:87, which writes the extracts; nothing reads them.

[C5] [MINOR] The claim ledger's status columns still record P-FLOOR-U as owed and the projector's eta consequence as BLIND / NOT MEASURED, both contradicted by committed artifacts
    WHERE: docs/45-error-ledger.md:30
    CLAIM: docs/45 is the campaign's ledger of record and is untouched by this change set. Row 6 (:30) still reads "MEASURED (4-pt ladder; dense grid owed)" with "Governed by: P-FLOOR-U (A7, owed)", after the dense grid closed at 6 metals and P-FLOOR-U was scored NOT MET. Its "Size (measured)" cell still quotes "0.447 eV intercept span" from the 4-point ladder, while the dense grid now measures Cr's intercept span at 0.687 eV over U = 0 -> 9. The same sweep is missing elsewhere: row 3 (:22) lists "P-PLS (A7, owed)" though A7.2 is CONFIRMED and banked; row 4 (:23) lists the projector row as "eta consequence BLIND | NOT MEASURED (fires this week)" though docs/figs/pproj_readout.json banks verdict FIRES at abs_d_eta_V = 0.4869 and the A0 artifact uses that measurement to stamp PROJECTOR-MISMATCHED on every Xu-anchor row and to exclude them from the A6.3 test (src/dft/a0main_readout.py:450-455); and the S4 stage row (:78) still reads "clear after S0(e)/P-PROJ" for a stage that has been executing for a week.
    FAILURE: A reviewer or the entrant checks the status of the Hubbard-U fragility error class in the ledger -- the document whose whole job is to hold current status -- and reads that the dense grid is owed and that P-FLOOR-U has not been scored, so the failed prediction is invisible from the one place designed to make failures visible. Reading row 4 they conclude the projector's eta consequence is unmeasured, and may then quote a Xu-anchor number without the PROJECTOR-MISMATCHED label the artifact requires, because the ledger says there is no measurement to condition on.
    EVIDENCE: docs/45-error-ledger.md:22, :23, :30, :78 (verified verbatim). Contradicting artifacts: docs/figs/a0main_readout.json a7_3.status = "NOT MET" / a7_2.status = "CONFIRMED"; docs/figs/pproj_readout.json {"abs_d_eta_V": 0.48685617462435893, "verdict": "FIRES"}; src/dft/a0main_readout.py:450-455. Cr dense-grid intercept span recomputed from the banked rows: c_M(0) = 3.4437, c_M(9) = 2.7567, span 0.687 eV.

[C6] [MINOR] A7.3 landed in a middle zone the registration does not define, and the artifact banks a disposition token A7.7's vocabulary does not contain
    WHERE: src/dft/a0main_readout.py:758
    CLAIM: docs/43:1370-1373 defines exactly two outcomes for P-FLOOR-U and a consequence for only one of them: CONFIRMED at >= 4 of 6, and "FALSIFIED if <= 1 of 6 exceeds 0.10 V, in which case U does not move the physical limit and the floor may serve as a U-invariant denominator -- a change of framing registered here, before the fact." The measured result, 3 of 6, is in neither band. The scorer invents the token at :758 (`a73_status = "NOT MET"`) and banks it as a7_3.status, and A7.7's registered disposition vocabulary (docs/43:1437-1439: WITHDRAWN-UNSCORED "shown alongside HELD and TRIGGERED", reused verbatim for the appendix ledger at docs/43:1930 and docs/50:138) contains no "NOT MET" and no mapping for it. Nothing in the repo records what a 2-or-3-of-6 outcome licenses or forbids: docs/58 §7's MAY / MAY-NOT list (:158-165) predates the score and says nothing about the floor's framing role, and the a7_3 note field (:795-797) covers only Cr's interval.
    FAILURE: At the Oct 15 freeze the six-row body-figure ledger is assembled (docs/43:1930 names P-FLOOR-U as one of the six). Its row must carry HELD, TRIGGERED or WITHDRAWN. At 3 of 6 the registration decides none of them, so whoever fills the cell chooses the token after the number is known -- and the two available readings are materially different claims: HELD-adjacent ("U moves the limit on half the roster") versus TRIGGERED-adjacent ("the >= 4 threshold failed"). Separately, the one framing consequence the registration does attach to a low outcome -- the floor serving as a U-invariant denominator -- is registered only for FALSIFIED, so at 3 of 6 the report has a measured result with no registered downstream instruction and nothing written to stop the instruction being written later to fit it.
    EVIDENCE: docs/43-prereg-week1-factorial.md:1368-1373 (the two bands and the single registered consequence), :1437-1440 (A7.7 P-DISPOSITION vocabulary), :1930 (the six-row ledger naming P-FLOOR-U). src/dft/a0main_readout.py:752-758 (the four-branch status ladder; "NOT MET" at :758), :791 (banked as status). docs/figs/a0main_readout.json a7_3.status = "NOT MET", a7_3.note covers only the Cr interval.

[C7] [MINOR] Two of the three metals carrying A7.3 over the floor have their U = 9 endpoint on a pls-1 row with a negative dG3, a pathology the scorer flags per-row for A7.2's benefit and never carries into a7_3
    WHERE: src/dft/a0main_readout.py:471
    CLAIM: src/dft/a0main_readout.py:471-478 writes "[no closed-form check: defined only for pls 2/3]" onto pls-1/4 rows with the comment that "those are exactly the rows where the dG ladder has gone non-monotonic -- including the upper endpoint of Fe's contribution to the A7.2 census." The author noticed the pathology and narrated it for A7.2. The same rows are A7.3 endpoints and a7_3 says nothing: Mn's U = 9 row is pls 1 with dG3 = dG_OOH - dG_O = -0.1253 eV, Fe's U = 9 row is pls 1 with dG3 = -0.1994 eV, both with closed_form_dev = null. Those two rows set c_M(9) for Mn (2.3558) and Fe (2.2977), which is what produces spans of 0.6307 and 0.6102 V -- two of the three values carrying A7.3's numerator. a7_3.per_metal (:748-750) banks u_lo, u_hi, c_M_lo, c_M_hi, span and exceeds_floor, and no pls or identity-check field, so the artifact gives a consumer no way to see that 2 of 3 supporting metals rest on a row where the registered closed-form identity does not run and the free-energy ladder inverts.
    FAILURE: A consumer reads a7_3.exceeds = [Cr, Fe, Mn] and reports that three rutiles show a U-driven movement of the physical limit above 0.10 V, on rows the same script privately classifies as ladder-inverted and identity-unverifiable. If either Mn or Fe were later re-scored on a corrected upper endpoint -- Mn's AFM arm is a registered unmet condition (A7.5, caveats.mn_afm) and Fe's *OOH column sits on a pilot-selected magnetic branch with a measured +276.60 meV alternative -- the numerator could drop to 1 or 2 of 6, moving A7.3 toward the FALSIFIED band (<= 1 of 6), which is the only branch of the registration that carries a registered change of framing.
    EVIDENCE: src/dft/a0main_readout.py:471-478 (the per-row disclosure and its comment), :746-750 (a7_3 per-metal fields). Recomputed from docs/figs/a0main_readout.json: Mn u = 9.0 pls 1, dG_OH 2.7496 / dG_O 5.2306 / dG_OOH 5.1053 -> dG3 = -0.1253, closed_form_dev null; Fe u = 9.0 pls 1, dG_OH 2.5788 / dG_O 5.0759 / dG_OOH 4.8765 -> dG3 = -0.1994, closed_form_dev null. a7_3.per_metal.Mn.c_M_hi = 2.3558, .Fe.c_M_hi = 2.2977.

==============================================================================
REFUTED (26) -- kept WITH the refutation reasoning
==============================================================================

[R1] [a73-scoring] spin_state caveat states a criterion for spin-conditionality, then omits the Ru A7.3 row that meets it — the row whose crossing flips NOT MET to CONFIRMED
    WHY REFUTED: REFUTED. The arithmetic in the finding checks out (Ru span/2 = 0.09224816 V against floor_V 0.10, i.e. 15.5 meV of span short; Ir is 72.6 meV short and does clear the 64 meV class top, so Ru is indeed the only row inside it). But the defect argument fails on four independent points, and two of its stated evidence items are factually inverted.
                 
                 1. The mn_afm evidence says the OPPOSITE of what the finding claims. The finding's whole "inconsistent within one dict literal" leg rests on: "mn_afm explicitly names 'A7.3's span' as a claim its FM-initialisation conditions." It does not. src/dft/a0main_readout.py:865-868 reads: the FM-initialised Mn column "may be used for the within-metal, U-response claims it was registered for (A7.2's flip census, A7.3's span) but NOT as a materials-facing absolute eta for beta-MnO2." mn_afm names A7.3's span as a PERMITTED use that survives an unresolved magnetic-state question — an exemption, not a conditioning. And that exemption is not a local convention: it is the readout applying registered A7.5 (docs/43:1396-1406), "the measured quantity of every arm is a difference between two treatments of the SAME slab; a difference is a valid method measurement whether or not the slab is a synthesisable electrode." Under that registered rule an unresolved magnetic initialisation strikes materials-facing absolute claims and leaves within-metal U-response differences standing. Omitting Ru's A7.3 span from spin_state's list applies to Ru exactly the rule the same dict literal applies to Mn. The convention the finding invokes exists — and it cuts against the finding.
                 
                 2. spin_state does not close the disclosure around Ru's A7.3 row; its first sentence is a blanket conditioning of the entire Ru column: "the Ru/Ir columns are nspin=1 nonmagnetic by construction, while gate (h) measured 4/4 ADOPT_AFM on the RuO2 anchors with 0.033-0.064 eV adsorption-energy movement (AFM re-run owed, S0(h))" (:836-838). Every Ru number in the readout, the A7.3 row included, is disclosed as sitting on an NM column with an AFM re-run owed. The following "margins smaller than that class" sentence adds where the class exceeds a specific comparison margin; it does not retract the blanket sentence. The failure scenario needs a reader who takes the appositive list as exhaustive while ignoring the sentence immediately before it.
                 
                 3. The scoping in that list is principled, not an oversight. Both listed items — the U<=4.5 ordering rows (Ir vs Ru at fixed U) and the Ir flip bracket's low edge (two step energies at fixed U) — are FIXED-U differences, in which gate (h)'s level shift does not cancel. A7.3's registered quantity (docs/43:1368-1371) is span(c_M)/2 = a difference ACROSS U of a fixed-U difference, and a U-independent level shift cancels identically in it. Gate (h) measured a level shift at one U (docs/54:331-333, on NM-relaxed RuO2 geometries); it measured no U-differential. The finding concedes this ("The differential ... has never been measured"), which means its 16 meV crossing scenario requires a number that no banked class contains. The readout's own rule at :639-642 is explicit: "no new thresholds invented here -- the classes are prior banked measurements." Attaching a hypothetical U-differential to the span would be inventing precisely such a threshold, i.e. the violation the finding is nominally defending against.
                 
                 4. Two descriptive claims about the a7_3 block are wrong. It is not true that it "carries no conditionality, error-class or label field at all": the banked block carries blind: ["Mn","Fe","Ru","Ir","Ti"], disclosed_non_blind: ["Cr"], and a note separating the registration's Cr 0.223 V (U = 0 -> 5.00) from this grid's endpoints. It carries no error_classes field, which is the narrower and accurate statement. Separately, the caveats dict is generated at :810 under the comment "registered + measured caveats (travel with every table above)" — it is not scoped to the ordering block.
                 
                 5. The "no warning" step also fails. The artifact banks floor_V: 0.1 and Ru.span_over_2_V: 0.09224816 in the same object, and docs/59-a0-roster-correction-2026-08-28.md:241 prints them side by side ("Ru 0.0922 and Ir 0.0637 under it") in the section that says A7.3 is "genuinely at stake." The proximity is on the face of two banked fields and in the dated correction of record.
                 
                 Finally, the remedy implied by the finding runs against the standing rules rather than with them. A7.3 was scored on the registered nspin=1 Ru column that the registration itself names blind over this grid; a future S0(h) AFM column is a different registered treatment of the slab whose span would be a separate measurement, reported as a discrepancy and never averaged with this one. Reporting the registered prediction as NOT MET on the banked column is what the discipline requires; the only escape route the finding surfaces is one that would convert a failed prediction into a confirmed one. What remains after all this is a wish that the a7_3 block carry a margin/error-class context field like the sibling ordering block — an improvement, not a defect, and not a missing disclosure.

[R2] [a73-scoring] ir_ooh_basin caveat's closed scope list omits A7.3 — the one banked quantity that is built entirely on the convicted saddle geometry at both endpoints
    WHY REFUTED: Every factual pointer checks out — recomputed from raw runs/a0/main/Ir/s0_{OH,OOH}__u{000,900}.out, span = 0.12742139780 eV and span/2 = 0.063710699 V, matching a0main_readout.json a7_3.per_metal.Ir to 9 digits; Ir is pls 3 at U<=3 and pls 2 at U>=4.5 with flip bracket [3.0, 4.5]; and c_M = dG_OOH - dG_OH (a0main_readout.py:745-747) does carry the convicted Ir *OOH energy at both endpoints. The finding still fails on three independent grounds.
                 
                 (1) THE TWO-ITEM LIST IS CORRECT AND COMPLETE UNDER THE MODEL THE CAVEAT DECLARES ON ITS FACE. src/dft/a0main_readout.py:875-877 says "under a rigid -0.291 eV shift". Under that stated model the correction moves exactly two things: eta on Ir's pls-3 rows (dG_OOH enters eta there) and the pls flip bracket (pls is a comparison of absolute dG steps at one fixed U, so a constant shift relocates the crossing). Both are listed. It does NOT move A6.3's inverted points (pls 2, excluded with a stated reason) and it does NOT move A7.3's span, which is a cross-U difference of the same quantity: c_M(0) - c_M(9) = [dG_OOH(0)+D - dG_OH(0)] - [dG_OOH(9)+D - dG_OH(9)], and D cancels identically. The measured correction moves Ir's span/2 by exactly 0.000 V. A7.3's absence from the list is the principled result, not an omission — this is the very distinction that puts A7.2 in the list and keeps A7.3 out. Adding A7.3 would attach a measured 0.291 eV to a number it provably cannot touch.
                 
                 (2) THE RESIDUAL THE FINDING ACTUALLY NEEDS IS ALREADY DISCLOSED, GLOBALLY. The only route to the failure scenario is a U-dependent correction, i.e. a U-driven geometry change. caveats.fixed_geometry — in the block headed "registered + measured caveats (travel with every table above)" (a0main_readout.py:866) and present verbatim in the banked docs/figs/a0main_readout.json — states: "A0 measures the U-response of energies at frozen geometry and cannot see a U-driven geometry change; where A0 and a relaxed point disagree, the relaxed point wins and the discrepancy is reported, not averaged. NOTHING in this readout is a relaxed result; no relaxed Ru/Ir point at U > 0 exists anywhere in the campaign." That closing sentence is Ru/Ir-specific and is precisely the disclosure the finding demands; every A7.3 row for every metal inherits it.
                 
                 (3) THE EVIDENCE OFFERED FOR U-DEPENDENCE IS THE WRONG AXIS. docs/45-error-ledger.md:20 row 1's "collapses to -0.018 eV at 2x1v half-coverage" is the same measurement recorded in row 7 under "Coverage / cell" ("Ir *OOH -0.285 -> -0.018 eV") — a cell/coverage variation, not a U variation. A7.3 is scored wholly inside the 1x1 cell, and caveats.cell separately registers that ("this grid lives in the 1x1 cell the campaign retired for production ... The 2x1v ordering at U > 0 is unmeasured"). So the cited "strongly configuration-dependent" fact establishes coverage-conditionality, which has its own disclosed caveat, and is silent on U-flatness.
                 
                 Minor additional inaccuracies: c_M is not "a pure dG_OOH quantity" (it is dG_OOH - dG_OH); and Ir is not the cheapest path to over=4 — Ru at 0.0922 needs only 7.8 meV in span/2 versus Ir's 36.3 meV, and Ru's *OOH geometry carries no saddle conviction, so the claimed unique pivotality of the Ir geometry is overstated. The 72.6 meV gap and the "25% of the saddle depth" plausibility are the finding's own hypothesis about an unmeasured quantity, not a defect in an existing disclosure.

[R3] [a73-scoring] A7.3 status ladder applies the denominator shrink asymmetrically: CONFIRMED can fire with a metal still pending, FALSIFIED can never fire
    WHY REFUTED: Refuted on mechanism, scenario, and direction.
                 
                 (1) The CONFIRMED test at src/dft/a0main_readout.py:751 uses no denominator. `len(over) >= A73_NEEDED` is an absolute count against the registered ">=4 of the 6" (docs/43:1370-1373). A missing metal cannot flatter an absolute count: 4 metals over the floor is 4 metals over the floor regardless of roster size, and added data can only grow `over`. There is no "denominator shrink" in the branch the finding says receives it.
                 
                 (2) The offered evidence does not produce the described outcome. I simulated the finding's own scenario (5 scored, 1 over, 1 pending): FALSIFIED is blocked by the numeric guard `len(a73_rows) - len(over) > len(METALS) - A73_FALSIFY_AT - 1` (4 > 4 is False), NOT by `and not a73_pending`. Deleting the pending clause leaves the verdict unchanged at "NOT YET MET -- UNDECIDED". The cited clause at :753 is not the cause of the behaviour attributed to it.
                 
                 (3) The alleged bias runs backwards. That numeric guard requires >=5 metals measured-and-below out of len(METALS)=6, i.e. it demands the UN-shrunk 6-metal denominator for falsification in every reachable cell but one. The code applies the registration's conditioning less on the falsification side, which is the conservative direction, not one that "keeps the prediction alive."
                 
                 (4) The registration's shrink is conditioned on a determination the scorer cannot make. src/dft/build_a0main_w2c.py:126-129 reads "If neither converges, A6.5(2)(iii) stands: Ti s0_OOH is NOT_CONVERGED, and A7.3's own registered text shrinks the span denominator" — a post-ladder NOT_CONVERGED ruling (docs/43:1267 (iii)), not a missing row. `a73_pending` is populated by absent endpoints for any reason (queued, unbuilt, gap). This commit's own Ti history is the empirical refutation: the *OOH relax failed twice, then re-anchoring to 1.781905 A converged in 52 steps at d(O,Ti) = 2.041 A. Treating "no endpoints yet" as "permanently outside the conditioned set" and firing FALSIFIED would have banked a falsification the very next job overturned.
                 
                 (5) The monotone-early-call rationale is already disclosed in the same file one prediction above: A7.2 confirms at `len(metals_with_flip) >= 3` (:706) with no pending guard and prints "additional metals or grid points can only add flips, never remove one" (:711-713); docs/59 s4 states the same monotonicity. A >= threshold is monotone-safe under added data and a <= threshold is not — the asymmetry is correct arithmetic, not directional bias.
                 
                 (6) The "contradicting rationale" at :768-771 prints nothing on the banked data (docs/figs/a0main_readout.json: pending_no_endpoints [], denominator 6, status "NOT MET") and describes the printed reporting denominator at :772, making no claim about branch logic.
                 
                 Residual, reported only for completeness and not as a surviving defect: `not a73_pending` is load-bearing in exactly one cell (5 scored, 0 over, 1 pending), where the pending metal cannot change the verdict (0 or 1 over of 6 is <=1 either way), so an arithmetically forced FALSIFIED prints as UNDECIDED. That direction withholds a verdict rather than fabricating one, prints the true counts regardless, and is unreachable from the banked roster (Cr 0.3435, Fe 0.6102, Mn 0.6307 over the floor).

[R4] [a72-census] Mn's 2->1 flip bracket is banked as a measured location on a 9.5 meV pls margin — smaller than the campaign's own measured same-deck run-to-run spread, with the A7.5 AFM arm still unrun
    WHY REFUTED: I verified every number the finding offers and they are correct — then the argument built on them fails on three independent legs.
                 
                 WHAT IS TRUE (re-derived from raw, not from the scorer). runs/a0/main/Mn/{slab,s0_O,s0_OH,s0_OOH}__u600.out give -1765.80261063 / -1807.17879606 / -1808.53553768 / -1850.04542638 Ry. With src/hea_oer/referencing.py's CHE scheme (ZPE-TS 0.35/0.05/0.40), g2-g1 = dG_O - 2*dG_OH = 0.6594892 - 0.65 = 0.0094892 eV. So Mn's U=6.0 margin IS 9.489 meV, it IS the low edge of the banked [6.0, 7.5, 2, 1] bracket, a 4.745 meV rise in dG_OH WOULD flip it, and docs/46 does record an 8.29 meV same-machine SCF run-to-run basin split. All five factual assertions check out.
                 
                 LEG 1 — THE FAILURE SCENARIO IS WRONG BY AN ORDER OF MAGNITUDE. The finding's stated harm is that the deliverable, "the U at which each metal's pls flips" (docs/43:1353), goes "off by 1.5 eV". It does not. Mn's (g2-g1) runs +0.155906 / +0.009489 / -0.135918 at U = 4.5 / 6.0 / 7.5, so the crossing sits at U = 6.098 eV. Apply exactly the perturbation the finding posits (dG_OH up 5 meV, so g2-g1 down 10 meV everywhere) and the crossing moves to U = 5.995 eV — the LOCATED flip moves 0.103 eV. Only the discretised label jumps a grid step, and that 1.5 eV granularity is inherent to, and visible in, the bracket format itself. The finding conflates the grid-step label with the physical quantity and reports the label's jump as the deliverable's error.
                 
                 LEG 2 — THE ARTIFACT BANKS THE IN-CLASS MEASUREMENT THAT REFUTES THE RISK. The premise is "the campaign has MEASURED that a single deck re-run on a fixed platform can move by more than [9.489 meV]". For this deck class the campaign has measured the opposite, in the same file the finding cites: docs/figs/a0main_readout.json metals.Mn.rerun_determinism_check_meV = {slab 0.0046, s0_O 0.0065, s0_OH 0.0046, s0_OOH 0.0087} meV — and that is the harshest class the campaign runs, a cross-machine Vast-audit-deck-re-run-on-Anvil control (a0main_readout.py:100-104). 0.0087 meV is ~545x under the 4.745 meV needed. The 8.29 meV figure is not jitter: docs/46:37-38,145 show it is a discrete jump between two named basins (11.00 vs 14.90 uB), and docs/46:160-164 show that once a basin is selected it re-runs to 0.0001-0.0060 meV, including row 5 reproducing the 14.90 basin at -0.0030 meV. That class announces itself in totmag — which IS banked per row and is flat and integer for Mn across all eight U points (18.0/16.0/17.0/17.0). The finding concedes this ("no basin change is OBSERVED") and then invokes the class anyway.
                 
                 LEG 3 — "UNDISCLOSED" AND "ANOMALOUS" BOTH FAIL. (a) A tight margin at a bracket edge is structural: the edge is near a crossing by definition. Every edge in the census is tight — Ru 44.3 meV, Ir 44.6 meV, Fe 68.3/81.7 meV, Cr 76.0/98.1 meV. Mn's 9.5 meV is the smallest but differs in degree, not kind; the finding's baseline (Mn's first bracket, 322/337 meV) is the census outlier, not the norm, so "the same authority as Mn's first bracket" is a standard no bracket meets. (b) The pre-registration anticipated exactly this and called it physics, not noise: docs/43:1356-1358, DISCLOSED NON-BLIND, "Cr flips 3->2 between U = 1.85 and 3.70 (its production U landed 7 meV from the crossing — the physical content of 'Cr sat 9 meV above its floor')". A 7 meV proximity to a pls crossing is the registered, pre-data precedent for how this deliverable behaves. (c) The Ir carve-out the finding cites as the missing precedent exists because Ir has a MEASURED 291 meV error class specific to it (docs/45 row 1); nothing comparable and Mn-specific is measured here. (d) caveats.mn_afm banks the A7.5 quote verbatim, states the whole column is FM-initialised, and explicitly licenses it for A7.2's flip census — so the AFM arm is disclosed. If it runs it produces a different column with its own bracket; it does not make this column's bracket wrong, because the claim is scoped to this column. (e) "No margin field" is wrong in its operative part: every row banks dG_OH, dG_O and dG_OOH, so I computed every margin in the census from the artifact in five lines.
                 
                 NEITHER VERDICT MOVES. A7.2 CONFIRMED is untouched (the finding concedes this; even if U=6.0 flipped, Mn keeps two flips). A7.3 is immune by registered design — span(c_M)/2 at fixed endpoints U=0 and U=U_max is pls-independent, and docs/43:1362-1365 says the pls-sensitive ratio was WITHDRAWN before any grid was read precisely because "the excess vanishes identically at a pls crossing, so the ratio is a grid artifact".
                 
                 What remains is the bare observation that Mn's second edge is the tightest in the census. That is a candidate for a courtesy note, not a defect: it is derivable from the banked artifact, anticipated by the registration, contradicted-as-a-risk by the artifact's own control, and its posited consequence is 0.10 eV rather than the 1.5 eV claimed.

[R5] [run-qc] Node identity of all 8 runs is banked nowhere, leaving the artifact's 'same-machine' control class unverifiable against the tranche's own registered node EXCLUDE list
    WHY REFUTED: REFUTED on three independent grounds; the finding's factual observations are mostly true but they do not support the defect it asserts.
                 
                 1. THE HEADLINE IS DEFINITIONALLY WRONG — "same-machine" is a CLUSTER label, not a node label, and it IS verifiable.
                 The finding assumes control_class="same-machine" is a claim about node identity. It is not. The campaign registers the machine axis explicitly at docs/43-prereg-week1-factorial.md:1606-1611 (A8.5, "The move to Anvil, registered as a change of machine, not of method"): "What is not identical is the microarchitecture — Vast EPYC 7B12 (Zen 2) against Anvil EPYC 7763 (Zen 3), which dispatch different OpenBLAS kernels." The machine axis is Vast-vs-Anvil / Zen 2-vs-Zen 3. The scorer's own comment says the same thing twice: src/dft/a0main_readout.py:80-86 ("Cr/Ru/Ir and Ti re-run on the same machine (determinism); Mn/Fe re-run Vast audit decks on Anvil (A8.5 cross-machine parity)") and :108-110 for the Fe override ("the honest same-machine pair is the u530 rung vs the accepted pilot deck (byte-identical except prefix, both Anvil)"). Every Anvil `shared` node is EPYC 7763, so ANY Anvil node satisfies the label. Verification needs only that both halves ran on Anvil, and that is banked: runs/a0/m_a0_ti_ooh.txt:29-30 puts probe/Ti_audit/s0_OOH__base and a0/main/Ti/s0_OOH__u000 in one Anvil manifest submitted as one array, and both .out headers show QE v.7.5, 128 ranks, npool 8, Sum G-vecs 921821 identical. Node identity is not required to verify the label the artifact actually stamps.
                 
                 2. THE FAILURE SCENARIO'S PREMISE IS UNSUPPORTED — node identity is recoverable from Slurm accounting, which is the channel docs/45 actually used.
                 The finding claims that once logs/a0_20215155_*.out ages out of scratch, node assignment is unrecoverable, and that this defeats the docs/45 node-forensics protocol. But docs/45's per-node tables are not built from the `node=$(hostname)` echo at anvil/46_a0.slurm:78. They are built from sacct/scontrol fields that appear in NO .out file and in no hostname echo: MaxRSS (docs/45:1091-1093 "8.65/8.66/8.70 GB", "35.06/35.14 GB", "16.93/16.94/16.95 GB"; :850-857), exit code 0:125, ExcNodeList (:93-94, :209), and NodeName/State/CPULoad/FreeMem/Reason drain strings (:857 "NodeName=a196 State=ALLOCATED+DRAIN ... Reason=NHC"). docs/45:2037 shows a retrospective `sacct` census run days after the jobs ("`sacct` since 2026-08-27 showed exactly 22 `a0` tasks"). Slurm's accounting DB is independent of scratch retention, so `sacct -j 20215155 --format=JobID,NodeList,MaxRSS,State` recovers exactly what the protocol needs. The finding checked one channel and not the one the protocol runs on.
                 
                 3. THE "SPECIFIC EXPOSURE" IS AN OVER-READ OF A FREE-MEMORY SNAPSHOT.
                 The memory numbers are quoted correctly (u000 149899 MiB, u900 149397 MiB, others 228115-234457), but the line is "available memory on the printing compute node when the environment starts" — a snapshot of free memory on rank 0's node at MPI init (page cache from the prior job), not a node fingerprint and not a health signal. Its significance collapses against the jobs' actual demand: runs/a0/main/Ti/s0_OOH__u000.out:187 estimates 93.41 MB per process, ~12 GB node-wide, so even the "low" node had ~12x headroom. Both runs completed normally — u000 "convergence has been achieved in 44 iterations", "JOB DONE", PWSCF 3m29.46s WALL; u900 likewise 44 iterations, JOB DONE, 3m41.09s WALL. docs/45's sick-node signature is the opposite shape (OOM kill at MaxRSS 8-35 GB against a 237 GB grant, a DRAIN reason string, or a hang producing zero SCF iterations); none of it is present here.
                 
                 4. THE DIVERGENCE OFFERED AS EVIDENCE IS ALREADY MEASURED AND BANKED.
                 base and u000 end at -1301.83147206 vs -1301.83147165 Ry = 4.1e-7 Ry = 0.0056 meV, which is exactly metals/Ti/rerun_determinism_check_meV/s0_OOH = 0.005578 in docs/figs/a0main_readout.json, against the 5 meV enforced tolerance (src/dft/a0main_readout.py:84-86). The control is a tolerance check and never claimed bitwise identity, so "the two runs are demonstrably not one execution" refutes a claim the artifact does not make.
                 
                 5. NOT A DEFECT OF THIS CHANGE SET. No run directory anywhere in the campaign banks per-run node identity (a search of runs/ for node/hostname/NodeList returns only a manifest comment in runs/s3/m_s3_wave2.txt), and nothing in docs/43 registers such a requirement. The anchor src/dft/a0main_readout.py:112 is the Ti METALS entry, a line that reads no node data for any metal — this is a uniform campaign convention, not something the uncommitted Ti work introduced.
                 
                 WHAT IS TRUE BUT DOES NOT RESCUE IT: the finding's A7.3 arithmetic is right. The registered quantity is span at FIXED endpoints (docs/43:1361-1379, quoted in the scorer at :727-730 and in the artifact's a7_3.quantity), so Ti's number does come from exactly u000 and u900: c_M(0)=3.456938, c_M(9)=3.369426, span/2=0.043756 V, which I re-derived from the raw rows. But since nothing about those two runs is anomalous, the coincidence carries no weight. Separately, the offered `a21[0-9]` grep is arbitrary — it presupposes a node-name range never observed anywhere in the repo, so its null result is not evidence about node banking.

[R6] [reanchor-bias] docs/59 s3c names only the numerics as unregistered, and proves the extension's bounded authority on the one branch that did not occur
    WHY REFUTED: REFUTED on both halves.
                 
                 HALF 1 — "only the numerics are flagged as unregistered; a reader auditing freedoms would never look at the geometry."
                 
                 False. The geometry freedom is disclosed twice, in the same section 3c, at HEAD (docs/59 is committed at bb17152; it is not among the four modified files in `git status`).
                 
                 (a) docs/59:174-183, "**Calculation-class disclosure (added 2026-08-29, after the wave-4 audit)**", quotes A6.6 back at itself — "registers block 6A as '**~160 fixed-geometry SCFs and zero relaxations**' and states it 'does **not** license ... any relaxation in any cell'" — names the decks individually ("3 escalation repairs (`s0_OOH_r1`/`r2`/`r3`), seven in total, all in the 1x1 cell"), and concludes "So they are new compute outside A6.6's declared footprint, and are stated here rather than absorbed." Lines 191-195 go further than the finding asks: "That is an explanation of why they were run, not a licence; the licence is the entrant's to grant or withhold when countersigning this document. If withheld, the consequence is already registered: A7.3's denominator shrinks and the Ti rows are WITHDRAWN-UNSCORED under A7.7." That is the exact disclosure the finding says is absent, plus a remedy path the finding does not account for.
                 
                 (b) docs/59:150-155 concedes the epistemic hazard verbatim — "The start DOES select which local minimum BFGS reaches -- that is the point of r3" — and the same text was in the builder docstring (src/dft/build_a0main_w2c.py:115-121) *before either deck ran*. The finding quotes line 150 as evidence that the doc is "self-contradicting"; it is instead evidence that the freedom is disclosed.
                 
                 HALF 2 — "line 164 denies verdict-relevance; the extension held sole, undisclosed power to flip A7.3."
                 
                 The arithmetic is right (src/dft/a0main_readout.py:735 A73_NEEDED = 4 is fixed while the denominator is `len(a73_rows)`, so 4 of 6 hits CONFIRMED at :751-752, and 3 of 5 cannot). But the dependency is disclosed, in writing, before the fact:
                 
                 (c) docs/59:254-257, in section 4 titled "What the extension can and cannot change": "**A7.3 (span(c_M)/2 at fixed endpoints)** becomes scorable on the full 6-metal denominator once Ti's *OOH converges; its own text conditions the denominator on 'a converged *OOH geometry', so a Ti convergence failure shrinks the denominator rather than blocking the readout." `git log -S` puts that sentence in ef56943 (2026-08-28) — the original draft, before tranche 2c was registered (bcd3cc0) and before the r3 deck existed (bb17152). The 5->6 denominator move was on the record before the escalation ran.
                 
                 (d) docs/59:238-245 states the stake: "A7.3 (P-FLOOR-U) is genuinely at stake: it stands at **3 of 5 against a registered threshold of >=4** ... Ti is the only metal left. That currently-failing state was scored, printed and banked BEFORE these decks were built." The finding cites bae6cd2's 3-of-5 score as evidence against the document; it is the document's own pre-registration guard.
                 
                 Two further corrections to the finding's mechanics:
                 - The "extension" it indicts is numerics-only, and its numerics reach no banked number. Raw check on runs/a0/main/Ti/s0_OOH__u900.out:54-56: "scf convergence threshold = 1.0E-06 / mixing beta = 0.3000 / number of iterations used = 8 local-TF" — mixing_ndim 8, not 16; maxstep untouched. docs/59:231-236 claimed exactly this and it holds. r2 and r3 ran identical numerics (docs/59:145) and r2 failed, so ndim/maxstep did not produce the convergence either. The thing with power over the verdict was the relaxed geometry, which is disclosed under (a) and (b) — not the extension line 164 is about.
                 - A7.3's registration itself names Ti among the six blind metals (docs/43:1377-1378) and denominates over "the 6 metals with a converged *OOH geometry." Six is the registered design; the gap is a degradation of it. Filling the gap restores the registered census rather than enlarging its reach, and the finding's counterfactual ("max reachable is 3 of 5") describes the degraded state, not the registered one. The finding also conflates two different objects both called "the extension": docs/59 §2 uses the term for the Mn/Fe/Ti roster extension (which is what §4's title means), while line 161's "Its authority" is the ladder numerics.
                 
                 What survives is cosmetic and below the reporting bar the brief sets ("do not report ... things that are already correctly disclosed"): line 157's "The one thing here that is NOT in the registration" is stale relative to the paragraph added 17 lines later, which itself announces that it is addressing "a second clause that was not addressed."

[R7] [reanchor-bias] runs/probe/Ti_audit/probe_manifest.json has no s0_OOH job, so the extraction control the readout points at does not exist for the one hand-built geometry
    WHY REFUTED: REFUTED. The factual observations are all true (Ti's probe_manifest.json has 3 jobs, not 4; a0main_readout.py:523-525 points at probe_manifest without exception; build_a0main_w3b.py bypasses cmd_build, which is what writes manifest rows; a0main_readout.py never reads probe_manifest), but the conclusion drawn from them is not supported, on three counts.
                 
                 (1) The extraction control's reference value EXISTS and is banked in a committed, pre-launch file. runs/a0/main/manifest.json:282 carries tranche_3b.selection_rule_outcome.winner_energy_eV = -17712.319507586788, with geometry.source = runs/Ti_slab/s0_OOH_r3.out and provenance "final" at lines 288-296. That is precisely the quantity relax_reference_ev holds: runs/Ti_slab/s0_OOH_r3.out:43513 "Final energy = -1301.8314722192 Ry" = -17712.31951 eV, versus runs/probe/Ti_audit/s0_OOH__base.out:1128 "! total energy = -1301.83147206 Ry" = -17712.31951 eV, a 2.2e-3 meV round-trip. The finding describes that file as recording only "geometry provenance"; it records the relax reference energy as well, in the tranche block named for the builder that made this deck, committed at HEAD bb17152 before the 8 jobs launched. So the headline claim -- that the control "does not exist" for this geometry -- is wrong; only its location differs from the prose pointer.
                 
                 (2) The exception is disclosed. build_a0main_w3b.py:56-57 states in the docstring that this base is built via write_probe directly because "cmd_build reads <job>.out, and the geometry of record is in s0_OOH_r3.out, not s0_OOH.out", and gates it behind a byte-identity self-test that rebuilds s0_OH__base.in through the same path. The rewritten caveats.fixed_geometry in docs/figs/a0main_readout.json flags this state as "the only geometry in this readout whose starting structure was not emitted by gen_rutile.py" and routes the reader to docs/59 s3c and build_a0main_w2c.py.
                 
                 (3) No consumer misreports, and the one automated path that uses the manifest fails loudly rather than silently. src/dft/probe_eta.py:88-93 populates E only from manifest rows, so the need set at line 177 ("slab" + all three adsorbates) is unsatisfiable, rows stays empty, and it prints "No variant has a complete slab + OH + O + OOH set yet" and returns -- it refuses to score, it does not emit a 3-state eta. a0main_readout.py's own control for this state (same-deck determinism, s0_OOH 0.00558 meV) is present and passing, and the control_note correctly distinguishes that check from the geometry round-trip.
                 
                 What genuinely remains is prose imprecision in one artifact string, affecting 1 of Ti's 4 states, whose referenced number is one file over in the ledger that registered the deck. No verdict, number, label, or reproducibility path is harmed: A7.3's Ti value does not depend on the manifest row, and the round-trip the note promises is computable from committed artifacts. That is below the bar for a reportable defect and is adjacent-disclosed.

[R8] [reanchor-bias] build_ti_r3's byte-level self-test cannot detect whitespace-only edits to the substrate rows it certifies as untouched
    WHY REFUTED: Refuted on four independent grounds.
                 
                 (1) THE PREMISE IS FALSE. The finding asserts "the whole defence of the re-anchor rests on it being a mechanical, auditable edit" certified by the byte-level `changed != [18, 19, 20]` guard at C:/Users/frank/sts-electrocatalyst/src/dft/build_a0main_w2c.py:412. It does not. Lines 440-442 re-parse the emitted deck FROM DISK and compare every substrate atom's (species, x, y, z) tuple against the source deck's parse, dying on any difference: `for i, (before, after) in enumerate(zip(sub, cpos[:-N_ADSORBATE])): if before != after: W.die("%s: substrate atom %d moved")`. I verified this independently with PYTHONPATH=src: parse_input_deck on s0_OOH.in vs s0_OOH_r3.in gives substrate positions identical True, substrate flags identical True, 18 atoms compared. The formatter self-test at 397-402 additionally pins each substrate line's selective-dynamics flags string, since `_fmt_like` interpolates `flags[i]` and must match the rstripped source line. A whitespace-only edit that slipped past line 412 would therefore still leave every physically meaningful field (coordinates and flags, on the file as written) verified by a semantic check that is immune to formatting.
                 
                 (2) THE FAILURE SCENARIO IS UNREACHABLE FROM THE CAMPAIGN'S OWN GENERATOR. src/dft/qe_slab.py:190 writes each position line as `f"  {s}  {p[0]:.8f}  {p[1]:.8f}  {p[2]:.8f}  {flags}\n"` -- the newline follows the flags with no intervening whitespace, so no ATOMIC_POSITIONS line this campaign produces can carry trailing whitespace. `grep -n ' $' runs/Ti_slab/*.in` returns nothing. The hypothesized source deck would have to be hand-edited, which is outside the builder's input contract.
                 
                 (3) THE CONSEQUENCE IS NIL EVEN IF IT FIRED. Quantum ESPRESSO's ATOMIC_POSITIONS card parsing is whitespace-insensitive; rstripping a substrate line alters no coordinate, no selective-dynamics flag, and no card boundary. There is no reachable state in which a wrong number enters the record. The finding itself concedes it did not fire: `diff runs/Ti_slab/s0_OOH.in runs/Ti_slab/s0_OOH_r3.in` (which I ran) shows only prefix s0_OOH -> s0_OOH_r3, mixing_beta 0.3 -> 0.15, mixing_ndim = 16 added, electron_maxstep 200 -> 400, and the z field of exactly rows 18/19/20 (19.408 -> 18.023, 20.308 -> 18.923, 21.258 -> 19.873).
                 
                 (4) THE DOCS/59 QUOTE IS MIS-ATTRIBUTED. docs/59-a0-roster-correction-2026-08-28.md:147-148 reads "the adsorbate rigidly translated -- orientation and internal geometry preserved exactly". That is a claim about the ADSORBATE, not about substrate row bytes, and it is enforced twice over: build_a0main_w2c.py:386-387 computes one shift vector and applies it identically to all three adsorbate atoms (a rigid translation by construction), and lines 434-439 check both intra-adsorbate pair distances to 1e-6 on the re-parsed on-disk deck. The finding borrows a sentence about the adsorbate to characterize a guard about the substrate.
                 
                 The blank-line sub-claim (line 393's `if l.strip()` silently dropping blank lines) is also inert: it is immediately followed at 394-396 by `if len(srclines) != len(pos): W.die("position line count mismatch")`, so any divergence between the raw block and the parsed atom list halts the build rather than producing a silently misaligned deck.
                 
                 What remains is at most a cosmetic normalization -- the builder emits rstripped lines -- on which no stated guarantee depends, with no reachable input that triggers it and no physical consequence if one did. That is not a defect worth reporting under the standing rules.

[R9] [reanchor-bias] The convergence quality quoted for the banked geometry is a total force above the deck's own forc_conv_thr
    WHY REFUTED: REFUTED on arithmetic. The finding's load-bearing mechanistic claim is that "Total force = 0.003092" is "the norm over all 21 atoms including the seven frozen ones (if_pos 0 0 0) whose printed force components run up to 0.12355722 Ry/au on atom 1." I re-derived this from the final force block at runs/Ti_slab/s0_OOH_r3.out:43481-43503, with the frozen set (atoms 1,2,7,8,9,10,12) read off runs/Ti_slab/s0_OOH_r3.in. The norm over all 21 atoms is 0.200478; the norm over the 14 free atoms is 0.003092 — reproducing the printed value to all six digits. The frozen atoms are NOT in it. QE applies force(:,:) = force(:,:) * DBLE(if_pos) before accumulating sumfor, while the per-atom block is printed before that masking, which is exactly why atom 1's 0.12355722 appears in the listing yet contributes nothing to the total. Had the frozen atoms been included, the line would read 0.200478, 65x larger. So the number is already correctly scoped to the relaxing degrees of freedom, and the stated failure scenario (a reviewer must re-open a 43k-line output to discover 7 of 21 atoms are constrained) collapses: knowing about the constraints changes the number not at all.
                 
                 The residual comparison (0.003092 vs forc_conv_thr = 2.0d-3) is a category error committed by the finding, not by the artifact. "Total force" is an L2 norm over 42 free force components; forc_conv_thr is checked by BFGS against MAXVAL(ABS(grad)), which QE prints two lines below as "Gradient error = 1.5E-03 Ry/Bohr" together with the explicit parenthetical "(criteria: energy < 1.0E-04 Ry, force < 2.0E-03 Ry/Bohr)". A 42-component norm exceeding a per-component max threshold is expected and carries no information about convergence. The docs use the quantity comparatively and self-consistently: r2's stall is quoted as 0.0738 Ry/bohr at src/dft/build_a0main_w3b.py:16 and r3's as 0.003092 at :21 — the same quantity, a 24x reduction, which is the number's actual rhetorical job.
                 
                 The claim that the change set leans on 0.003092 as the evidence of convergence is also a misreading. src/dft/build_a0main_w3b.py:19 quotes QE's own verdict verbatim in the same sentence — "bfgs converged in 53 scf cycles and 52 bfgs steps" — and the manifest header at :247 leads with "bfgs converged". More decisively, the builder's machine-enforced gate at src/dft/build_a0main_w3b.py:110 is ok = "bfgs converged" in txt and "convergence NOT achieved" not in txt; 0.003092 appears nowhere in the selection logic that decided r3 over r2. docs/59:219 likewise bolds "converged" and cites "52 ionic steps", both taken straight from QE's convergence line, with the force value as a trailing descriptor.
                 
                 The finding's own evidence concedes the relaxation is genuinely converged, so nothing about the banked Ti *OOH geometry is at risk; what remains is an assertion that the wrong figure was quoted, and that assertion fails because the figure is QE's standard, correctly-scoped, free-atom convergence descriptor, quoted alongside QE's own categorical statement that the criteria were met.

[R10] [caveat-accuracy] Caveat declares the Ti geometry question closed while A6.6's zero-relaxations licence for the seven Ti relaxations is still open and unmentioned anywhere in the readout
    WHY REFUTED: I went to the four places the finding cites and the finding does not hold up.
                 
                 1. The sentence's referent is the physics, not the licence. Line 826 sits at the end of a paragraph whose entire subject is the placement artefact: "...*OOH walked UP (3.167 -> 3.414 A) into a desorbed-radical region... So TiO2 DOES bind *OOH; the campaign's default placement produced an artefact of the starting height, not a measurement of non-binding. The residual open question for Ti is the spin convention, not the geometry -- see [spin_state]." The parallel construction pins "the geometry" to the binding-vs-desorption question just argued and settled by s0_OOH_r3. The A6.6 item is not a claim that the geometry is wrong; docs/59 titles it exactly "**Calculation-class disclosure**" and states inside itself that the relaxations "are the geometry INPUT the Ti ladder stands on, not A0 points. ... A6.4 is untouched." Calling that "an open question about the GEOMETRY" conflates a compute-scope question with a structural one.
                 
                 2. The readout mirrors its source document's own designation. docs/59 s3c contains a paragraph headed "**An open question for the entrant, deliberately not settled here.**" — and that paragraph is the nspin=2 convention, not the licence. The readout's "the residual open question for Ti is the spin convention" tracks docs/59's own labelling of what it left open; the licence paragraph is framed there as a disclosure plus an entrant countersignature item, not as a question the readout was supposed to re-open.
                 
                 3. "The readout never carries it" rests on a token grep, and the underlying fact IS disclosed in the same caveat. Immediately before the disputed sentence the caveat says Ti's "slab, *O, *OH AND *OOH geometries were all built and relaxed inside tranche 3 (2026-08-28/29, docs/59 s3/s3c)", and mentions two further failed relaxations plus the r3 re-anchor — i.e. it discloses that this block ran relaxations, the exact fact A6.6's "zero relaxations" clause bears on, and cites the precise section holding the licence question. [spin_state] cites docs/59 s3c a second time. Verified in the artifact: docs/figs/a0main_readout.json carries "...were all built and relaxed inside tranche 3 (2026-08-28/29, docs/59 s3/s3c)". A grep returning 0 for the string "A6.6" is not the same as the point being unmentioned.
                 
                 4. The failure scenario is self-defeating. It requires the entrant to countersign docs/59 while missing the paragraph inside docs/59 that says "the licence is the entrant's to grant or withhold **when countersigning this document**. If withheld, the consequence is already registered: A7.3's denominator shrinks and the Ti rows are WITHDRAWN-UNSCORED under A7.7." The countersignature instrument is docs/59 itself; the licence item is placed in it deliberately. Moreover the readout is not silent about that instrument: the `coverage_shortfall` caveat (src/dft/a0main_readout.py:883-905) already tells the reader "the dated correction of record is docs/59, drafted for the entrant to countersign and deposit" and quotes "Under A7.7's disposition rule, whatever stays unscored at freeze is WITHDRAWN-UNSCORED, not quietly dropped." So both the pending countersignature and the WITHDRAWN-UNSCORED mechanism travel into the artifact.
                 
                 5. Not introduced by the uncommitted change. git diff shows the pre-edit caveat already said Ti's "slab, *O and *OH geometries were built and relaxed inside tranche 3 (2026-08-28/29, docs/59 s3)" with no mention of A6.6 either. A6.6's scope guard applied identically to those three relaxations. The change under audit did not create the alleged omission, and it added disclosure rather than removing any.
                 
                 What survives is at most a wish for one more cross-reference in the a7_3 block. Under the rule to default to refuted when the issue is already disclosed elsewhere in the artifact or docs, that does not clear the bar, let alone a blocker.

[R11] [caveat-accuracy] Caveat places the *OOH geometry inside tranche 3, whose own registered gate would have blocked it; the manifest registers it under tranche 2c and 3b
    WHY REFUTED: The finding rests on a category error and is contradicted by three independent places in the record.
                 
                 1) It cites the wrong manifest key. The caveat sentence at src/dft/a0main_readout.py:806-809 is about GEOMETRIES ("its slab, *O, *OH AND *OOH geometries were all built and relaxed inside tranche 3"). The finding answers it with tranche_3.stage_2.states, which is the fixed-geometry SCF-LADDER stage, not the geometry stage. The geometry stage is tranche_3.stage_1, and it explicitly contains an s0_OOH key: "s0_OOH": "FAILED -- SCF after first ionic step hit 200 iterations at accuracy 0.0097 Ry (conv_thr 1e-6), still falling; A6.5(2)(ii) repair s0_OOH_r1 (beta 0.15, last trajectory geometry) in runs/Ti_slab/m_ti_relax_r1.txt; failed .out retained per A8.4". So the manifest itself registers the Ti *OOH build, its first relaxation, and its rung-(ii) repair INSIDE tranche 3. *OOH is not "deliberately excluded" from tranche 3; it is absent only from stage 2's banked-state list, which is a statement about the SCF ladder that had not yet been built at that moment, not about where the geometry was made.
                 
                 2) The registration of record says the same thing. docs/59-a0-roster-correction-2026-08-28.md section 3 registers "Tranche 3 - Ti (geometry chain first). ... Chain: qe_slab.py build Ti --supercell 1 ... -> relax slab + 3 adslabs -> probe-style base SCFs -> 7-point REF_GRID ladder (28 SCFs...)". Three adslabs = *O, *OH, *OOH; 28 SCFs = 4 states x 7 rungs. Tranche 3's registered scope is all four Ti states including *OOH. Section 3c reinforces it: "The Ti arm is built on relaxations: 4 in tranche 3 (slab + 3 adslabs) and 3 escalation repairs (s0_OOH_r1/r2/r3)". Tranche 2c is an A6.5(2) escalation round applied WITHIN that chain, not a competing registration of the geometry.
                 
                 3) The 2c authority is not hidden - it is named in the same caveat. The very sentence the finding quotes cites "docs/59 s3/s3c", i.e. both the tranche-3 chain and the 3c escalation round; and four sentences later the same caveat says: "The banked geometry comes from s0_OOH_r3, RE-ANCHORED by build_a0main_w2c.py to the mean of Ti's own two converged Ti-O bond lengths (1.781905 A) before relaxing ... while the plain continuation r2 failed again. Tranche 2c's exactly-one-converged selection rule was fixed before either ran." The load-bearing authority (tranche 2c / build_a0main_w2c.py / docs/59 s3c) is stated by name, by builder path, by numeric anchor, and by selection rule. The claimed failure scenario - a reader following the label to tranche 3 and never finding the real authority - cannot occur, because the label and the real authority sit in the same paragraph.
                 
                 The gate argument also does not land. tranche_3.s0_OOH_gate governs whether the base + 7 SCF rungs get BUILT, not where the geometry was relaxed; and its failure branch is exactly what docs/59 section 3c registers and bounds: the ladder extension "can only FILL the gap (iii) would leave; cannot move a threshold or flip a verdict", with the recorded outcome identical to (iii) alone had both decks failed. Whether that dated extension was licensed is a separate (and separately disclosed, countersign-pending) question about escalation legitimacy - not a mislabelling defect in this caveat.
                 
                 The only residual is that the banked *OOH geometry (r3) was relaxed under 2c's escalation rather than tranche 3's first array, so "all built and relaxed inside tranche 3" is a compression. But the immediately following sentence flags "The *OOH geometry carries a further provenance difference that travels with every Ti number below" and then spells that difference out in full, and the sentence's actual contrast (Ti's geometry is NOT inherited from a banked production tier, unlike Cr/Ru/Ir/Mn/Fe) is correct as written. Qualified in place; refuted.

[R12] [caveat-accuracy] "~3.2 A off the nearest Ti" for every Ti adsorbate; the *O deck is built at 3.117 A and the registered records say 3.1-3.2 A
    WHY REFUTED: REFUTED on the finding's own load-bearing assertion, though its arithmetic checks out.
                 
                 VERIFIED CORRECT: I re-derived the built distances from the decks. All three adsorbates sit in the same (x,y) column as their nearest Ti (x=0.0, y=4.87235463), so the min-image distance reduces to Delta-z. runs/Ti_slab/s0_O.in: anchor O z=19.35799431, nearest Ti z=16.24118210 -> 3.116812 A. runs/Ti_slab/s0_OH.in and s0_OOH.in: anchor O z=19.40799431 -> 3.166812 A. The finder's numbers reproduce exactly.
                 
                 REFUTED: the finding's central claim is "The single-value restatement is the caveat's own drift, not the record's." That is factually wrong. The exact phrase "starts every Ti adsorbate ~3.2 A off the nearest Ti" is the record's own, in two committed pre-launch places: (1) runs/Ti_slab/m_ti_relax_r2.txt:4 -- the ACTUAL LAUNCHED DECK MANIFEST for tranche 2c, tracked and committed at 1c60cc2 (the launch commit); and (2) src/dft/build_a0main_w2c.py:478 (HDR_TI, the builder header that emitted that manifest), committed at bcd3cc0, the pre-launch registration commit. The readout caveat at a0main_readout.py:814 is propagating the registered manifest's sentence verbatim. Under the campaign's own standing rule that labels travel unchanged into every artifact, that is the correct behaviour. The finding inverts which text is the deviation: it treats the readout as having drifted from docs/59, when the readout is quoting the launched manifest word for word.
                 
                 NOT CONTRADICTED BY ITS OWN TABLE: the failure scenario claims an auditor would find the caveat "contradicted by the very table in build_a0main_w2c.py it is summarising." It would not. The caveat's very next clause prints 3.167 -> 3.414, matching build_a0main_w2c.py:79-81's s0_OOH row exactly. The tilde-approximation is bounded on the low side by an exact number in the same sentence.
                 
                 NOT LOAD-BEARING: the single deviating state is s0_O at 3.1168, 0.083 A / 2.6% under 3.2 -- within what a tilde signals, and it is not the state the caveat's argument rests on. The scientific claim under test (the *OOH start was too high, it desorbed, re-anchoring fixed it) is carried entirely by 3.167 -> 3.414, the re-anchor at 1.781905 A, 52 ionic steps, zero SCF failures, and d(O,Ti)=2.041 A -- every one printed exactly and every one reproducing. Neither A7.2 (5/6 pls flips) nor A7.3 (3/6 over the 0.10 V floor, NOT MET) shifts by any amount if s0_O started at 3.117 rather than 3.167.
                 
                 ALREADY DISCLOSED: the tighter range and the per-state table sit one hop away in the two sources the caveat names by name -- build_a0main_w2c.py:72-81 ("about 3.1-3.2 A" plus s0_O 3.117 / s0_OH 3.167 / s0_OOH 3.167) and docs/59 s3c line 137 ("~3.1-3.2 A"). An auditor following the caveat's own citations reaches the exact numbers immediately.
                 
                 This is a rounding-precision preference on a tilde-qualified approximation whose phrasing is inherited from the pre-registration, not a caveat-accuracy defect.

[R13] [caveat-accuracy] Caveat localises the nspin=1 limitation to the desorbed region, but the banked bonded geometry is equally odd-electron and equally spin-restricted
    WHY REFUTED: REFUTED — the alleged scope disagreement is closed explicitly inside the very caveat under attack, and the disputed clause is mischaracterized.
                 
                 1. THE CAVEAT ITSELF CLOSES THE GAP. src/dft/a0main_readout.py:825-827, two sentences after the disputed clause and inside the same fixed_geometry string, reads: "So TiO2 DOES bind *OOH; the campaign's default placement produced an artefact of the starting height, not a measurement of non-binding. The residual open question for Ti is the spin convention, not the geometry -- see [spin_state]." I confirmed this sentence travels into the artifact: a membership test on docs/figs/a0main_readout.json caveats.fixed_geometry returned True. So fixed_geometry does NOT imply the banked 2.041 A geometry escaped the nspin=1 pathology — it states the opposite, declaring the spin convention the one thing still OPEN for Ti after the geometry was fixed, and naming [spin_state] as the cross-reference. The finding's core assertion that "the two caveats travel together and disagree on scope" is false; fixed_geometry hands the spin question forward to spin_state by name rather than closing it.
                 
                 2. THE FAILURE SCENARIO IS BLOCKED BY THE PARAGRAPH IT PRESUPPOSES. The scenario posits a reader who takes fixed_geometry at face value and concludes Ti's c_M span of 0.0438 V is spin-clean. A reader taking fixed_geometry at face value reaches, in the same paragraph, the sentence saying Ti's residual open question IS the spin convention. spin_state then states it unconditionally: "every Ti row is spin-convention-conditional in a way the nspin=2 Cr/Mn/Fe rows are not." The registered position the finding says is contradicted is in fact stated twice and cross-linked.
                 
                 3. THE DISPUTED CLAUSE IS MISCHARACTERIZED. "a desorbed-radical region that nspin=1 cannot describe" is a claim about spin-restricted DFT's QUALITATIVE breakdown for a dissociating open-shell fragment, not a claim that the odd electron count is region-specific. Both statements hold simultaneously at different severities: the 157-electron system is odd at every geometry and cannot be spin-split (spin_state, unconditional), while at the desorbing geometry the restricted solution additionally becomes qualitatively wrong — which is exactly why two relaxations failed there and the re-anchored bonded one converged in 52 steps with zero SCF failures. docs/59-a0-roster-correction-2026-08-28.md:140 registers the same reading ("into the desorbed-radical region, where the chain's nspin = 1 convention cannot..."), and lines 167-169 keep nspin=2 open for the entire Ti arm, not just the desorbed region.
                 
                 4. EVIDENCE VERIFIED BUT NON-PROBATIVE. I independently confirmed every raw fact offered: "number of electrons = 157.00" at line 50 of runs/a0/main/Ti/s0_OOH__u900.out, runs/probe/Ti_audit/s0_OOH__base.out and runs/a0/main/Ti/s0_OOH__u000.out; grep -c nspin runs/a0/main/Ti/s0_OOH__u900.in = 0; spin_state's unconditional odd-electron sentence present in the JSON. These are all true and none of them support the claim, because the caveat never asserts that the electron count or the spin restriction varies with d(O,Ti).
                 
                 The issue is already correctly disclosed in the artifact, in the exact caveat named, with an explicit pointer to the caveat that carries the unconditional statement.

[R14] [staleness] docs/58 still lists A7.3 as an unscored deferred readout, hiding that the registered prediction has now FAILED
    WHY REFUTED: REFUTED. The finding's arithmetic checks out but its characterization does not survive contact with the files.
                 
                 (1) THE CENTRAL PREMISE IS FACTUALLY FALSE. The failure scenario asserts "The failure is recorded only inside a JSON blob." It is recorded in at least four human-readable places outside the JSON. tasks/todo.md:727 (checked, banked yesterday): "**A7.3 was never scored while its sibling A7.2 was** -- now scored: **3 of 5 vs registered >=4, NOT YET MET, Ti deciding**." tasks/todo.md:728, written BEFORE Ti stage 3 landed, pre-registers both branches in plain English: "A7.3 DECIDED (Ti over 0.10 V = CONFIRMED at 4/6, under = NOT MET at 3/6, never FALSIFIED which needs <=1)." docs/45-error-ledger.md:2304-2313 devotes a titled paragraph to it — "A prediction was being omitted while its sibling was reported... Reporting the one that passes and silently omitting the one that does not is the exact failure pre-registration exists to prevent." And docs/59:239 already flagged "A7.3 (P-FLOOR-U) is genuinely at stake." A reader cannot reach the STS report without passing the campaign's task ledger and error ledger, both of which carry the miss.
                 
                 (2) docs/58 IS A DATED, SELF-SCOPED 3-METAL SNAPSHOT, AND :171 WAS TRUE WHEN WRITTEN. Its provenance block (docs/58:8-10) names its evidence commits — 06a99d0 (wave 2) and 904a40b (wave 3) — and states its registration scope verbatim: "Registration: docs/43 A6.0-A6.7, A7.1-A7.2." A7.3 is explicitly outside what the document claims to answer. It was committed d4c167b on 2026-08-28, when the scorer computed no A7.3 at all (the wave-4 completeness critic found that on 2026-08-29). Listing A7.3 under "Deferred readouts" was the correct disposition on that date.
                 
                 (3) THE A7.3 LINE IS NOT THE DOCUMENT'S UNIQUELY STALE SENTENCE, AND THE DOCUMENT DISCLOSES ITS OWN OBSOLESCENCE ON ITS FACE. docs/58:117 reads "The unrun blind metals (Mn, Fe, Ti) can only add flips"; :134 reads "Mn/Fe/Ti were never built"; §5 caveat 5 is a standing COVERAGE SHORTFALL disclosure. All three metals are now built and scored, A7.2 is now a full 6/6 census, and the Ti *OOH re-anchoring result did not exist. Nobody can mistake this document for a six-metal readout — it says half its roster is unrun. Singling out one line about A7.3 as a concealment mis-locates a whole-document supersession that is already declared inside the document. That supersession is also PRE-EXISTING AT HEAD: docs/58 is untouched by the uncommitted change set (git status shows only run_oc20.py, s1-controls.yml, a0main_readout.json, a0main_readout.py modified), so committing this work does not create the inconsistency.
                 
                 (4) THE REWRITE IS AN EXPLICITLY TRACKED, UNCHECKED PREREQUISITE OF BANKING, not a quiet drop: tasks/todo.md:728 "Then docs/58 update + adversarial verify + bank", and again at :732 "Analysis doc (docs/58): the A0 story end-to-end."
                 
                 (5) THE "RESCOPING" CHARGE INVERTS WHAT HAPPENED. Nothing changed A7.3's quantity, endpoints, floor, or threshold. The denominator moved 5 -> 6 because Ti's *OOH converged, which is A7.3's own registered conditioning at docs/43:1372 ("the 6 metals with a converged *OOH geometry") — and it moved the count in the direction that made the threshold HARDER to clear: 4 of 6 would have been CONFIRMED. The campaign spent an escalation round and a re-anchored relax (build_a0main_w2c.py, s0_OOH_r3, 52 steps to d(O,Ti)=2.041 A) to win Ti a point that could only have helped, then reported the miss. That is the opposite of rescoping to dodge a failure.
                 
                 (6) "FAILED" OVERSTATES THE REGISTERED VOCABULARY. docs/43:1369-1373 defines three outcomes; 3 of 6 is threshold-not-met and explicitly NOT falsified (needs <=1). The banked artifact says "NOT MET", with needed:4, denominator:6, exceeds:[Cr,Fe,Mn], and per-metal spans.
                 
                 WHAT I CONFIRMED AS ACCURATE in the finding: docs/58:171 is quoted correctly; a0main_readout.json a7_3.status = "NOT MET", denominator 6, needed 4, exceeds [Cr, Fe, Mn]; Ti span/2 = 0.04375590908239 (Cr 0.34347, Fe 0.61020, Mn 0.63072, Ru 0.09225, Ir 0.06371). The JSON also now carries a provenance key (scored_at_utc 2026-08-29T23:10:32Z, commit bb17152, dirty:true) with the note that the A7.2/A7.3 status fields are live at that timestamp. The numbers are right; the concealment claim is not.

[R15] [staleness] tasks/todo.md lists A7.3 as "still unscored" three lines after recording that it was scored
    WHY REFUTED: The line exists and reads as quoted, but every element of the failure scenario fails on inspection.
                 
                 1. It is a dated log entry, not a live contradiction introduced by this work. `git blame` shows tasks/todo.md:730 was written 2026-08-28 in commit 904a40b, when A7.3 genuinely was unscored. Lines 726-728 were added the next day in bb17152. todo.md is an append-only dated ledger (sections "## 2026-08-28", "## 2026-08-28 later", newest at bottom); older bullets are not rewritten in place. Nothing in this session's uncommitted change set touches todo.md at all (git status: only .github/ci/run_oc20.py, .github/workflows/s1-controls.yml, docs/figs/a0main_readout.json, src/dft/a0main_readout.py, plus the 24 untracked Ti outputs).
                 
                 2. The correction is already recorded, twice, above the stale line and in the record of record. tasks/todo.md:727 states "**A7.3 was never scored while its sibling A7.2 was** -- now scored: **3 of 5 vs registered >=4, NOT YET MET, Ti deciding**", and :728 pre-registers the exact re-score rule ("A7.3 DECIDED (Ti over 0.10 V = CONFIRMED at 4/6, under = NOT MET at 3/6, never FALSIFIED which needs <=1)") together with the follow-on action "Then docs/58 update + adversarial verify + bank". docs/45-error-ledger.md:2305-2311 carries the same correction as a numbered ledger entry ("A7.3 now scores in the artifact, and Ti's *OOH convergence is named as the deciding metal"). A reader reaching :730 has passed both.
                 
                 3. The claimed "re-scored from scratch, producing a second differently-worded verdict" cannot happen: :728 fixes the wording, the denominator and the decision rule in advance, and the scorer is deterministic. I re-ran the artifact: a7_3 = {status "NOT MET", needed 4, denominator 6, exceeds [Cr, Fe, Mn], Cr 0.3435 / Fe 0.6102 / Mn 0.6307 / Ru 0.0922 / Ir 0.0637 / Ti 0.0438}. The verdict is banked with its threshold and denominator; a re-run reproduces it, it does not compete with it.
                 
                 4. The A7.7 escalation is unsupported. docs/43:1435-1441 (P-DISPOSITION) and docs/43:1930 make disposition a property of whether a prediction is *scored*, and the scored place is docs/figs/a0main_readout.json plus the ledger — nothing in A7.7 names tasks/todo.md as the disposition input, and no dispositioning has occurred. A prediction whose status string is literally "NOT MET" in the committed-artifact JSON is not at risk of being disposed of as WITHDRAWN-UNSCORED because a checkbox two lines below the correction was not struck.
                 
                 The one substantive residual is elsewhere and is itself dated and queued: docs/58-a0-analysis-2026-08-28.md:171 mirrors the same sentence ("A7.3 span(c_M)/2" among deferred zero-DFT readouts) and contains no A7.3 verdict — but that file is a dated 2026-08-28 snapshot written before the wave-4 audit, and its update is the explicit open item at todo.md:728. That is scheduled housekeeping for the bank, not a defect in the work under audit, and it is not what the finding points at.

[R16] [staleness] docs/58 asserts Mn/Fe/Ti are unrun blind metals that "can only add flips" -- all three are now measured and Ti added none
    WHY REFUTED: REFUTED — the finding's raw facts check out, but nothing it points at is a defect, and the staleness is disclosed in three places including the artifact docs/58 itself defers to.
                 
                 1. Not in the change set, and self-pinned to its own date. docs/58 is committed at d4c167b and is UNMODIFIED in the working tree (git status lists only .github/ci/run_oc20.py, .github/workflows/s1-controls.yml, docs/figs/a0main_readout.json, src/dft/a0main_readout.py plus 24 untracked run files). Its provenance block, docs/58:3-10, pins it to commits 06a99d0 (wave 2) and 904a40b (wave 3) and titles itself "2026-08-28". Every Mn/Fe/Ti job was built and run in commits AFTER it (ef56943 -> bb17152). Line 117 was true when written and is date-stamped as such.
                 
                 2. No verdict or number in docs/58 is falsified. The sentence reads "≥3 of 6 metals flip inside the grid" — a floor, satisfied more strongly at 5 of 6. A7.2's status is CONFIRMED in both docs/58:115 and the regenerated docs/figs/a0main_readout.json ("status": "CONFIRMED"). The report-facing permission line, docs/58:160, says "≥3 metals' limiting step flips with U" — still true. Nothing in docs/58 licenses the report to write "the remaining three are unmeasured"; the failure scenario's sentence appears nowhere in the doc.
                 
                 3. The title's central assertion is not a contradiction. "The unrun blind metals (Mn, Fe, Ti) can only add flips" is a MONOTONICITY claim about the census count. Ti adding zero flips does not violate "can only add" — it is the boundary case the phrase admits. The finding presents Ti's flatness as refuting the sentence; it does not. (docs/59:238-239 states the same monotonicity independently: "A7.2 is already CONFIRMED and cannot be harmed (more metals can only add flips).")
                 
                 4. Already disclosed, three times over. (a) docs/58's own caveat 5, lines 130-136, states Mn/Fe/Ti "were never built (allocation chosen 2026-08-27, no dated amendment) — a dated correction of record is owed (Frank)", and docs/58:169 tracks it as an open item. (b) That correction exists and is the campaign's designated instrument: docs/59-a0-roster-correction-2026-08-28.md, created in the very next commit (ef56943), whose §4:252-253 reads "A7.2 is already CONFIRMED ... Mn/Fe/Ti complete the census from 3/6 scored to 6/6." (c) docs/58:120 declares the banked JSON the authority for its caveats ("banked in a0main_readout.json['caveats']; they travel"), and that artifact is CURRENT: caveats.coverage_shortfall ends "Live status at scoring time: Mn scored complete; Fe scored complete; Ti scored complete", and a7_2 carries pending_data = [] and unrun_blind_metals = []. A reader following docs/58's own stated chain of authority lands on the complete census, not the stale one.
                 
                 5. The proposed remedy conflicts with repo convention. The finding asks that line 117 "should now read" the completed census — i.e. rewrite pre-result narrative in a dated analysis doc. This repo's established practice for overtaken text is the opposite: preserve the original sentence and stamp an in-place dated banner (docs/29:48 "⚠ CORRECTION 2026-08-06 (docs/38 §3). The sentence above is true as written and ..."; docs/33:91; docs/34:33; docs/38:132 "⚠ SUPERSEDED 2026-08-06 by docs/39 §6"), and docs/59:85-86 states the same stance for its own dated additions ("kept separate from the pre-launch text above so the record of what was written before results existed stays intact").
                 
                 Verified as accurate but not defect-bearing: readout a7_2.metals_with_flip = [Cr, Fe, Ir, Mn, Ru], metals_measured = [Cr, Mn, Fe, Ru, Ir, Ti], flip_brackets gives Mn two brackets ([0,1.5] 3->2 and [6,7.5] 2->1) and no Ti entry, so 5-of-6 with Ti flat is correct. That does not make docs/58:117 wrong; it makes it a dated snapshot that the live artifact and docs/59 have already superseded on the record.

[R17] [staleness] docs/58's coverage caveat says Mn/Fe/Ti "were never built"
    WHY REFUTED: REFUTED on five independent grounds.
                 
                 (1) TIMELINE — the sentence was true when written. docs/58 was committed at d4c167b, 2026-08-28 13:47:14 -0400. The extension that built Mn/Fe/Ti was directed and staged at ef56943, 2026-08-28 16:06:09 -0400 — two hours nineteen minutes LATER (it is the commit that first creates docs/59 and runs/Ti_slab/). At the moment docs/58 was authored and committed, Mn/Fe/Ti genuinely had no A0 grid. The file is date-stamped in its own filename (58-a0-analysis-2026-08-28.md) and its provenance header pins it to specific frozen artifacts: "Evidence: commits `06a99d0` (wave 2) and `904a40b` (wave 3)". It is a dated one-shot analysis record, in the same series as 44/46/48/49/51/52/53/54/55/56/57 — not a living document. A dated record that was accurate at its timestamp is not "stale"; that is what a dated record is.
                 
                 (2) THE CAVEATS' CARRIER IS THE JSON, AND THE JSON IS CURRENT. docs/58:130 heads the caveat list "## 5. Caveats (banked in `a0main_readout.json["caveats"]`; they travel)". The doc names the artifact, not itself, as the thing that travels into downstream text. That artifact is correct: caveats.coverage_shortfall now carries the original shortfall AND a "REMEDIATION:" clause naming build_a0main_w2/_w2b/_w3/_w2c, the entrant's verbatim direction, and "Live status at scoring time: Mn scored complete; Fe scored complete; Ti scored complete." The failure scenario's premise — that the report's limitations section is written from docs/58's caveat list — contradicts the document's own stated mechanism. Reinforcing this: docs/58 §7, the "What the report may and may not say" table that actually governs report claims, contains no coverage/roster sentence at all, so the disputed wording has no path into the report through the doc's own guidance channel.
                 
                 (3) ALREADY DISCLOSED AS AN OPEN ITEM, NOT ASSERTED AS SETTLED. The very sentence quoted ends "— **a dated correction of record is owed (Frank)**; unscored-at-freeze becomes WITHDRAWN-UNSCORED under A7.7", and §8 Open items repeats "Frank: dated roster correction (item 5 above)". The doc flags the roster as an unresolved defect with a named owner and a registered disposition rule. That is disclosure, not a false claim of record.
                 
                 (4) THE CORRECTION WAS MADE THROUGH THE CORRECT MECHANISM AND IT EXISTS. The remedy the finding demands is retro-editing a dated analysis snapshot. The campaign's convention is the opposite: corrections go into a NEW dated document. docs/59-a0-roster-correction-2026-08-28.md is exactly that, titled "Correction of record: the A0-main metal roster, and its completion", created 2h19m after docs/58 and updated through 2026-08-29 (its §1 even records its own in-place correction: "An earlier draft of this document cited docs/43:279 ... corrected 2026-08-29 after the wave-4 audit"). Its §1 states the shortfall "has been disclosed in the banked readout ... since the same day"; §2 quotes the entrant's direction; §3 documents tranches 2/2b/3 and §3d what 2c returned. The record as a whole is complete and coherent; only an obsolete snapshot reads otherwise.
                 
                 (5) THE PROPOSED REPLACEMENT TEXT IS ITSELF WRONG, in the opposite direction. It would have docs/58 say "all six metals score complete" and that "what remains owed is Frank's countersignature and deposit of docs/59, not the build." Both halves overstate. docs/59 §3c records that A6.6 (docs/43:1283-1289) registers "**~160 fixed-geometry SCFs and zero relaxations**" and "does **not** license ... any relaxation in any cell", while the Ti arm ran SEVEN relaxations in the 1x1 cell (4 in tranche 3 + s0_OOH_r1/r2/r3); docs/59 states plainly "the licence is the entrant's to grant or withhold when countersigning this document. If withheld ... A7.3's denominator shrinks and the Ti rows are WITHDRAWN-UNSCORED under A7.7." So Ti's completeness is contingent on a licence not yet granted — more than countersignature is owed, and the build's admissibility is precisely what is open. Separately, caveats.mn_afm records A7.5's Mn AFM condition as "REGISTERED CONDITION, CURRENTLY UNMET". Writing "all six metals score complete" into a dated doc would be the quiet-rescoping error the finding accuses docs/58 of.
                 
                 FACT CHECK OF THE UNDERLYING NUMBERS (the finding's factual premises are correct, they just do not support the defect): runs/a0/main .out counts are Cr 76 / Ru 32 / Ir 32 / Mn 32 / Fe 39 / Ti 28 = 239 total; the JSON a7_3 block scores all six metals. So the metals were indeed built. That makes docs/58 an outdated snapshot, not an inaccurate record.
                 
                 RESIDUAL (deliberately not reported as a defect): docs/58 carries no by-name forward pointer to docs/59. That is a courtesy, not a defect — docs/58 already names the correction as owed, and docs/59 sits at the next doc number with a self-describing title. Per the brief, this is a wish, not a finding.

[R18] [staleness] docs/58 reports the A0 block as 162/162 converged with zero escalation events; the block is now 239+ SCFs with 7 escalation events
    WHY REFUTED: Refuted on four independent grounds.
                 
                 (1) SCOPE IS EXPLICIT IN THE SENTENCE ITSELF. docs/58:39 reads in full: "Compute: Anvil (ACCESS CHE260157), arrays 20183040/20183041/20183150. **162/162 SCFs converged; zero A6.5(2) escalation events across the whole block.**" Those three arrays ARE tranche 1. The failures the finding cites came from different arrays: docs/45:2174 records "Array 20196817 (tranche 2, Mn/Fe, 59 tasks) and array 20196856 (tranche 3 stage 1, TiO2 geometry chain, 4 tasks)". The sentence never claimed over the arrays that failed. "The whole block" distinguishes all three arms plus the gate from just the main array (docs/45:2121 makes the narrower claim "Array 20183040 completed 140/140"), which §1 of docs/58 enumerates immediately above as 140 + 20 + 2.
                 
                 (2) THE NUMBER IS TRUE AS WRITTEN. I re-derived it from raw evidence rather than the doc: runs/a0/main/{Cr,Ru,Ir} contain 76 + 32 + 32 = 140 .out files and `grep -l "convergence NOT achieved"` returns ZERO for all three metals; runs/a0/cell/*.out = 20; P-PROJ = 2. 140 + 20 + 2 = 162, with zero convergence failures. There is no arithmetic or factual error to correct.
                 
                 (3) THE EXCLUSION IS DISCLOSED TWICE INSIDE docs/58 ITSELF. Line 117: "The unrun blind metals (Mn, Fe, Ti) can only add flips." Caveat 5, lines 133-136: "A6.3 registers 'Ru and Ir as well as the 3d metals'; Mn/Fe/Ti were never built (allocation chosen 2026-08-27, no dated amendment) — **a dated correction of record is owed (Frank)**; unscored-at-freeze becomes WITHDRAWN-UNSCORED under A7.7." No reader can take 162/162 as a six-metal claim when the same document states three of the six have no grid at all. docs/59 is that owed correction and §3, §3c and §3d record every failure explicitly.
                 
                 (4) THE FAILURE SCENARIO IS MECHANICALLY IMPOSSIBLE UNDER THE REGISTRATION. docs/43:1594-1605 registers A8.4 as "a **per-metal, per-state** convergence-failure rate as a reported quantity ... A metal whose failure rate exceeds **20%** on any state has that state's contribution marked low-confidence." The registered reporting unit is per-metal/per-state, so no A8.4 table can be assembled from a block-level aggregate, and docs/58 supplies no Fe or Ti row that could be mistaken for a clean one. Moreover the true A8.4 numbers are already written down in two places: docs/45 ("Fe s0_O pre-repair failure rate is 2/8 = 25%, over the 20% line; reported either way, and the failed .outs stay on disk") and docs/59:98 ("Failed .outs retained; A8.4 reports the 2/8 = 25% pre-repair rate either way"). Nothing in the campaign would source an A8.4 rate from docs/58's compute line.
                 
                 Additionally: docs/58 is a DATED snapshot (filename 58-a0-analysis-2026-08-28.md, header "— 2026-08-28") whose provenance block pins specific commits (06a99d0 wave 2, 904a40b wave 3) and specific banked artifacts. The failures it allegedly omits are dated 2026-08-29 events (docs/45 heading: "A0 tranches 2b/3 -- the first convergence failures ... (2026-08-29)"). And the revision is ALREADY a tracked open item: tasks/todo.md:728 reads "When Ti stage 3 lands (8 jobs, build_a0main_w3b.py): re-score ... Then **docs/58 update** + adversarial verify + bank." So this is a scheduled revision of a correctly-scoped dated snapshot, not undisclosed staleness.
                 
                 Finally, docs/58 is not among the uncommitted files this audit gates — `git status` shows it unmodified; the uncommitted set is the 24 Ti run files, docs/figs/a0main_readout.json, src/dft/a0main_readout.py, and two CI files.
                 
                 Incidental correction to the finding's own evidence, not enough to save it: the Ti chain failed THREE times, not the two implied elsewhere — runs/Ti_slab/s0_OOH.out, s0_OOH_r1.out and s0_OOH_r2.out each contain "convergence NOT achieved" with no "Begin final coordinates" block, and only s0_OOH_r3.out converged. That is already correctly stated in docs/59 §3d and in the regenerated a0main_readout.json caveat.

[R19] [staleness] tasks/todo.md prices Frank's open nspin=2 decision at 24 banked SCFs; the Ti arm now holds 32
    WHY REFUTED: REFUTED on the definition of "banked", verified against git rather than the filesystem.
                 
                 1. The number is currently EXACT, not stale. `git ls-files "runs/a0/main/Ti/*.out"` returns 21 (slab/s0_O/s0_OH x 7 U; zero s0_OOH rungs) and `git ls-files "runs/probe/Ti_audit/*.out"` returns 3 (slab__base, s0_O__base, s0_OH__base). 21 + 3 = exactly 24 banked Ti SCFs. The finding's count of 32 came from `ls` on the working tree, which includes the 8 stage-3 files that `git status` reports as untracked (`?? runs/a0/main/Ti/s0_OOH__u000..u900.out` and `?? runs/probe/Ti_audit/s0_OOH__base.out`). Those 8 are on disk and NOT banked -- and this very audit is the gate deciding whether they may be committed. The finding counts unbanked, audit-pending files as banked.
                 
                 2. "Banked" is a defined term in this campaign meaning committed to the repo, not "run". tasks/todo.md:705 draws the line explicitly: the few-kB Lowdin extract "is banked as `<job>.lowdin.txt` per deck; the ~4.3 MB full projection bodies + pdos files are retained on Anvil beside the decks, not in git." tasks/todo.md:727 "Wave-4 audit before banking" and :728 "adversarial verify + bank" both place banking after the audit, at the commit. So 24 is the correct count under the repo's own vocabulary and stays correct until this audit greenlights the commit.
                 
                 3. The line is not an orphaned stale item in a live frame. tasks/todo.md is clean at HEAD bb17152 (git status shows only .github/ci/run_oc20.py, .github/workflows/s1-controls.yml, docs/figs/a0main_readout.json, src/dft/a0main_readout.py modified). The unchecked item immediately above line 729, at :728, reads "When Ti stage 3 lands (8 jobs, build_a0main_w3b.py): re-score ... Then docs/58 update + adversarial verify + bank" -- the whole tail sits in an explicitly pre-bank frame, and the 8 extra jobs are named on the adjacent line, so a reader arriving at 729 has just been told the increment.
                 
                 4. The failure scenario points the wrong way and the correction offered would not fix it. An nspin=2 convention change re-does the GEOMETRY, so it also re-runs the Ti arm's relaxations. docs/59 s3c's dated addendum ("Calculation-class disclosure (added 2026-08-29, after the wave-4 audit)") already enumerates them: "4 in tranche 3 (slab + 3 adslabs) and 3 escalation repairs (`s0_OOH_r1`/`r2`/`r3`), seven in total, all in the 1x1 cell", plus "Scale update: the ~246 above becomes ~250 with this round". The document the todo line cites therefore already carries current, dated cost accounting. Substituting 32 for 24 would replace a term-correct figure with a term-incorrect one while still omitting the seven relaxations that dominate the real cost.
                 
                 Default-to-refuted also applies: the increment is disclosed on the adjacent line and the cost accounting is disclosed in the cited document.

[R20] [staleness] docs/56 calls Ti "a permanent one-cell metal this campaign unless 1x1 TiO2 decks are commissioned" -- they have been commissioned and all four states are relaxed
    WHY REFUTED: The physical premise is true but the defect is not. Five independent grounds:
                 
                 (1) docs/56 is a FROZEN DATED SNAPSHOT, not a living document. `git log -- docs/56-s3-analysis-2026-08-24.md` returns exactly ONE commit (8ab8f31, 2026-08-24) and the date is in the filename. Every peer doc in the series is likewise date-stamped (54-...-2026-08-23, 55-...-08-24, 57-...-08-27, 58-...-08-28, 59-...-08-28) and superseded by a HIGHER NUMBER, never edited in place. The one living document, docs/43, is amended — and when it is, an `-archive-` copy is written first (three such files sit in docs/). Rewriting a 08-24 analysis to reflect 08-29 facts is the convention violation, not the fix.
                 
                 (2) The sentence was TRUE on its date and already carries its own escape clause. runs/Ti_slab was created by `git log --diff-filter=A`: slab.out/s0_O.out at d1e4bc2 (2026-08-29), s0_OOH_r3.out at bb17152 (HEAD, 2026-08-29). Nothing existed on 2026-08-24. And :562 explicitly conditions itself — "unless 1×1 TiO2 decks are commissioned" — so it never asserts unconditional permanence; a reader who tests that condition is directed straight to the successor doc.
                 
                 (3) ALREADY DISCLOSED in the successor of record. docs/59:74-78 records the chain verbatim ("`qe_slab.py build Ti --supercell 1` ... -> relax slab + 3 adslabs"), and docs/59:180 records all seven, "all in the 1×1 cell." The finding cites this text as its own evidence, which concedes the disclosure exists.
                 
                 (4) ALL THREE VERDICTS STILL HOLD TODAY, so no reader is misled about status. Δ_sym @1×1 = E(off) - E(mir) (docs/56:74); runs/Ti_slab holds only ONE placement per state, so :98 UNSCORED stands. A8.1 needs four corners, so :212 NOT COMPUTABLE stands. And :562 is scoped to "unreachable by ROUND 3" — a concluded 13-deck compute round enumerated at :545-558 containing zero Ti decks; the A0 tranche-3 build is a different work item entirely. Only a parenthetical rationale is overtaken, never a verdict.
                 
                 (5) The proposed replacement wording would assert MORE than the record permits, and its cost model is wrong twice over. (a) LICENSING: docs/59 §3c (2026-08-29) says these exact seven relaxations are "new compute outside A6.6's declared footprint" — A6.6 (docs/43:1283-1289) registers "zero relaxations" and "does not license ... any relaxation in any cell" — and states "the licence is the entrant's to grant or withhold when countersigning this document. If withheld ... the Ti rows are WITHDRAWN-UNSCORED under A7.7." Writing "the decks exist, only the off members are missing" into a settled table would launder unlicensed, un-countersigned compute into banked status. (b) MIR STANDING: runs/Ti_slab/s0_O.in carries `nosym=.true. / noinv=.true.`, byte-identical in class to runs/Fe_slab/s0_O.in — precisely the reuse class docs/56 §5-A (:423-431) calls "nosym ON_PLANE/EXPLORED — 'neither registered arm'" and names the campaign's "highest-leverage single ruling," the one that swings the P-SYMCOV both-coverage count 5->3. Mn and Fe 1×1 rows are tagged MIR-MEMBER-STANDING-OPEN for exactly this reason (:82-86). A Ti deck of that class is not a banked mir member. (c) ARITHMETIC: three adsorbate states need THREE off decks, not the finding's "cheap two-deck top-up."
                 
                 The finding is a request to edit a dated record so it reads as of a later date, replacing a correct, self-qualified, already-superseded statement with a stronger claim the licensing record explicitly withholds.

[R21] [prereg-discipline] docs/59's scope claim names only the numerics as unregistered, and its bounded-authority guarantee is proved only over the branch that did not occur
    WHY REFUTED: REFUTED — the two things the finding says are concealed are both disclosed, in the same section of the same document and in the banked artifact.
                 
                 (1) "Only one unregistered element, and it is numerics." docs/59:174-195 (the "Calculation-class disclosure," same section §3c, 17 lines below :157) quotes A6.6's "~160 fixed-geometry SCFs and zero relaxations" / "does not license ... any relaxation in any cell," names s0_OOH_r3 explicitly among "4 in tranche 3 ... and 3 escalation repairs (s0_OOH_r1/r2/r3), seven in total," states "they are new compute outside A6.6's declared footprint, and are stated here rather than absorbed," and then: "That is an explanation of why they were run, NOT A LICENCE; the licence is the entrant's to grant or withhold when countersigning this document. If withheld ... A7.3's denominator shrinks and the Ti rows are WITHDRAWN-UNSCORED under A7.7." That is precisely the second unregistered, entrant-licensable element the finding says the entrant is never told about — and it sits above the §5 countersign ask (:262-266). Its discretionary/basin-selecting character is conceded one sentence above the attacked line (:150-151, "The start DOES select which local minimum BFGS reaches -- that is the point of r3").
                 
                 (2) "Bounded authority proved only over the branch that did not occur, hiding that it flipped a verdict." The verdict-deciding role is disclosed three times: docs/59:238-245 ("A7.3 (P-FLOOR-U) is genuinely at stake: it stands at 3 of 5 against a registered threshold of >=4 ... Ti is the only metal left"); docs/59:254-257 ("A7.3 ... becomes scorable on the full 6-metal denominator once Ti's *OOH converges; its own text conditions the denominator on 'a converged *OOH geometry'"); and in the artifact itself, docs/figs/a0main_readout.json caveats.fixed_geometry: "Ti is the metal that DECIDES A7.3 ... The banked geometry comes from s0_OOH_r3, RE-ANCHORED by build_a0main_w2c.py to the mean of Ti's own two converged Ti-O bond lengths (1.781905 A) before relaxing." No entrant reaching the countersign line can hold the belief the failure scenario requires.
                 
                 Also checked and true on the merits: the extension's numerics reach no banked number. runs/a0/main/Ti/s0_OOH__u450.out:54-56 shows "scf convergence threshold = 1.0E-06 / mixing beta = 0.3000 / number of iterations used = 8 local-TF", i.e. campaign-standard ndim 8 and maxstep 200, matching docs/59:231-236. The 5->6 denominator movement is real (git show bae6cd2:docs/figs/a0main_readout.json gives denominator 5, pending_no_endpoints ['Ti'], status "NOT YET MET -- UNDECIDED"; working tree gives 6, [], "NOT MET"), but that movement is the mechanism A7.3's own registered text prescribes (docs/43:1370-1371, ">=4 of the 6 metals WITH A CONVERGED *OOH GEOMETRY"), and docs/59:161-162 names that mechanism itself ("the only thing this round can do is FILL a gap that (iii) would otherwise leave").
                 
                 Residual, not reportable as a defect: the phrase "The one thing here that is NOT in the registration" (:157) is locally over-broad relative to the paragraph added below it the same day, and "cannot ... flip a verdict" (:164) is loose shorthand for the precise and correct claim at :231-236. Both are wording residue from layered same-day edits after the wave-4 audit, corrected in the same section rather than left standing — a copy-edit, not a scope misrepresentation, and the standing rule that labels travel into every artifact is satisfied by the fixed_geometry caveat.

[R22] [prereg-discipline] The a7_3 result block carries no conditional label for the metal that decided it, while its sibling a7_2 does
    WHY REFUTED: The finding asserts that Ti's conditionality is "stated only in caveats.spin_state" and that a7_3 carries no label for its deciding metal. Both halves fail on the files.
                 
                 1. The label the finding says is missing is the exact thing the uncommitted hand-edit added. `git diff src/dft/a0main_readout.py` shows the rewrite of `caveats.fixed_geometry` (src/dft/a0main_readout.py:805-826) replacing the stale "its *OOH geometry does not exist at all -- which is why the Ti grid scores 0 of 7" with: "The *OOH geometry carries a further provenance difference that travels with every Ti number below, **and Ti is the metal that DECIDES A7.3**: it is the only geometry in this readout whose starting structure was not emitted by gen_rutile.py..." It then gives the full re-anchor provenance (3.167 -> 3.414 A walk-up, two failed relaxations, s0_OOH_r3 re-anchored to 1.781905 A, 52 ionic steps, d(O,Ti) = 2.041 A, r2 failed again) and chains explicitly to the other condition: "The residual open question for Ti is the spin convention, not the geometry -- see [spin_state]." So the geometry provenance is labelled, bound to A7.3 *by prediction ID*, bound to Ti *by deciding role*, and cross-linked to spin_state. Grepping the banked artifact for "A7.3" returns four hits and one of them is this caveat (docs/figs/a0main_readout.json offset ~37944).
                 
                 2. The claimed a7_2/a7_3 asymmetry at the per-metal level does not exist. a7_2.flip_brackets["Ir"] = [[3.0, 4.5, 3, 2]] -- four bare numbers, no label field, exactly the shape the finding objects to in a7_3.per_metal["Ti"]. Neither block labels per-metal entries; both put conditionality at note/caveats level. And a7_2's note is itself a *pointer into the same sibling key* ("see caveats.ir_ooh_basin"), so the "sibling key it never reads" failure mode applies identically to the block the finding holds up as correct.
                 
                 3. There is no per-metal labelling scheme anywhere in this artifact, by design, and the finding's consumer scenario would break every number in it, not Ti's. caveats.fixed_geometry's first sentence ("A6.4, registered: every point is a fixed-geometry single-point SCF") conditions all 6 metals x 7-8 rows. caveats.mn_afm names both siblings by ID -- "it may be used for the within-metal, U-response claims it was registered for (A7.2's flip census, A7.3's span) but NOT as a materials-facing absolute eta" -- and Mn is one of the three metals *over* the floor, i.e. a metal carrying the CONFIRMED side of the count, also unlabelled in per_metal. caveats.cell, caveats.u000_decks and the gas-reference disclosure are likewise top-level and universal. The container is annotated as such at src/dft/a0main_readout.py:799 ("registered + measured caveats (travel with every table above)") and printed under that banner at :911. A consumer that reads per_metal and drops caveats loses the Mn FM condition, the 1x1 retired-cell condition, and the fixed-geometry condition on literally every point -- Ti's row is not a special exposure, and Ti is in fact the single most-labelled metal in the file, the only one whose deciding role is named in prose.
                 
                 4. Adjacency on stdout is as tight as the script allows: the A7.3 verdict prints at :772-785, `result["a7_3"]` is assigned at :786, and the very next section printed is CAVEATS at :911, whose first entry is [fixed_geometry] naming Ti as the metal that decides A7.3.
                 
                 The residual true observation -- a7_3.note discusses the Cr interval mismatch and does not restate Ti -- is a choice about where a disclosure lives, not a missing disclosure: the note field in both blocks is scoped, the cross-reference exists in the artifact (caveats -> A7.3 by name), and the campaign's uniform convention is that physics/method conditionalities live in caveats and are keyed back to prediction IDs there. That is taste about redundancy, and the material is already correctly disclosed. Nothing here is quiet rescoping: A7.3 is reported "NOT MET" against the registered >=4 of 6 with the failure stated plainly.

[R23] [evidence-integrity] The pull banks a 4th state into runs/probe/Ti_audit/ without extending that directory's probe_manifest.json, so the campaign's own probe scorer still reports TiO2 as having no *OOH
    WHY REFUTED: Both load-bearing claims are refuted against the raw files.
                 
                 (1) "Nor is the *OOH relaxation reference banked in the other manifest ... the tranche_3b block at line 277 carries none" is FALSE. runs/a0/main/manifest.json tranche_3b.selection_rule_outcome.winner_energy_eV = -17712.319507586788 IS the s0_OOH relaxation reference, computed by the SAME function that fills relax_reference_ev: build_a0main_w3b.py:119 calls P.relax_final_energy_ev(runs/Ti_slab/s0_OOH_r3.out), and probe_decks.py:415 calls relax_final_energy_ev(outp) to populate the manifest field at probe_decks.py:424. Same quantity, same code path, different key name. The finding's own Evidence paragraph concedes this ("The missing reference IS recoverable"), contradicting its Claim.
                 
                 (2) "Anyone re-deriving Ti from the probe layer after this commit gets the wrong answer with a clean bill of health" is FALSE on both halves. I ran the control by hand: qe_qc.trusted_energy_ev("runs/probe/Ti_audit/s0_OOH__base.out") = -17712.319505409876 eV, drift +0.0022 meV vs the banked reference -- three orders of magnitude inside probe_eta's 5 meV GATE-1 tolerance. So the extraction control is not merely recoverable, it PASSES. And probe_eta.py emits no answer at all for Ti: its terminal line (src/dft/probe_eta.py:194) is "No variant has a complete slab + OH + O + OOH set yet." -- a batch-completeness statement carrying the word "yet", not the physical claim "TiO2 has no *OOH". No eta, no pls, no binding assertion is printed. Getting no number is not getting a wrong number, and nothing in that output is a "conclusion" that could be "re-emitted".
                 
                 (3) Had the manifest listed s0_OOH, the number probe_eta would print is eta = 1.740651 V, pls 2 (I computed it with the campaign's own hea_oer.referencing.delta_G + descriptors.oer_overpotential and the same cached gas refs). That is identical to the readout of record's Ti u000 row (docs/figs/a0main_readout.json, Ti rows[0] eta = 1.7406499). The probe layer and the a0 layer agree exactly, so there is no divergence for any reader to be misled by.
                 
                 (4) Ti_audit is not a scoring layer. build_a0main_w3.py:97-106 builds it through cmd_build with variants=["base"] only; a single-variant batch has nothing to score against a control (d_eta vs base is definitionally zero). It exists as the staging directory for the A0 base decks and as the u000 comparator named at a0main_readout.py:112. lit1_urobustness.py:283 reads only runs/probe/Cr and Co_uladder; nothing in .github/ touches Ti_audit. The only consumer that would reach it is the legacy Vast-box script collect_probes.sh:59, whose loop prints the completeness line above.
                 
                 (5) The verdict-bearing scorer is a0main_readout.py over runs/a0/main/Ti, and its own control DOES cover the deciding state: rerun_determinism_check_meV.s0_OOH = 0.00558 meV, control_uncompared_states = [], gaps = [], scored_rows = 7.
                 
                 (6) Already disclosed: docs/45 error-ledger trap 2 (~line 2094) and docs/58:62 both state that A0-main's U=0 "control" is a same-deck re-run determinism check and where the genuine geometry-extraction control lives per arm; build_a0main_w3b.py:55-57 discloses the cmd_build bypass explicitly.
                 
                 What survives is a documentation-precision residual far below the claimed "major" severity, recorded in corrected_claim: the Ti_audit manifest inventories 3 of the 4 states now in that directory, so the readout's control_note pointer is imprecise for one state. No number, no control, and no verdict is affected, and the A7.3 NOT MET verdict is untouched.

[R24] [evidence-integrity] Slurm array 20215155 is recorded in no committed file, so 24 outputs are banked with no execution record
    WHY REFUTED: The one factually true kernel — `grep -rn '20215155'` across the repo returns zero hits — does not support the defect the finding builds on it. Four independent checks refute it.
                 
                 (1) THE DECISIVE ONE: recording the array id happens at BANK time, one or two commits AFTER the .out files are committed, and this audit runs BEFORE the bank commit. `git grep -c '20214003' 903f15f` returns ZERO HITS: at the commit that banked tranche 2c's Fe `.out`/`.lowdin.txt`/`.projwfc.in` files, array 20214003 appeared nowhere in the repo. It first entered at bae6cd2 (the wave-4 audit findings file) and reached tasks/todo.md only at bb17152 — one and two commits later. `git log -S` first-appearance for 20196817/20196856 is 92b8d87 and for 20204305 is bcd3cc0, both the pre-launch REGISTRATION commits. So the campaign has exactly two recording moments, pre-launch registration or post-audit bank, and the current tree is sitting in the gap between them exactly as it did for tranche 2c. The finding condemns a transient the workflow resolves within the same day.
                 
                 (2) The pending step is explicitly disclosed in the COMMITTED ledger: tasks/todo.md:728 (unchecked) reads "When Ti stage 3 lands (8 jobs, build_a0main_w3b.py): re-score ... Then docs/58 update + adversarial verify + bank". The bank is a scheduled, open checklist item naming this exact builder and job count.
                 
                 (3) The manifest.json pattern claim compares against the exception. Only 1 of 5 tranche blocks carries an "array" field (tranche_3/stage_1 = 20196856); tranche_2, tranche_2b, tranche_2c and tranche_3b all have none, and their ids live in tasks/todo.md:722-726. runs/a0/main/manifest.json is not the campaign's array register; tasks/todo.md is, and it is written at bank time.
                 
                 (4) The failure scenario overstates what an array id buys. `find runs -iname '*slurm*' -o -iname '*.err'` returns NOTHING — no slurm/node logs are committed for ANY array in this campaign, including the recorded ones. The 8 .out files carry "MPI processes distributed on 1 nodes" and a memory figure but no node name, and so do the tranche-2/2b/2c outputs. Recording 20215155 would therefore NOT make EXCLUDE=a024,... compliance checkable inside the repo for this tranche any more than it is for 20196856; it only yields a key to an external `sacct` query, and that query is trivially reconstructible from the manifest plus the timestamps already inside the outputs ("29Aug2026 at 16:48:16"–16:51:21, 8 tasks, user x-fcai3). The docs/45:92 precedent is also mis-read: that OOM was diagnosed from the retained `.out.attempt1` files (the ledger says so — "failed attempts preserved as `.out.attempt1`", and the same convention is live untracked right now at runs/probe/Co_uladder/slab__base.out.attempt1), with the array ids recording the resubmission after the fact, not enabling the diagnosis.
                 
                 No registered clause in docs/43 requires array-id logging, so no standing rule is violated either. What remains is a housekeeping reminder for a commit that has not happened, not a defect in the work under audit.

[R25] [evidence-integrity] A stale untracked Co artifact sits in this pull's commit path and would be swept into the Ti commit
    WHY REFUTED: REFUTED on both prongs; the underlying facts are true but support neither defect argument.
                 
                 PRONG 1 (would be swept into the Ti commit) — empirically falsified. The file's mtime is 2026-08-09 01:52. Since then there have been 117 commits touching runs/ and 52 touching runs/probe/, none of which swept it in. The decisive instance: HEAD bb17152 — the immediately preceding commit of this very Ti campaign — added files under runs/probe/ (Ti stage 3) and did not touch runs/probe/Co_uladder/. The "natural staging command" the finding predicts has now been not-used 117 consecutive times, including once by this campaign. The operator's standing rule is also explicit: "Use specific file paths when staging — never `git add .` blindly."
                 
                 Even in the counterfactual, no harm: src/dft/qe_qc.py:255 and src/dft/qe_frames.py:174 both use `pat = "**/*.out*" if all_attempts else "**/*.out"`, so attempt files are excluded from QC/frame ingestion by default and are opt-in only. docs/figs/symops_audit.csv:47 lists probe/Co_uladder/slab__base.out and not the attempt. Committing it would pollute no scored quantity.
                 
                 THE COMPARISON CLASS IS WRONG. The finding cites runs/Co_slab/s0_O.out.attempt1 etc. as proof the repo tracks this class. It does — but only in a documented, different scope. All 89 tracked .out.attempt* files live in runs/*_slab/, runs/s3/*, and runs/a0/cell; ZERO live under runs/probe/, across all 29 probe directories. That matches docs/26:136 ("every attempt's output archived as `runs/<M>_slab/<job>.out.attempt*`") and docs/26:214 ("the full `.out.attempt*` failure trail committed on this branch (`runs/<M>_slab/`)"). The Co probe attempt being untracked is the uniform state of the probe tranche, not an anomaly within it.
                 
                 PRONG 2 (the "inverse defect": A8.4 unhonoured) — refuted on scope and chronology. docs/45:95 is not a policy statement; it is a parenthetical inside the S3 wave-1 execution record dated 2026-08-24, describing array 20097663 -> 20101963's node-a024 OOM resubmission. A8.4 itself (docs/43:1594, "Convergence-failure budget (error class 5)") requires that "S3 records a per-metal, per-state convergence-failure rate as a reported quantity," with a 20% low-confidence flag; it says nothing about archiving .out.attempt files, is scoped to S3, and was adopted 2026-08-23 — two weeks AFTER the Aug-8/9 Vast probe. Applying it retroactively to that probe is itself the quiet-rescoping error the campaign's rules forbid.
                 
                 Moreover the retry IS already disclosed in the committed record: runs/probe/Co_uladder/slab__base.in.hard is tracked and differs from slab__base.in only in the SCF-hardening knobs (mixing_beta 0.3 -> 0.05, electron_maxstep 200 -> 500, + mixing_ndim = 16). A stall-then-harden retry is legible from the committed decks alone. Only the stalled output is absent, and it carries no bankable number — it dies mid "iteration #116" at estimated scf accuracy < 0.00123780 Ry with no JOB DONE.
                 
                 VERIFIED-TRUE SUB-FACTS (which nonetheless do not add up to a defect): git status --porcelain shows 25 untracked, not 24; git check-ignore -v exits 1 (not ignored — .gitignore covers tmp_*/ and *.run.in only); git log --oneline --all over the path is empty; size is 80,721 bytes; tail ends at "iteration #116 / Davidson diagonalization with overlap" with no JOB DONE while the tracked sibling slab__base.out completed; probe_manifest.json does not mention it. One count is understated: the repo tracks 89 attempt files, not the ~20 implied by "and 17 more."
                 
                 The file is a three-week-old, convention-consistent local artifact of the Aug-8/9 Vast probe wave. It is not part of this pull, was not created by it, and is no more endangered by this commit than by the 117 before it.

[R26] [evidence-integrity] The blindness claim for these 8 jobs cites a mutable path plus a date stamp that cannot distinguish the pre-launch artifact from the post-launch one this pull writes over it
    WHY REFUTED: The finding's raw facts check out (HEAD's artifact is 20:41:03 / 903f15f / denominator 5 / "NOT YET MET -- UNDECIDED"; the working-tree replacement is 23:10:32 / bb17152 / denominator 6 / "NOT MET"; both fall on 2026-08-29), but the failure scenario it builds on those facts does not occur.
                 
                 (1) The citing sentences carry the discriminating content themselves. runs/a0/m_a0_ti_ooh.txt:18-20 states A7.3 "stands at 3 of 5 against a registered threshold of >=4 with Ti the only metal left", and runs/a0/main/manifest.json:310 states "stood at 3 of 5 vs a registered >=4". The post-pull artifact is 3 of 6 with pending_no_endpoints []. A reader opening the cited path after the commit does not face an indistinguishable artifact -- they face one whose denominator contradicts the sentence that cited it, which is exactly the signal that it is not the banked one.
                 
                 (2) The pre-launch state is preserved in committed prose in four places that this pull does not touch (git status shows only .github/ci/run_oc20.py, .github/workflows/s1-controls.yml, docs/figs/a0main_readout.json and src/dft/a0main_readout.py as modified): docs/59-a0-roster-correction-2026-08-28.md:240-245 records the full per-metal blind state ("3 of 5 against a registered threshold of >=4, with Cr 0.3435, Fe 0.6102 and Mn 0.6307 over the 0.10 V floor and Ru 0.0922 and Ir 0.0637 under it. Ti is the only metal left"); docs/45-error-ledger.md:2307 records the same 3-of-5; src/dft/build_a0main_w3b.py:69-70 records it again, and m_a0_ti_ooh.txt:2-3 explicitly orders the reader to that docstring. The number the blindness claim rests on is therefore recoverable from text, not only from a superseded JSON.
                 
                 (3) Recovering the artifact needs no foreknowledge of a hash. `git log --oneline -- docs/figs/a0main_readout.json` returns bae6cd2 at the top with a self-describing message ("bank the 6-metal readout: A7.3 now SCORED and NOT YET MET at 3/5 with Ti deciding"). `git diff bae6cd2 HEAD -- docs/figs/a0main_readout.json` is empty, so bb17152 -- the commit that introduced both manifests, returned by `git log -1 -- runs/a0/m_a0_ti_ooh.txt`, and left unmodified by this pull -- holds the pre-launch artifact. Path-plus-commit is the ordinary way a repo citation resolves; the claim that recovery "requires knowing to run git show 903f15f:..." is not accurate.
                 
                 (4) Blindness does not actually depend on the timestamp at all. `git ls-tree -r HEAD runs/a0/main/Ti/` contains the seven s0_OOH__u*.in decks and zero .out files -- every deciding Ti output is untracked and first enters the repo in this pull -- while the banked artifact independently records pending_no_endpoints ["Ti"] and denominator 5, i.e. it demonstrably scored no Ti endpoint whatever existed on disk. The date stamp is a convenience label over a proof that is already structural.
                 
                 What remains is that the manifests abbreviate a second-resolution ISO stamp plus commit hash down to a calendar date. That is a citation that could be tightened, not a defect that loses evidence: the artifact is self-identifying, the state is in prose in four committed files, and the commit graph proves the blindness independently. Reporting it would be a wish about citation precision on a record that is already disclosed elsewhere.
