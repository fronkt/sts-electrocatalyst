# 58 — A0 closed: the η(U) grid, end to end — 2026-08-28

**Provenance:** AI-drafted disclosed infrastructure (analysis narrative over banked,
adversarially verified artifacts; every number below is quoted from a committed JSON or
output, never recomputed here). Evidence: commits `06a99d0` (wave 2) and `904a40b`
(wave 3); banked artifacts `docs/figs/pproj_readout.json`, `docs/figs/a0cell_readout.json`,
`docs/figs/a0main_readout.json`; verification record
`docs/figs/a0_verification_findings_2026-08-28.txt` (waves 2 + 3, eight verifier reports);
error-ledger entries docs/45 "A0 wave 2" and "A0 wave 3". Registration: docs/43 A6.0–A6.7,
A7.1–A7.2 (Zenodo 10.5281/zenodo.21963144, restricted deposit).

**One-paragraph summary.** A0 asked what the Hubbard U does to this campaign's
overpotentials at frozen geometry. Answer: everything. The projector pairing fired at
5× threshold (0.487 V from the projector choice alone at Xu's Cr U). The Cr η(U) curve
is a V with its minimum (0.381 V) at U = 3.5 — production U = 3.70 sat essentially on
the volcano apex, confirming the P7 withdrawal's mechanism at 19-point resolution. The
cell test moved the located apex crossing by 1.47 eV, so the crossing's location is
CELL-CONDITIONAL. And the reference anchors themselves invert: η(Ir) < η(Ru) fails at
U ≥ 4.5, so the anchors against which every 3d result is reported are U-conditional —
with the symmetric finding that the ordering was never *positively* resolved at any U
either, because every "holds" margin is under the registered 0.20 eV distinguishability
floor. Nothing in A0 is a relaxed result; every claim below carries the banked caveats.

## 1. What A0 was (as registered)

Three arms, one gate, all pre-registered in docs/43 before any deck ran:

- **A0-main (A6.1a):** fixed-geometry SCF grids of η(U). Cr on a 19-point 0–9 eV grid
  (0.5 eV step, 1×1 cell, 4 states) — the *location arm*, bounding withdrawn P7. Ru and
  Ir on the 7-point REF_GRID {0, 1.5, 3, 4.5, 6, 7.5, 9} plus Xu 2015 anchor points
  (Ru 6.73, Ir 5.91) — the *ordering arm* (A6.3). 140 SCFs + projwfc.
- **A0-cell (A6.1b):** the Cr 2×1v ladder at five U rungs — the A6.2 cell-additivity
  test. 20 SCFs, fifth rung (U = 7.15) gated behind A7.1.
- **P-PROJ (A7.1 gate):** atomic vs ortho-atomic projector at fixed U — scored before
  the fifth cell rung could be built.
- **A7.2:** pls flips as first-class deliverables, with the closed form
  η = (c_M/2 − 1.23) + |ΔG2 − ΔG3|/2 (pls ∈ {2,3}) as an arithmetic identity check.

Compute: Anvil (ACCESS CHE260157), arrays 20183040/20183041/20183150. **162/162 SCFs
converged; zero A6.5(2) escalation events across the whole block.**

## 2. Wave 1 — the projector gate fires

**P-PROJ: |Δη(Cr)| = 0.487 V** (atomic 1.155 V pls 2, ortho-atomic 1.642 V pls 1, all
four pairs branch-matched) against a 0.10 V threshold — **FIRES at ~5×**
(`pproj_readout.json`). Consequence, executed as registered: the whole η(U) grid runs in
ONE projector (HUBBARD atomic, the production tier's); U = 7.15 (Xu's linear-response
value, derived under a different projector) is labelled **PROJECTOR-MISMATCHED** before
any result existed, as are both Xu anchor rows of A0-main; the 0.487 V delta is its own
labelled sub-row wherever those numbers appear.

## 3. Wave 2 — the cell verdict and the Cr location arm

From `a0cell_readout.json` (four-verifier audited before banking):

- **A6.2: I_U = −0.201 eV → INCONCLUSIVE** (bins: <0.05 additive / ≥0.30 not separable).
  The prior on record was *additive*; it did not survive. Robust to dropping the
  mismatched rung (clean-point I_U = −0.155, same bin).
- **Crossing shift 1.47 eV → the located-crossing claim is CELL-CONDITIONAL** (>1.0 eV
  threshold). This does not rest on the mismatched rung: the clean-point lower bound is
  1.13 eV, still >1.0.
- The genuine geometry-extraction control lives here (2×1v leg, ≤5 meV); A0-main's
  U = 0 "control" is a same-deck re-run determinism check and is named exactly that.

From `a0main_readout.json`, Cr arm (19/19 trusted):

- **η(U) is a V-shape: min 0.381 V at U = 3.5**, rising to 1.452 V at U = 0 and 1.558 V
  at U = 9 (still rising at the grid edge — edge-limited).
- **Apex crossing (D = 1.6 eV) inside [3.5, 4.0]**, linear interpolation 3.87 eV,
  **[CELL-CONDITIONAL per A6.2]**; the pls flips 3 → 2 inside the same bracket.
  Production U = 3.70 sits inside that bracket: the withdrawn η(Cr) = 0.330 V headline
  was indeed an artifact of U placing Cr on the apex — now shown at 0.5 eV resolution,
  not inferred from five points.
- **P7 bound, two windows, quoted separately:** swing 1.177 V over the grid's own
  [0, 9] window vs 1.072 V restricted to P7's [0, 7.15] window (grid straddles the η
  minimum; the withdrawn headline was 1.122 V on a different sampling — **neither number
  confirms the other**, and the readout refuses "confirms" phrasing).

## 4. Wave 3 — the anchors invert

From `a0main_readout.json`, ordering arm (all margins = η(Ir) − η(Ru)):

| U (eV) | η(Ir) | η(Ru) | margin | reading |
|---|---|---|---|---|
| 0.0 | 0.781 | 0.787 | −0.006 | holds — below floor |
| 1.5 | 0.700 | 0.726 | −0.027 | holds — below floor |
| 3.0 | 0.618 | 0.650 | −0.031 | holds — below floor |
| 4.5 | 0.583 | 0.561 | **+0.021** | INVERTED — inside every error class |
| 6.0 | 0.641 | 0.464 | **+0.177** | INVERTED — inside the 1×1 band |
| 7.5 | 0.698 | 0.358 | **+0.340** | INVERTED — inside the 1×1 band top |
| 9.0 | 0.754 | 0.290 | **+0.464** | INVERTED — **clears every measured error class** |

- **A6.3 VERDICT: INVERTED at U ∈ {4.5, 6.0, 7.5, 9.0}.** Per the registration,
  verbatim: the anchors against which every 3d result in this campaign is reported are
  themselves U-conditional, and every ranking claim in the report — *including the ones
  that survived P7* — inherits that caveat. Blast radius as registered: a **sensitivity,
  not a correction**; production stays at each tier's own U.
- **The binary prediction is carried outright by U = 9.0 alone** (+0.464 V, above the
  top of the 1×1 cell/coverage class 0.11–0.36 eV, the NM-vs-AFM class 0.033–0.064 eV,
  and the Ir *OOH saddle 0.291 eV). The other three inverted points are context, not
  independent evidence — banked per-point in `ordering.margin_context`.
- **Symmetric finding:** every "holds" margin (max 0.031 V) is below A5.1(b)'s
  registered 0.20 eV distinguishability floor. The ordering Ir < Ru was **never
  positively resolved at any measured U — production U = 0 included**. The report may
  not claim the ordering holds anywhere; this strengthens U-conditionality.
- **The refuter's verdict: INVERTED SURVIVES, and it is in-model volcano physics, not
  an artifact.** Ir starts essentially ON the 1.6 eV apex (D = 1.642 at U = 0) and
  climbs the weak-binding leg (pls 2, η rising); Ru approaches the apex from the
  strong-binding side (pls 3, η falling). Two catalysts traversing the same volcano
  from different starting descriptors must swap order when one crosses the apex — the
  Ir pls flip in [3, 4.5] coinciding with the first inversion is that signature.
  Corroboration: the PROJECTOR-MISMATCHED Xu anchor rows (Ru 6.73 → η 0.413; Ir 5.91 →
  η 0.637) reproduce Xu 2015's finding that linear-response U restores the experimental
  activity ordering (Ru more active) — the high-U side of this same grid.
- **A7.2: CONFIRMED.** ≥3 of 6 metals flip inside the grid: Cr 3→2 in [3.5, 4.0],
  Ir 3→2 in [3.0, 4.5] (bracket saddle-conditional; the flip's *existence* is not),
  Ru 3→2 in [7.5, 9.0]. The unrun blind metals (Mn, Fe, Ti) can only add flips. Closed
  form holds to ≤2.3e-16 on every pls-{2,3} row.
  > **SUPERSEDED 2026-08-29 → docs/60 §5.** The blind metals were built. The census is
  > now closed at **5 of 6** (Cr, Fe, Ir, Mn, Ru; Ti flat), still CONFIRMED — but the
  > monotonicity argument above no longer protects its strength, and Fe and Ru each rest
  > on a single row inside a measured error class, leaving exactly 3 robust members
  > against the registered ≥3.

## 5. Caveats (banked in `a0main_readout.json["caveats"]`; they travel)

1. **Fixed geometry (A6.4, registered):** every point is a single-point SCF on the
   production-tier geometry; no relaxed Ru/Ir point at U > 0 exists anywhere in the
   campaign; where a relaxed point disagrees, it wins.
2. **Spin state (measured):** Ru/Ir columns are nspin=1 while gate (h) adopted AFM
   (0.033–0.064 eV class; re-run owed) — the U ≤ 4.5 rows and the Ir flip bracket's
   low edge are state-conditional. Cr is nspin=2.
3. **Ir *OOH saddle (measured):** cannot manufacture the inversion (inverted points are
   pls 2; correcting it pushes the other way) but conditions Ir's U ≤ 3 rows and the
   flip bracket (≈[0, 1.5] under a rigid −0.291 eV shift).
4. **Cell (registered choice):** the grid lives in the retired 1×1 cell to bound the
   1×1-era claims; the 2×1v ordering at U > 0 is unmeasured.
5. **Coverage shortfall:** A6.3 registers "Ru and Ir as well as the 3d metals";
   Mn/Fe/Ti were never built (allocation chosen 2026-08-27, no dated amendment) —
   **a dated correction of record is owed (Frank)**; unscored-at-freeze becomes
   WITHDRAWN-UNSCORED under A7.7.
   > **REMEDIED 2026-08-29 → docs/60.** Mn, Fe and Ti were built and scored in tranches
   > 2/2b/3/2c. The dated correction (docs/59) is still **DRAFT, awaiting Frank's
   > countersignature**, and its §3c licence decision moves A7.3's denominator.
6. **Gas references:** one calculation per species, md5-identical copies across the
   three metals (measured live in the readout). Same-pls comparisons are reference-free;
   different-pls comparisons — every inverted point — inherit the absolute H2O
   reference one-for-one.

## 6. Verification record

Both waves were adversarially audited **before** their verdicts were banked
(four parallel verifiers each: rederive / registration fidelity / QC+magnetization /
refute; full reports in `docs/figs/a0_verification_findings_2026-08-28.txt`):

- Wave 2 (cell + Cr): 0 BLOCKER, 10 MAJOR = 5 distinct issues, all fixed same-day.
- Wave 3 (ordering + flips): 0 BLOCKER / 6 MAJOR / 6 MINOR / 11 NOTE — every MAJOR a
  disclosure/robustness gap, all fixed same-day; **every banked number reproduced
  independently to <1.5e-9 eV/V** from raw outputs through an independently-driven CHE
  ladder; branch integrity positively verified (smooth dE/dU and Hubbard occupations,
  no magnetic manifold to hop in nspin=1); deck discipline verified (grid vs anchor
  decks differ by exactly the prefix + U line).

## 7. What the report may and may not say

- MAY: η(Cr) is U-conditional with a measured 1.072–1.177 V swing (window-labelled);
  production U sat on the apex; the apex location is cell-conditional; the Ru/Ir
  anchor ordering is U-conditional (INVERTED, carried by U = 9.0); ≥3 metals' limiting
  step flips with U; the projector alone is worth 0.487 V at Xu's Cr U.
- MAY NOT: claim Ir < Ru holds anywhere (**reason corrected 2026-08-29 → docs/60 §8:
  not "below the registered floor everywhere" — that applied A5.1(b)'s 0.20 eV G_max-gap
  floor to η margins, the wrong quantity. Scored correctly, the pair is claimable with
  Ru better at U ≥ 4.5 and not distinguishable at U ≤ 3.0 including production U; the
  registered prior η(Ir) < η(Ru) is unrecoverable at any U**);
  quote the crossing at 3.87 eV without CELL-CONDITIONAL; quote any U = 7.15 or Xu
  anchor number without PROJECTOR-MISMATCHED; present any A0 row as relaxed; use the
  two P7 windows as confirming each other; count the gas references as replications.

## 8. Open items (tracked in tasks/todo.md)

Frank: dated roster correction (item 5 above); RCAC ticket. Deferred readouts of this
same grid (zero new DFT): A5.1(a) valence classification (Löwdin banked beside every
output), A5.1(c) G_max maps, A5.1(d) intercept test, A7.3 span(c_M)/2. Optional
hardening: AFM spot-check of the U = 4.5 point, bundled with the owed S0(h) re-anchors.

> **UPDATED 2026-08-29 → docs/60 §11.** **A7.3 is no longer deferred: it was scored and
> it FAILED — NOT MET at 3 of 6 against a registered ≥4.** Filing it as future work
> while reporting its passing sibling A7.2 was the selectivity pre-registration exists
> to prevent, and docs/60 is the correction. A5.1(b) was additionally found to have been
> scored on the wrong quantity and is now scored on its own (docs/60 §8). A5.1(d)'s
> number is already banked inside `a7_3.per_metal` (the scaling intercept *is* c_M).
> A5.1(a) and A5.1(c) remain genuinely unscored. The S0(h) AFM re-run is no longer
> optional hardening: it acts on Ru, which sits 15.5 meV from flipping A7.3's verdict.

> **UPDATED 2026-09-02 → docs/68 §2, §8.** The S0(h) AFM re-run and the A11 spin arm both
> ran (106 + 5 SCFs). **Zero of 16 spin-polarised Ru SCFs converge at U = 9** (12 FM-seeded
> 1×1 rows and the 4 AFM 2×1v rows), so under the registered A11.6 rule every Ru U = 9 cell
> resolves to the banked nspin = 1 row and the 15.5 meV distance to A7.3's floor **cannot be
> priced at U = 9 under the registered numerics**; the equalised Ru span/2 is 0.0957 V (4.3
> meV short) and is guard-3 flagged. The spin-equalised sensitivity census returns the same
> three metals over the floor as the as-built one (Cr, Mn, Fe: 3 of 6; sensitivity only,
> A11.5). The sentence above, "acts on Ru, which sits 15.5 meV from flipping A7.3's
> verdict", is now a measured negative, not an open item.
