# 66 — The entrant's directive of record, and the elections executed under it — 2026-08-31

**Status:** directive of record + election record. AI-drafted disclosed infrastructure
(per docs/43 A7.7); the decisions are the entrant's, issued by the directive quoted
verbatim in §1 and executed here by his instruction. **The entrant may override any
line in this document by a later dated line.** Companion instruments this document
indexes: the `# AMENDMENT 11` text and the `## Dated addendum — 2026-08-31:
P-DISPOSITION date, amended` appended to docs/43 this same day; the `[§3c LICENCE
2026-08-31: GRANTED …]` line written into docs/59; docs/67 (the Mn AFM arm design of
record). Verification provenance: every election below was derived and then
adversarially attacked by a 13-agent verification session on 2026-08-31 (8 derive
tracks, 4 adversarial refuters, 1 completeness critic; run `wf_3866bd01-d35`); the
verdict on each candidate is recorded beside it. Three candidates were AMENDED by
that session (rows 4, 9, 19; the FREEZE line's terminus wording was also amended)
and the amended positions are the ones elected here. A post-draft audit pass (4
hostile auditors, run `wf_4ac7a2ec-f94`, 44 findings) was applied before commit;
its one legitimacy finding is load-bearing: **the docs/59 §3c countersignature is
EXECUTED UNDER DIRECTIVE but completes only at the entrant's own dated confirmation
line in docs/59 §5 — no Ti deck submits before it.**

## 1. The directive (verbatim, and what it is)

On 2026-08-31 the entrant wrote, in session, in his own words:

> Freeze is not Oct 15. We go until we cant anymore. Verify the oens that are the
> most rigorous and most compute / better for STS 2027 application. If it is not the
> most rigorous, amend so it is. remember, we are trying to be impactful and most
> rigor

This is the entrant speaking — the same voice that signs dated lines. It does three
things: (i) it amends P-DISPOSITION's date (executed as the dated addendum at the
bottom of docs/43, in the :1979 instrument style, because docs/43:4-5 forbids in-place
edits); (ii) it states the election criterion for every open decision row of docs/65 —
**among the drafted options, the most rigorous: measurement over
withdrawal/omission, best-justified thresholds, strictest anti-cherry-picking,
fullest disclosure — with compute not a constraint**; (iii) it authorises the compute
the elections unlock. The per-row mapping from criterion to option is AI-drafted
disclosed infrastructure and is the part the entrant can override; the criterion and
the authority are his.

**What the directive does NOT do:** it does not touch any banked verdict, threshold,
scoring rule, sample, or registered consequence (the freeze addendum moves the
calendar only); it does not decide report-presentation questions (docs/52 row 63
stays open, owed before Sep 20); and it does not license any deck ahead of the
deposit ordering (A7.8/A8.9/A9.7: the deposit precedes the first governed job).

## 2. Elections executed (each line dated 2026-08-31, per the §1 directive)

The full sign-line texts live in the named instruments; this table is the index.
Verification verdicts: U = UPHELD as the maximum-rigor option, A = AMENDED (the
amended position is what is elected).

| docs/65 row | election | verdict | instrument |
|---|---|---|---|
| 1 + 8 | **§3c LICENCE GRANTED — EXECUTED UNDER DIRECTIVE, COUNTERSIGNATURE PENDING** (completes at the entrant's dated confirmation line in docs/59 §5; no Ti deck submits before it), non-precedent + future-violation clauses, `s0_OH@u900 FIRST` among Ti compute | U | docs/59 §3c |
| 2 | deposit to be executed promptly after the countersign execution (act, not election; owed — §10 S5) | U | docs/59 §5 |
| 3 | **A11.5 HEADLINE CENSUS: AS-BUILT 3-of-6**; equalised census is a registered sensitivity and cannot promote A7.3 | U | docs/43 A11 |
| 4 | **A11.6 SEEDS+SELECTION: AS PROPOSED** + two dated riders: extension seed 0.05 pre-named for the Ir-slab contingency only; the banked null-seed row −1298.17043625 Ry named into the (Ti s0_OOH, u900) candidate pool | A | docs/43 A11 |
| 5 | **A11.3 THRESHOLD: 0.026 eV** (gate-(h) class re-anchored through c_M, docs/63 §4.2; a level standing proxy for a swing; relaxed 0.0325 eV disclosed alongside); **FALSIFICATION 0.005 eV**; the A11.4 FALSIFIED claim-scope cell re-worded to bound the swing D_M, not endpoint levels | U | docs/43 A11 |
| 6 | **A11.7 NULL-SEED RE-REGISTRATION: AUTHORISED** (docs/62 §5.2 replacement; tolerance: leg reproduction within ≤25× conv_thr with absmag ≈ 0, as measured on the even-electron control) | U | docs/43 A11 |
| 7 | **IR-SLAB CONTINGENCY: EXTENDED-SEEDS(0.05, named now) THEN EQUALISED-BY-SELECTION(nspin=1)** with the full rejection record; §A11.6's candidate-set reading adopted over docs/62:109-110's "no row" sentence; floor equality passes ("must be ≤ 0", docs/62:86), no additional tolerance introduced | U | docs/43 A11 |
| 9 | **TI CONVENTION: NSPIN=1 STANDS, pending the equalised census** — the drafted question is the Ti BASE ladder (4 states × 7 U rungs, 28 banked SCFs; docs/65:252's "24" carries the same undercount this corrects); the equalised A0-SPIN arm is already nspin=2 by construction and is the pre-registered symmetric instrument for exactly this question. Revisit ONLY after D_Ti is measured; any future THROUGHOUT election carries the full re-run scope, its own A6.6 disclosure, and PULL_TO-remedied relaxations under a new licence | A | this doc (the dated line is this cell) |
| 10 | **A7.7 MIDDLE-BAND DISPOSITION**: 2-or-3 (per operative denominator row) = SCORED — MIDDLE BAND / NOT MET; a dated *supplement* to A7.7's vocabulary, never quoted bare, conditionality always attached, licenses no registered consequence | U | docs/43 A11 |
| 11 | **A7.3 DENOMINATOR RULE: TABLE** (explicit per-denominator consequences at 6 and 5; withdrawal never lowers the confirmation bar; FALSIFIED stays absolute ≤1; unenumerated denominators need a new dated line before scoring) | U | docs/43 A11 |
| 12 | **CR/MN/FE SEED SEARCH: RUNS** (exactly 28 SCFs: Cr 12, Mn 8, Fe 8 — banked-seed-equals-grid-member covers a cell at both endpoints, verified in the decks); FM-uniform-seed only; winners enter the a7_3_spin sensitivity census only; selection by the A11.6-analogue rule with the banked FM row as incumbent | U | docs/43 A11 |
| 13 | **A7.2 EQUALISED RE-READ: RE-READ**; scope = Ru/Ir now (Family C: 20 slab/s0_O ladder decks, previously unpriced anywhere) + Ti (+12) under the §3c grant; endpoint-only flip-detection limitation stated (the registered flip-U crossing deliverable needs the dense ladder — §6) | U | docs/43 A11 |
| 14 | **A7.5 MN AFM ARM: IN SCOPE** — design of record docs/67; ordering MEASURED (2×1 slab set FM/P-A/P-B1/P-B2, ±20 meV adoption rule pre-stated); fixed-first then relax family under GATE-1; gate-(h) protocol verbatim; u390 + endpoints, firewalled from A7.2/A7.3; collinear-approximant limitation registered with citations flagged unverified; new tree, builders and the AFM-capable η extractor committed before any run | U | docs/43 A11 + docs/67 |
| 15 | **RU AFM PROBE: RUNS (both U endpoints)**; primary = the 4 pre-staged NM-relaxed decks, DERIVED enumeration and geometry choice countersigned; + AFM-relaxed-geometry **robustness arm, 6 SCFs, its own registered line**, inheriting the no-A7.3-score limit; AFM u000 legs = the banked `__g1` fresh-density children (−32.12 meV), pre-stated before any U=9 result | U | docs/43 A11 |
| 16 | **A11 LEDGER PLACEMENT**: amendment numbered A11; P-FLOOR-U-SPIN + P-SPIN-DELTA take APPENDIX ledger rows; body stays at the registered six; docs/52 row 63 stays open (before Sep 20) | U | docs/43 A11 |
| 17 | DISCHARGED-BY-BUILD recorded (extractor committed 9bd19e1, before any spin-arm Löwdin extraction). Residue: the Mn AFM η extractor is a SEPARATE obligation under the same precedent (docs/67) | U | this doc (this cell) |
| 18 | **DEPOSIT VEHICLE: OWN VERSION NOW** (new version off Zenodo id 22072991; restricted access; full per-file manifest in the DOI line) | U | docs/43 A11 deposit section |
| 19 | **docs/60 §6 fact 2 RESTATED** — the level-vs-swing sentence (full text in docs/43 A11); supersedes docs/63 §6's "exceeds … so not settled" phrasing on the same point; conclusion unchanged | A | docs/43 A11 |
| FREEZE | **P-DISPOSITION DATE AMENDED: OCT 15 → REPORT LOCK, BACKSTOP NOV 5 2026 8:00 PM ET**, self-executing at the backstop | U (A on terminus wording, adopted) | docs/43 dated addendum |

## 3. Positions considered and NOT elected (recorded so the road not taken is visible)

- **Row 9 THROUGHOUT** (flip the Ti base ladder to nspin=2 now): rejected by the
  adversarial pass — a production-convention change resting on one lower-bound point,
  electing it before the equalised census runs would discard the pre-registered
  instrument built to answer exactly this question. NSPIN=1 STANDS *pending the
  census* is the elected line (§2 row 9).
- **Row 5 at 0.033** (either justification): option (i) is refuted by the committed
  record (docs/63:176-177); option (iii) (relaxed re-justification) was judged
  second-best — it keeps a registered number by swapping its justification after the
  fact, and the direction argument does not survive both-sided scrutiny. 0.026 is the
  value whose justification chain and quantity match.
- **Row 7 WITHDRAWN-ROW / bare STATED-OMISSION**: pure withdrawal where a measured
  record exists; STATED-OMISSION survives only as the fallback wording inside the
  elected two-stage line.
- **Mn noncollinear spot-check**: not elected — a first-in-campaign machinery class
  (new code path, new failure modes) with non-gating payoff; registered as an
  available follow-on in docs/67, not licensed.
- **docs/52 row 63** (body-ledger displacement) and every report-presentation
  decision: untouched by the directive, still the entrant's, before Sep 20.

## 4. Operational elections (infrastructure, 0 SU)

- **[PIPELINE-GUARDS 2026-08-31: LICENCE-GREP + EXCLUDE-CHECK ADOPTED]** — the five
  array submitters (41/43/45/47/49_submit_*.sh) gain two fail-closed refusals before
  their sbatch line: (1) refuse any manifest whose header matches `NOT LICENSED` (no
  override variable, matching 47's own "No FORCE override" posture); (2) parse the
  manifest's `# SUBMIT WITH EXCLUDE=` header and refuse if the invocation's
  `$EXCLUDE` does not contain every named node — and refuse any manifest LACKING the
  header (fail-closed, not fail-open). AI-drafted disclosed maintainer edit to
  un-run infrastructure; touches no deck, no banked number.
- **[EXCLUDE EXTENDED 2026-08-31]**: the submit-time exclude list is the manifest
  list **plus a120 and a200** (the two OOM nodes of record, docs/64 §5; a200
  intermittent). F1's standing rule is unchanged: a third early OOM sends the drafted
  RCAC ticket.
- **F2 residue recorded:** electing OC20 mechanism (a) requires the third repository
  variable (the bare 64-hex sha256) to be set on GitHub; open until set.

## 5. The compute program this session licenses (after the deposit)

| wave | family | decks | state | SU (5–19/SCF) |
|---|---|---|---|---|
| A | Ru/Ir Stage-1 seed ladders | 20 | pre-built 2026-08-31 | 100–380 |
| A | Ru AFM probe, primary (NM-relaxed geo) | 4 | pre-built 2026-08-31 | 20–76 |
| B | Ti Stage-1 (s0_OH@u900 first among Ti) | 12 | build after §3c line | 60–228 |
| B | Probe robustness arm (AFM-relaxed geo) | 6 | build | 30–114 |
| B | Cr/Mn/Fe seed search | 28 | build (new builder) | 140–532 |
| B | Family C: Ru/Ir slab+s0_O ladders (re-read) | 20 | build (new builder) | 100–380 |
| B | Ti A7.2 re-read: slab+s0_O ladders (§2 row 13, under the §3c grant; submits with the Ti wave) | 12 | build (new builder) | 60–228 |
| B | Mn AFM stage 1: ordering measurement (2×1) | 4 | build (new builder) | 20–76 |
| — | contingent: Ir slab 0.05 (2), Mn core 12, Mn relax 4+4 g1, k-bridge 4, Mn prod-2×1 4 (+8 E4 FM twins if P-B wins) | ≤38 SCFs + 4 relax | on their pre-stated triggers | fixed SCFs ≤190–722; relax family per docs/67 §5's measured band |
| | **total licensed now** | **106** | | **≈ 530–2,014** |

Balance 69,783.7 SU; the licensed program is ≈ 0.8–2.9 % of it. Anvil queue verified
EMPTY 2026-08-31 (`squeue` check, recorded in tasks/todo.md's 2026-08-31 block along
with the remote-tree check: `/anvil/projects/x-che260157/sts` present via sacct
WorkDir + ls, same session; the tree also carried the 2026-08-30 S0(h) waves,
docs/64). The two Ti families submit only after the entrant's docs/59 §5
confirmation line.

## 6. Phase-2 compute decision points, registered as DECISIONS OWED (not licensed)

Under "most compute," the elected program plus every contingent and the docs/67
relax-family measured band spends ~6 % of the balance at absolute worst case (§5's
licensed-now band alone is 0.8–2.9 %).
What the remaining balance could rigorously buy, each requiring its own pre-result
registered line before any deck runs — the entrant elects after the §5 waves land:

1. **The equalised dense flip-U ladder** — A7.2's registered deliverable includes the
   U at which each metal's pls flips *inside* the grid (docs/43:1352-1353); the
   endpoint-only re-read detects a flip but cannot locate the crossing. Price to add
   the interior rungs on the equalised rows: ~5 interior U × 4 states × 3 seeds × 6
   metals worst case ≈ 360 SCFs ≈ 1,800–6,840 SU. The single highest-value phase-2
   item: it converts the re-read from an endpoint check into the registered crossing
   deliverable.
2. **A10 / P-BEEF** — still NOT DRAFTED, gated on S0(a) (docs/52 row 62); the
   schedule's actual large-compute item; untouched by this session.
3. **Mn noncollinear spot-check** (docs/67 §E6, non-gating).
4. **k-mesh / cutoff robustness arms** on the production rows — no drafted design;
   would need its own amendment.

## 7. Corrections of record entering with this session (zero compute)

- docs/65's SU tops are cosmetically low against its own 5–19 band (28→532 printed
  520; 12→228 printed 225; 20→380 printed 370; the probe's 4→76 printed 75; the
  76-SCF closing total's top reads 1,444, printed 1,420) — corrected here; do not
  reverse-derive a per-SCF rate from the sheet.
- docs/65's 76-SCF tally omitted Family C (Row 13's 20 Ru/Ir slab/s0_O decks) — the
  gated total was ≈ 96 SCFs, and §5's licensed total is 106 + contingents.
- `runs/s0/m_h_afm_probe.txt`'s "crossing would need dc_M(9.0) ≤ −41.4 meV
  (docs/figs/a0main_readout.json)": of these quantities the JSON stores only
  `/a7_3/per_metal/Ru/delta_to_floor_eV = 0.015503671954502363` (0.0155 rounded,
  +15.5 meV); −41.4 = −25.9 − 15.5 is a DERIVED sum of that pin and the banked
  dc_M(0) — neither −41.4 nor −25.9 appears in the JSON. Citation corrected in the
  manifest header at licensing.
- docs/63 §6's MAY bullet ("exceeds … so not settled") is superseded on that point by
  the Row-19 restated sentence (docs/43 A11); a dated row goes to the error ledger.
- runs/Ti_slab holds 21 banked files (not 22 as one verification line said): 7
  relaxation .in + 7 .out, H2/H2O .in/.out, manifest.json, m_ti_relax_r1.txt,
  m_ti_relax_r2.txt.

## 8. AI-use log entries (A7.7) for this session

AI drafted: the 13-agent verification session and its verdicts; this document;
docs/67; the Amendment 11 consolidated text and the P-DISPOSITION addendum appended
to docs/43; the docs/59 licence-line text; the docs/65 correction block; the
pipeline-guard code; the Stage-1/probe/CMF/Family-C/Mn builders and their manifests;
the Zenodo deposit mechanics. The entrant supplied: the directive (§1 verbatim), the
election authority, and every override right. Per A7.7, no sentence of any of these
is reproduced verbatim in the report, essays, or application answers — the entrant
paraphrases.

## 9. What the report must disclose (inventory, so nothing is discovered late)

1. The freeze amendment: original date, amendment date + DOI, ledger state at
   amendment (A7.2 CONFIRMED 5/6; A7.3 at 3-of-6 middle band, Ru +15.5 meV from the
   floor, item-10 probe undecided), uniform-extension fact, both risk directions, the
   writing-window rationale the original date served (docs/44:129-130), and the sweep
   executed once at the dated REPORT LOCK line.
2. The directive-executed election mechanism itself (§1-§2): criterion the
   entrant's, mapping AI-drafted and adversarially verified, override path open.
3. The A6.6 scale-overage running disclosure (~356 licensed now, up to ~395 at full
   contingent trigger, vs the registered ~160; the A11 text carries the summed
   figure).
4. Every VIOLATION-IF trap the verification pass registered: the probe never enters
   the A7.3 score; no A7.3-only re-read without A7.2 on the same rows; the middle
   band never quoted bare; no silent seventh body row; no Ti evidence used while §3c
   was unsigned (it no longer is).

## 10. Assembly sequence of record (S1–S4 authored 2026-08-31 in this order, landing together in one commit — the intra-commit ordering is authorship order, asserted, not verifiable from git history; S5–S8 executed or owed as marked)

(S1) this document + docs/67 authored; (S2) the docs/59 §3c line executed under
directive (countersignature completes at the entrant's confirmation line); (S3) all
elections written into the A11 text; (S4) A11 + the P-DISPOSITION addendum appended
to docs/43; living-ledger correction rows (docs/45, docs/65, tasks/todo.md); the
audit pass applied; commit; (S5) Zenodo deposit (OWN VERSION NOW), DOI lines
filled, commit; (S6) builders + pipeline guards built and verified; decks
committed; (S7) submission — wave A and the non-Ti wave B immediately after S5; the
two Ti families additionally wait on the entrant's dated docs/59 §5 confirmation
line (or his override of any row); array IDs recorded in tasks/todo.md at bank
time; (S8) the REPORT LOCK line remains the entrant's, materially before the Nov 5
backstop.
